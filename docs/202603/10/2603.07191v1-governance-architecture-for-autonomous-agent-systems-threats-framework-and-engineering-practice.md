---
title: "Governance Architecture for Autonomous Agent Systems: Threats, Framework, and Engineering Practice"
title_zh: 自主智能体系统的治理架构：威胁、框架与工程实践
authors: Yuxu Ge
date: 2026-03-07
pdf: "https://arxiv.org/pdf/2603.07191v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 自主智能体中不受控工具调用的治理
tldr: "针对大模型驱动的自主智能体面临的提示词注入、检索污染及工具调用失控等安全威胁，本文提出了分层治理架构（LGA）。该架构包含执行沙箱、意图验证、零信任授权和不可篡改审计四层。通过构建包含1081个样本的双语基准测试，验证了该架构在拦截恶意调用方面的有效性，其中LLM评判器在保持低延迟的同时实现了高达98.5%的拦截率，为智能体系统的安全工程实践提供了系统化方案。"
selection_source: fresh_fetch
motivation: 现有的安全防护措施无法系统性地解决自主智能体在执行层面临的提示词注入和工具调用失控等新型漏洞。
method: 提出了由执行沙箱、意图验证、零信任跨智能体授权和不可篡改审计日志组成的四层治理架构（LGA）。
result: "实验显示LLM评判器能拦截93.0%-98.5%的恶意工具调用，且端到端流水线在保持96%拦截率的同时，中值延迟仅约980毫秒。"
conclusion: LGA架构能有效防御多种针对智能体的攻击，通过多层协同工作在安全性和系统性能之间取得了良好平衡。
---

## 摘要
由大语言模型驱动的自主智能体引入了一类执行层漏洞——提示词注入、检索污染和不受控的工具调用——现有的防护措施（guardrails）无法系统地解决这些问题。在这项工作中，我们提出了分层治理架构（LGA），这是一个包含执行沙箱（L1）、意图验证（L2）、智能体间零信任授权（L3）和不可篡改审计日志（L4）的四层框架。为了评估 LGA，我们构建了一个包含 1,081 个工具调用

## Abstract
Autonomous agents powered by large language models introduce a class of execution-layer vulnerabilities -- prompt injection, retrieval poisoning, and uncontrolled tool invocation -- that existing guardrails fail to address systematically. In this work, we propose the Layered Governance Architecture (LGA), a four-layer framework comprising execution sandboxing (L1), intent verification (L2), zero-trust inter-agent authorization (L3), and immutable audit logging (L4). To evaluate LGA, we construct a bilingual benchmark (Chinese original, English via machine translation) of 1,081 tool-call samples -- covering prompt injection, RAG poisoning, and malicious skill plugins -- and apply it to OpenClaw, a representative open-source agent framework. Experimental results on Layer 2 intent verification with four local LLM judges (Qwen3.5-4B, Llama-3.1-8B, Qwen3.5-9B, Qwen2.5-14B) and one cloud judge (GPT-4o-mini) show that all five LLM judges intercept 93.0-98.5% of TC1/TC2 malicious tool calls, while lightweight NLI baselines remain below 10%. TC3 (malicious skill plugins) proves harder at 75-94% IR among judges with meaningful precision-recall balance, motivating complementary enforcement at Layers 1 and 3. Qwen2.5-14B achieves the best local balance (98% IR, approximately 10-20% FPR); a two-stage cascade (Qwen3.5-9B->GPT-4o-mini) achieves 91.9-92.6% IR with 1.9-6.7% FPR; a fully local cascade (Qwen3.5-9B->Qwen2.5-14B) achieves 94.7-95.6% IR with 6.0-9.7% FPR for data-sovereign deployments. An end-to-end pipeline evaluation (n=100) demonstrates that all four layers operate in concert with 96% IR and a total P50 latency of approximately 980 ms, of which the non-judge layers contribute only approximately 18 ms. Generalization to the external InjecAgent benchmark yields 99-100% interception, confirming robustness beyond our synthetic data.

---

## 论文详细总结（自动生成）

这篇论文针对大语言模型（LLM）驱动的自主智能体系统，提出了一套系统化的治理架构，旨在解决执行层的安全威胁。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：随着智能体从“对话型”转向“执行型”（具备写文件、执行Shell命令、调用API的能力），安全威胁从认知层（错误文本）扩展到了执行层（不可逆的系统状态改变）。
*   **研究动机**：现有的防护措施（如 Llama Guard）主要关注内容安全（过滤有害文本），而无法拦截语义看似无害但实际越权的工具调用。此外，提示词注入、RAG（检索增强生成）污染和恶意插件等新型漏洞缺乏统一的防御框架。

### 2. 方法论：分层治理架构 (LGA)
论文提出了 **LGA (Layered Governance Architecture)**，由四个独立部署的防御层组成：
*   **L1：执行沙箱 (Execution Sandbox)**：在操作系统层面（如 Linux 命名空间 `unshare` 或容器）隔离智能体代码执行，实施路径白名单和网络代理，确保物理隔离。
*   **L2：意图验证 (Intent Verification)**：核心层。引入独立的“评判模型（Judge Model）”，在执行高风险工具前，验证工具调用（Action）是否与原始任务（Intent）语义一致。
*   **L3：零信任跨智能体协议 (Zero-Trust Protocol)**：智能体间通信需持有最小权限的权能令牌（Capability Tokens），并对结构化数据进行 HMAC 签名和 JSON Schema 校验，防止横向移动攻击。
*   **L4：不可篡改审计日志 (Immutable Audit Log)**：所有工具调用及其参数被记录在追加式存储中，为事后取证和合规性提供基础。

### 3. 实验设计
*   **数据集/基准测试**：
    *   **自建双语基准**：包含 1,081 个工具调用样本（中文原创，英文经机器翻译）。涵盖三类威胁：TC1（提示词注入）、TC2（RAG 污染）、TC3（恶意技能插件）。
    *   **外部基准**：使用 InjecAgent 进行泛化性验证。
*   **对比方法**：
    *   **LLM 评判器**：Qwen3.5 (4B/9B), Llama-3.1 (8B), Qwen2.5 (14B), GPT-4o-mini。
    *   **轻量级基线**：基于 NLI（自然语言推理）的模型，如 BART-MNLI 和 mDeBERTa-NLI。
    *   **级联架构**：研究“本地模型 -> 云端模型”或“本地模型 -> 本地大模型”的组合效果。

### 4. 资源与算力
*   **硬件环境**：本地实验在一台配备 **NVIDIA RTX 5080 (16GB VRAM)** 的工作站上运行。
*   **软件框架**：本地模型通过 **Ollama** 部署，量化版本多为 Q4_K_M。
*   **推理配置**：使用 `num_predict=50`（非思考模式）以降低延迟。
*   **云端算力**：调用 OpenAI API (gpt-4o-mini-2024-07-18)。

### 5. 实验数量与充分性
*   **实验规模**：进行了 1,081 个样本的完整测试，并对其中 100 个样本进行了端到端流水线评估。
*   **充分性**：
    *   **多维度评估**：涵盖了拦截率（IR）、误报率（FPR）、安全-可用性 F 分数（Fsu）以及延迟。
    *   **消融与对比**：对比了不同参数规模的模型，并针对 TC3 插件威胁做了专门的子类型分析。
    *   **客观性**：通过 McNemar 检验验证了结果的统计显著性，并分析了 GPT-4o-mini 作为生成器和评判器可能存在的循环偏见。

### 6. 主要结论与发现
*   **LLM 评判器效能显著**：所有 LLM 评判器在 TC1/TC2 上的拦截率均达到 93.0%–98.5%，而 NLI 基线模型低于 10%，证明结构化推理对意图验证至关重要。
*   **本地模型表现强劲**：Qwen2.5-14B 实现了最佳的本地平衡（98% 拦截率，约 10-20% 误报率），接近云端模型水平。
*   **级联架构优势**：通过“Qwen3.5-9B -> GPT-4o-mini”级联，可以将误报率降至 1.9%–6.7%，同时保持高拦截率。
*   **TC3 挑战**：恶意插件由于其行为与声明部分重合，拦截难度较大（75%–94%），需结合 L1 和 L3 层进行防御。
*   **低延迟开销**：非评判层（L1, L3, L4）的总延迟仅约 18ms，系统主要开销取决于 L2 评判模型的推理速度。

### 7. 优点
*   **系统性强**：不仅关注算法，还从工程实践角度提出了完整的四层架构，并对开源框架 OpenClaw 进行了合规性差距分析。
*   **双语支持**：构建了中英双语基准，揭示了模型在不同语言下处理权限边界时的性能差异。
*   **实用性高**：提出的级联架构和输出限制（Output Capping）技术，为在生产环境中平衡安全、成本与延迟提供了可行方案。

### 8. 不足与局限
*   **合成数据偏差**：基准测试主要由 GPT-4o-mini 生成，可能无法完全覆盖真实世界中人类黑客的创造性攻击。
*   **TC3 识别率尚待提高**：对于隐蔽的插件侧信道攻击，单纯依靠语义对齐（L2）仍有漏报风险。
*   **对抗性鲁棒性**：初步探测显示，在精心设计的对抗性样本（如双重否定、权限伪装）下，拦截率会显著下降（降至 50%–63%）。
*   **机器翻译误差**：英文数据集由机器翻译生成，可能存在翻译伪影，影响了对原生英文环境下的泛化性评估。

（完）
