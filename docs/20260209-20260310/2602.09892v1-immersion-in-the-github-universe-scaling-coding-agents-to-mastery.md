---
title: "Immersion in the GitHub Universe: Scaling Coding Agents to Mastery"
title_zh: 沉浸于 GitHub 宇宙：将编程智能体规模化提升至大师水平
authors: "Jiale Zhao, Guoxin Chen, Fanzhe Meng, Minghao Li, Jie Chen, Hui Xu, Yongshuai Sun, Xin Zhao, Ruihua Song, Yuan Zhang, Peng Wang, Cheng Chen, Jirong Wen, Kai Jia"
date: 2026-02-10
pdf: "https://arxiv.org/pdf/2602.09892v1"
tags: ["query:sr-ai"]
score: 10.0
evidence: 用于大规模软件工程数据的自动化多智能体工作流
tldr: 本研究针对软件工程（SWE）领域高质量训练数据匮乏的挑战，提出了ScaleSWE自动化多智能体工作流。该系统通过协调环境配置、测试生成和问题合成智能体，从海量GitHub仓库中构建了包含10万个验证实例的目前最大规模SWE数据集。利用该数据微调的ScaleSWE Agent在基准测试中表现卓越，显著提升了模型解决真实世界编程问题的能力，为软件工程智能体的规模化训练提供了新路径。
selection_source: fresh_fetch
motivation: 真实世界软件工程任务的掌握受限于大规模高质量训练数据的稀缺，以及环境搭建和测试生成的复杂性。
method: 提出ScaleSWE自动化多智能体工作流，通过三个专业智能体协同处理600万个拉取请求，构建了包含10万个验证实例的大规模数据集。
result: "基于该数据集微调的ScaleSWE Agent在SWE-Bench Verified上取得了64%的解决率，性能较基础模型提升了近三倍。"
conclusion: ScaleSWE为提升大语言模型在软件工程领域的表现提供了一种可扩展且可复现的数据构建与模型训练方案。
---

## 摘要
在现实世界的软件工程任务中达到大师水平，从根本上受限于大规模、高质量训练数据的稀缺。扩展此类数据一直受到环境搭建、单元测试生成以及问题陈述策划复杂性的限制。在本文中，我们提出了 ScaleSWE，这是一种自动化的、沙箱化的多智能体工作流，旨在规模化构建高质量的软件工程（SWE）数据。该系统协调三个专门的智能体（分别用于环境搭建、测试创建和问题描述合成），处理了 5200 个仓库中的 600 万个拉取请求，生成了 Scale SWE 数据集：包含 10 万个经过验证的 SWE 实例，是迄今为止最大的此类数据集。它在仓库多样性方面大幅超越了现有的现实世界数据集，并反映了真实的任复杂性。我们通过蒸馏 71498 条高质量轨迹并微调 Qwen30BA3BInstruct 来生成 ScaleSWE 智能体，进一步证明了该数据集在训练中的效用。我们的智能体在 SWE-Bench Verified 上实现了 64% 的解决率，相比基础模型提升了近三倍。ScaleSWE 为数据构建提供了一种可扩展、可复现的方法，以推进基于大语言模型（LLM）的软件工程。Scale SWE 将公开发布。

## Abstract
Achieving mastery in real world software engineering tasks is fundamentally bottlenecked by the scarcity of large scale, high quality training data. Scaling such data has been limited by the complexity of environment setup, unit test generation, and problem statement curation. In this paper, we propose ScaleSWE, an automated, sandboxed multi agent workflow designed to construct high quality SWE data at scale. The system coordinates three specialized agents for environment setup, test creation, and problem description synthesis to process 6 million pull requests across 5200 repositories, producing Scale SWE Data: 100k verified SWE instances, the largest such dataset to date. It substantially surpasses existing real world datasets in repository diversity and reflects realistic task complexity. We further demonstrate the dataset utility for training by distilling 71498 high quality trajectories and finetuning Qwen30BA3BInstruct to produce ScaleSWE Agent. Our agent achieves a 64 resolve rate on SWE Bench Verified a nearly three fold improvement over the base model. ScaleSWE provides a scalable, reproducible approach for data construction to advance LLM based software engineering. Scale SWE will be publicly available.

---

## 论文详细总结（自动生成）

这是一份关于论文《Immersion in the GitHub Universe: Scaling Coding Agents to Mastery》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
该论文针对当前大语言模型（LLM）在解决真实世界软件工程（SWE）任务时的核心瓶颈——**高质量、大规模训练数据的匮乏**。
*   **研究动机**：现有的 SWE 数据集（如 SWE-bench）虽然有用，但规模较小，且构建过程高度依赖人工或简单的规则，导致数据在多样性、环境可执行性和任务复杂性上存在不足。
*   **核心目标**：通过自动化、沙箱化的多智能体工作流，从海量开源代码库中提取并验证高质量的软件修复任务，从而实现 SWE 训练数据的规模化生产。

### 2. 方法论：Scale-SWE 工作流
论文提出了 **Scale-SWE**，一个由三个专门智能体协作的自动化系统，运行在隔离的 Docker 沙箱中：
*   **环境构建智能体 (Environment Builder Agent, EBA)**：自动分析仓库配置文件（如 `setup.py`, `requirements.txt`），交互式解决依赖冲突，并生成可复现的 Docker 镜像。为了提高效率，系统采用“最近邻环境复用”策略。
*   **单元测试创建智能体 (Unit-test Creator Agent, UCA)**：基于 Pull Request (PR) 的元数据和代码变更，在沙箱中通过“执行-分析-重构”循环，自动生成**Fail-to-Pass (F2P)**（复现 Bug）和 **Pass-to-Pass (P2P)**（防止回归）测试用例。
*   **问题陈述编写智能体 (Problem Statement Writer Agent, PSWA)**：利用 LLM（如 Gemini 3 Pro）根据代码变更和测试用例合成自洽的任务描述，同时严格过滤掉可能导致“解题泄露”的实现细节，确保任务的挑战性。
*   **数据规模**：处理了 5200 个仓库的 600 万个 PR，最终产出 **100,000 个经过验证的实例 (Scale-SWE-Data)**。

### 3. 实验设计
*   **数据集与场景**：
    *   **训练集**：从 Scale-SWE-Data 中选取 2.5 万个实例，利用 DeepSeek-V3.2 蒸馏出 71,498 条高质量解决轨迹（约 35 亿 Token）。
    *   **测试基准 (Benchmark)**：主要使用 **SWE-bench Verified**（包含 500 个经人工校验的高质量 Python 任务）。
*   **对比方法**：
    *   **闭源模型**：GPT-5.2 Thinking, Claude 4.5, Gemini 3 Pro, DeepSeek-V3.2 等。
    *   **开源/专门模型**：SWE-Gym, SWE-smith, SWE-rebench, KAT-Dev-32B 等。
    *   **基座模型**：Qwen3-30B-A3B-Instruct。

### 4. 资源与算力
*   **算力细节**：论文**未明确说明**具体的 GPU 型号（如 H100/A100）或具体集群规模。
*   **训练参数**：提到了微调 Qwen3-30B-A3B-Instruct 的超参数：学习率 1e-5，Batch Size 128，Warmup 比例 0.05，最大上下文长度 131,072，训练 3 个 Epoch。
*   **推理配置**：推理时将上下文限制扩展至 262,144。

### 5. 实验数量与充分性
*   **实验组数**：
    *   对比了超过 15 种主流模型在 SWE-bench Verified 上的表现。
    *   进行了**数据源对比实验**：在相同训练框架下，分别使用 SWE-Gym、SWE-smith 和 Scale-SWE 数据进行微调，验证数据质量。
    *   提供了 Bug 类型的分布分析（涵盖 API 误用、逻辑错误、安全漏洞等 10 类）。
*   **充分性与公平性**：实验设计较为严谨。通过统一使用 OpenHands 框架进行评估，并实施了严格的“防作弊”策略（清理 Git 历史元数据），确保了对比的客观性。

### 6. 主要结论与发现
*   **性能飞跃**：Scale-SWE-Agent 在 SWE-bench Verified 上取得了 **64.0%** 的解决率，相比基座模型（22.0%）提升了近 3 倍，超越了许多参数量更大的模型（如 Llama3-70B）。
*   **真实数据胜过合成数据**：实验显示，尽管合成数据集（如 SWE-smith）规模很大，但在提升模型解决真实复杂问题的能力上，不如来自真实仓库的 Scale-SWE 数据。
*   **多样性的重要性**：Scale-SWE 覆盖了 5200 个仓库，这种极高的多样性使模型能够学习到更广泛的编程模式和调试技巧。

### 7. 优点与亮点
*   **规模突破**：构建了目前全球最大的、带执行环境的验证级 SWE 数据集（100k 实例）。
*   **全自动化流水线**：成功解决了环境配置和测试生成这两个最难规模化的环节，实现了从原始 PR 到训练数据的闭环。
*   **高质量蒸馏**：通过多轮采样和验证，确保了用于微调的轨迹数据是“正确且高效”的。

### 8. 不足与局限
*   **语言局限性**：目前该研究主要集中在 **Python** 语言，尚未扩展到 Java、C++ 或 Rust 等其他主流语言。
*   **模型依赖**：数据构建过程（尤其是问题合成和轨迹蒸馏）高度依赖于 DeepSeek-V3.2 和 Gemini 3 Pro 等顶级闭源/强力模型，这可能引入特定的模型偏见。
*   **长尾仓库质量**：虽然覆盖了 5.2k 仓库，但对于一些极小众或文档极差的仓库，环境构建的成功率和准确性仍有提升空间。

（完）
