---
title: "Tell Me What To Learn: Generalizing Neural Memory to be Controllable in Natural Language"
title_zh: 告诉我该学什么：实现自然语言可控的泛化神经记忆
authors: "Max S. Bennett, Thomas P. Zollo, Richard Zemel"
date: 2026-02-26
pdf: "https://arxiv.org/pdf/2602.23201v2"
tags: ["query:sr-ai"]
score: 9.0
evidence: 适用于自适应智能体的通用神经记忆系统
tldr: 针对机器学习模型在动态环境中持续适配的需求，本文提出了一种可由自然语言指令控制的通用神经记忆系统。传统神经记忆方法往往受限于固定目标和同质化信息流，缺乏用户控制力。该方法允许用户通过自然语言指定学习指令，使智能体能从异构信息源中选择性地提取知识。这为医疗、客服等需要灵活调整记忆策略的领域提供了更高效、可控的持续学习方案。
selection_source: fresh_fetch
motivation: 现有神经记忆模型通常采用固定目标，无法让用户根据需求灵活控制模型在持续学习过程中应记忆或忽略的信息。
method: 提出一种通用的神经记忆系统，通过自然语言指令引导模型进行灵活的记忆更新，从而实现对异构信息源的选择性学习。
result: 该方法使自适应智能体能够根据特定指令处理复杂任务，解决了传统固定目标记忆更新在医疗和客服等动态场景中的局限性。
conclusion: 通过引入自然语言控制，该研究显著提升了神经记忆系统的可控性和泛化能力，为构建更灵活的持续学习系统提供了新方向。
---

## 摘要
现代机器学习模型被部署在多样化且非平稳的环境中，必须不断适应新任务和不断演进的知识。持续微调和上下文学习成本高昂且脆弱，而神经记忆方法则有望实现遗忘极小的轻量级更新。然而，现有的神经记忆模型通常假设单一的固定目标和同质信息流，导致用户无法控制模型随时间推移记住或忽略的内容。为了应对这一挑战，我们提出了一种泛化神经记忆系统，该系统能够根据自然语言指定的学习指令进行灵活更新。我们的方法使自适应智能体能够从异构信息源中进行选择性学习，从而支持医疗保健和客户服务等固定目标记忆更新不足的场景。

## Abstract
Modern machine learning models are deployed in diverse, non-stationary environments where they must continually adapt to new tasks and evolving knowledge. Continual fine-tuning and in-context learning are costly and brittle, whereas neural memory methods promise lightweight updates with minimal forgetting. However, existing neural memory models typically assume a single fixed objective and homogeneous information streams, leaving users with no control over what the model remembers or ignores over time. To address this challenge, we propose a generalized neural memory system that performs flexible updates based on learning instructions specified in natural language. Our approach enables adaptive agents to learn selectively from heterogeneous information sources, supporting settings, such as healthcare and customer service, where fixed-objective memory updates are insufficient.

---

## 论文详细总结（自动生成）

### 论文总结：Tell Me What To Learn: Generalizing Neural Memory to be Controllable in Natural Language

#### 1. 论文的核心问题与整体含义
现代大语言模型（LLM）在部署后面临动态环境，需要不断吸收新知识。现有的适配方法存在局限：微调（Fine-tuning）成本高且易导致灾难性遗忘；上下文学习（ICL）受限于窗口长度且计算开销随内容增加而激增；传统的神经记忆（Neural Memory）虽然高效，但通常采用**固定目标**（即默认学习所有输入内容），用户无法干预模型“该记住什么”或“该忽略什么”。

本论文提出了**通用神经记忆（Generalized Neural Memory, GNM）**框架，核心目标是实现**自然语言可控的记忆更新**。用户可以通过指令（如“学习医疗协议，但忽略个人隐私信息”）引导模型选择性地更新记忆，从而在医疗、客服等对信息敏感度要求极高的领域实现安全、灵活的持续适配。

#### 2. 论文提出的方法论
*   **核心思想**：将记忆更新过程建模为一个由指令引导的函数。
*   **数学表达**：更新规则定义为 $M_t = U_\psi(M_{t-1}, I_t, D_t)$。其中 $M$ 是记忆状态，$I_t$ 是自然语言指令，$D_t$ 是输入文档，$\psi$ 是可学习的更新参数。
*   **技术细节**：
    *   **架构基础**：基于 MemoryLLM（以 Llama-3 为骨干），将记忆存储为每层 Transformer 预挂的连续嵌入（Prefix Embeddings）。
    *   **指令注入**：在“学习阶段”，将指令和文档拼接输入，模型通过注意力机制决定哪些信息应被压缩进 256 个记忆 Token 中。
    *   **训练目标**：采用序列损失函数 $L_{seq}$，通过“正向探针”（应记住的事实）和“负向探针”（应忽略或拒绝的事实）来优化更新参数 $\psi$。
    *   **推理过程**：推理时模型参数固定，仅通过更新后的记忆状态 $M_t$ 调制输出，实现 $O(1)$ 的推理复杂度（不随文档数量增加而增加）。

#### 3. 实验设计
*   **数据集与场景**：构建了一个基于 **CounterFACT** 的合成基准测试。包含 21,918 个事实，分为 16 个语义类别（其中 4 类完全留出用于测试 OOD 泛化性）。
*   **指令类型**：
    1.  **事实指令**：仅学习特定类别的知识。
    2.  **格式指令**：仅学习文档的排版风格（如 JSON/XML），忽略内容。
    3.  **拒绝指令**：学习内容但对特定类别触发拒绝响应。
    4.  **复合指令**：同时处理多种学习/忽略要求。
*   **对比方法**：
    *   **MemoryLLM**：原始的固定目标神经记忆模型。
    *   **ICL-FT**：经过微调的上下文学习（将所有历史指令和文档放入 Context）。
    *   **RAG-FT**：经过微调的检索增强生成。

#### 4. 资源与算力
*   **硬件**：使用了 **6 台 NVIDIA A100 GPU**。
*   **训练细节**：采用梯度累积，有效 Batch Size 为 6 个序列（共 24 篇文档）。
*   **时长**：使用早停法（Early Stopping），所有模型在 **10 个 Epoch 内**完成收敛。

#### 5. 实验数量与充分性
*   **实验规模**：进行了多轮实验，包括长度为 10 到 20 的长序列持续学习实验、跨模态（知识 vs 风格）的迁移实验、以及复杂的复合指令实验。
*   **消融实验**：对比了“仅在推理时学习”与“在记忆写入时学习”的效果，证明了**主动选择性写入**是性能提升的关键。
*   **充分性评价**：实验设计较为全面，涵盖了准确性、特异性（不误伤邻近事实）和选择性（成功忽略无关信息）等多个维度。通过留出类别测试 OOD 泛化，验证了模型对**未见指令**的处理能力，实验逻辑客观且公平。

#### 6. 论文的主要结论与发现
*   **泛化能力强**：GNM 能够理解并执行在训练期间从未见过的自然语言指令。
*   **选择性优势**：在“选择性忽略”任务上，GNM 显著优于 ICL 和 RAG。ICL 往往会受到上下文干扰，而 GNM 在写入阶段就过滤了无关信息。
*   **高效性**：GNM 的推理成本不随学习文档的数量增加而增长，且在处理长序列时表现出更好的稳定性。
*   **机理发现**：通过层级因果干预发现，模型的前部层（5-14层）负责表征指令，中后部层（15-31层）负责根据指令将信息路由至记忆 Token。

#### 7. 优点
*   **首创性**：首次提出了自然语言可控的神经记忆更新框架，解决了神经记忆“全盘接收”的痛点。
*   **高效率**：相比 ICL，极大地节省了推理时的显存和计算开销。
*   **可解释性**：通过层级分析揭示了指令如何引导记忆写入的内部机制。
*   **灵活性**：支持知识、风格和行为（拒绝策略）的混合学习。

#### 8. 不足与局限
*   **数据合成性**：主要基于合成的 CounterFACT 数据集，缺乏在真实世界复杂非结构化文档上的大规模验证。
*   **记忆容量限制**：受限于固定数量的记忆 Token，当序列极长（如超过 20 步）时，旧记忆会被新记忆覆盖导致性能下降。
*   **冲突处理**：目前尚未深入探讨当先后指令或文档发生事实冲突（Inconsistent Information）时，模型应如何决策。
*   **架构依赖**：该方法目前高度依赖于特定的神经记忆架构（如 MemoryLLM），在其他记忆模型上的普适性有待验证。

（完）
