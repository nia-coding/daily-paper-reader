---
title: "ProgAgent:A Continual RL Agent with Progress-Aware Rewards"
title_zh: ProgAgent：一种具有进度感知奖励的持续强化学习智能体
authors: "Jinzhou Tan, Gabriel Adineera, Jinoh Kim"
date: 2026-03-08
pdf: "https://arxiv.org/pdf/2603.07784v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 通过无标签专家视频和进度感知奖励进行持续强化学习的智能体
tldr: ProgAgent 是一种针对机器人终身学习的持续强化学习智能体。它通过感知模型从无标注专家视频中提取任务进度以生成稠密奖励，并利用对抗性回推机制应对分布偏移。该系统采用 JAX 原生架构，集成了 PPO、核心集回放和突触智能，有效平衡了稳定性与塑性，在多个基准测试和真实机器人实验中显著减少了遗忘并提升了学习效率。
selection_source: fresh_fetch
motivation: 旨在解决机器人终身学习中灾难性遗忘严重以及手动定义奖励函数成本高昂的挑战。
method: 通过感知模型从专家视频中学习进度感知的状态势能函数作为奖励，并结合对抗性正则化与 JIT 编译的高吞吐架构进行持续学习。
result: 在 ContinualBench 和 Meta-World 基准测试中，该方法在减少遗忘和学习速度上均优于 Rank2Reward、TCN 等主流基准。
conclusion: ProgAgent 成功实现了从少量噪声人类演示中高效习得复杂操作技能，为构建稳健的持续强化学习系统提供了有效方案。
---

## 摘要
我们提出了 ProgAgent，这是一种持续强化学习（CRL）智能体，它将进度感知奖励学习与高吞吐量的 JAX 原生系统架构相结合。终身机器人学习面临着灾难性遗忘和奖励规范成本高昂的挑战。ProgAgent 通过感知模型从无标签的专家视频中推导出稠密的塑形奖励，该模型能够根据初始、当前和目标观测值来估计任务进度。我们在理论上将其解释为一种学习到的状态势函数，能够提供与专家行为一致的鲁棒引导。为了在在线探索过程中（此时会出现新颖的分布外状态）保持稳定性，我们引入了一种对抗性回推细化机制来正则化奖励模型，从而抑制对非专家轨迹的过度自信预测，并应对分布偏移。通过将该奖励机制嵌入到即时编译（JIT）循环中，ProgAgent 支持大规模并行展开和全微分更新，使得复杂的统一目标函数变得可行：它将 PPO 与核心集回放和突触智能相结合，以增强稳定性与可塑性之间的平衡。在 ContinualBench 和 Meta-World 基准测试上的评估突显了 ProgAgent 的优势：它显著减少了遗忘，提高了学习速度，并在视觉奖励学习（如 Rank2Reward、TCN）和持续学习（如 Coreset、SI）方面优于关键基准模型，甚至超过了理想化的完美记忆智能体。真实机器人试验进一步验证了其从嘈杂、少样本的人类演示中获取复杂操作技能的能力。

## Abstract
We present ProgAgent, a continual reinforcement learning (CRL) agent that unifies progress-aware reward learning with a high-throughput, JAX-native system architecture. Lifelong robotic learning grapples with catastrophic forgetting and the high cost of reward specification. ProgAgent tackles these by deriving dense, shaped rewards from unlabeled expert videos through a perceptual model that estimates task progress across initial, current, and goal observations. We theoretically interpret this as a learned state-potential function, delivering robust guidance in line with expert behaviors. To maintain stability amid online exploration - where novel, out-of-distribution states arise - we incorporate an adversarial push-back refinement that regularizes the reward model, curbing overconfident predictions on non-expert trajectories and countering distribution shift. By embedding this reward mechanism into a JIT-compiled loop, ProgAgent supports massively parallel rollouts and fully differentiable updates, rendering a sophisticated unified objective feasible: it merges PPO with coreset replay and synaptic intelligence for an enhanced stability-plasticity balance. Evaluations on ContinualBench and Meta-World benchmarks highlight ProgAgent's advantages: it markedly reduces forgetting, boosts learning speed, and outperforms key baselines in visual reward learning (e.g., Rank2Reward, TCN) and continual learning (e.g., Coreset, SI) - surpassing even an idealized perfect memory agent. Real-robot trials further validate its ability to acquire complex manipulation skills from noisy, few-shot human demonstrations.

---

## 论文详细总结（自动生成）

这是一份关于论文《ProgAgent: A Continual RL Agent with Progress-Aware Rewards》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于**机器人终身学习（Lifelong Learning）**中的两大核心瓶颈：
*   **灾难性遗忘（Catastrophic Forgetting）：** 智能体在学习新任务时会迅速覆盖旧知识，导致长期自主性受损。
*   **奖励规范难题（Reward Specification）：** 为复杂的操作任务手动设计稠密奖励函数既耗时又难以扩展。
*   **现有研究的脱节：** 持续学习（CL）算法往往忽略了系统级的优化（如并行化），而视觉奖励模型在面对在线探索产生的分布外（OOD）状态时表现脆弱。

**ProgAgent 的核心含义**在于通过一个 JAX 原生的、高吞吐量的架构，将“进度感知奖励学习”与“持续强化学习算法”有机统一，旨在实现既能高效获取新技能又能稳健保留旧知识的机器人智能体。

### 2. 方法论：核心思想与关键技术
ProgAgent 的核心思想是利用无标签专家视频构建一个**状态势能函数（State-potential Function）**，并结合对抗性正则化和高效系统架构。

*   **进度感知奖励模型：**
    *   输入初始、当前和目标观测值三元组 $(o_i, o_j, o_g)$。
    *   模型预测任务进度比率 $\delta$ 的高斯分布，通过 KL 散度最小化与专家视频目标的差异。
    *   **理论支撑：** 将预测均值解释为势能函数 $\Phi$，根据势能差生成奖励 $r_t = \gamma \Phi(o_t) - \Phi(o_{t-1})$，这在理论上保证了策略的最优性不变。
*   **对抗性回推细化（Adversarial Push-back）：**
    *   针对在线探索中的非专家轨迹，引入正则化项将预测推向“低置信度先验”（零均值、高方差），防止模型对未知状态产生过度自信的错误奖励。
*   **统一的持续学习目标：**
    *   在 PPO 算法基础上集成了**核心集回放（Coreset Replay）**和**突触智能（Synaptic Intelligence, SI）**。
    *   公式化为：$L_{total} = L_{PPO} + \lambda_1 L_{replay} + \lambda_2 L_{SI}$。
*   **JAX 原生架构：**
    *   利用 JAX 的 JIT 编译和 `vmap` 向量化，将环境模拟、奖励计算、策略更新全部封装在 GPU 上的纯函数循环中，支持数千个环境的大规模并行展开。

### 3. 实验设计
*   **数据集/场景：**
    *   **ContinualBench：** 包含按顺序排列的机器人操作任务（按按钮、开门、关窗）。
    *   **Meta-World：** 六个具有挑战性的操作任务序列。
    *   **真实机器人：** 验证从少量、带噪声的人类演示中学习技能的能力。
*   **对比方法（Baselines）：**
    *   **持续学习类：** OA (Online Agent), SI, Coreset, Fine-tuning, 以及理想化的 Perfect Memory（上限）。
    *   **奖励/模仿学习类：** Rank2Reward, GAIL, TCN。
*   **评估指标：** 成功率（Success Rate）、评估奖励（Eval Reward）、平均性能（AP）和悔恨值（Regret）。

### 4. 资源与算力
*   **文中说明：** 论文强调了使用 **JAX 原生架构** 和 **XLA 编译** 来实现高吞吐量，提到支持“数千个环境的并行展开”。
*   **具体细节：** 提取文本中**未明确说明**具体的 GPU 型号（如 A100 或 H100）、GPU 数量以及具体的训练总时长。但从其系统设计来看，该方法旨在通过计算效率弥补内存限制。

### 5. 实验数量与充分性
*   **实验规模：**
    *   在 ContinualBench 上进行了 3 个任务的序列测试。
    *   在 Meta-World 上进行了 6 个任务的扩展测试。
    *   包含详细的**消融实验**（验证对抗回推和 CL 正则化的必要性）。
    *   包含**定性分析**（可视化势能函数曲线，验证其单调性）。
*   **充分性评价：** 实验设计较为充分，涵盖了从仿真基准到真实机器人的跨度。对比了多种不同类型的基准模型，且通过消融实验清晰地展示了各组件对“稳定性-塑性平衡”的贡献，结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能领先：** ProgAgent 在所有指标上均优于主流基准，甚至在某些任务上超过了拥有全部历史数据访问权的“完美记忆”智能体。
*   **架构红利：** 实验证明，系统架构的效率（高吞吐并行处理）有时比无限的内存容量对寻找全局最优解更有帮助。
*   **鲁棒性：** 对抗性回推机制有效解决了视觉奖励模型常见的“奖励黑客（Reward Hacking）”问题，使智能体在探索 OOD 状态时保持稳定。
*   **现实可行性：** 即使专家演示数据中有一半是失败案例，ProgAgent 仍能从中提取有效的进度信号。

### 7. 优点（亮点）
*   **理论与工程结合：** 将进度预测严谨地定义为势能函数，并利用 JAX 实现了极高的工程效率。
*   **无需动作标签：** 仅通过视频观测即可学习奖励，极大地降低了数据获取成本。
*   **对抗性防御：** 引入 Push-back 机制增强了奖励模型在动态探索环境中的鲁棒性。
*   **统一框架：** 成功将感知、奖励学习、策略优化和持续学习机制整合进一个全微分的闭环中。

### 8. 不足与局限
*   **依赖专家视频质量：** 如果专家演示极度匮乏或视觉多样性不足，势能函数可能引导智能体陷入局部最优。
*   **Sim-to-Real 挑战：** 尽管有真机实验，但视觉域的差异（Domain Gap）仍是模拟到现实迁移中的潜在障碍。
*   **超参数敏感性：** 统一目标函数中的多个系数（如 $\beta, \lambda_1, \lambda_2$）需要针对不同任务套件进行精细调优，未来可能需要元学习方法来自动调整。

（完）
