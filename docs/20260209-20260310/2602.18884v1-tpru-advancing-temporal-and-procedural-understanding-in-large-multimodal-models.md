---
title: "TPRU: Advancing Temporal and Procedural Understanding in Large Multimodal Models"
title_zh: TPRU：提升大型多模态模型的时间与过程理解能力
authors: "Zhenkun Gao, Xuhong Wang, Xin Tan, Yuan Xie"
date: 2026-02-21
pdf: "https://arxiv.org/pdf/2602.18884v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 具身智能的时序与过程理解
tldr: 针对多模态大模型在时序和过程理解上的不足，本文提出了大规模数据集TPRU，涵盖机器人操作和GUI导航等具身场景。通过时序重排、下一帧预测和前一帧回顾三项任务，结合强化学习微调，显著提升了轻量级模型的性能。实验显示TPRU-7B在测试集上表现优于GPT-4o，且具有良好的泛化能力。
selection_source: fresh_fetch
motivation: 现有模型在处理具身智能所需的时序和过程化视觉数据时存在严重缺陷，且缺乏大规模高质量的相关训练数据。
method: 提出包含时序重排、预测和回顾任务的TPRU数据集，并引入负样本，配合强化学习微调方法增强模型的跨模态验证能力。
result: "TPRU-7B在专用测试集上的准确率从50.33%大幅提升至75.70%，性能超越了包括GPT-4o在内的大型基准模型。"
conclusion: 通过系统化的时序任务设计和强化学习，可以显著增强资源受限模型对复杂过程的理解与泛化能力。
---

## 摘要
多模态大语言模型（MLLMs），尤其是较小的可部署变体，在理解时间性和过程性视觉数据方面表现出严重的缺陷，这一瓶颈阻碍了它们在现实世界具身智能（Embodied AI）中的应用。这一差距很大程度上源于训练范式的系统性失效，即缺乏大规模、过程连贯的数据。为了解决这一问题，我们推出了 TPRU，这是一个源自机器人操作和 GUI 导航等多种具身场景的大规模数据集。TPRU 经过系统化设计，通过三个互补的任务来培养时间推理能力：时间重排序（Temporal Reordering）、下一帧预测（Next-Frame Prediction）和前一帧回顾（Previous-Frame Review）。其核心特征是包含了具有挑战性的负样本，迫使模型从被动观察转向主动的跨模态验证。我们利用 TPRU 结合强化学习（RL）微调方法，专门针对资源高效型模型的增强。实验表明，我们的方法取得了显著的提升：在人工构建的 TPRU-Test 上，TPRU-7B 的准确率从 50.33% 飙升至 75.70%，这一 SOTA 结果显著优于包括 GPT-4o 在内的规模大得多的基准模型。至关重要的是，这些能力具有良好的泛化性，在既有基准测试中也表现出实质性的改进。代码库可在 https://github.com/Stephen-gzk/TPRU/ 获取。

## Abstract
Multimodal Large Language Models (MLLMs), particularly smaller, deployable variants, exhibit a critical deficiency in understanding temporal and procedural visual data, a bottleneck hindering their application in real-world embodied AI. This gap is largely caused by a systemic failure in training paradigms, which lack large-scale, procedurally coherent data. To address this problem, we introduce TPRU, a large-scale dataset sourced from diverse embodied scenarios such as robotic manipulation and GUI navigation. TPRU is systematically designed to cultivate temporal reasoning through three complementary tasks: Temporal Reordering, Next-Frame Prediction, and Previous-Frame Review. A key feature is the inclusion of challenging negative samples, compelling models to transition from passive observation to active, cross-modal validation. We leverage TPRU with a reinforcement learning (RL) fine-tuning methodology, specifically targeting the enhancement of resource-efficient models. Experiments show our approach yields dramatic gains: on our manually curated TPRU-Test, the accuracy of TPRU-7B soars from 50.33\% to 75.70\%, a state-of-the-art result that significantly outperforms vastly larger baselines, including GPT-4o. Crucially, these capabilities generalize effectively, demonstrating substantial improvements on established benchmarks. The codebase is available at https://github.com/Stephen-gzk/TPRU/ .

---

## 论文详细总结（自动生成）

以下是对论文《TPRU: Advancing Temporal and Procedural Understanding in Large Multimodal Models》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **核心缺陷**：现有的多模态大语言模型（MLLM），尤其是参数量较小、易于部署的变体，在理解**时间时序（Temporal）**和**操作过程（Procedural）**的视觉数据方面存在严重瓶颈。
*   **研究动机**：这种缺陷限制了模型在具身智能（如机器人操作、GUI导航）中的应用。作者认为，这并非完全由于模型规模限制，而是因为现有的训练范式缺乏大规模、过程连贯的序列数据，且大多将多图输入视为无序集合。
*   **整体含义**：本文旨在通过构建高质量的序列数据集和引入强化学习训练范式，填补小模型在复杂过程推理能力上的空白。

### 2. 方法论：核心思想与关键技术
*   **TPRU 数据集构建**：
    *   **数据来源**：涵盖机器人操作（ShareRobot）、乐高组装（YouTube 视频）、移动端 GUI 操作（GUI Odyssey）和具身导航（Habitat 仿真）四个真实场景。
    *   **三项互补任务**：
        1.  **时间重排序（Temporal Reordering）**：将打乱顺序的图像恢复正确时序。
        2.  **下一帧预测（Next-Frame Prediction）**：给定部分序列，从干扰项中选出后续正确步骤。
        3.  **前一帧回顾（Previous-Frame Review）**：根据后续状态推断起始或前置步骤。
    *   **负样本设计**：引入文本与图像不匹配的“负样本”，迫使模型进行跨模态验证，而非盲目猜测，选项中包含“以上皆非”。
*   **训练策略**：
    *   采用**强化学习（RL）**而非单纯的监督微调（SFT）。
    *   使用 **GRPO（Group-wise Preference Optimization）** 算法，通过群体偏好优化提升模型的推理逻辑。
    *   设计了格式奖励（Format Reward）和准确性奖励（Accuracy Reward）来引导模型生成结构化的推理过程（Think-Answer 模式）。

### 3. 实验设计
*   **数据集与场景**：
    *   **训练集**：TPRU-25k（包含 24,750 个 QA 对，126,000 张图像）。
    *   **测试集**：TPRU-Test（461 个手动标注的挑战性样本，额外引入了 EPIC-KITCHENS 厨房活动场景以测试泛化性）。
*   **Benchmark（基准测试）**：
    *   **时序相关**：MuirBench, LEGO-Puzzles。
    *   **通用能力**：MMMU, BLINK, RealWorldQA, MMStar, MMTBench 等。
*   **对比方法**：
    *   **开源模型**：Qwen2.5-VL (3B/7B/32B/72B), InternVL3 系列。
    *   **闭源模型**：GPT-4o, Claude-3.5-Sonnet, Gemini-2.5-Flash。
    *   **视频模型**：LLaVA-Video, Qwen2.5-Omni 等。

### 4. 资源与算力
*   **硬件环境**：使用了由 **8 台 NVIDIA A800 GPU** 组成的集群。
*   **训练细节**：
    *   使用 AdamW 优化器，学习率为 1e-6。
    *   训练周期为 2 个 Epoch。
    *   基于 Easy-R1 框架实现，KL 正则化系数设为 0.01。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的评估，包括在 10 多个主流基准测试上的性能对比。
*   **消融实验**：
    *   验证了三项任务（重排、预测、回顾）的协同效应。
    *   对比了有无负样本对模型鲁棒性的影响。
    *   对比了数据量规模（从 5k 到 25k）的边际效应。
    *   对比了 GRPO 强化学习与传统 SFT 的效果差异。
*   **充分性评价**：实验设计非常全面且客观。通过引入第三方视频模型对比和通用能力测试，证明了该方法在提升特化能力的同时，没有产生“灾难性遗忘”。

### 6. 主要结论与发现
*   **性能飞跃**：TPRU-7B 在专用测试集上的准确率从 50.33% 提升至 **75.70%**，超越了参数量大得多的 GPT-4o。
*   **泛化能力强**：在 LEGO-Puzzles 和 MuirBench 等外部时序基准上也取得了 SOTA 性能。
*   **规模非唯一因素**：证明了通过高质量、结构化的任务数据和强化学习，较小的模型（如 7B）也能具备极强的逻辑推理能力。
*   **视频模型局限性**：发现现有的视频大模型在处理离散状态变化和精确过程排序方面，依然不如经过专门时序训练的多图模型。

### 7. 优点（亮点）
*   **任务设计精巧**：三项任务（重排、预测、回顾）构成了对时间逻辑的闭环训练。
*   **强调主动验证**：通过负样本设计，有效缓解了多模态模型常见的“幻觉”问题。
*   **实战导向**：数据源自真实的具身场景，对机器人和自动化系统的落地具有直接参考价值。
*   **开源贡献**：提供了高质量的数据集和测试集，填补了该领域的空白。

### 8. 不足与局限
*   **图像序列长度限制**：目前主要处理 3-4 张图像的短序列，对于极长步骤（如几十步）的复杂过程理解尚待验证。
*   **算力门槛**：虽然针对小模型，但强化学习（RL）过程中的 Rollout 采样对显存和算力仍有一定要求。
*   **场景覆盖**：虽然涵盖了四个领域，但对于更复杂的自然科学实验或长程工业制造流程的覆盖可能仍显不足。

（完）
