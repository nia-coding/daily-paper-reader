---
title: "TopoCurate:Modeling Interaction Topology for Tool-Use Agent Training"
title_zh: TopoCurate：为工具使用智能体训练建模交互拓扑
authors: "Jinluan Yang, Yuxin Liu, Zhengyu Chen, Chengcheng Han, Yueqing Sun, Qi Gu, Hui Su, Xunliang Cai, Fei Wu, Kun Kuang"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.01714v1"
tags: ["query:ai-ux"]
score: 8.0
evidence: 用于训练工具使用智能体和多轮演练的交互拓扑
tldr: 针对工具调用智能体训练中仅依赖结果过滤而忽视交互动态的问题，本文提出 TopoCurate 框架。该框架通过将多轮尝试投影到统一的语义商拓扑中，构建结构化流形以捕捉工具调用与环境反馈的演化。通过双重选择机制，在 SFT 阶段优化轨迹质量，在 RL 阶段提升任务信息量，显著提高了模型在复杂任务中的恢复能力和策略多样性。
selection_source: carryover_cache
motivation: 现有的工具调用智能体训练范式仅关注成功率，忽略了交互过程中的错误恢复能力、冗余性以及任务本身的结构信息。
method: 提出 TopoCurate 框架，通过合并等效状态将线性轨迹转化为语义商拓扑，并基于此拓扑结构为 SFT 和 RL 设计了针对性的数据与任务选择机制。
result: "在 BFCLv3 和 Tau2 Bench 基准测试中，该方法相比现有最先进基线分别在 SFT 和 RL 阶段实现了 4.2% 和 6.9% 的性能提升。"
conclusion: 建模交互拓扑能有效识别高质量训练数据和高价值强化学习任务，是提升工具调用智能体鲁棒性和效率的关键。
---

## 摘要
训练工具使用智能体通常依赖于基于结果的过滤：在成功轨迹上进行有监督微调（SFT），以及在按通过率筛选的任务上进行强化学习（RL）。然而，这种范式忽略了交互动力学：成功的轨迹可能缺乏错误恢复能力或表现出冗余，而通过率无法区分具有结构化信息量的任务与平庸任务。我们提出了 TopoCurate，这是一个交互感知的框架，它将来自同一任务的多轮尝试投影到一个统一的语义商拓扑中。通过合并等效的动作-观察状态，这种投影将分散的线性轨迹转化为结构化的流形，从而显式地捕捉工具调用和环境响应如何驱动有效策略与失败模式之间的分歧。利用这种表示，我们引入了一种双重选择机制：对于 SFT，我们优先选择展示了反思性恢复、语义效率和策略多样性的轨迹，以缓解协变量偏移和模式崩溃；对于 RL，我们选择具有高错误分支率和策略异质性的任务，从而最大化梯度信噪比，以解决稀疏奖励设置中的信号消失问题。在 BFCLv3 和 Tau2 Bench 上的评估表明，TopoCurate 相比最先进的基准模型，在 SFT 和 RL 上分别实现了 4.2% 和 6.9% 的持续提升。我们将很快发布代码和数据以供进一步研究。

## Abstract
Training tool-use agents typically relies on outcome-based filtering: Supervised Fine-Tuning (SFT) on successful trajectories and Reinforcement Learning (RL) on pass-rate-selected tasks. However, this paradigm ignores interaction dynamics: successful trajectories may lack error recovery or exhibit redundancy, while pass rates fail to distinguish structurally informative tasks from trivial ones. We propose \textbf{TopoCurate}, an interaction-aware framework that projects multi-trial rollouts from the same task into a unified semantic quotient topology. By merging equivalent action-observation states, this projection transforms scattered linear trajectories into a structured manifold that explicitly captures how tool invocations and environmental responses drive the divergence between effective strategies and failure modes. Leveraging this representation, we introduce a dual-selection mechanism: for SFT, we prioritize trajectories demonstrating reflective recovery, semantic efficiency, and strategic diversity to mitigate covariate shift and mode collapse; for RL, we select tasks with high error branch ratios and strategic heterogeneity, maximizing gradient Signal-to-Noise Ratio to address vanishing signals in sparse-reward settings. Evaluations on BFCLv3 and Tau2 Bench show that TopoCurate achieves consistent gains of 4.2\% (SFT) and 6.9\% (RL) over state-of-the-art baselines. We will release the code and data soon for further investigations.

---

## 论文详细总结（自动生成）

# TopoCurate: 为工具使用智能体训练建模交互拓扑 —— 论文深度总结

## 1. 核心问题与整体含义（研究动机和背景）
论文关注的核心问题是**工具使用智能体（Tool-use Agent）训练中的数据质量瓶颈**。
*   **研究背景**：当前训练智能体主要依赖有监督微调（SFT）成功轨迹和基于通过率筛选任务的强化学习（RL）。
*   **核心痛点**：作者提出了“**结果等效幻觉（Outcome Equivalence Illusion）**”。即现有的“结果导向”过滤机制忽略了交互过程的动态性：
    1.  **SFT 偏差**：简单的成功轨迹可能缺乏错误恢复能力、包含冗余步骤，或导致模型陷入单一模式（Mode Collapse）。
    2.  **RL 梯度消失**：在稀疏奖励下，如果任务的轨迹结构过于单一，会导致策略梯度信噪比（SNR）极低，模型难以学到关键决策边界。
*   **整体含义**：论文旨在通过建模“交互拓扑”来识别真正具有训练价值的轨迹和任务，从而提升智能体的鲁棒性、效率和多样性。

## 2. 方法论：TopoCurate 框架
TopoCurate 的核心思想是将分散的线性交互轨迹投影到一个统一的**语义商拓扑（Semantic Quotient Topology）**中。

### 关键技术细节：
1.  **拓扑建模（Topological Modeling）**：
    *   将“动作-观察”元组定义为状态。
    *   利用语义等效关系（通过 Embedding 相似度计算）合并不同轨迹中的相似状态，将线性链转化为**有向无环图（DAG）**。
    *   在图中计算每个节点的“成功潜力（Success Potential）”。

2.  **SFT 轨迹双重选择机制**：
    *   **反思性恢复（Reflective Recovery, $S_{ref}$）**：优先选择那些经历过性能下降（出错）并最终恢复成功的轨迹。
    *   **语义效率（Semantic Efficiency, $S_{eff}$）**：惩罚冗余循环，优先选择路径更简洁的轨迹。
    *   **分布多样性（Distributional Diversity, $S_{rare}$）**：通过加权稀有分支，防止模型过拟合于常见路径。

3.  **RL 任务结构化选择**：
    *   **错误分支率（Error Branch Ratio, $V_{struct}$）**：选择那些决策点会导致严重后果（成功率骤降）的任务，以提供高信噪比的对比信号。
    *   **策略异质性（Strategic Heterogeneity, $V_{div}$）**：选择具有多种有效解法的任务，增强模型的策略塑性。

## 3. 实验设计
*   **数据集与场景**：
    *   **Tau2 Bench (领域内/IID)**：模拟零售（Retail）、航空（Airline）和电信（Telecom）三个复杂环境，涉及双重控制（用户与环境共同演化）。
    *   **BFCL v3 Multiturn (领域外/OOD)**：伯克利函数调用基准测试，评估模型在未见过的 API 和长上下文中的泛化能力。
*   **基准模型（Backbones）**：使用 Qwen3-8B-Thinking 和 Qwen3-32B-Thinking。
*   **对比方法**：
    *   **APIGen-MT**：基于 LLM 委员会验证的合成数据方法。
    *   **MUA**：基于结果过滤和通过率启发式的 RL 方法。
    *   **Simia-Tau**：利用推理模型模拟环境反馈的方法。

## 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号（如 A100 或 H100）及具体数量。
*   **训练细节**：提到了 SFT 阶段使用全局 Batch Size 256，训练 3 个 Epoch；RL 阶段使用 GRPO 算法，Rollout Batch Size 为 1024，训练 80 个 Step（约 8 个 Epoch）。
*   **Embedding 模型**：使用了 `jina-embeddings-v2-base-en` 进行拓扑构建。

## 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了从 8B 到 32B 的模型缩放。
    *   在 Tau2 的三个子领域分别进行了 Pass@1, 4, 8 的详尽测试。
    *   进行了 8 次重复实验以确保结果的统计稳定性。
*   **消融实验**：
    *   针对 SFT 的三个指标（恢复、效率、多样性）进行了拆解实验。
    *   针对 RL 的两个结构化指标进行了对比实验。
    *   分析了 SFT 初始化质量与 RL 任务选择对最终性能的协同贡献。
*   **评价**：实验设计非常充分，不仅验证了最终指标，还深入分析了模型行为（如反思率、交互轮数等），具有较高的客观性和公平性。

## 6. 主要结论与发现
1.  **性能显著提升**：TopoCurate 在 SFT 阶段平均提升 4.2%，在 RL 阶段平均提升 6.9%，刷新了 Tau2 Bench 的 SOTA。
2.  **打破结果等效**：证明了“成功轨迹并不平等”，包含错误恢复过程的轨迹对提升智能体鲁棒性至关重要。
3.  **梯度效率**：通过选择结构复杂的任务，RL 训练的收敛速度和最终奖励显著优于基于通过率的随机采样。
4.  **泛化能力**：在领域外（OOD）的 BFCL 基准上同样表现优异，说明拓扑学习有助于模型掌握通用的工具调用逻辑。

## 7. 优点
*   **视角新颖**：首次将拓扑流形引入智能体数据治理，从“关注结果”转向“关注交互结构”。
*   **理论扎实**：将数据选择机制与 KL 散度最小化（SFT）和 Fisher 信息最大化（RL）等数学理论相结合。
*   **行为优化**：不仅提升了成功率，还显著降低了智能体的冗余交互轮数，提升了推理效率。

## 8. 不足与局限
*   **计算开销**：构建拓扑结构需要对大量轨迹进行两两语义比对和 Embedding 计算，虽然是离线过程，但在超大规模数据集上的扩展性仍有待观察。
*   **种子数据依赖**：该方法依赖于初始生成的候选轨迹池，如果初始 Rollout 质量极差（例如全部失败且无任何探索），拓扑结构将难以提供有效指导。
*   **阈值敏感性**：拓扑合并时的语义相似度阈值（如 0.95）可能需要根据不同领域进行微调。

（完）
