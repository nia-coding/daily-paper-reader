---
title: "FastCode: Fast and Cost-Efficient Code Understanding and Reasoning"
title_zh: FastCode：快速且具有成本效益的代码理解与推理
authors: "Zhonghang Li, Zongwei Li, Yuxuan Chen, Han Shi, Jiawei Li, Jierun Chen, Haoli Bai, Chao Huang"
date: 2026-03-01
pdf: "https://arxiv.org/pdf/2603.01012v2"
tags: ["query:sr-ai"]
score: 9.0
evidence: AI辅助软件工程与仓库级代码推理
tldr: 针对大规模代码库推理中上下文成本高、效率低的问题，本文提出 FastCode 框架。该框架通过解耦代码库探索与内容消耗，利用结构化侦察机制在轻量级语义结构图上导航，精准定位相关代码。实验证明，FastCode 在多个基准测试中显著提升了推理准确性，并大幅降低了 Token 消耗，实现了高效且低成本的代码理解与推理。
selection_source: fresh_fetch
motivation: 现有的智能体方法在处理大规模代码库时，因低效的迭代式全文探索导致计算资源浪费和高昂的上下文成本。
method: 引入结构化侦察机制，通过轻量级语义结构图进行导航和依赖追踪，并结合成本感知策略构建高价值上下文。
result: 在 SWE-QA、LongCodeQA 等多个基准测试中，FastCode 在推理准确度上超越了现有最先进模型，同时显著减少了 Token 使用量。
conclusion: 采用“侦察优先”的策略能有效平衡大规模代码推理的准确性与成本，为高效 AI 辅助软件工程提供了新思路。
---

## 摘要
仓库级代码推理是现代 AI 辅助软件工程的基石，使大语言模型（LLMs）能够处理从程序理解到复杂调试的各种复杂工作流。然而，平衡准确性与上下文成本仍然是一个重大瓶颈，因为现有的智能体（agentic）方法往往通过低效的、迭代式的全文探索浪费计算资源。为此，我们推出了 FastCode，这是一个将仓库探索与内容消耗解耦的框架。FastCode 利用结构化侦察机制在代码库的轻量级语义结构图上进行导航，使系统能够追踪依赖关系并精准定位相关目标，而无需承担全文摄取的开销。通过利用受成本感知策略调节的结构感知导航工具，该框架在单个优化步骤中构建高价值上下文。在 SWE-QA、LongCodeQA、LOC-BENCH 和 GitTaskBench 基准测试上的广泛评估表明，FastCode 在推理准确性方面始终优于最先进的基准模型，同时显著降低了 Token 消耗，验证了“侦察优先”策略在大规模代码推理中的效率。源代码可在 https://github.com/HKUDS/FastCode 获取。

## Abstract
Repository-scale code reasoning is a cornerstone of modern AI-assisted software engineering, enabling Large Language Models (LLMs) to handle complex workflows from program comprehension to complex debugging. However, balancing accuracy with context cost remains a significant bottleneck, as existing agentic approaches often waste computational resources through inefficient, iterative full-text exploration. To address this, we introduce FastCode, a framework that decouples repository exploration from content consumption. FastCode utilizes a structural scouting mechanism to navigate a lightweight semantic-structural map of the codebase, allowing the system to trace dependencies and pinpoint relevant targets without the overhead of full-text ingestion. By leveraging structure-aware navigation tools regulated by a cost-aware policy, the framework constructs high-value contexts in a single, optimized step. Extensive evaluations on the SWE-QA, LongCodeQA, LOC-BENCH, and GitTaskBench benchmarks demonstrate that FastCode consistently outperforms state-of-the-art baselines in reasoning accuracy while significantly reducing token consumption, validating the efficiency of scouting-first strategies for large-scale code reasoning. Source code is available at https://github.com/HKUDS/FastCode.

---

## 论文详细总结（自动生成）

这是一份关于论文《FastCode: Fast and Cost-Efficient Code Understanding and Reasoning》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在大规模代码库（Repository-scale）推理中，如何在保证**推理准确性**的同时，显著降低**上下文成本（Token 消耗）**。
*   **研究背景**：
    *   现有的 LLM 智能体（Agent）在处理代码仓库时，通常采用“迭代式全文探索”模式，即反复读取文件内容以寻找线索。
    *   这种方式导致了巨大的 Token 开销，且随着对话轮数增加，冗余上下文会干扰模型判断，导致推理效率低下且成本昂贵。
    *   传统的 RAG（检索增强生成）虽然成本低，但往往会割裂代码间的结构化依赖关系（如调用链、继承关系）。

### 2. 方法论
FastCode 的核心思想是**“侦察与消费解耦”**，即先通过轻量级元数据进行“侦察”，确定目标后再进行全文读取。
*   **语义-结构化代码表示**：
    *   **层级化单元**：将仓库解析为文件、类、函数和文档四个层级，仅提取签名、文档字符串等轻量级元数据。
    *   **双索引机制**：结合 BM25（词法检索）和向量嵌入（语义检索）。
    *   **符号感知关系建模**：利用 AST（抽象语法树）构建包含调用图（Call）、依赖图（Dep）和继承图（Inh）的多层有向图。
*   **代码库上下文导航**：
    *   **查询增强**：生成伪代码提示（Pseudocode hints）作为结构锚点，对齐查询意图与代码逻辑。
    *   **结构化侦察**：智能体利用目录遍历和正则搜索工具，仅查看元数据而非全文，快速剪枝无关路径。
*   **成本感知上下文管理**：
    *   **动态预算策略**：根据查询复杂度和仓库熵值动态分配 Token 预算。
    *   **认识置信度（Epistemic Confidence）**：实时评估当前上下文是否足以回答问题，若置信度达标则提前终止搜索。
    *   **优先级排序**：基于相关性、工具发现标记和代码密度对单元进行评分，确保在预算内填入最高价值的内容。

### 3. 实验设计
*   **数据集与基准测试**：
    *   **SWE-QA**：评估跨文件、多跳推理的仓库级问答能力。
    *   **LongCodeQA**：测试在长上下文（最高 1M Token）下的理解力。
    *   **LOC-BENCH**：评估在真实维护场景中的故障定位准确率。
    *   **GitTaskBench**：评估端到端的任务执行能力（如修复 Bug、实现功能）。
*   **对比方法**：
    *   **直接调用**：Zero-shot LLM, Full-context LLM。
    *   **RAG 类**：Func RAG, Sliding RAG, BM25 RAG。
    *   **智能体类**：SWE-agent, OpenHands, LocAgent, Agentless。
    *   **商业工具**：Claude Code, Cursor, Gemini Code, DeepWiki。

### 4. 资源与算力
*   **算力说明**：论文未提及具体的模型训练算力（如 GPU 数量/时长），因为 FastCode 主要是**推理侧的框架**。
*   **模型使用**：实验涵盖了多种后端模型，包括闭源的 Gemini-3-Flash、Claude 3.5/3.7 Sonnet，以及开源的 Qwen3-Coder-30B、DeepSeek-R1-8B 和 Ministral-3B。
*   **API 成本**：详细记录了各模型在 Poe 和 OpenRouter 平台上的 Token 单价，用于计算实验的总成本。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个主流基准测试上进行了广泛评估，涵盖了从定位、问答到执行的全流程。
*   **消融实验**：针对检索（R）、图扩展（G）、成本管理（C）、导航与增强（N&C）等组件进行了详细的消融分析，验证了各模块的贡献。
*   **客观性与公平性**：
    *   使用了 LLM-as-a-judge（如 GPT-5.2）进行评分，减少主观偏差。
    *   对比了从轻量级开源模型到顶级商业工具的广泛梯度，实验设计较为严谨、客观。

### 6. 主要结论与发现
*   **性能卓越**：FastCode 在所有基准测试中均达到或超过了 SOTA（最先进）水平。例如在 LOC-BENCH 上，Acc@1 达到 86.13%，远超之前的 LocAgent（77.74%）。
*   **成本极低**：相比于 OpenHands 等智能体，FastCode 的成本降低了 **15 倍到 2000 倍**不等。在长文本任务中，相比直接输入全文，成本降低了 **90% 以上**。
*   **小模型赋能**：FastCode 能显著提升小参数模型（如 3B）的表现，使其在特定任务上超越未加持的大模型。

### 7. 优点
*   **高效的解耦设计**：通过“先侦察后读取”打破了智能体反复读取全文的成本陷阱。
*   **结构感知力强**：利用多层图结构弥补了传统 RAG 丢失代码逻辑关联的缺陷。
*   **极高的实用价值**：不仅提升了准确率，更解决了 AI 编程工具在企业级应用中昂贵的 Token 成本痛点。

### 8. 不足与局限
*   **静态分析限制**：依赖于 AST 进行关系建模，对于高度动态的语言特性（如 Python 的动态导入或反射）可能存在解析不全的问题。
*   **侦察决策依赖**：如果模型在初始“侦察”阶段做出了错误的剪枝决策，可能会导致关键上下文丢失，虽然框架有纠错机制，但仍存在风险。
*   **环境依赖**：需要预先构建代码索引和图结构，对于频繁变动的实时代码流，索引更新的开销未在文中详细讨论。

（完）
