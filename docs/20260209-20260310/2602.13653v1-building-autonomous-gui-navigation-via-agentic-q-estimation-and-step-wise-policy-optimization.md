---
title: Building Autonomous GUI Navigation via Agentic-Q Estimation and Step-Wise Policy Optimization
title_zh: 通过 Agentic-Q 估计与逐步策略优化构建自主 GUI 导航
authors: "Yibo Wang, Guangda Huzhang, Yuwei Hu, Yu Xia, Shiyin Lu, Qing-Guo Chen, Zhao Xu, Weihua Luo, Kaifu Zhang, Lijun Zhang"
date: 2026-02-14
pdf: "https://arxiv.org/pdf/2602.13653v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 用于GUI导航和决策的自主智能体
tldr: 针对多模态大模型在GUI导航中面临的环境非平稳及优化成本高的问题，本文提出了一种新型框架。该框架包含agentic-Q估计和分步策略优化两个核心组件，通过Q模型评估动作贡献，并利用强化学习在解耦环境下优化策略。该方法降低了数据采集成本并提升了优化效率，使Ovis2.5-9B模型在多个GUI基准测试中表现优异，甚至超越了更大规模的模型。
selection_source: fresh_fetch
motivation: 旨在解决GUI智能体在真实非平稳环境中面临的高昂数据采集成本和策略优化不稳定的难题。
method: 提出了一种结合agentic-Q价值估计与分步策略优化的框架，通过自生成轨迹和环境解耦的强化学习进行训练。
result: 实验表明Ovis2.5-9B在GUI导航和定位任务上表现卓越，性能甚至优于参数量更大的模型。
conclusion: 该研究证明了通过Q值引导的解耦策略优化可以有效提升GUI智能体的自主交互能力与训练效率。
---

## 摘要
多模态大语言模型 (MLLMs) 的最新进展显著推动了图形用户界面 (GUI) 自主智能体的发展。然而，在实际应用中，GUI 智能体经常面临非平稳环境，导致数据整理和策略优化的计算成本高昂。在本报告中，我们介绍了一种新型的以 MLLM 为核心的 GUI 智能体框架，该框架由两个组件组成：Agentic-Q 估计和逐步策略优化。前者旨在优化一个 Q 模型，该模型可以生成逐步价值，以评估给定动作对任务完成的贡献。后者将状态-动作轨迹中的逐步样本作为输入，并通过我们的 Agentic-Q 模型利用强化学习来优化策略。值得注意的是：(i) 所有状态-动作轨迹均由策略本身生成，因此数据收集成本是可控的；(ii) 策略更新与环境解耦，确保了稳定且高效的优化。实证评估表明，我们的框架赋予了 Ovis2.5-9B 强大的 GUI 交互能力，在 GUI 导航和定位基准测试中取得了卓越的性能，甚至超越了更大规模的竞争对手。

## Abstract
Recent advances in Multimodal Large Language Models (MLLMs) have substantially driven the progress of autonomous agents for Graphical User Interface (GUI). Nevertheless, in real-world applications, GUI agents are often faced with non-stationary environments, leading to high computational costs for data curation and policy optimization. In this report, we introduce a novel MLLM-centered framework for GUI agents, which consists of two components: agentic-Q estimation and step-wise policy optimization. The former one aims to optimize a Q-model that can generate step-wise values to evaluate the contribution of a given action to task completion. The latter one takes step-wise samples from the state-action trajectory as inputs, and optimizes the policy via reinforcement learning with our agentic-Q model. It should be noticed that (i) all state-action trajectories are produced by the policy itself, so that the data collection costs are manageable; (ii) the policy update is decoupled from the environment, ensuring stable and efficient optimization. Empirical evaluations show that our framework endows Ovis2.5-9B with powerful GUI interaction capabilities, achieving remarkable performances on GUI navigation and grounding benchmarks and even surpassing contenders with larger scales.

---

## 论文详细总结（自动生成）

这是一份关于论文《Building Autonomous GUI Navigation via Agentic-Q Estimation and Step-Wise Policy Optimization》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于构建能够自主操作图形用户界面（GUI）的智能体。尽管多模态大模型（MLLM）提升了智能体的感知能力，但在实际应用中仍面临三大挑战：
*   **数据整理成本高**：高质量的交互轨迹需要专家标注，成本昂贵。
*   **监督信号稀疏**：GUI 任务通常路径较长，仅在最后一步有成功/失败的反馈，中间步骤缺乏指导。
*   **优化过程不稳定且低效**：现有的强化学习（RL）训练通常与非平稳的 GUI 环境（如网页加载延迟、崩溃）强耦合，导致训练效率低下且难以收敛。

**整体含义**：本文提出了一套新型框架，通过训练一个“Agentic-Q”模型来评估每一步动作的价值，并将策略优化与真实环境解耦，从而实现高效、稳定的 GUI 智能体训练。

### 2. 方法论：核心思想与关键技术
该框架包含三个阶段：
*   **冷启动（Cold Start）**：在基础模型 Ovis2.5-9B 上，利用少量的专家轨迹和定位（Grounding）数据进行有监督微调（SFT），使其具备基本的 GUI 理解和操作能力。
*   **Agentic-Q 估计**：
    *   **核心思想**：训练一个 Q 模型来预测给定状态下某个动作导致任务成功的概率。
    *   **技术细节**：采用“后向标注”策略，将轨迹的最终结果（1 或 0）传播回中间每一步。
    *   **优化策略**：引入**滑动窗口（Sliding-window）**限制历史上下文长度，防止熵崩溃；采用**动作聚焦（Action-focus）**掩盖思维链（Thought）部分，强迫模型关注动作本身的价值。
*   **逐步策略优化（SWPO）**：
    *   **核心思想**：利用 Agentic-Q 模型提供的逐步奖励（Step-wise Reward）代替环境的延迟奖励。
    *   **算法流程**：基于 GRPO、RLOO 等算法的变体，在策略更新时引入**动作/奖励过滤**（剔除多样性差或奖励过于集中的样本）和**自适应组权重**（根据任务难度调整权重，加大对困难任务的关注）。
    *   **环境解耦**：由于奖励由 Q 模型提供，策略更新不再需要实时连接不稳定的网页环境。

### 3. 实验设计
*   **数据集/场景**：
    *   **GUI 定位**：使用 **ScreenSpot v1 & v2**，评估模型在移动端、桌面和网页上的元素定位能力。
    *   **GUI 导航**：使用 **WebVoyager**（包含 Amazon、Google Maps 等 12 个真实网站任务）和 **Online-Mind2Web**（跨 136 个网站的更具挑战性的任务）。
*   **Benchmark 与对比方法**：
    *   对比了闭源模型：**GPT-4o**、**Claude 3.7/4 Sonnet (Computer Use)**。
    *   对比了开源模型：**Qwen3-VL (8B/32B)**、**UI-TARS 系列 (7B/72B)**。
    *   基座模型：**Ovis2.5-9B**。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号（如 A100 或 H100）、GPU 数量以及具体的训练总时长。
*   **效率提及**：作者强调了该框架由于实现了环境解耦，其训练效率显著高于传统的 RLVR（带验证奖励的强化学习）框架。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 WebVoyager 和 Online-Mind2Web 上进行了全面的端到端测试。
    *   针对数据分层，每个任务重复运行了 8 次以确定难度等级。
    *   进行了详细的**消融实验**，验证了滑动窗口大小、动作过滤和自适应权重对性能的影响。
*   **充分性与客观性**：实验涵盖了从基础定位到复杂多步导航的任务，且 Online-Mind2Web 的结果属于分布外（OOD）测试（训练集仅包含 WebVoyager 网站），这有力地证明了模型的泛化能力。实验对比了当前最顶尖的闭源和开源模型，评价标准客观。

### 6. 主要结论与发现
*   **性能卓越**：Ovis2.5-9B 在 SWPO 优化后，在 WebVoyager 上提升了约 10%，在 Online-Mind2Web 上提升了约 11%，性能超越了 UI-TARS-72B 等更大规模的模型，并与 Claude 4 Sonnet 持平。
*   **导航效率提升**：优化后的模型完成任务所需的平均步数显著减少（例如从 22 步降至 13 步）。
*   **边界穿越（Boundary Traversal）行为**：发现模型具备高级推理能力，当在当前网站无法完成任务时，会自动切换到搜索引擎（如 Google）寻找替代方案。
*   **定位能力保持**：在强化学习优化导航能力的同时，模型在 ScreenSpot 上的定位精度并未下降，反而略有提升。

### 7. 优点（亮点）
*   **环境解耦**：解决了 GUI 训练中环境非平稳导致的效率瓶颈，使大规模 RL 训练更具可行性。
*   **自生成数据**：训练轨迹完全由策略自生成，无需昂贵的专家实时标注，降低了成本。
*   **细粒度监督**：Agentic-Q 提供了步级别的指导，有效缓解了长程任务中的奖励稀疏问题。
*   **鲁棒性设计**：通过检测-重跑策略和数据分层，过滤了环境噪声，确保了训练数据的质量。

### 8. 不足与局限
*   **Q 模型依赖性**：策略的上限受限于 Agentic-Q 模型的准确性，如果 Q 模型产生偏差，可能会引导策略进入局部最优。
*   **算力细节缺失**：未披露具体算力消耗，不便于其他研究者评估复现成本。
*   **任务领域限制**：虽然在网页端表现极佳，但在复杂的原生桌面软件或移动端深度交互（如需要多指触控或长按的场景）上的表现尚需更多验证。

（完）
