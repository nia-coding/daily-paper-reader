---
title: "HieraMAS: Optimizing Intra-Node LLM Mixtures and Inter-Node Topology for Multi-Agent Systems"
title_zh: HieraMAS：针对多智能体系统的节点内 LLM 混合与节点间拓扑优化
authors: "Tianjun Yao, Zhaoyi Li, Zhiqiang Shen"
date: 2026-02-23
pdf: "https://arxiv.org/pdf/2602.20229v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 具有节点内 LLM 混合的多智能体系统分层协作框架
tldr: HieraMAS是一个分层协作框架，旨在优化多智能体系统（MAS）。它通过在节点内部引入异构LLM混合（超节点）并结合节点间的通信拓扑，打破了将智能体视为单一单元的局限。该框架采用两阶段算法，通过多级奖励归因和图分类拓扑选择，解决了复杂配置下的信用分配难题，在推理和编程任务中实现了性能与成本的最优平衡。
selection_source: fresh_fetch
motivation: 现有MAS研究通常将智能体视为不可分割的单元，忽视了通过节点内LLM混合来增强特定角色能力以及协同优化拓扑的机会。
method: 提出HieraMAS框架，利用“提议-合成”结构的超节点实现节点内LLM混合，并结合多级奖励归因与图分类算法优化通信拓扑。
result: 在推理和编程基准测试中，HieraMAS显著优于现有方法，并提供了更佳的性价比。
conclusion: 通过分层优化节点内模型组合与节点间拓扑结构，可以有效提升多智能体系统的整体协作效能与任务处理能力。
---

## 摘要
基于大语言模型（LLM）构建的多智能体系统（MAS）在许多任务中表现出强大的性能。大多数现有方法一次只改进一个方面，例如通信拓扑、角色分配或 LLM 路由，同时将每个智能体视为一个单一且不可分割的单元。这错失了在智能体内部使用 LLM 混合来增强特定角色能力的机会。我们提出了 HieraMAS，这是一个将节点内 LLM 混合与节点间通信拓扑相结合的分层协作框架。HieraMAS 引入了超节点（supernodes），其中每个功能角色由多个异构 LLM 使用“提议-综合”（propose-synthesis）结构实现。优化 HieraMAS 带来了独特的信用分配（credit-assignment）挑战：最终任务性能在很大程度上取决于底层 LLM 的能力，这可能导致强化学习方法错误地奖励次优配置。为了解决这个问题，我们使用了一种两阶段算法：（1）多级奖励归因，在节点级别和整个系统级别提供细粒度的反馈；（2）用于拓扑选择的图分类，将选择通信结构视为一个整体决策，而不是逐个优化边。在推理和编程基准测试上的实验表明，HieraMAS 显著优于现有方法，同时还提供了更好的性价比权衡。

## Abstract
Multi-agent systems (MAS) built on large language models (LLMs) have shown strong performance across many tasks. Most existing approaches improve only one aspect at a time, such as the communication topology, role assignment, or LLM routing, while treating each agent as a single, indivisible unit. This misses the opportunity to use mixtures of LLMs within an agent to strengthen role-specific abilities. We propose HieraMAS, a hierarchical collaboration framework that combines intra-node LLM mixtures with an inter-node communication topology. HieraMAS introduces supernodes, where each functional role is implemented by multiple heterogeneous LLMs using a propose-synthesis structure. Optimizing HieraMAS creates unique credit-assignment challenges: final task performance depends heavily on the underlying LLMs' capabilities, which can lead reinforcement methods to incorrectly reward suboptimal configurations. To address this, we use a two-stage algorithm: (1) multi-level reward attribution, which provides fine-grained feedback at both the node level and the overall system level; (2) graph classification for topology selection, which treats choosing the communication structure as a holistic decision rather than optimizing edges one by one. Experiments on reasoning and coding benchmarks show that HieraMAS substantially outperforms existing methods while also delivering better cost-performance trade-offs.

---

## 论文详细总结（自动生成）

以下是对论文《HieraMAS: Optimizing Intra-Node LLM Mixtures and Inter-Node Topology for Multi-Agent Systems》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的多智能体系统（MAS）优化研究通常将每个智能体视为一个“黑盒”单体，仅在宏观层面优化通信拓扑、角色分配或模型路由。这种做法忽视了智能体内部通过不同LLM混合（Intra-node mixture）来增强特定角色能力的潜力。
*   **研究动机**：
    *   **单体局限性**：单个LLM在特定角色（如代码审查、数学分析）上的能力有限。
    *   **信用分配难题**：在复杂的MAS中，最终任务的成功可能掩盖了某个节点的错误，或者失败可能被错误地归因于某个通信边缘，导致强化学习难以收敛。
    *   **协同效应**：作者假设增强节点内部能力可以简化外部通信拓扑，从而实现更高效、更廉价的协作。

### 2. 方法论
HieraMAS 提出了一个分层协作框架，核心思想是将 MAS 节点升级为“超节点（Supernodes）”，并采用两阶段优化算法。
*   **超节点结构**：每个超节点由多个异构 LLM 组成，采用“提议-合成（Propose-Synthesis）”结构。多个 Proposer 生成多样化方案，一个 Synthesizer 进行整合。
*   **关键技术细节**：
    *   **LLM 选择与剪枝**：通过策略网络为超节点内的每个位置选择最合适的 LLM。引入特殊的 `skip` 令牌，若 Synthesizer 选择 skip，则整个角色被剪枝；若 Proposer 选择 skip，则减少内部混合规模。
    *   **两阶段训练算法**：
        *   **第一阶段（超节点优化）**：在随机拓扑下优化 LLM 选择。使用**多级奖励（Multi-level Reward）**，结合节点自身输出的正确性和系统最终奖励，解决节点级信用分配问题。
        *   **第二阶段（拓扑优化）**：固定 LLM 选择器，将拓扑优化视为**整体图分类任务**。从预生成的候选图池中选择最适合当前查询的拓扑，避免了逐条边优化带来的信用分配混乱。
    *   **成本感知奖励函数**：奖励函数结合了任务正确性和 Token 消耗，鼓励系统在保证准确率的前提下选择更廉价的模型。

### 3. 实验设计
*   **数据集/场景**：
    *   **HumanEval++**：增强版代码生成基准。
    *   **MATH**：具有挑战性的多步数学推理任务。
    *   **MMLU-Redux**：去重后的通用知识基准，涵盖 STEM、人文和社会科学。
*   **对比方法（Baselines）**：
    *   **单智能体**：Base 提示词、思维链（CoT）。
    *   **固定 MAS**：自我一致性（SC）、多轮辩论（Debate）、全连接图、随机图。
    *   **学习型 MAS**：AFlow（基于 MCTS 优化工作流）、GDesigner（基于 GNN 优化拓扑）、MASRouter（在预定义拓扑间路由）。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出具体的 GPU 型号和数量**。
*   **模型调用**：实验主要通过 API 调用多种模型，包括 Qwen3 系列（8B, 80B）、DeepSeek 系列（V3, R1-Distill）、Llama-3.1-8B、Gemma-3-27B 以及 GPT-5-Mini/Nano 等。
*   **训练成本**：作者强调了 HieraMAS 的经济性。例如，在 HumanEval++ 上，其训练成本比 AFlow 便宜 18.41 倍。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了编程、数学、通用知识三大领域，实验设置了 GPT-5-Mini 和 Qwen3-80B 两种不同的主模型环境。
*   **消融实验**：对比了“无图优化”和“无 LLM 选择”的情况，验证了各组件的必要性。
*   **充分性与公平性**：
    *   进行了**泛化性分析**，在未见过的 MMLU 类别上测试模型表现。
    *   进行了**成本分析**，对比了不同方法在达到相似性能时的资金消耗。
    *   实验设计较为客观，通过对比多种最先进的学习型 MAS 框架，证明了其在性能和成本上的双重优势。

### 6. 主要结论与发现
*   **性能领先**：HieraMAS 在三大基准上均达到 SOTA，平均准确率达到 94.61%。
*   **性价比高**：通过异构模型混合，HieraMAS 能以更低的成本超越全部使用最强模型（如 GPT-5-Mini）的系统。
*   **拓扑特征**：学习到的最优拓扑通常是**稀疏且不规则**的。特定角色（如“心理学家”或“医生”）常作为汇聚信息的汇点（Sink nodes），而“评论家”常作为信息源（Source nodes）。
*   **内部混合效应**：超节点内部仅需少量 Proposer（如 2 个）即可达到最佳平衡，过多 Proposer 反而可能因上下文过长导致性能下降。

### 7. 优点
*   **分层视角**：首次统一了节点内 LLM 混合与节点间拓扑优化，打破了智能体单体化的假设。
*   **信用分配方案**：通过多级奖励和图分类方法，巧妙规避了强化学习在复杂 MAS 结构中难以收敛的痛点。
*   **自适应剪枝**：利用 `skip` 机制实现了角色级和模型级的动态剪枝，提高了推理效率。

### 8. 不足与局限
*   **图池依赖**：拓扑优化依赖于预生成的候选图池（K=200），虽然实验证明其不敏感，但理论上仍受限于搜索空间的覆盖度。
*   **角色定义依赖**：系统仍需预定义智能体角色（如数学分析师、程序员等），尚未实现完全自动化的角色发现。
*   **API 延迟**：由于超节点涉及多个模型提议，在实际部署中可能会面临比单体智能体更高的网络延迟。

（完）
