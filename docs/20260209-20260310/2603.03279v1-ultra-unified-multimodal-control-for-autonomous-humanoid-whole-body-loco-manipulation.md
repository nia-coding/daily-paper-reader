---
title: "ULTRA: Unified Multimodal Control for Autonomous Humanoid Whole-Body Loco-Manipulation"
title_zh: ULTRA：自主类人机器人全身运动操作的统一多模态控制
authors: "Xialin He, Sirui Xu, Xinyao Li, Runpei Dong, Liuyu Bian, Yu-Xiong Wang, Liang-Yan Gui"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.03279v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 基于感知的类人机器人运动与操作的多模态控制
tldr: 针对人形机器人全身运动控制中数据稀缺、难以扩展及过度依赖预定义参考轨迹的问题，本文提出ULTRA框架。该框架包含物理驱动的神经重定向算法，可将大规模动捕数据转化为物理合理的机器人动作。通过学习统一的多模态控制器，ULTRA支持从密集参考到稀疏任务指令的多种输入，并结合强化学习微调，实现了仅凭第一视角感知即可完成自主的目标导向全身运动控制，在Unitree G1机器人上验证了其优越性。
selection_source: fresh_fetch
motivation: 现有的人形机器人控制方法受限于高质量数据匮乏、技能扩展性差以及对预定义运动参考的过度依赖。
method: 提出一种结合物理驱动神经重定向、多模态控制器蒸馏、技能空间压缩及强化学习微调的统一框架。
result: 在仿真和真实Unitree G1机器人上实现了基于第一视角感知的自主目标导向全身运动控制，性能优于传统跟踪基准。
conclusion: ULTRA框架通过整合多模态感知与物理约束，显著提升了人形机器人在复杂任务中的自主性与鲁棒性。
---

## 摘要
实现自主且通用的全身运动操作（loco-manipulation）仍然是使类人机器人具有实际用途的核心障碍。然而，现有方法受到根本性限制：重定向（retargeted）数据通常稀缺或质量低下；方法难以扩展到大规模技能库；最重要的是，它们依赖于跟踪预定义的运动参考，而不是根据感知和高层任务规范生成行为。为了解决这些局限性，我们提出了 ULTRA，这是一个包含两个关键组件的统一框架。首先，我们引入了一种物理驱动的神经重定向算法，将大规模动作捕捉数据转化为类人机器人构型，同时在富接触交互中保持物理合理性。其次，我们学习了一个统一的多模态控制器，支持稠密参考和稀疏任务规范，感知范围涵盖从精确的动作捕捉状态到带噪声的第一视角（egocentric）视觉输入。我们将通用跟踪策略蒸馏到该控制器中，将运动技能压缩到紧凑的潜空间中，并应用强化学习微调以扩大覆盖范围并提高在分布外（out-of-distribution）场景下的鲁棒性。这使得在测试阶段无需参考运动即可根据稀疏意图实现协调的全身行为。我们在仿真环境和真实的 Unitree G1 类人机器人上对 ULTRA 进行了评估。结果表明，ULTRA 能够泛化到基于第一视角感知的自主、目标条件化的全身运动操作，其表现始终优于技能有限的仅跟踪基准方法。

## Abstract
Achieving autonomous and versatile whole-body loco-manipulation remains a central barrier to making humanoids practically useful. Yet existing approaches are fundamentally constrained: retargeted data are often scarce or low-quality; methods struggle to scale to large skill repertoires; and, most importantly, they rely on tracking predefined motion references rather than generating behavior from perception and high-level task specifications. To address these limitations, we propose ULTRA, a unified framework with two key components. First, we introduce a physics-driven neural retargeting algorithm that translates large-scale motion capture to humanoid embodiments while preserving physical plausibility for contact-rich interactions. Second, we learn a unified multimodal controller that supports both dense references and sparse task specifications, under sensing ranging from accurate motion-capture state to noisy egocentric visual inputs. We distill a universal tracking policy into this controller, compress motor skills into a compact latent space, and apply reinforcement learning finetuning to expand coverage and improve robustness under out-of-distribution scenarios. This enables coordinated whole-body behavior from sparse intent without test-time reference motions. We evaluate ULTRA in simulation and on a real Unitree G1 humanoid. Results show that ULTRA generalizes to autonomous, goal-conditioned whole-body loco-manipulation from egocentric perception, consistently outperforming tracking-only baselines with limited skills.

---

## 论文详细总结（自动生成）

### ULTRA：自主类人机器人全身运动操作的统一多模态控制

#### 1. 核心问题与研究动机
类人机器人在实际应用中面临“全身运动操作（Loco-Manipulation）”的挑战。现有方法存在三大瓶颈：
*   **数据质量差：** 传统的运动重定向（Retargeting）往往忽略物理约束，导致机器人与物体交互时出现穿模或打滑。
*   **扩展性不足：** 难以将技能扩展到大规模、多样化的动作库。
*   **过度依赖参考：** 现有的控制器大多只能“复现”预定义的参考轨迹，缺乏根据感知（如第一视角视觉）和稀疏指令（如“走到那并拿起盒子”）自主生成行为的能力。
**ULTRA** 旨在构建一个统一的框架，使机器人能平滑切换于“稠密轨迹跟踪”与“稀疏目标导向”之间。

#### 2. 方法论：核心思想与技术细节
ULTRA 采用四阶段训练范式，核心在于将物理驱动的数据生成与多模态策略蒸馏相结合：
*   **阶段 1：物理驱动的神经重定向（Neural Retargeting）：** 将人类动捕数据（SMPL-X）映射到机器人（Unitree G1）。不同于传统的运动学优化，它将重定向建模为强化学习（RL）任务，利用物理引擎强制执行接触约束和动力学一致性，并支持对物体和轨迹进行零样本（Zero-shot）缩放增强。
*   **阶段 2：特权教师策略（Privileged Teacher）：** 在仿真中训练一个拥有全知状态（物体精确位姿、稠密参考轨迹）的教师模型，学习如何稳定地跟踪重定向后的运动。
*   **阶段 3：多模态学生策略蒸馏：** 使用 Transformer 架构，通过“可用性掩码（Availability Masking）”技术，将教师的技能蒸馏给学生。学生模型仅接收部分观测（如本体感知、第一视角点云、稀疏目标），掩码机制使其在某些传感器失效或参考轨迹缺失时仍能保持稳定。
*   **阶段 4：强化学习微调（RL Finetuning）：** 在蒸馏基础上引入任务导向的 RL 奖励，扩大机器人在分布外（OOD）场景下的覆盖范围，增强其在面对噪声感知时的闭环稳定能力。

#### 4. 实验设计与对比基准
*   **实验平台：** 仿真环境使用 IsaacGym，物理验证使用 MuJoCo，硬件部署使用 **Unitree G1** 类人机器人。
*   **数据集：** 使用 OMOMO 大规模人-物交互动捕数据集，并进行了 6 倍的数据增强。
*   **对比基准（Baselines）：**
    *   **重定向对比：** PHC、GMR、OmniRetarget（运动学方法）。
    *   **跟踪与控制对比：** HDMI、OmniRetarget、以及 ULTRA 的消融版本（如无 RL 微调版本）。
*   **评估指标：** 成功率（Succ）、穿模深度、足部滑动距离、接触漂移时间、MPJPE（关节位置误差）等。

#### 5. 资源与算力
*   **仿真规模：** 论文提到在 IsaacGym 中使用了 **4096 个并行环境** 进行大规模训练。
*   **具体算力：** 文中**未明确说明**具体的 GPU 型号（如 A100 或 H100）及总训练时长。但根据此类研究的惯例，通常需要高性能 GPU 集群支持数天的训练。

#### 6. 实验数量与充分性
*   **实验规模：** 论文进行了多维度的评估，包括：
    *   **重定向质量评估：** 针对不同物体（大盒子、手提箱）的物理一致性测试。
    *   **运动跟踪实验：** 包含分布内（ID）和分布外（OOD）动作的定量对比。
    *   **消融实验：** 验证了 RL 微调对 OOD 目标的提升（成功率提升达 80%-200%）。
    *   **真机部署：** 在 Unitree G1 上完成了基于动捕和基于第一视角点云的多种任务。
*   **充分性评价：** 实验设计较为全面，涵盖了从底层物理指标到高层任务成功率的对比，且通过真机实验证明了算法的迁移能力，具有较高的客观性和说服力。

#### 7. 主要结论与发现
*   **物理驱动优于运动学：** 物理约束的重定向能显著减少足部滑动和接触失效，生成更高质量的训练数据。
*   **统一模型的优越性：** 单个 ULTRA 模型在有参考时能精准跟踪，在无参考时能通过感知自主完成任务，性能优于专门的跟踪模型。
*   **RL 微调的关键作用：** 蒸馏能保留技能原型，而 RL 微调能显著提升模型在面对随机目标偏移和感知噪声时的鲁棒性。
*   **语义化潜空间：** 发现学习到的运动潜空间（Latent Space）在语义上是自然聚类的，能够区分不同的控制模式和动作意图。

#### 8. 优点与亮点
*   **全能型控制器：** 首次在类人机器人上实现了从稠密跟踪到稀疏目标感知的单一策略控制。
*   **数据增强能力：** 物理驱动的重定向允许对物体大小和动作轨迹进行自由缩放，解决了高质量交互数据匮乏的问题。
*   **感知鲁棒性：** 通过掩码机制和点云处理，使机器人能够在不依赖外部动捕系统的情况下，仅凭机载传感器完成复杂操作。

#### 9. 不足与局限
*   **感知限制：** 在极端遮挡或深度图噪声剧烈的情况下，点云提取可能失效，导致任务失败。
*   **应用边界：** 对于过于激进或物理上不可行的操作指令（如目标跳变过大），模型仍可能崩溃。
*   **硬件依赖：** 实验主要针对 Unitree G1，虽然理论上通用，但在不同构型机器人上的泛化成本（如重定向映射）仍需进一步验证。
*   **触觉缺失：** 目前主要依赖视觉和本体感知，缺乏触觉反馈，这在精细操作（如防止物体滑落）中可能是一个短板。

（完）
