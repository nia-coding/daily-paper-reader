---
title: "SWE-MiniSandbox: Container-Free Reinforcement Learning for Building Software Engineering Agents"
title_zh: SWE-MiniSandbox：用于构建软件工程智能体的无容器强化学习
authors: "Danlong Yuan, Wei Wu, Zhengren Wang, Xueliang Zhao, Huishuai Zhang, Dongyan Zhao"
date: 2026-02-11
pdf: "https://arxiv.org/pdf/2602.11210v1"
tags: ["query:sr-ai"]
score: 10.0
evidence: 用于软件工程智能体的无容器强化学习
tldr: "强化学习是训练软件工程（SWE）智能体的关键，但传统容器化方案存在存储开销大、启动慢等问题。本文提出 SWE-MiniSandbox，一种无需容器的轻量级沙箱方法。它利用内核级隔离机制和环境预缓存技术，在保证隔离性的同时显著降低系统开销。该方法将磁盘占用降至容器方案的5%，环境准备时间缩短至25%，且性能表现相当，为资源受限环境下的智能体大规模训练提供了高效基础。"
selection_source: fresh_fetch
motivation: 传统的基于容器的强化学习训练流程在存储、启动速度和权限管理方面存在巨大开销，限制了软件工程智能体的规模化训练。
method: 提出一种基于内核级隔离机制和轻量级环境预缓存的无容器沙箱方法，通过隔离工作区替代笨重的容器镜像。
result: "实验表明该方法将磁盘占用减少了95%，环境准备时间缩短了75%，且在评估性能上与标准容器方案持平。"
conclusion: SWE-MiniSandbox 为大规模训练强化学习驱动的软件工程智能体提供了一个实用、高效且低门槛的基础设施方案。
---

## 摘要
强化学习 (RL) 已成为训练软件工程 (SWE) 智能体的关键范式，但现有的流水线通常依赖于每个任务的容器来实现隔离。在大规模应用时，预构建的容器镜像会产生巨大的存储开销，导致环境搭建缓慢，并需要容器管理权限。我们提出了 SWE-MiniSandbox，这是一种轻量级的无容器方法，能够在不牺牲隔离性的情况下实现 SWE 智能体的大规模强化学习训练。SWE-MiniSandbox 不再依赖于每个实例的容器，而是在由内核级机制支持的隔离工作空间中执行每个任务，从而大幅降低了系统开销。它利用轻量级环境预缓存技术，消除了对庞大容器镜像的需求。结果表明，我们的方法将磁盘占用降低到基于容器的流水线所需的大约 5%，并将环境准备时间缩短到容器基准的大约 25%。实验结果表明，SWE-MiniSandbox 达到了与标准基于容器的流水线相当的评估性能。通过消除对沉重容器基础设施的依赖，SWE-MiniSandbox 为扩展基于强化学习的 SWE 智能体提供了一个实用且易于获取的基础，特别是在资源受限的研究环境中。

## Abstract
Reinforcement learning (RL) has become a key paradigm for training software engineering (SWE) agents, but existing pipelines typically rely on per-task containers for isolation. At scale, pre-built container images incur substantial storage overhead, slow environment setup, and require container-management privileges. We propose SWE-MiniSandbox, a lightweight, container-free method that enables scalable RL training of SWE agents without sacrificing isolation. Instead of relying on per-instance containers, SWE-MiniSandbox executes each task in an isolated workspace backed by kernel-level mechanisms, substantially reducing system overhead. It leverages lightweight environment pre-caching techniques to eliminate the need for bulky container images. As a result, our approach lowers disk usage to approximately 5\% of that required by container-based pipelines and reduces environment preparation time to about 25\% of the container baseline. Empirical results demonstrate that SWE-MiniSandbox achieves evaluation performance comparable to standard container-based pipelines. By removing the dependency on heavy container infrastructure, SWE-MiniSandbox offers a practical and accessible foundation for scaling RL-based SWE agents, particularly in resource-constrained research environments.

---

## 论文详细总结（自动生成）

这是一份关于论文《SWE-MiniSandbox: Container-Free Reinforcement Learning for Building Software Engineering Agents》的结构化深入总结：

### 1. 核心问题与整体含义
在训练能够解决复杂软件工程（SWE）任务的智能体时，强化学习（RL）已成为主流范式。然而，现有的训练框架（如 SWE-agent, SkyRL）高度依赖**容器化（如 Docker）**来保证任务环境的隔离性和可复现性。这带来了三大痛点：
*   **存储开销巨大**：每个任务或镜像动辄数 GB，大规模训练（如 SWE-bench 的数千个任务）需要 TB 级的磁盘空间。
*   **启动延迟高**：容器的创建、镜像拉取和运行时初始化非常耗时，成为 RL 训练中 Rollout（采样）阶段的瓶颈。
*   **权限与门槛**：运行容器通常需要 root 或特定管理权限，限制了在普通科研环境或受限服务器上的部署。

本论文提出了 **SWE-MiniSandbox**，旨在通过一种**无容器（Container-Free）**的轻量级沙箱方案，在不牺牲隔离性的前提下，显著提升 SWE 智能体训练的效率并降低资源门槛。

### 2. 方法论
核心思想是利用 Linux 内核原生的轻量级隔离机制替代沉重的容器运行时。

*   **内核级隔离**：
    *   使用 `mount namespaces`（挂载命名空间）为每个任务实例创建独立的视图。
    *   结合 `chroot` 命令切换根目录，确保进程只能访问其私有的工作目录，实现文件系统隔离。
    *   通过 `bind-mount` 将必要的系统目录（如 `/dev`, `/root`）和共享的 Python 基础环境挂载到沙箱中。
*   **轻量级环境预缓存**：
    *   **venv 替代 Conda**：由于 Conda 环境过于庞大且复杂，该方法采用 Python 原生 `venv`。每个任务环境仅需约 100MB。
    *   **打包与重用**：将配置好的虚拟环境和代码仓库打包成 `tar.gz` 缓存。在训练时，直接解压到沙箱目录。
*   **I/O 瓶颈管理**：
    *   **并发控制模型**：建立 I/O 预算模型 $B$，通过公式 $\sum_{j=1}^{C} b_j \le B$ 限制并发解压任务数 $C$，防止大规模并行采样时磁盘 I/O 饱和。
    *   **分布式支持**：集成 Ray 框架，支持多节点扩展，通过资源标签（Resource Tags）管理各节点的 I/O 负载。

### 3. 实验设计
*   **数据集**：
    *   **SWE-bench Verified**：包含 500 个高质量 Python 任务，用于最终性能评估。
    *   **SWE-smith**：包含 50k 个自动生成的任务，用于大规模 RL 训练。
*   **对比基准（Benchmark）**：
    *   标准的基于 **Docker** 的容器化训练流水线。
*   **对比方法与模型**：
    *   基础模型：Qwen2.5-3B/7B-Coder-Instruct。
    *   对比维度：存储占用、环境准备时间、Rollout 效率、RL 训练后的任务解决成功率。

### 4. 资源与算力
*   **硬件配置**：
    *   单节点配备 **8 × NVIDIA B200 GPU**。
    *   184 个 CPU 核心，800GB SSD 存储。
*   **对比节点**：
    *   Docker 服务器配置为 32 CPU 核心和 2TB SSD。
*   **训练细节**：
    *   RL 训练设置：Batch Size 为 16，每个样本进行 8 次 Rollout（即 128 个并行环境）。
    *   最大交互时间限制为 300 秒。

### 5. 实验数量与充分性
论文进行了多维度的实验：
*   **性能一致性实验**：对比了在 Docker 和 MiniSandbox 下训练出的模型在 SWE-bench 上的得分，证明了该方案不会降低训练质量。
*   **效率压力测试**：测试了从 64 到 320 个并行环境下的扩展性，涵盖单节点和双节点场景。
*   **存储对比**：对比了 SWE-Gym、SWE-Mirror 等多种方案的磁盘占用。
*   **消融与分解**：详细分析了环境准备时间的各个组成部分（初始化、仓库复制、venv 解压等）。
*   **充分性评价**：实验设计较为全面，不仅关注准确率，还深入探讨了系统层面的开销，且通过“评估一致性”实验（Verify-c vs Verify-s）证明了沙箱环境的可靠性，实验结果客观公平。

### 6. 主要结论与发现
*   **资源大幅节省**：磁盘占用仅为容器方案的 **5%**（例如在 SWE-smith 上从 295GB 降至 13.5GB）。
*   **速度显著提升**：环境准备时间缩短至容器方案的 **25%**（从约 90 秒降至 23 秒左右）。
*   **性能持平**：在 SWE-bench Verified 上的评估得分与 Docker 方案几乎一致（例如 7B 模型从 7.8 提升至 11.8-12.4 左右，两种环境表现相当）。
*   **扩展性良好**：在多节点配置下表现出近乎线性的扩展能力，有效缓解了单机 I/O 压力。

### 7. 优点
*   **极高的实用性**：为资源受限的实验室提供了进行大规模 SWE 智能体训练的可能性。
*   **无缝集成**：作为插件式后端，兼容现有的 SWE-agent 和 SkyRL 等主流框架。
*   **系统级优化**：不仅是简单的脚本封装，还引入了 I/O 预算模型和内核级隔离，技术方案扎实。

### 8. 不足与局限
*   **语言局限性**：目前主要针对 Python 环境进行了优化和测试，对于需要复杂系统级依赖（如特定 C 库、非 Python 语言）的任务，其隔离效果和配置难度可能增加。
*   **内核依赖**：依赖 Linux 特有的命名空间机制，在非 Linux 系统（如 macOS/Windows）上无法直接原生运行。
*   **网络隔离风险**：虽然实现了文件系统隔离，但在网络层面的精细化隔离（如完全禁用外网）相比 Docker 的 Network Namespace 配置可能稍显简陋，存在一定的安全或干扰风险（尽管文中提到通过 local httpbin 解决部分问题）。

（完）
