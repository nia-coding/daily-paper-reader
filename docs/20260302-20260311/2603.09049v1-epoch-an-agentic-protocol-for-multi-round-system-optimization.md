---
title: "EPOCH: An Agentic Protocol for Multi-Round System Optimization"
title_zh: EPOCH：一种用于多轮系统优化的智能体协议
authors: "Zhanlin Liu, Yitao Li, Munirathnam Srikanth"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09049v1"
tags: ["query:ai-ux"]
score: 9.0
evidence: 用于多轮系统优化和任务自动化的智能体协议
tldr: 针对当前自主智能体在系统优化中缺乏统一协议且多为特定任务设计的问题，本文提出了EPOCH。这是一个面向多轮系统优化的工程协议，通过基准构建和迭代自我改进两个阶段，结合角色受限的阶段划分（规划、执行、评估）以及标准化的执行接口，实现了对提示词、代码及模型配置的协同优化，显著提升了生产环境下的稳定性、可追溯性和评估完整性。
selection_source: fresh_fetch
motivation: 现有的自主优化方法通常针对特定任务设计，缺乏一个统一的协议来管理多轮自我改进过程并确保评估的严谨性。
method: EPOCH将优化过程分为基准构建和迭代改进两个阶段，并通过角色受限的阶段划分和标准化命令接口来规范执行。
result: 该协议实现了跨提示词、模型配置和代码的协调优化，同时保证了优化过程的稳定性、可重复性和可追溯性。
conclusion: EPOCH为生产导向的自主改进工作流提供了一个实用且标准化的框架，有效提升了异构环境下系统优化的效率。
---

## 摘要
自主智能体越来越多地被用于通过迭代执行和反馈来改进提示词、代码和机器学习系统。然而，现有的方法通常被设计为特定任务的优化循环，而不是用于建立基准和管理受追踪的多轮自我改进的统一协议。我们介绍了 EPOCH，这是一种用于异构环境下多轮系统优化的工程协议。EPOCH 将优化组织为两个阶段：基准构建和迭代自我改进。它通过角色受限的阶段进一步结构化每一轮，将规划、实现和评估分离，并通过规范的命令接口和轮级追踪来标准化执行。这种设计实现了跨提示词、模型配置、代码和基于规则的组件的协同优化，同时保持了评估的稳定性、可复现性、可追溯性和完整性。在各种任务中的实证研究说明了 EPOCH 在面向生产的自主改进工作流中的实用性。

## Abstract
Autonomous agents are increasingly used to improve prompts, code, and machine learning systems through iterative execution and feedback. Yet existing approaches are usually designed as task-specific optimization loops rather than as a unified protocol for establishing baselines and managing tracked multi-round self-improvement. We introduce EPOCH, an engineering protocol for multi-round system optimization in heterogeneous environments. EPOCH organizes optimization into two phases: baseline construction and iterative self-improvement. It further structures each round through role-constrained stages that separate planning, implementation, and evaluation, and standardizes execution through canonical command interfaces and round-level tracking. This design enables coordinated optimization across prompts, model configurations, code, and rule-based components while preserving stability, reproducibility, traceability, and integrity of evaluation. Empirical studies in various tasks illustrate the practicality of EPOCH for production-oriented autonomous improvement workflows.

---

## 论文详细总结（自动生成）

这是一份关于论文《EPOCH: An Agentic Protocol for Multi-Round System Optimization》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：虽然自主智能体在改进提示词、代码和机器学习系统方面表现出潜力，但现有方法多为“特定任务”的优化循环（如 DSPy 针对提示词，RepairAgent 针对代码修复），缺乏一个统一的、可用于生产环境的工程协议。
*   **核心问题**：如何在异构环境下（涉及提示词、代码、配置等多种组件）建立一个稳定、可复现、可追溯且具备评估完整性的多轮自我改进协议？
*   **整体含义**：EPOCH 并非一个新的优化算法，而是一个**工程协议层**。它将优化过程标准化，类似于决策领域的 OODA（观察-判断-决策-行动）循环，旨在解决生产系统中多组件协同优化时的混乱和不可控问题。

### 2. 方法论
EPOCH 将优化过程分为两个阶段，并定义了六种受限角色：
*   **第一阶段：基准构建 (Phase I)**
    *   **Seed Planner**：分析任务规范，设计初始系统和评估接口。
    *   **Baseline Executor**：将设计转化为可运行的实现，生成初始度量指标。
*   **第二阶段：多轮自我改进 (Phase II)**
    *   **Orchestrator (编排者)**：管理控制流、预算、重试机制和状态转换。
    *   **Investigator (调查者)**：分析当前系统的失败模式，提出改进假设（仅限访问训练数据）。
    *   **Executor (执行者)**：在允许的动作空间内实施更改（如修改代码、调整超参数）。
    *   **Reviewer (评审者)**：在标准接口下评估候选状态，决定接受或拒绝（确保评估独立性）。
*   **关键技术细节**：
    *   **角色分离**：提出假设的人（调查者）与评估结果的人（评审者）必须分离，以防止数据泄露或过拟合。
    *   **规范化接口**：所有运行使用统一的命令接口和轮级（Round-level）追踪，记录每次更改的证据、Delta 和决策。

### 3. 实验设计
论文通过四个具有代表性的异构任务验证了协议的通用性：
*   **代码改进 (Fibonacci Calculator)**：优化斐波那契计算器的性能。目标是在 2 秒内计算 $10^6$。
*   **超参数微调 (MNIST)**：使用 MobileNetV2 在 MNIST 子集上微调。优化目标是准确率，限制超参数范围和过拟合阈值。
*   **提示词微调 (SST-2)**：对情感分类任务的提示词进行迭代优化。强制执行训练/评估集分离和泄露检查。
*   **基于规则的优化 (Iris)**：优化鸢尾花分类的符号规则集。
*   **对比基准**：实验主要关注协议在不同任务中的**行为表现**（如重试行为、自动终止、评估完整性），而非单纯追求 SOTA 性能。

### 4. 资源与算力
*   **模型使用**：提示词微调任务明确使用了 `gpt-4.1-nano`。
*   **算力细节**：文中**未详细说明**具体的 GPU 型号、数量或总训练时长。
*   **实验规模**：由于侧重于协议的可解释性，实验使用了较小规模的数据集（如 MNIST 训练集 200 张图，SST-2 训练集 20 个样本），以便于观察每一轮的决策逻辑。

### 5. 实验数量与充分性
*   **实验组数**：共进行了 4 组核心实验，涵盖了代码、超参数、提示词和符号规则四个维度。
*   **充分性评价**：
    *   **客观性**：实验展示了协议在面对性能下降时的“拒绝”和“重试”行为，证明了其非盲目改进。
    *   **局限性**：实验样本量较小，主要验证了协议的**可行性**和**鲁棒性**，但在大规模、高并发生产环境下的压力测试尚不充分。

### 6. 主要结论与发现
*   **协议通用性**：EPOCH 能够成功协调不同类型的优化任务，保持统一的编排结构。
*   **稳定性与可追溯性**：通过轮级追踪，系统演进的每一步（为什么改、改了什么、结果如何）都清晰可见。
*   **自动终止机制**：协议能识别收益递减点。例如在斐波那契任务中，当性能达到原生库极限时，协议会自动终止优化。
*   **评估完整性**：通过角色分离和严格的训练/测试隔离，有效防止了迭代优化中常见的过拟合和数据泄露问题。

### 7. 优点
*   **工程严谨性**：将 AI 优化从“黑盒搜索”提升为“可审计的工程流程”。
*   **角色约束设计**：通过限制 Investigator 和 Reviewer 的权限，解决了自主智能体优化中常见的“自我作弊”风险。
*   **高度可配置**：支持根据任务需求启用或禁用特定阶段（如已有基准则跳过 Phase I）。

### 8. 不足与局限
*   **实验规模较小**：目前的实证研究多基于小型数据集，在大规模复杂系统上的表现有待验证。
*   **单目标限制**：目前主要针对单一目标的迭代优化，尚未深入探讨多个相互关联的子系统（如训练流水线+推理策略）的并行协同优化。
*   **依赖底层模型能力**：协议的效果高度依赖于担任 Investigator 和 Reviewer 角色的 LLM 的推理能力。

（完）
