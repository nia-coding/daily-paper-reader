---
title: "Act-Observe-Rewrite: Multimodal Coding Agents as In-Context Policy Learners for Robot Manipulation"
title_zh: Act-Observe-Rewrite：作为机器人操纵上下文内策略学习者的多模态编程智能体
authors: Vaishak Kumar
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.04466v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: LLM智能体通过视觉观察合成可执行Python代码来改进机器人操作策略
tldr: 本研究提出Act-Observe-Rewrite (AOR)框架，旨在探索多模态大模型在无需梯度更新、演示或奖励工程的情况下，通过自我反思失败来学习机器人操作。AOR让LLM代理在试验间根据视觉观察和结构化结果，重新编写完整的Python底层控制代码。这种将可解释代码作为策略表示的方法，使代理能诊断系统性故障并重写其根源，实现了不同于传统神经策略的上下文学习，显著提升了机器人操作的灵活性和自主性。
selection_source: fresh_fetch
motivation: 旨在解决机器人学习中对大量演示、梯度更新及复杂奖励工程的依赖问题，探索通过自我推理实现策略优化的可能性。
method: 提出AOR框架，利用LLM作为代理，根据视觉观察和任务反馈在试验间不断重写和优化底层的Python控制器代码。
result: 在三项robosuite机器人操作任务中，该方法在没有任何演示或奖励设计的情况下，均取得了极高的任务成功率。
conclusion: 研究证明将可解释代码作为策略表示，能让LLM通过上下文学习有效地诊断并修复机器人控制中的系统性错误。
---

## 摘要
多模态语言模型能否在没有梯度更新、演示或奖励工程的情况下，通过对其自身失败的推理来学习操纵物理对象？我们认为答案是肯定的，并精确地刻画了其成立的条件。我们提出了“行动-观察-重写”（Act-Observe-Rewrite, AOR）框架。在该框架中，大语言模型（LLM）智能体在视觉观察和结构化回合结果的指导下，通过在尝试之间合成全新的可执行 Python 控制器代码来改进机器人操纵策略。与以往将 LLM 锚定在预定义技能库或使用代码生成进行单次规划合成的工作不同，AOR 将完整的底层电机控制实现作为 LLM 推理的单元，使智能体不仅能改变机器人的动作内容，还能改变其执行方式。核心论点是，将可解释代码作为策略表示，创造了一种与不透明神经策略在性质上完全不同的上下文内学习：智能体可以诊断系统性失败并重写其原因。我们在三个 robosuite 操纵任务中验证了这一点，并报告了令人鼓舞的结果：在没有演示、奖励工程或梯度更新的情况下，智能体实现了极高的成功率。

## Abstract
Can a multimodal language model learn to manipulate physical objects by reasoning about its own failures-without gradient updates, demonstrations, or reward engineering? We argue the answer is yes, under conditions we characterise precisely. We present Act-Observe-Rewrite (AOR), a framework in which an LLM agent improves a robot manipulation policy by synthesising entirely new executable Python controller code between trials, guided by visual observations and structured episode outcomes. Unlike prior work that grounds LLMs in pre-defined skill libraries or uses code generation for one-shot plan synthesis, AOR makes the full low-level motor control implementation the unit of LLM reasoning, enabling the agent to change not just what the robot does, but how it does it. The central claim is that interpretable code as the policy representation creates a qualitatively different kind of in-context learning from opaque neural policies: the agent can diagnose systematic failures and rewrite their causes. We validate this across three robosuite manipulation tasks and report promising results, with the agent achieving high success rates without demonstrations, reward engineering, or gradient updates.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Act-Observe-Rewrite (AOR)** 的框架，旨在利用多模态大语言模型（LLM）作为机器人操纵的“上下文内策略学习者”。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的机器人操纵模型（如 VLA 模型）通常依赖海量的预训练数据、昂贵的梯度更新或复杂的奖励工程。当模型在特定任务中失败时，难以诊断原因并快速适应。
*   **研究动机**：作者探索是否可以利用 LLM 的推理能力，在**无需梯度更新、无需人类演示、无需奖励工程**的情况下，通过分析失败原因并直接重写底层控制代码，实现机器人策略的自我进化。

### 2. 方法论
AOR 框架采用了一种**双时间尺度循环**结构：
*   **快循环（Fast Loop）**：在回合内运行。机器人执行由 LLM 生成的 Python 控制器类（包含视觉处理和电机控制逻辑）。
*   **慢循环（Slow Loop）**：在回合之间运行。多模态 LLM（如 Claude Code）充当智能体，执行以下步骤：
    1.  **观察（Observe）**：接收回合结果（奖励、相位日志、振荡标志等）和关键帧图像（RGB-D）。
    2.  **诊断（Diagnose）**：分析失败是由于视觉偏差、控制逻辑还是参数设置引起的。
    3.  **重写（Rewrite）**：合成全新的 Python 控制器代码，并进行动态编译和验证。
*   **关键技术细节**：
    *   **代码作为策略表示**：不同于调整参数或选择技能，AOR 重写整个控制器实现，允许进行架构级更改（如增加控制相位、修正几何计算公式）。
    *   **安全机制**：通过编译沙箱验证代码，并对输出动作进行强制限幅（Clamping），确保物理安全。

### 3. 实验设计
*   **实验场景**：基于 **robosuite** 仿真平台，使用了 UR5e 机械臂。
*   **任务设置**：
    1.  **Lift（提升）**：抓取红色方块。用于验证视觉校准和快速收敛。
    2.  **PickPlaceCan（抓取放置罐子）**：将可乐罐放入箱子。测试处理系统性感知错误的能力。
    3.  **Stack（堆叠）**：将红色方块堆叠在绿色方块上。高精度任务，测试对复杂物理交互的推理。
*   **对比基准（Benchmark）**：
    *   对比了 **OpenAI Codex (GPT-5.3)**。结果显示 Codex 在相同预算下无法完成任务，强调了底层编程智能体能力的重要性。
    *   定性对比了 Reflexion、Code as Policies、Eureka 等现有框架。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号或训练时长。
*   **原因**：由于 AOR 属于**上下文内学习（In-Context Learning）**，不需要进行模型权重的梯度训练。主要的计算开销在于多模态 LLM 的 API 调用（使用了 Claude-Sonnet-4-x 系列模型）以及仿真环境的运行。

### 5. 实验数量与充分性
*   **实验规模**：
    *   Lift 任务：3 次 LLM 调用即收敛，进行了 4 次随机重复实验。
    *   PickPlaceCan 任务：2 次 LLM 调用收敛，进行了 20 次随机重复实验。
    *   Stack 任务：进行了 20 个版本的控制器迭代，最终达到 91% 成功率。
*   **充分性评价**：实验设计较为**客观且具有针对性**。作者不仅报告了成功案例，还详细记录了失败过程（如 Stack 任务中因夹具碰撞导致的 9% 失败率），展示了智能体如何从 0% 成功率逐步提升。虽然任务种类较少（3 个），但深度分析了视觉管线调试等底层细节，证明了方法的有效性。

### 6. 主要结论与发现
*   **代码表示的优越性**：LLM 能够发现并修复极其隐蔽的系统性 Bug（例如：自动发现 robosuite 中 OpenGL 与 OpenCV 坐标系不一致导致的 y 轴翻转问题），这是神经策略或参数微调无法做到的。
*   **自主感知调试**：智能体能通过图像和代码的对应关系，自主判断是“视觉算错了”还是“控制没跟上”。
*   **性能表现**：在完全没有演示的情况下，AOR 在简单任务上达到 100% 成功率，在复杂堆叠任务上达到 91%。

### 7. 优点与亮点
*   **高度可解释性**：生成的策略是人类可读的 Python 代码，便于审计和调试。
*   **零样本学习**：不需要任何人类演示或预定义的技能库，极大地降低了部署门槛。
*   **闭环进化**：通过视觉反馈闭环，实现了真正的“从错误中学习”，而非简单的单次代码生成。

### 8. 不足与局限
*   **仅限于仿真环境**：尚未在真实机器人上验证，现实世界的噪声和光照变化可能对视觉管线提出更高要求。
*   **搜索效率**：采用顺序迭代，每次只测试一个假设，效率较低。
*   **局部最优风险**：在 Stack 任务中，智能体虽然诊断出了碰撞问题，但在迭代预算内未能找到避障策略，陷入了局部最优。
*   **LLM 依赖性**：框架的效果高度依赖于底层 LLM 的推理和编程能力。

（完）
