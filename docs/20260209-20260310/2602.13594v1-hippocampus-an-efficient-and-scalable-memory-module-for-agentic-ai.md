---
title: "Hippocampus: An Efficient and Scalable Memory Module for Agentic AI"
title_zh: Hippocampus：一种用于智能体 AI 的高效且可扩展的存储模块
authors: "Yi Li, Lianjie Cao, Faraz Ahmed, Puneet Sharma, Bingzhe Li"
date: 2026-02-14
pdf: "https://arxiv.org/pdf/2602.13594v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 智能体 AI 的高效可扩展记忆模块
tldr: 针对智能体AI在长程部署中面临的内存存储与检索效率瓶颈，本文提出了Hippocampus系统。该系统摒弃了传统的高开销向量数据库或知识图谱，转而采用紧凑的二进制签名进行语义搜索，并利用动态小波矩阵（DWM）对无损Token-ID流进行压缩与协同索引。实验证明，Hippocampus在保持高准确率的同时，显著降低了检索延迟和Token开销，实现了极佳的可扩展性。
selection_source: fresh_fetch
motivation: 现有的智能体内存系统依赖稠密向量或知识图谱，导致检索延迟高且存储扩展性差。
method: 引入基于动态小波矩阵（DWM）的内存管理系统，通过二进制签名和压缩Token流实现超快速的域内搜索与重建。
result: 在LoCoMo和LongMemEval基准测试中，检索延迟降低了高达31倍，单次查询的Token占用减少了14倍。
conclusion: Hippocampus为长程智能体提供了一种高效、线性可扩展且低成本的持久化内存解决方案。
---

## 摘要
智能体 AI 需要持久化存储来保存超出大语言模型（LLM）有限上下文窗口的用户特定历史记录。现有的存储系统使用稠密向量数据库或知识图谱遍历（或两者混合），导致高检索延迟和较差的存储可扩展性。我们推出了 Hippocampus，这是一种智能体存储管理系统，它使用紧凑的二进制签名进行语义搜索，并使用无损 Token-ID 流进行精确的内容重构。其核心是动态小波矩阵（DWM），它对这两种流进行压缩和协同索引，以支持在压缩域中的超快速搜索，从而避免了昂贵的稠密向量或图计算。该设计随存储容量线性扩展，使其适用于长周期的智能体部署。实验评估表明，Hippocampus 将端到端检索延迟降低了高达 31 倍，并将单次查询的 Token 占用减少了高达 14 倍，同时在 LoCoMo 和 LongMemEval 基准测试中保持了准确性。

## Abstract
Agentic AI require persistent memory to store user-specific histories beyond the limited context window of LLMs. Existing memory systems use dense vector databases or knowledge-graph traversal (or hybrid), incurring high retrieval latency and poor storage scalability. We introduce Hippocampus, an agentic memory management system that uses compact binary signatures for semantic search and lossless token-ID streams for exact content reconstruction. Its core is a Dynamic Wavelet Matrix (DWM) that compresses and co-indexes both streams to support ultra-fast search in the compressed domain, thus avoiding costly dense-vector or graph computations. This design scales linearly with memory size, making it suitable for long-horizon agentic deployments. Empirically, our evaluation shows that Hippocampus reduces end-to-end retrieval latency by up to 31$\times$ and cuts per-query token footprint by up to 14$\times$, while maintaining accuracy on both LoCoMo and LongMemEval benchmarks.

---

## 论文详细总结（自动生成）

以下是对论文《Hippocampus: An Efficient and Scalable Memory Module for Agentic AI》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的智能体（Agentic AI）内存系统（如基于 RAG 的向量数据库或知识图谱）在处理长程对话和大规模历史记录时，面临**检索延迟高**、**存储扩展性差**以及**Token 消耗成本昂贵**的瓶颈。
*   **背景**：智能体需要在超出 LLM 上下文窗口的情况下保持长期记忆。然而，频繁的嵌入（Embedding）生成、总结（Summarization）和高维向量搜索会导致智能体在“观察-计划-行动-学习”循环中产生严重停滞。
*   **整体含义**：论文提出了一种名为 **Hippocampus**（海马体）的新型内存模块，旨在通过底层数据结构的创新，将内存表示从“以 Token 为中心、依赖嵌入”转向“轻量级、原生压缩”的结构，从而实现超高效的内存插入与检索。

### 2. 方法论
Hippocampus 采用了**双重表示策略**，核心技术包括：
*   **动态小波矩阵（Dynamic Wavelet Matrix, DWM）**：这是系统的核心数据结构。它扩展了传统静态小波矩阵，支持**增量更新（Append-only）**。DWM 将 Token-ID 流和二进制签名流进行协同索引，支持在压缩域内直接进行 `access`（访问）、`rank`（计数）和 `select`（定位）操作。
*   **内容 DWM（Content DWM）**：以无损方式存储 Token-ID 序列，用于在检索到相关片段后精确重构原始文本。
*   **签名 DWM（Signature DWM）**：存储通过**随机索引（Random Indexing）**生成的紧凑二进制签名（Binary Signatures）。
*   **语义哈希与汉明球搜索（Hamming-ball Search）**：
    1.  利用随机投影将高维语义向量转换为 $d$ 位的二进制签名。
    2.  检索时，通过 LLM 提取关键词并转为签名。
    3.  利用 CPU 原生的位运算（XOR 和 POPCOUNT）在 Signature DWM 中进行汉明距离搜索，快速筛选候选内存块。
*   **算法流程**：查询输入 -> 关键词提取 -> 签名生成 -> Signature DWM 汉明搜索 -> 获取元数据（起始/结束索引） -> Content DWM 精确重构内容 -> 返回结果。

### 3. 实验设计
*   **数据集/场景**：
    *   **LoCoMo**：评估极长程对话记忆（包含单跳、多跳、时间推理和开放域任务）。
    *   **LongMemEval (S & M)**：评估聊天助手的长期交互记忆（包括信息提取、知识更新、多会话推理等）。
*   **对比方法**：对比了 6 种最先进（SOTA）的内存模块：ReadAgent, MemoryBank, MemGPT, A-mem, MemoryOS, MemOS。
*   **评估指标**：F1 分数、BLEU-1、准确率（Accuracy）、LLM-as-a-Judge（基于 GPT-5 的评分）、平均 Token 消耗、端到端检索延迟。

### 4. 资源与算力
*   **硬件设施**：使用 HPE DL380a Gen11 服务器。
*   **核心配置**：
    *   **CPU**：2 x Intel Xeon Platinum 8470。
    *   **GPU**：4 x NVIDIA H100 (80GB)。
    *   **内存**：1 TB DDR4 DRAM。
*   **软件环境**：Ubuntu 22.04, Python 3.10, PyTorch 2.7, CUDA 12.9。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流基准测试（LoCoMo 和 LongMemEval）的多个子任务上进行了全面对比。
    *   **消融实验**：针对随机索引维度 $D$ 和签名长度 $d$ 进行了多组实验，验证了性能与准确率的权衡。
    *   **效率分析**：详细分解了检索延迟（搜索时间 vs 其他时间）和内存构建成本（时间与 Token 消耗）。
*   **充分性评价**：实验设计非常充分。不仅涵盖了准确性指标，还深入探讨了工业界关注的推理成本和延迟问题。通过数学证明（附录 G）支撑了汉明空间近似语义搜索的合理性，具有较强的客观性和说服力。

### 6. 主要结论与发现
*   **极高的效率**：相比 SOTA 方法，Hippocampus 将端到端检索延迟降低了高达 **31 倍**，单次查询的 Token 消耗降低了 **14 倍**。
*   **性能保持**：在大幅降低开销的同时，其 F1 分数和 LLM 评分在多数任务中优于或持平于 MemGPT 和 A-mem 等重型系统。
*   **构建优势**：内存构建速度比最快的基线快 5.3 倍，且在索引阶段**完全不消耗 LLM Token**（因为不需要总结或重写）。
*   **线性可扩展性**：DWM 的设计保证了系统随内存容量增长呈线性扩展，适合长周期部署。

### 7. 优点
*   **底层创新**：摒弃了昂贵的向量数据库，利用简洁数据结构（DWM）实现压缩域搜索，思路新颖且高效。
*   **软硬结合**：充分利用现代 CPU 的位运算指令（POPCOUNT），将复杂的向量比对简化为极速的位运算。
*   **低成本**：显著减少了对 LLM API 的依赖（无需在存储阶段调用 LLM 进行总结），大幅降低了运营成本。
*   **理论支撑**：提供了关于汉明距离集中不等式的数学证明，确保了近似检索的可靠性。

### 8. 不足与局限
*   **关键词依赖**：检索的第一步依赖 LLM 提取关键词，如果关键词提取不准确，可能会影响后续的签名匹配。
*   **近似误差**：虽然证明了汉明空间可以近似语义空间，但二进制签名相比原始高维稠密向量仍存在一定的信息损失。
*   **实现复杂度**：相比直接调用现成的向量数据库（如 ChromaDB 或 FAISS），DWM 的实现和维护复杂度更高。
*   **应用限制**：目前主要针对文本/Token 数据，对于多模态（图像、视频）内存的直接支持尚未在文中深入探讨。

（完）
