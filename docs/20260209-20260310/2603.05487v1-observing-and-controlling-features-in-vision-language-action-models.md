---
title: Observing and Controlling Features in Vision-Language-Action Models
title_zh: 观察与控制视觉-语言-动作模型中的特征
authors: "Hugo Buurmeijer, Carmen Amo Alonso, Aiden Swann, Marco Pavone"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.05487v1"
tags: ["query:sr-ai"]
score: 10.0
evidence: 观察和控制具身智能中视觉-语言-动作模型的特征
tldr: 本研究探讨了视觉-语言-动作模型（VLA）的内部机制可解释性。针对VLA多模态及混合架构带来的复杂性，提出了特征可观测性和可控制性概念。通过线性分类器识别表征空间中的特征，并利用基于最优控制的轻量级线性干预手段，在无需微调的情况下实时引导机器人行为。实验证明该方法在pi_0.5和OpenVLA模型上能有效实现任务对齐和偏好调整，提升了具身智能的可控性。
selection_source: fresh_fetch
motivation: 旨在解决大语言模型的可解释性研究成果难以直接迁移至结构更复杂的视觉-语言-动作模型（VLA）的问题。
method: 提出了特征可观测性与可控制性概念，通过线性分类器识别内部特征，并利用基于最优控制的线性干预来引导模型输出。
result: 在pi_0.5和OpenVLA等模型上的仿真实验表明，轻量级干预能可靠地引导机器人行为并保持闭环控制性能。
conclusion: VLA模型具有可解释的内部结构，支持通过在线干预而非微调来实现实时的任务对齐和用户偏好调整。
---

## 摘要
视觉-语言-动作模型 (VLAs) 在具身智能方面取得了显著进展。虽然它们的架构在一定程度上类似于大语言模型 (LLMs)，但由于其多模态输入/输出以及通常由 Transformer 和扩散头组成的混合性质，VLAs 表现出更高的复杂性。这也是为什么 LLMs 中机械可解释性（解释内部模型表示如何与其输出行为相关联）的见解无法简单地迁移到 VLA 对应模型的部分原因。在这项工作中，我们建议通过引入和分析两个主要概念来弥补这一差距：特征可观测性 (feature-observability) 和特征可控性 (feature-controllability)。具体而言，我们首先研究了在表示空间中线性编码的特征，并展示了如何通过线性分类器来观察它们。然后，我们使用一种基于最优控制的极小线性干预，来精确放置内部表示，并将 VLA 的输出引导至目标区域。我们的结果表明，有针对性的轻量级干预可以可靠地引导机器人的行为，同时保持闭环能力。我们通过仿真实验在不同的 VLA 架构（$π_{0.5}$ 和 OpenVLA）上证明，VLAs 具有可解释的内部结构，适用于无需微调的在线自适应，从而实现与用户偏好和任务要求的实时对齐。

## Abstract
Vision-Language-Action Models (VLAs) have shown remarkable progress towards embodied intelligence. While their architecture partially resembles that of Large Language Models (LLMs), VLAs exhibit higher complexity due to their multi-modal inputs/outputs and often hybrid nature of transformer and diffusion heads. This is part of the reason why insights from mechanistic interpretability in LLMs, which explain how the internal model representations relate to their output behavior, do not trivially transfer to VLA counterparts. In this work, we propose to close this gap by introducing and analyzing two main concepts: feature-observability and feature-controllability. In particular, we first study features that are linearly encoded in representation space, and show how they can be observed by means of a linear classifier. Then, we use a minimal linear intervention grounded in optimal control to accurately place internal representations and steer the VLA's output towards a desired region. Our results show that targeted, lightweight interventions can reliably steer a robot's behavior while preserving closed-loop capabilities. We demonstrate on different VLA architectures ($π_{0.5}$ and OpenVLA) through simulation experiments that VLAs possess interpretable internal structure amenable to online adaptation without fine-tuning, enabling real-time alignment with user preferences and task requirements.

---

## 论文详细总结（自动生成）

以下是对论文《Observing and Controlling Features in Vision-Language-Action Models》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：视觉-语言-动作模型（VLA）是具身智能的核心，但其行为往往难以预测且难以实时修正。虽然大语言模型（LLM）在“机械可解释性”和“激活转向（Activation Steering）”方面已有成熟研究，但 VLA 涉及多模态输入、连续动作输出以及物理世界的闭环交互，这些复杂性使得 LLM 的经验无法直接迁移。
*   **核心问题**：如何在不进行模型微调的前提下，通过访问和修改 VLA 的内部表示，实现对机器人行为的实时观察与精确控制，并确保干预后的行为依然自然且能完成任务。

### 2. 方法论
论文提出了一个统一框架，将控制理论中的“可观测性”和“可控性”引入 VLA 的内部表示分析：
*   **特征可观测性（Feature-Observability）**：基于“线性表示假设”，认为机器人状态（位置、姿态、夹爪状态）和动作（位移增量）在 Transformer 的隐藏层中是线性编码的。通过训练线性观测器 $f_\ell(x) = W_\ell x + b_\ell$，从第 $\ell$ 层的激活值 $x$ 中提取特征 $\zeta$。
*   **特征可控性（Feature-Controllability）**：设计了一个线性控制器 $g_\ell(x) = x + u_\ell$。其中 $u_\ell$ 是通过求解一个受限优化问题得到的**最小加性扰动**。其目标是在满足特征约束（如“夹爪必须保持开启”）的前提下，对原始激活值的改动最小，从而保留模型的原始生成能力和自然度。
*   **算法流程**：
    1.  **离线阶段**：在标注数据集上运行前向传播，收集激活值与特征对，训练线性观测器。
    2.  **在线推理阶段**：在每一层（或指定层）计算观测值，若观测值违反用户设定的约束区间，则计算并应用最小线性干预 $u_\ell$，随后继续后续层的传播。

### 3. 实验设计
*   **模型架构**：对比了两种主流架构：**OpenVLA**（纯 Transformer 架构）和 **$\pi_{0.5}$**（Transformer 与流匹配混合架构）。
*   **数据集与场景**：
    *   **Libero**：用于 $\pi_{0.5}$ 的仿真实验，包含 10 个空间操作任务。
    *   **BridgeData V2**：用于 OpenVLA 的数据验证。
*   **Benchmark 与对比方法**：
    *   **No Intervention**：原始模型输出。
    *   **Prompting**：通过改变输入提示词（如加入“open gripper”）来引导模型。
    *   **Control（本文方法）**：在内部表示空间进行线性干预。
*   **评估指标**：约束满足率（能否准确执行指令）和任务成功率（干预后是否还能完成抓取等任务）。

### 4. 资源与算力
*   **推理算力**：实验明确提到使用单张 **NVIDIA RTX 5090 GPU** 进行推理。
*   **计算开销**：由于观测器和控制器均为线性结构且有闭式解，计算开销极低，相对于原始 VLA 的推理时间增加几乎可以忽略不计。
*   **训练时长**：文中未明确给出训练线性观测器的具体时长，但指出这是一种“轻量级”的训练过程，仅需在现有数据集上进行简单的线性回归/分类。

### 5. 实验数量与充分性
*   **实验规模**：针对 10 个不同的机器人操作任务，每种方法在每个任务上进行了 10 次 Rollout（共计数百次闭环实验）。
*   **维度覆盖**：实验涵盖了夹爪状态（开/合）、末端执行器高度（高/低）以及移动速度（快/慢）等多个维度的控制。
*   **充分性评价**：实验设计较为充分，不仅验证了控制的有效性，还通过“扰动强度分析”验证了观测器的鲁棒性。通过对比 Prompting 方法，客观地展示了直接干预表示空间在精确度上的优势。

### 6. 主要结论与发现
*   **线性编码存在**：证实了 VLA 的 Transformer 层确实线性编码了关键的动作和状态特征。
*   **层级差异**：干预在较早的层通常更有效，随着深度增加，激活值的模长（L2-norm）增大，固定强度的干预效果会减弱。
*   **闭环有效性**：尽管 VLA 处于闭环系统中（动作会改变下一步的输入），但源自 LLM 的静态干预技术在 VLA 的动态交互中依然表现出极强的鲁棒性。
*   **性能保持**：本文方法在实现近乎 100% 的约束满足率的同时，仍能保持 90% 以上的任务成功率，远优于简单的提示词引导。

### 7. 优点
*   **非侵入性**：无需对庞大的 VLA 进行昂贵的微调或重新训练。
*   **实时性强**：线性闭式解支持毫秒级的实时在线干预。
*   **自然度高**：通过“最小扰动”原则，避免了暴力修改激活值导致的机器人动作僵硬或崩溃。
*   **通用性**：在两种截然不同的 VLA 架构上均验证有效，证明了框架的普适性。

### 8. 不足与局限
*   **依赖标注数据**：训练观测器仍需要带标签的数据集，对于缺乏标注的野外数据难以直接应用。
*   **特征局限**：目前主要集中在低级物理特征（位置、速度），尚未验证对高级语义特征（如“安全意识”、“物体偏好”）的控制效果。
*   **架构覆盖**：虽然测试了混合架构，但干预主要发生在 Transformer 部分，未探讨如何直接干预流匹配（Flow Matching）或扩散（Diffusion）头。
*   **分布偏移风险**：如果干预导致机器人进入训练数据从未覆盖的极端状态，模型可能会失效。

（完）
