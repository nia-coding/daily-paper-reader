---
title: "SiMO: Single-Modality-Operable Multimodal Collaborative Perception"
title_zh: SiMO：单模态可运行的多模态协同感知
authors: "Jiageng Wen, Shengjie Zhao, Bing Li, Jiafeng Huang, Kenan Ye, Hao Deng"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08240v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 多智能体协作感知与多模态融合
tldr: 针对多智能体协同感知中多模态融合在传感器失效（如LiDAR缺失）时性能大幅下降的问题，本文提出了SiMO框架。该框架通过长度自适应多模态融合（LAMMA）和“预训练-对齐-融合-RD”训练策略，实现了在模态缺失情况下仍能保持语义一致性，并解决了模态竞争问题，确保了各单模态分支的独立性与协同感知的鲁棒性。
selection_source: fresh_fetch
motivation: 现有的多模态协同感知方法在关键传感器失效时，由于特征融合导致的语义不匹配，容易出现感知失败。
method: 提出了SiMO框架，包含长度自适应多模态融合（LAMMA）机制和“预训练-对齐-融合-RD”训练策略，以增强模态缺失时的适应性。
result: 实验证明SiMO能有效对齐多模态特征并保留模态特有特征，在各种单模态缺失场景下均能保持最优性能。
conclusion: SiMO首次解决了协同感知中的单模态可操作性问题，显著提升了多模态感知系统的鲁棒性和灵活性。
---

## 摘要
协同感知整合了多智能体视角，以扩大感知范围并克服遮挡问题。虽然现有的多模态方法利用互补传感器来提高性能，但它们极易发生故障——特别是当激光雷达（LiDAR）等关键传感器不可用时。其根本原因在于特征融合导致了单模态特征与下游模块之间的语义不匹配。本文在协同感知领域首次解决了这一挑战，引入了单模态可运行的多模态协同感知（SiMO）。通过采用所提出的长度自适应多模态融合（LAMMA），SiMO 可以在模态失效期间自适应地处理剩余的模态特征，同时保持语义空间的一致性。此外，利用创新的“预训练-对齐-融合-RD”（Pretrain-Align-Fuse-RD）训练策略，SiMO 解决了现有方法通常忽略的模态竞争问题，确保了每个独立模态分支的独立性。实验表明，SiMO 在有效对齐多模态特征的同时，保留了模态特有特征，使其能够在所有单个模态上保持最佳性能。实现细节可在 https://github.com/dempsey-wen/SiMO 找到。

## Abstract
Collaborative perception integrates multi-agent perspectives to enhance the sensing range and overcome occlusion issues. While existing multimodal approaches leverage complementary sensors to improve performance, they are highly prone to failure--especially when a key sensor like LiDAR is unavailable. The root cause is that feature fusion leads to semantic mismatches between single-modality features and the downstream modules. This paper addresses this challenge for the first time in the field of collaborative perception, introducing Single-Modality-Operable Multimodal Collaborative Perception (SiMO). By adopting the proposed Length-Adaptive Multi-Modal Fusion (LAMMA), SiMO can adaptively handle remaining modal features during modal failures while maintaining consistency of the semantic space. Additionally, leveraging the innovative "Pretrain-Align-Fuse-RD" training strategy, SiMO addresses the issue of modality competition--generally overlooked by existing methods--ensuring the independence of each individual modality branch. Experiments demonstrate that SiMO effectively aligns multimodal features while simultaneously preserving modality-specific features, enabling it to maintain optimal performance across all individual modalities. The implementation details can be found in https://github.com/dempsey-wen/SiMO.

---

## 论文详细总结（自动生成）

### 论文总结：SiMO —— 单模态可运行的多模态协同感知

#### 1. 核心问题与整体含义
在自动驾驶的多智能体协同感知（MACP）领域，现有的多模态方法（如同时使用 LiDAR 和摄像头）虽然提升了精度，但存在一个致命弱点：**系统鲁棒性差**。一旦某个核心传感器（如 LiDAR）失效，整个感知系统往往会崩溃。
*   **研究动机**：作者发现失效的根本原因在于特征融合过程导致了“语义空间偏移”，使得单模态特征无法适配为多模态设计的下游任务头。此外，多模态联合训练中普遍存在的“模态竞争”导致模型过度依赖强模态（LiDAR），使得弱模态（Camera）分支训练不足，无法独立运行。
*   **核心目标**：开发一种像“并联电路”一样的系统，无论哪种模态失效，剩余模态仍能独立且高效地完成感知任务。

#### 2. 方法论
论文提出了 **SiMO** 框架，其核心由两个关键技术组成：
*   **长度自适应多模态融合（LAMMA）**：
    *   **结构设计**：采用基于注意力机制（Attention）的融合模块，能够自适应处理不同数量的输入特征（1个或多个）。
    *   **语义对齐**：在融合前使用 ConvNeXt 块作为对齐器（Aligner），将不同模态的 BEV 特征映射到统一空间。
    *   **加法融合**：采用元素级加法（Addition）而非拼接（Concat），确保融合前后的特征在数学和语义空间上保持一致，从而直接适配下游检测头。
*   **“预训练-对齐-融合-RD”（PAFR）训练策略**：
    *   **Step 1 (Pretrain)**：独立预训练各模态分支，确保每个分支都具备完整的特征提取能力。
    *   **Step 2 (Align)**：冻结提取器，依次训练各模态的对齐器。
    *   **Step 3 (Fuse)**：冻结对齐器，训练融合模块和检测头。
    *   **Step 4 (RD)**：引入随机丢弃（Random Drop）进行微调，增强模型对模态缺失的适应力。

#### 3. 实验设计
*   **数据集**：
    *   **OPV2V-H**：主要的异构协同感知仿真数据集。
    *   **V2XSet**：用于验证车路协同（V2X）场景。
    *   **DAIR-V2X**：真实世界数据集，验证实车场景下的表现。
*   **Benchmark 与对比方法**：
    *   对比了 **BM2CP**（现有的多模态协同方法）、**BEVFusion**（单车多模态 SOTA 迁移至协同场景）、**UniBEV**（解决单车模态缺失的方法）以及 **HEAL/AttFusion**（单模态协同 SOTA）。
*   **评估指标**：不同 IoU 阈值（0.3, 0.5, 0.7）下的平均精度（AP）。

#### 4. 资源与算力
*   **硬件**：使用 **NVIDIA GeForce RTX 3090 GPU**。
*   **训练细节**：
    *   Batch size 为 1。
    *   训练阶段分为：预训练（20+20 epoch）、对齐（15+5 epoch）、融合（5 epoch）、RD 微调（5 epoch）。
    *   论文提到，虽然多阶段训练增加了总时长，但由于分段训练时显存占用较低，使得在 3090 级别的显卡上即可完成复杂模型的训练。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了大量的定量实验，涵盖了三种不同的数据集。
*   **消融实验**：针对训练策略（PAFR）、随机丢弃（RD）和 LAMMA 模块分别做了详尽的消融分析。
*   **鲁棒性测试**：模拟了不同程度的 LiDAR 失效（点云稀疏化）以及异构失效（不同智能体缺失不同传感器）。
*   **可视化分析**：通过 t-SNE 降维和 Procrustes 分析验证了特征空间的对齐效果。
*   **评价**：实验设计非常全面，不仅对比了精度，还深入探讨了模态竞争的内在机制，逻辑严密且客观。

#### 6. 主要结论与发现
*   **单模态独立性**：SiMO 在 LiDAR 完全失效（仅剩图像）时，性能远超现有方法，且在双模态全开时仍能保持 SOTA 级别的精度。
*   **语义一致性是关键**：实验证明，通过 LAMMA 实现的特征空间对齐和加法融合，是解决模态失效后系统崩溃的有效途径。
*   **克服模态竞争**：PAFR 策略成功解决了联合训练中弱势模态被抑制的问题，使得相机分支在没有 LiDAR 辅助时也能输出可靠结果。

#### 7. 优点
*   **首创性**：首次在协同感知领域系统性地解决了多模态失效下的单模态可操作性问题。
*   **即插即用**：LAMMA 模块具有高度的通用性，可以轻松集成到现有的协同感知框架（如 AttFusion 或 Pyramid Fusion）中。
*   **理论深度**：附录中引入了维特根斯坦的“家族相似性”哲学理论来解释多模态对齐，提升了论文的思想深度。

#### 8. 不足与局限
*   **训练流程复杂**：PAFR 策略涉及四个阶段，相比端到端训练，流程较为繁琐，增加了工程实现的复杂度。
*   **物理信息瓶颈**：在单视角相机场景（如 DAIR-V2X）下，受限于缺乏深度信息，SiMO 虽然能运行，但精度提升有限。
*   **噪声敏感性**：由于采用加法融合而非卷积融合，系统对高强度传感器噪声的隐式过滤能力较弱，可能需要额外的去噪模块。

（完）
