---
title: Evolving Prompt Adaptation for Vision-Language Models
title_zh: 视觉-语言模型的演化提示适配
authors: "Enming Zhang, Jiayang Li, Yanru Wu, Zhenyu Liu, Yang Li"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09493v1"
tags: ["query:ai-ux"]
score: 9.0
evidence: 演化提示词适配与引导提示词轨迹
tldr: 针对大规模视觉语言模型在下游任务微调时易出现灾难性遗忘的问题，本文提出 EvoPrompt 框架。该框架通过模态共享提示投影器生成层级提示，并采用进化训练策略将低秩更新解耦为方向和幅值，在保留预训练语义方向的同时仅调整幅值。结合特征几何正则化，EvoPrompt 在提升少样本学习性能的同时，有效保留了模型的零样本泛化能力。
selection_source: fresh_fetch
motivation: 旨在解决视觉语言模型在参数高效提示学习过程中容易丢失预训练知识（灾难性遗忘）的挑战。
method: 提出 EvoPrompt 框架，利用模态共享投影器生成提示，并通过解耦方向与幅值的进化策略及特征几何正则化来稳定微调过程。
result: 在少样本学习任务中达到了最先进的性能，并能稳健地保持预训练模型原有的零样本能力。
conclusion: 显式引导提示的演化轨迹是实现无遗忘适配的关键，为视觉语言模型的高效微调提供了新思路。
---

## 摘要
将大规模视觉-语言模型（VLMs）适配到标注数据有限的下游任务中仍然是一个重大挑战。虽然参数高效的提示学习方法提供了一条有前景的路径，但它们往往面临预训练知识灾难性遗忘的问题。为了解决这一局限性，本研究基于以下见解：控制提示的演化路径对于实现无遗忘适配至关重要。为此，我们提出了 EvoPrompt，这是一个旨在显式引导提示轨迹以实现稳定且保留知识的微调的新型框架。具体而言，我们的方法采用模态共享提示投影器（MPP），从统一的嵌入空间生成分层提示。关键在于，一种演化训练策略将低秩更新解耦为方向和幅度分量，在仅调整幅度的同时保留早期学习到的语义方向，从而使提示能够在不丢弃基础知识的情况下进行演化。该过程通过特征几何正则化（FGR）进一步稳定，该正则化强制执行特征去相关以防止表示崩溃。大量实验表明，EvoPrompt 在少样本学习中实现了最先进的性能，同时稳健地保留了预训练 VLM 原始的零样本能力。

## Abstract
The adaptation of large-scale vision-language models (VLMs) to downstream tasks with limited labeled data remains a significant challenge. While parameter-efficient prompt learning methods offer a promising path, they often suffer from catastrophic forgetting of pre-trained knowledge. Toward addressing this limitation, our work is grounded in the insight that governing the evolutionary path of prompts is essential for forgetting-free adaptation. To this end, we propose EvoPrompt, a novel framework designed to explicitly steer the prompt trajectory for stable, knowledge-preserving fine-tuning. Specifically, our approach employs a Modality-Shared Prompt Projector (MPP) to generate hierarchical prompts from a unified embedding space. Critically, an evolutionary training strategy decouples low-rank updates into directional and magnitude components, preserving early-learned semantic directions while only adapting their magnitude, thus enabling prompts to evolve without discarding foundational knowledge. This process is further stabilized by Feature Geometric Regularization (FGR), which enforces feature decorrelation to prevent representation collapse. Extensive experiments demonstrate that EvoPrompt achieves state-of-the-art performance in few-shot learning while robustly preserving the original zero-shot capabilities of pre-trained VLMs.

---

## 论文详细总结（自动生成）

这是一份关于论文《Evolving Prompt Adaptation for Vision-Language Models》的结构化深度总结：

### 1. 核心问题与研究背景
*   **核心问题**：大规模视觉-语言模型（VLM，如 CLIP）在适配下游任务时，如何在有限的标注数据下（Few-shot）既能学到任务特定知识，又能避免**灾难性遗忘**（即丢失预训练模型原有的强大零样本泛化能力）。
*   **研究背景**：现有的提示学习（Prompt Learning）方法（如 CoOp, MaPLe）存在两个主要缺陷：
    1.  **结构孤立**：提示在不同层之间独立学习，忽略了语义的层级演进。
    2.  **轨迹偏移**：在微调过程中，可学习的提示容易迅速偏离预训练的语义锚点，导致在未见类别（Novel classes）上性能大幅下降。

### 2. 方法论：EvoPrompt 框架
EvoPrompt 的核心思想是**显式引导提示的演化轨迹**，通过结构设计和训练策略的协同来稳定适配过程。

*   **模态共享提示投影器 (MPP)**：
    *   不再为每层独立设置提示，而是从一个统一的、可学习的嵌入空间 $E$ 出发。
    *   使用投影矩阵 $W_i$ 将 $E$ 映射到各层的视觉和文本提示中。
    *   **参数高效设计**：将 $W_i$ 分解为全局共享部分 $W_{shared}$ 和层特定低秩适配器 $A_i B_i$（类似 LoRA），减少参数量的同时增强层间协同。
*   **演化轨迹感知学习策略**：
    *   **方向与幅值解耦**：将低秩更新 $\Delta W$ 分解为“方向”（Direction）和“幅值”（Magnitude, $\alpha$）。
    *   **冻结历史方向**：在训练过程中，冻结早期 Epoch 学习到的语义方向，仅允许调整它们的幅值，并为当前 Epoch 学习新的方向。这确保了模型在吸收新知识时不会破坏已建立的几何结构。
    *   **自适应秩缩减**：随着训练推进，逐步降低新学习权重的秩（Rank），以减少后期微调的过拟合风险。
*   **特征几何正则化 (FGR)**：
    *   基于 Soft-HGR 最大相关理论，引入正则化项强制执行特征去相关（Decorrelation）。
    *   通过最小化模态协方差矩阵的乘积，防止特征空间坍缩，保持表示的独立性和丰富性。

### 3. 实验设计
*   **数据集与场景**：
    *   **基类到新类泛化**：在 11 个标准数据集（ImageNet, Caltech101, OxfordPets, EuroSAT 等）上进行测试。
    *   **跨数据集迁移**：在 ImageNet 上训练，直接在其他 10 个数据集上测试。
    *   **领域泛化**：在 ImageNet 的变体（V2, Sketch, A, R）上测试鲁棒性。
    *   **少样本学习**：测试 1, 2, 4, 8, 16 shot 的性能。
*   **Benchmark 与对比方法**：
    *   对比了 CLIP (Zero-shot)、CoOp、CoCoOp、MaPLe、PromptSRC、TCP、MMA 等十余种最先进的（SOTA）参数高效适配方法。

### 4. 资源与算力
*   **硬件环境**：实验在一台 **NVIDIA A800 GPU** 上完成。
*   **训练效率**：
    *   训练时间约为 **4.5ms/image**。
    *   推理速度达到 **1282.1 FPS**（Batch size=100）。
    *   可训练参数量约为 **0.764M**（以 5 个 Epoch 为例），比 MaPLe 减少了约 4.6 倍。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了 11 个分类数据集，进行了四种主流评估协议的测试，实验总数非常可观。
*   **消融实验**：详细分析了 MPP、共享权重 $W_s$、演化策略（E.T.）、正则化项（FGR）等每个组件的贡献。
*   **充分性评价**：实验设计非常充分且客观。作者不仅报告了 Top-1 准确率，还重点关注了**调和平均值 (HM)**，这是衡量“适配性能”与“泛化能力”平衡的关键指标。此外，还通过训练动力学曲线（Overfitting Phenomenon）直观展示了该方法如何抑制过拟合。

### 6. 主要结论与发现
*   **性能领先**：EvoPrompt 在 11 个数据集的平均新类（Novel）准确率和调和平均值（HM）上均刷新了 SOTA 记录。
*   **知识保留**：通过解耦方向和幅值，模型能够有效保留预训练的零样本能力，解决了提示学习中的过拟合难题。
*   **演化规律**：分析发现，提示的幅值 $\alpha$ 在训练初期迅速增长以确立核心特征，随后逐渐下降进行微调，验证了“演化引导”的合理性。

### 7. 优点与亮点
*   **理论支撑**：引入 Soft-HGR 理论进行特征几何正则化，相比纯启发式的微调更具解释性。
*   **策略创新**：将 LoRA 式的更新做时间维度的解耦（冻结历史方向），是一种非常巧妙的防止遗忘的手段。
*   **高效性**：在提升性能的同时，显著降低了可训练参数量和计算开销，具有很强的实用价值。

### 8. 不足与局限
*   **超参数敏感性**：虽然论文给出了敏感性分析，但演化策略涉及秩缩减的时机（$\mu, \nu$）和损失权重（$\gamma, \eta$），在不同规模的模型或极度差异化的数据集上可能需要重新调优。
*   **任务覆盖**：实验主要集中在图像分类任务，尚未在目标检测、分割或更复杂的视觉问答（VQA）任务中验证其演化策略的有效性。
*   **训练轮数依赖**：演化策略依赖于多个 Epoch 的累积，对于追求极速（如 1-2 个 Epoch）完成适配的场景，其优势可能无法完全发挥。

（完）
