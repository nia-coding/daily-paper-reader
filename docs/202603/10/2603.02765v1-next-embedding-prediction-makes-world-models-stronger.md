---
title: Next Embedding Prediction Makes World Models Stronger
title_zh: 下个嵌入预测让世界模型更强大
authors: "George Bredis, Nikita Balagansky, Daniil Gavrilov, Ruslan Rakhimov"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.02765v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 世界模型与表示空间中的时间预测对齐
tldr: 本研究针对部分可观测高维环境中的模型强化学习，提出了 NE-Dreamer 框架。该方法摒弃了传统的解码器结构，利用时间 Transformer 直接在表示空间中预测下一时刻的编码器嵌入，从而优化时间预测一致性。NE-Dreamer 无需重构损失或辅助监督即可学习到连贯的预测性状态表示。实验表明，该方法在 DeepMind Control Suite 和 DMLab 任务中表现优异，尤其在处理记忆和空间推理方面具有显著优势。
selection_source: fresh_fetch
motivation: 在部分可观测的高维领域中，捕捉时间依赖性对于基于模型的强化学习至关重要。
method: 提出 NE-Dreamer，一种无解码器的智能体，通过时间 Transformer 预测下一时刻的编码器嵌入以优化表示空间的一致性。
result: 在 DeepMind Control Suite 上性能媲美 DreamerV3，并在涉及记忆和空间推理的 DMLab 任务中取得大幅领先。
conclusion: 基于时间 Transformer 的下一嵌入预测为复杂环境下的模型强化学习提供了一个高效且可扩展的框架。
---

## 摘要
在部分可观测的高维领域中，捕捉时间依赖性对于基于模型的强化学习（MBRL）至关重要。我们提出了 NE-Dreamer，这是一种无解码器（decoder-free）的 MBRL 智能体，它利用时间 Transformer 从潜状态序列中预测下一步的编码器嵌入，从而直接优化表示空间中的时间预测对齐。这种方法使 NE-Dreamer 能够在无需重构损失或辅助监督的情况下，学习到连贯且具有预测性的状态表示。在 DeepMind Control Suite 上，NE-Dreamer 的性能达到或超过了 DreamerV3 以及领先的无解码器智能体。在涉及记忆和空间推理的 DMLab 任务挑战性子集中，NE-Dreamer 取得了显著的提升。这些结果表明，结合时间 Transformer 的下个嵌入预测是复杂、部分可观测环境下 MBRL 的一种有效且可扩展的框架。

## Abstract
Capturing temporal dependencies is critical for model-based reinforcement learning (MBRL) in partially observable, high-dimensional domains. We introduce NE-Dreamer, a decoder-free MBRL agent that leverages a temporal transformer to predict next-step encoder embeddings from latent state sequences, directly optimizing temporal predictive alignment in representation space. This approach enables NE-Dreamer to learn coherent, predictive state representations without reconstruction losses or auxiliary supervision. On the DeepMind Control Suite, NE-Dreamer matches or exceeds the performance of DreamerV3 and leading decoder-free agents. On a challenging subset of DMLab tasks involving memory and spatial reasoning, NE-Dreamer achieves substantial gains. These results establish next-embedding prediction with temporal transformers as an effective, scalable framework for MBRL in complex, partially observable environments.

---

## 论文详细总结（自动生成）

以下是对论文《Next Embedding Prediction Makes World Models Stronger》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
在部分可观测（Partially Observable）的高维环境中，基于模型的强化学习（MBRL）依赖于学习紧凑且具有预测性的潜状态表示。
*   **现有问题：** 传统的 Dreamer 类方法依赖**像素解码器（Pixel Decoder）**进行重构，这会带来沉重的计算负担，且模型容易过度关注与任务无关的视觉细节。现有的**无解码器（Decoder-free）**方法虽然提高了效率，但大多只强调“瞬时一致性”（即同一时刻的表示对齐），缺乏对时间依赖性的显式建模，导致在需要长期记忆和空间推理的任务（如 DMLab）中表现乏力。
*   **研究动机：** 探索一种无需像素重构、但能通过“预测未来”来强制模型学习时间连贯表示的新机制。

### 2. 方法论：核心思想与关键技术
NE-Dreamer 的核心思想是将表示学习转化为一个**因果序列预测任务**，直接在嵌入空间进行时间对齐。
*   **核心架构：** 基于 RSSM（循环状态空间模型）骨干，移除像素解码器。
*   **下个嵌入预测（NEP）：** 引入一个轻量级的**因果时间 Transformer**。该 Transformer 输入历史潜状态序列，预测下一时刻编码器生成的嵌入向量 $\hat{e}_{t+1}$。
*   **Barlow Twins 对齐损失：** 使用冗余减少（Redundancy Reduction）准则，将预测的 $\hat{e}_{t+1}$ 与真实的下个时刻编码器嵌入 $e_{t+1}$（应用停止梯度 stop-gradient）进行对齐。该损失函数通过鼓励互相关矩阵趋近于单位矩阵，既保证了预测的准确性，又防止了表示崩溃（Collapse）。
*   **Actor-Critic 学习：** 沿用 DreamerV3 的框架，在世界模型生成的“想象”轨迹中训练策略和价值函数。

### 3. 实验设计
*   **场景与数据集：**
    *   **DeepMind Lab (DMLab) Rooms：** 包含四个极具挑战性的任务（Collect, Exploit, Select, Watermaze），侧重于长期记忆、导航和空间推理。
    *   **DeepMind Control Suite (DMC)：** 包含 20 个标准的连续控制任务，用于验证基础控制能力。
*   **对比基准（Baselines）：**
    *   **DreamerV3：** 经典的基于重构的世界模型。
    *   **R2-Dreamer：** 无解码器且使用同阶（same-step）对齐的方法。
    *   **DreamerPro：** 使用强数据增强和原型表示的无解码器方法。
    *   **DrQv2：** 强力的无模型（Model-free）基准。
*   **消融实验：** 针对 Transformer 模块、时间偏移（预测未来 vs 匹配当前）、投影头等关键组件进行了对比。

### 4. 资源与算力
*   **模型规模：** 采用 Dreamer-S 架构，参数量约为 **12M**。
*   **训练步数：** DMLab 任务训练 **50M** 步，DMC 任务训练 **1M** 步。
*   **算力说明：** 论文强调在“匹配的计算资源和模型容量”下进行对比，但**未明确给出具体的 GPU 型号、数量及总训练时长**。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 2 个大类基准，共计 24 个子任务。
*   **重复性：** 所有实验均使用 **5 个随机种子**运行，并报告了均值和标准差。
*   **充分性：** 实验设计较为全面。除了性能对比，还通过**后验解码诊断（Representation Diagnostics）**可视化了潜状态保留的信息，并利用消融实验隔离了 Transformer 和时间预测机制的贡献。对比实验在统一的代码库和参数量下进行，确保了公平性。

### 6. 主要结论与发现
*   **性能飞跃：** 在 DMLab Rooms 任务中，NE-Dreamer 显著优于所有基准方法，证明了时间预测对于解决部分可观测环境下的记忆/导航问题至关重要。
*   **无损替代：** 在 DMC 连续控制任务中，NE-Dreamer 达到了与 DreamerV3 持平甚至略优的水平，证明移除解码器不会损害标准控制任务的性能。
*   **关键机制：** 消融实验证实，**“因果 Transformer”**和**“预测未来嵌入（而非当前）”**是提升性能的核心，缺一不可。
*   **表示稳定性：** 诊断显示 NE-Dreamer 学习到的表示在时间上更稳定，能更好地保留任务相关的物体身份和空间布局。

### 7. 优点
*   **架构简洁高效：** 摆脱了重构像素的沉重负担，简化了优化目标。
*   **时间建模能力强：** 通过 Transformer 显式捕捉长程依赖，解决了无解码器方法在复杂任务中的短板。
*   **通用性：** 无需特定领域的数据增强（如随机裁剪）或复杂的辅助任务，具有良好的扩展性。

### 8. 不足与局限
*   **视觉细节缺失风险：** 在某些极度依赖精细视觉纹理（而非宏观结构）的任务中，完全放弃重构可能会丢失关键信息。
*   **超参数敏感性：** 引入了 Transformer 和 Barlow Twins 损失，可能需要针对不同环境微调相关超参数（如 $\lambda_{BT}$）。
*   **算力细节不透明：** 缺乏具体的硬件运行时间数据，难以评估其相对于重构方法在实际训练速度上的具体增益。

（完）
