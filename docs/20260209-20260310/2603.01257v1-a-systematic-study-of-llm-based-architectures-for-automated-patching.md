---
title: A Systematic Study of LLM-Based Architectures for Automated Patching
title_zh: 基于 LLM 的自动补丁修复架构的系统性研究
authors: "Qingxiao Xu, Ze Sheng, Zhicheng Chen, Jeff Huang"
date: 2026-03-01
pdf: "https://arxiv.org/pdf/2603.01257v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 对基于大语言模型的自动软件补丁架构的评估
tldr: 本研究针对基于大语言模型（LLM）的自动补丁修复系统，系统性地评估了四种主流架构：固定工作流、单智能体、多智能体及通用代码智能体。通过统一基准测试，分析了各架构在修复正确性、成本及效率上的权衡。研究发现，通用代码智能体表现最优，且架构设计与迭代深度对修复效果的影响超过了模型本身的能力，为构建高效自动修复系统提供了重要指导。
selection_source: fresh_fetch
motivation: 尽管LLM在自动补丁修复中展现出潜力，但目前缺乏对不同集成架构优劣的系统性比较与评估。
method: 在统一基准下，对比评估了固定工作流、单智能体、多智能体和通用代码智能体四种范式在真实漏洞修复任务中的表现。
result: 固定工作流效率高但脆弱，多智能体泛化性强但开销大且易产生推理漂移，而通用代码智能体凭借灵活的工具接口取得了最佳综合性能。
conclusion: 自动补丁修复系统的可靠性和成本主要由架构设计和迭代深度决定，而非单纯依赖模型本身的能力。
---

## 摘要
大语言模型（LLM）在自动补丁修复方面展现出巨大潜力，但其有效性在很大程度上取决于它们如何集成到修复系统中。尽管先前的工作探索了提示策略和单个智能体设计，但该领域仍缺乏对修复架构的系统性比较。在本文中，我们使用统一的基准测试和评估框架，对四种基于 LLM 的修复范式——固定工作流、单智能体系统、多智能体系统和通用代码智能体——进行了受控评估。我们分析了在真实漏洞任务中的补丁正确性、失败模式、Token 使用量和执行时间。研究结果揭示了明显的架构权衡：固定工作流效率高但脆弱，单智能体系统平衡了灵活性和成本，而多智能体设计提高了泛化能力，但代价是显著更高的开销以及在复杂任务上推理漂移风险的增加。令人惊讶的是，通用代码智能体实现了最强的整体修复性能，这得益于支持跨漏洞类型有效适配的通用工具接口。总之，我们表明架构设计和迭代深度，而非单纯的模型能力，决定了基于 LLM 的自动补丁修复的可靠性和成本。

## Abstract
Large language models (LLMs) have shown promise for automated patching, but their effectiveness depends strongly on how they are integrated into patching systems. While prior work explores prompting strategies and individual agent designs, the field lacks a systematic comparison of patching architectures. In this paper, we present a controlled evaluation of four LLM-based patching paradigms -- fixed workflow, single-agent system, multi-agent system, and general-purpose code agents -- using a unified benchmark and evaluation framework. We analyze patch correctness, failure modes, token usage, and execution time across real-world vulnerability tasks. Our results reveal clear architectural trade-offs: fixed workflows are efficient but brittle, single-agent systems balance flexibility and cost, and multi-agent designs improve generalization at the expense of substantially higher overhead and increased risk of reasoning drift on complex tasks. Surprisingly, general-purpose code agents achieve the strongest overall patching performance, benefiting from general-purpose tool interfaces that support effective adaptation across vulnerability types. Overall, we show that architectural design and iteration depth, rather than model capability alone, dominate the reliability and cost of LLM-based automated patching.

---

## 论文详细总结（自动生成）

这是一份关于论文《A Systematic Study of LLM-Based Architectures for Automated Patching》的结构化深入分析报告：

### 1. 核心问题与研究动机
*   **核心问题**：在利用大语言模型（LLM）进行自动漏洞补丁修复（Automated Patching）时，**系统架构的设计**（而非单纯的模型能力）如何影响修复的正确性、效率和成本？
*   **研究动机**：
    *   虽然 LLM 在漏洞修复中表现出色（如 DARPA AIxCC 竞赛所示），但现有研究多关注提示词工程（Prompt Engineering）或单一智能体设计，缺乏对不同系统架构的系统性对比。
    *   许多领先的修复系统（如 DeepMind 的 CodeMender）是闭源的，其架构决策的通用性难以评估。
    *   开发者需要了解在不同复杂度的任务下，应选择何种架构来平衡修复成功率与运行成本。

### 2. 方法论：四种架构范式
论文定义并实现了四种主流的 LLM 补丁修复架构：
1.  **固定工作流 (Fixed Workflow)**：
    *   **核心思想**：遵循确定性的、规则驱动的线性管道。
    *   **流程**：静态分析定位 -> LLM 生成补丁 -> 文件编辑 -> 自动化验证 -> 失败后反馈重试。其结构简单、透明，但缺乏动态调整策略的能力。
2.  **单智能体系统 (Single-Agent System)**：
    *   **核心思想**：赋予 LLM 自主规划和工具调用的权力。
    *   **技术细节**：使用模型上下文协议（MCP）集成搜索、编辑、生成和验证工具。模型在连续的推理上下文中根据工具反馈动态决定下一步行动。
3.  **多智能体系统 (Multi-Agent System)**：
    *   **核心思想**：将任务分解为专门的角色（如 Context Retriever、Root-Cause Analyst、SWE、QE、Reflection Agent）。
    *   **关键技术**：通过“共享状态（Shared State）”进行协作。这种设计旨在降低单个智能体的认知负荷，通过角色分工处理复杂逻辑。
4.  **通用代码智能体 (General Code Agent)**：
    *   **核心思想**：使用非领域特定的端到端编程助手（以 Claude Code 为代表）。
    *   **关键技术**：具备动态规划、上下文压缩和通用的 Bash/文件操作能力，不针对补丁任务做特殊优化，完全依靠其通用的推理和工具链。

### 3. 实验设计
*   **数据集/Benchmark**：使用 **DARPA AIxCC (AI Cyber Challenge)** 的官方 Java 漏洞数据集，包含 19 个真实的 delta-scan 任务。
*   **涉及项目**：涵盖 Apache Commons Compress、ZooKeeper、Log4j、Tika、PDFBox 和 POI 等大型复杂开源项目。
*   **对比维度**：
    *   **正确性**：是否通过 Proof-of-Vulnerability (PoV) 测试及功能回归测试。
    *   **效率**：Token 消耗量、执行时间（Latency）。
    *   **行为分析**：工具调用频率、智能体间的状态转移图。
*   **对比方法**：在统一框架下运行上述四种架构，并使用 GPT-5 和 Claude Sonnet 4.5 作为底层模型进行交叉验证。

### 4. 资源与算力
*   **算力说明**：论文未明确提及具体的本地 GPU 型号，因为实验主要基于云端 LLM API（OpenAI 和 Anthropic）。
*   **环境开销**：所有任务在 Docker 容器化环境中运行，以确保漏洞复现的确定性。
*   **成本记录**：详细记录了每个任务的 Token 消耗（从几千到几十万不等）和执行时间（通常在 10-60 分钟之间）。

### 5. 实验数量与充分性
*   **实验规模**：针对 19 个复杂漏洞，测试了多种架构与模型的组合（如单/多智能体分别搭配 GPT-5 和 Sonnet 4.5），并与 Claude Code 进行对比。
*   **充分性评价**：
    *   **客观性**：使用了统一的运行器和验证脚本，确保了不同架构在相同输入（代码、PoV、测试用例）下的公平竞争。
    *   **深度分析**：不仅看成功率，还通过日志分析了失败模式（如多智能体在复杂任务中的“推理漂移”），实验设计较为全面且具有实战意义。

### 6. 主要结论与发现
*   **通用智能体胜出**：**Claude Code（通用代码智能体）表现最强**，成功修复了 16/19 个漏洞，证明了灵活的通用工具接口优于僵化的领域特定设计。
*   **架构权衡明显**：
    *   **固定工作流**：最省 Token，但面对复杂逻辑时非常脆弱。
    *   **单智能体**：在灵活性和成本之间取得了较好的平衡。
    *   **多智能体**：虽然理论上更强，但**开销极大**（Token 消耗高出 1-2 个数量级），且在复杂任务中容易出现“推理漂移”（Reasoning Drift），即智能体之间传递错误信息导致偏离目标。
*   **迭代深度是关键**：修复的可靠性更多取决于架构允许的迭代深度和工具反馈质量，而非单纯依赖模型本身的参数量。

### 7. 优点与亮点
*   **系统性分类**：首次对 LLM 补丁修复架构进行了清晰的分类学定义，为后续研究提供了标准参考。
*   **实战导向**：直接使用 DARPA 竞赛级别的真实漏洞，而非简单的合成题目，实验结果对工业界极具参考价值。
*   **深入的行为洞察**：通过智能体转移图和工具使用统计，揭示了系统在失败时的具体表现（如过度搜索而无有效编辑）。

### 8. 不足与局限
*   **样本量限制**：虽然漏洞很真实，但 19 个样本在统计学上规模较小，可能存在特定项目的偏差。
*   **语言局限性**：实验仅限于 Java 语言，未验证在 C/C++ 或 Python 等其他语言上的表现。
*   **闭源工具依赖**：对 Claude Code 的分析受限于其闭源性质，无法完全拆解其内部的提示词策略或上下文管理机制。
*   **成本敏感性**：高性能架构（如多智能体和通用智能体）的 Token 成本极高，对于大规模自动化部署可能存在经济障碍。

（完）
