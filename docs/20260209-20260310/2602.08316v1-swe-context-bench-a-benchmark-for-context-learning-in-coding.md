---
title: "SWE Context Bench: A Benchmark for Context Learning in Coding"
title_zh: SWE Context Bench：编程上下文学习基准测试
authors: "Jared Zhu, Minhao Hu, Junde Wu"
date: 2026-02-09
pdf: "https://arxiv.org/pdf/2602.08316v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 针对仓库级软件工程任务中上下文学习的基准测试
tldr: 针对现有编程智能体基准测试忽略跨任务经验复用的问题，本文提出了 SWE-ContextBench。该基准基于 SWE-Bench Lite，通过挖掘 GitHub 真实依赖关系构建了包含相关任务序列的测试集。它从准确率、时间及成本效率三个维度评估智能体利用先前经验的能力。研究发现，高质量的经验总结能显著提升复杂任务的解决效率，为开发具备持续学习能力的编程智能体提供了重要评估工具。
selection_source: fresh_fetch
motivation: 现有的编程基准测试通常将任务视为独立事件，无法衡量智能体在处理相关问题时积累和复用经验的能力。
method: 在 SWE-Bench Lite 基础上，利用 GitHub 真实依赖关系增加了 99 个相关任务，构建了包含 300 个基础任务的任务序列，并设计了多维度的效率评估指标。
result: 实验表明，正确选择的总结性经验能显著提高任务解决的准确率并降低运行成本，而错误或未过滤的经验则可能产生负面影响。
conclusion: SWE-ContextBench 证明了经验表示和检索质量对编程智能体的重要性，是研究智能体经验复用机制的有效基准。
---

## 摘要
大语言模型正日益广泛地被用作处理仓库级软件工程任务的编程智能体。虽然近期的基准测试评估了模型在真实代码库中的正确性，但它们大多将任务视为相互独立的，未能评估智能体是否能在相关问题间重用经验。因此，智能体积累、检索和应用过往经验的能力，以及这种重用带来的效率提升，仍难以衡量。我们推出了 SWE-ContextBench，这是一个专门用于显式评估编程智能体经验重用能力的基准测试。SWE-ContextBench 基于 SWE-Bench Lite 构建，通过从 GitHub issue 和 pull request 之间的真实依赖及引用关系中提取 99 个相关任务，对 300 个基础任务进行了扩充，从而形成了具有共享上下文的任务序列。该基准测试从预测准确性、时间效率和成本效率三个互补维度对智能体进行评估。利用 SWE-ContextBench，我们研究了多种经验重用设置，包括真值引导（oracle guided）检索、自主检索，以及完整执行轨迹和紧凑摘要。研究结果表明，正确选择的摘要经验能提高解决问题的准确性，并显著降低运行时间和 token 成本，在较难的任务上表现尤为突出。相比之下，未经过滤或选择错误的经验带来的收益有限，甚至会产生负面影响。这些发现强调了经验表示和检索质量的重要性，并将 SWE-ContextBench 确立为研究编程智能体经验重用的规范化基准。

## Abstract
Large language models are increasingly used as programming agents for repository level software engineering tasks. While recent benchmarks evaluate correctness in realistic codebases, they largely treat tasks as independent and do not assess whether agents can reuse experience across related problems. As a result, the ability of agents to accumulate, retrieve, and apply prior experience, as well as the efficiency gains from such reuse, remains difficult to measure. We introduce SWE-ContextBench, a benchmark designed to explicitly evaluate experience reuse in programming agents. Built on SWE-Bench Lite, SWE-ContextBench augments 300 base tasks with 99 related tasks derived from real dependency and reference relationships among GitHub issues and pull requests, forming task sequences with shared context. The benchmark evaluates agents along three complementary dimensions: prediction accuracy, time efficiency, and cost efficiency. Using SWE-ContextBench, we study multiple experience reuse settings, including oracle guided and autonomous retrieval, as well as full execution trajectories and compact summaries. Our results show that correctly selected summarized experience improves resolution accuracy and substantially reduces runtime and token cost, particularly on harder tasks. In contrast, unfiltered or incorrectly selected experience provides limited or negative benefits. These findings highlight the importance of experience representation and retrieval quality, and position SWE-ContextBench as a principled benchmark for studying experience reuse in programming agents.

---

## 论文详细总结（自动生成）

### SWE-ContextBench 论文深度总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
当前的编程智能体（Programming Agents）评估主要集中在独立任务的解决能力上（如 HumanEval 或 SWE-Bench），将每个任务视为孤立事件。然而，在真实的软件工程中，开发者往往会通过解决一系列相关的 Issue 或 Pull Request (PR) 来积累经验。
*   **研究动机**：现有的基准测试无法衡量智能体**积累、检索和复用过往经验**的能力。
*   **核心问题**：智能体能否通过复用相关任务的经验来提高解决新问题的准确率，并降低时间和计算成本？
*   **整体含义**：本文提出了 **SWE-ContextBench**，这是首个专门用于评估编程智能体在仓库级别进行“上下文学习”和“经验复用”的基准测试。

#### 2. 论文提出的方法论
核心思想是构建一个具有“经验池”和“相关任务序列”的测试环境。
*   **数据构建流程**：
    1.  **基础任务选择**：选取 SWE-Bench Lite 中的 300 个实例作为“经验任务”。
    2.  **经验轨迹收集**：使用高性能智能体（如 Claude 3.5 Sonnet）解决这 300 个任务，记录完整的推理、工具调用和文件导航轨迹，形成**经验池**。
    3.  **相关任务挖掘**：通过分析 GitHub 的引用关系（如“一个 PR 修复多个 Issue”、“Issue 间的相互引用”等），识别出 99 个与基础任务高度相关的“相关任务”。
    4.  **递归扩展**：通过二阶依赖关系进一步扩充任务序列。
*   **经验表示形式**：
    *   **完整轨迹（Full Trajectory）**：包含所有原始交互步骤（平均约 2.4 万词）。
    *   **紧凑摘要（Compact Summary）**：对解决过程的精简总结（平均约 200 词）。
*   **评估维度**：不仅关注**准确率**（是否通过测试），还引入了**时间效率**（墙钟时间）和**成本效率**（Token 消耗和 API 费用）。

#### 3. 实验设计
*   **数据集**：涵盖 12 个主流 Python 开源仓库（如 Django, Sympy, Scikit-learn 等），包含 300 个经验任务和 99 个相关测试任务。
*   **对比设置（5 种模式）**：
    1.  **No-Experience (Baseline)**：无经验接入，独立解决。
    2.  **Free Experience Reuse**：允许智能体自主从完整轨迹池中检索。
    3.  **Oracle Experience Reuse**：直接提供最相关的完整轨迹（真值引导）。
    4.  **Free Summary Reuse**：自主从摘要池中检索。
    5.  **Oracle Summary Reuse**：直接提供最相关的摘要。
*   **智能体模型**：主要使用 Claude Code (基于 Claude 3.5 Sonnet)。

#### 4. 资源与算力
*   **算力说明**：论文未提及具体的本地 GPU 训练算力，因为实验主要基于 API 调用（Claude 3.5 Sonnet）。
*   **成本记录**：详细记录了 API 成本。例如，Oracle Summary Reuse 的平均成本为每实例 0.77 美元，而 Free Experience Reuse 则升至 0.98 美元。
*   **环境**：所有任务在独立的 Conda 虚拟环境中执行，以确保测试的隔离性和可重复性。

#### 5. 实验数量与充分性
*   **实验规模**：针对 99 个相关任务，在 5 种不同设置下进行了总计 **495 次完整运行**。
*   **充分性评价**：
    *   **多维度评估**：实验覆盖了从代码正确性到推理成本的全方位指标，较为全面。
    *   **真实性**：任务源于真实的 GitHub 依赖链，而非人工合成，具有较高的客观性。
    *   **局限性**：相关任务的数量（99个）相对较小，且主要集中在 Python 语言，对于其他编程语言或更大规模任务序列的泛化性有待进一步验证。

#### 6. 论文的主要结论与发现
*   **摘要优于原始轨迹**：提供精简的“经验摘要”比提供“完整执行轨迹”更有效。Oracle Summary Reuse 将解决率从 26.26% 提升至 **34.34%**。
*   **检索质量至关重要**：自主检索（Free Reuse）的效果远不如真值引导（Oracle）。如果检索到错误的经验，甚至会导致性能下降（负迁移）。
*   **复杂任务收益更高**：在简单任务上经验复用提升有限，但在高难度、耗时长的任务中，经验复用可减少超过 **60%** 的运行时间。
*   **成本降低**：正确的经验复用能显著减少冗余的仓库探索步骤，从而降低 Token 消耗。

#### 7. 优点
*   **视角新颖**：打破了“任务独立”的假设，填补了编程智能体跨任务学习评估的空白。
*   **评估指标全面**：引入时间与金钱成本维度，更符合工业界对 AI 编程工具的实际需求。
*   **数据质量高**：基于 GitHub 真实引用链构建，反映了软件开发中真实的增量式修复和逻辑关联。

#### 8. 不足与局限
*   **样本量限制**：99 个相关任务虽然质量高，但统计规模尚不足以支撑极细粒度的消融分析。
*   **语言单一性**：目前仅限于 Python 仓库，未测试编译型语言（如 C++、Java）在复杂构建环境下的表现。
*   **检索机制依赖**：实验显示自主检索效果不佳，说明目前的 RAG（检索增强生成）技术在处理长程编程经验时仍存在瓶颈，基准测试更多揭示了问题而非提供了完美的检索解决方案。

（完）
