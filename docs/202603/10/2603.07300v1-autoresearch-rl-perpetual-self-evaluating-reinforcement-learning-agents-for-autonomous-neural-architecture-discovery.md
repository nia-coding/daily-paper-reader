---
title: "AutoResearch-RL: Perpetual Self-Evaluating Reinforcement Learning Agents for Autonomous Neural Architecture Discovery"
title_zh: AutoResearch-RL：用于自主神经架构发现的永续自我评估强化学习智能体
authors: "Nilesh Jain, Rohit Yadav, Sagar Kotian, Claude AI"
date: 2026-03-07
pdf: "https://arxiv.org/pdf/2603.07300v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 用于自主研究的永久自我评估强化学习智能体
tldr: AutoResearch-RL 是一个无需人工干预的自动研究框架，利用强化学习（PPO）在固定资源预算下自主探索神经网络架构和超参数。该框架通过将研究过程建模为马尔可夫决策过程，使智能体能够不断修改训练脚本并根据验证性能进行自我演进。实验证明，该系统在单 GPU 预训练任务中，经过约 300 次迭代即可达到或超越人工调优的基准水平，实现了真正意义上的自动化神经架构发现。
selection_source: fresh_fetch
motivation: 旨在消除神经架构搜索和超参数优化中对人工监督的依赖，实现完全自主且持续的科研探索。
method: 采用基于 PPO 的强化学习智能体，在保持环境固定的前提下，通过不断修改和执行目标训练脚本来优化验证集性能。
result: 在单 GPU 的 nanochat 预训练基准测试中，该框架在约 300 次迭代后发现的配置达到或超过了人工调优的基准。
conclusion: AutoResearch-RL 证明了强化学习智能体能够通过闭环自我评估，在无需人类干预的情况下有效地进行复杂的神经架构发现。
---

## 摘要
我们提出了 AutoResearch-RL，这是一个强化学习智能体在无需人工监督的情况下进行开放式神经架构和超参数研究的框架，该框架会持续运行，直到终止预测器（termination oracle）发出收敛信号或资源耗尽。在每一步中，智能体都会对目标训练脚本提出代码修改建议，在固定的实际运行时间预算内执行该脚本，观察源自验证每字节比特数（val-bpb）的标量奖励，并通过近端策略优化（PPO）更新其策略。关键的设计见解是分离了三个关注点：(i) 冻结的环境（数据流水线、评估协议和常量），确保了跨实验比较的公平性；(ii) 可变的目标文件（train.py），代表智能体的可编辑状态；以及 (iii) 元学习器（强化学习智能体本身），它积累不断增长的实验结果轨迹，并利用这些结果为后续提案提供信息。我们将其形式化为马尔可夫决策过程，在温和的假设下推导出收敛保证，并在单 GPU nanochat 预训练基准上通过实证表明，AutoResearch-RL 在大约 300 次隔夜迭代后，在无需人工干预的情况下，发现了达到或超过手动调优基准的配置。

## Abstract
We present AutoResearch-RL, a framework in which a reinforcement learning agent conducts open-ended neural architecture and hyperparameter research without human supervision, running perpetually until a termination oracle signals convergence or resource exhaustion. At each step the agent proposes a code modification to a target training script, executes it under a fixed wall clock time budget, observes a scalar reward derived from validation bits-per-byte (val-bpb), and updates its policy via Proximal Policy Optimisation (PPO).   The key design insight is the separation of three concerns: (i) a frozen environment (data pipeline, evaluation protocol, and constants) that guarantees fair cross-experiment comparison; (ii) a mutable target file (train.py) that represents the agent's editable state; and (iii) a meta-learner (the RL agent itself) that accumulates a growing trajectory of experiment outcomes and uses them to inform subsequent proposals.   We formalise this as a Markov Decision Process, derive convergence guarantees under mild assumptions, and demonstrate empirically on a single GPU nanochat pretraining benchmark that AutoResearch-RL discovers configurations that match or exceed hand-tuned baselines after approximately 300 overnight iterations, with no human in the loop.

---

## 论文详细总结（自动生成）

### AutoResearch-RL 论文深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
深度学习的研究长期依赖于人类研究员的“假设-实现-训练-分析”循环，这一过程缓慢、昂贵且受限于人类工作时间。传统的自动化机器学习（AutoML）和神经架构搜索（NAS）通常局限于预定义的搜索空间或固定的评估器，无法处理训练动力学、损失函数或优化器设计等根本性的代码变更。
**AutoResearch-RL** 旨在构建一个**永续运行的自主研究智能体**。它将代码研究建模为强化学习任务，使智能体能够像人类研究员一样，通过修改源代码、观察实验结果并从中学习，从而在没有人工干预的情况下自主发现更优的神经网络架构和训练方案。

#### 2. 方法论：核心思想与关键技术
该框架的核心是将自主研究形式化为一个**马尔可夫决策过程（MDP）**，并采用以下关键设计：
*   **关注点分离**：
    *   **冻结环境**：数据流水线、评估协议和常量保持不变，确保实验对比的公平性。
    *   **可变目标文件（train.py）**：作为智能体的“状态”，允许其进行任意代码级别的修改。
    *   **元学习器（RL 智能体）**：基于 PPO 算法，根据实验历史轨迹优化其代码修改策略。
*   **PPO 策略架构**：智能体采用 Transformer 语言模型（如 Claude-Sonnet-4），通过 LoRA 进行微调。输入（State）包含研究议程、当前代码和最近 32 次实验的历史记录；输出（Action）为代码的结构化差异（diff）。
*   **奖励机制**：主要奖励信号是验证集每字节比特数（val-bpb）的改进量，并辅以计算效率奖金。
*   **自我评估模块（SE）**：通过在线曲线预测（幂律模型）监控训练损失。如果预测结果不佳，则提前终止实验，从而将实验吞吐量提高约 1.35 倍（累计效率提升达 2.4 倍）。
*   **固定时间预算**：每个实验限制在 300 秒实际运行时间内，确保不同规模的模型在相同算力下进行公平竞争。

#### 3. 实验设计
*   **基准测试（Benchmark）**：单 GPU nanochat 预训练任务。
*   **数据集**：FineWeb 子集（10B token），使用 5M token 的验证集。
*   **对比方法**：
    1.  **人类专家**：手动调优的 GPT-2-small（124M 参数）基准。
    2.  **随机搜索**：在预定义超参数网格上进行均匀采样。
    3.  **贪婪 LLM（无 RL）**：直接使用 GPT-4o 作为研究智能体（零样本），不进行 RL 微调。
    4.  **AutoResearch-RL（本文方法）**。

#### 4. 资源与算力
*   **硬件**：单台 **NVIDIA H100 80GB SXM** GPU。
*   **训练时长**：
    *   隔夜运行（约 8 小时）：完成约 100 次实验。
    *   长期运行：扩展至 48 小时（周末）甚至 168 小时（一周）。
*   **单次实验预算**：300 秒（5 分钟）墙钟时间。

#### 5. 实验数量与充分性
*   **实验规模**：论文报告了从 100 次到 2147 次不等的实验迭代结果。
*   **充分性**：实验涵盖了从短期（隔夜）到长期（一周）的性能演进，并进行了消融实验（如带与不带 SE 模块的对比）。
*   **客观性**：通过固定时间预算和 token 无关的 val-bpb 指标，确保了不同架构间比较的客观性。此外，论文还提供了收敛性的理论证明（超鞅收敛）。

#### 6 主要结论与发现
*   **性能超越**：AutoResearch-RL 在 8 小时内发现的配置（val-bpb 2.681）显著优于人类专家（2.847）和贪婪 LLM（2.734）。
*   **自主发现能力**：智能体自主发现了多种先进技术，包括 **Muon 优化器缩放、QK-norm（查询-键归一化）、动态梯度裁剪计划**以及在时间预算内增加模型深度。
*   **持续进化**：随着计算时间的增加，智能体能持续找到更好的配置，未表现出明显的性能停滞。

#### 7. 优点
*   **高效性**：自我评估模块（SE）大幅减少了在无望实验上的算力浪费，显著提升了 GPU 利用率。
*   **开放性**：不同于传统 NAS，该方法允许智能体修改优化器、调度器等任何代码逻辑，搜索空间几乎不受限。
*   **内化启发式搜索**：通过 RL，智能体能够学习到“哪些修改倾向于有效”的科研直觉，而不仅仅是简单的上下文学习。

#### 8. 不足与局限
*   **算力环境单一**：目前仅在单 GPU 环境下验证，扩展到多节点、多 GPU 集群可能面临复杂的工程协调问题。
*   **搜索范围限制**：虽然代码可变，但数据流水线和词表（BPE）目前仍是固定的，尚未实现完全端到端的机器学习流水线进化。
*   **安全风险**：自主代码编辑存在潜在的安全隐患，虽然文中提到了沙箱隔离和无网络运行，但在更复杂的系统环境中仍需更严密的监控。

（完）
