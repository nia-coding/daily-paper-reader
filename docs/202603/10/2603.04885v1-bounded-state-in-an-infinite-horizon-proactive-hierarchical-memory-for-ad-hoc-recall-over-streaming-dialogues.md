---
title: "Bounded State in an Infinite Horizon: Proactive Hierarchical Memory for Ad-Hoc Recall over Streaming Dialogues"
title_zh: 无限时界中的有界状态：面向流式对话即时召回的主动分层记忆
authors: "Bingbing Wang, Jing Li, Ruifeng Xu"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.04885v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 无限时界流式对话的有界状态记忆机制
tldr: 针对无限流式对话中的记忆召回挑战，本文提出了STEM-Bench基准测试，揭示了现有方法在保真度与效率间的矛盾。为此，作者开发了ProStream框架，通过主动式分层记忆和自适应时空优化，在保持有限状态的同时实现了高效、准确的即时记忆召回，显著提升了流式对话系统的性能与响应速度。
selection_source: fresh_fetch
motivation: 现有的记忆机制难以在无限流式对话中平衡即时召回的保真度与推理效率，导致长程对话处理能力受限。
method: 提出了ProStream框架，通过多粒度蒸馏的主动分层记忆和自适应时空优化技术，动态维护有界的知识状态。
result: 实验结果显示，ProStream在STEM-Bench基准测试中的准确率和推理效率均显著优于现有的检索式及全上下文模型。
conclusion: 该研究通过主动式记忆管理有效解决了流式对话中的保真度-效率困境，为无限时域下的智能对话系统提供了可行方案。
---

## 摘要
现实世界的对话通常以无限流的形式展开。因此，它需要有界状态记忆机制在无限时界内运行。然而，现有的“先读后思”记忆模式与该场景根本不匹配，因为它无法在流展开过程中支持即时（ad-hoc）记忆召回。为了探索这一挑战，我们推出了 STEM-Bench，这是首个用于记忆流式评估（STreaming Evaluation of Memory）的基准测试。它包含对话流中的 1.4 万余个问答对，旨在评估无限时界约束下的感知保真度、时间推理和全局意识。在 STEM-Bench 上的初步分析表明存在一个关键的“保真度-效率困境”：基于检索的方法使用片段上下文，而全上下文模型则会产生无限的延迟。为解决这一问题，我们提出了 ProStream，一种面向流式对话的主动分层记忆框架。它通过多粒度蒸馏对连续流进行推理，从而实现按需的即时记忆召回。此外，它采用自适应时空优化，根据预期效用动态优化记忆保留。它在不牺牲推理保真度的前提下，实现了有界知识状态，从而降低了推理延迟。实验表明，ProStream 在准确性和效率方面均优于基准模型。

## Abstract
Real-world dialogue usually unfolds as an infinite stream. It thus requires bounded-state memory mechanisms to operate within an infinite horizon. However, existing read-then-think memory is fundamentally misaligned with this setting, as it cannot support ad-hoc memory recall while streams unfold. To explore this challenge, we introduce \textbf{STEM-Bench}, the first benchmark for \textbf{ST}reaming \textbf{E}valuation of \textbf{M}emory. It comprises over 14K QA pairs in dialogue streams that assess perception fidelity, temporal reasoning, and global awareness under infinite-horizon constraints. The preliminary analysis on STEM-Bench indicates a critical \textit{fidelity-efficiency dilemma}: retrieval-based methods use fragment context, while full-context models incur unbounded latency. To resolve this, we propose \textbf{ProStream}, a proactive hierarchical memory framework for streaming dialogues. It enables ad-hoc memory recall on demand by reasoning over continuous streams with multi-granular distillation. Moreover, it employs Adaptive Spatiotemporal Optimization to dynamically optimize retention based on expected utility. It enables a bounded knowledge state for lower inference latency without sacrificing reasoning fidelity. Experiments show that ProStream outperforms baselines in both accuracy and efficiency.

---

## 论文详细总结（自动生成）

这是一份关于论文《Bounded State in an Infinite Horizon: Proactive Hierarchical Memory for Ad-Hoc Recall over Streaming Dialogues》的结构化深入分析报告：

### 1. 论文的核心问题与整体含义
*   **研究动机**：现实中的对话是持续不断的“无限流”，但现有的语言模型（LLM）记忆机制大多遵循“先读后思”（read-then-think）的静态模式，假设上下文是完整且静态的。
*   **核心挑战**：在流式对话中，系统面临**保真度-效率困境（Fidelity-Efficiency Dilemma）**。传统的检索增强生成（RAG）会打碎上下文导致推理能力下降；而全上下文（Full-Context）模型随着对话增长，计算成本和延迟呈爆炸式增长，无法满足实时性需求。
*   **研究目标**：探索如何在无限的对话流中，通过“有界状态”（Bounded State）记忆实现高效、准确的即时（Ad-Hoc）记忆召回。

### 2. 方法论：ProStream 框架
ProStream 将记忆维护视为一个有界状态演化过程，包含四个关键阶段：
*   **主动语义流感知（Proactive Semantic Stream Perception）**：利用 Whisper ASR 将音频转为文本，并通过语义漂移阈值（$\tau_{drift}$）和短期感知缓冲区（STSB）将连续流切分为离散的语义块。
*   **分层多粒度蒸馏（Hierarchical Multi-Granular Distillation）**：将语义块递归蒸馏为三层拓扑结构：**场景（Scene）**、**事件（Event）**和**原子记忆单元（AMU）**。利用 GLiNER 提取实体关系三元组，确保记忆的细粒度。
*   **自适应时空优化（Adaptive Spatiotemporal Optimization）**：
    *   **效用公式**：$u_{v,t} = \alpha \cdot \log(f_{v,t} + 1) + \beta \cdot \exp(-\frac{\Delta t_v}{\tau})$，综合考虑访问频率（频率先验）和最近使用时间（时间漂移）。
    *   **优化逻辑**：将其建模为“带衰减值的在线背包问题”，通过贪婪边际效用策略进行剪枝、语义合并和级联抽象，确保记忆库大小恒定。
*   **概率证据增强生成（Probabilistic Evidence-Grounded Reasoning）**：在提问时，通过自顶向下的分层检索，结合短期缓冲区和分层树路径，合成最终答案。

### 3. 实验设计
*   **基准测试（STEM-Bench）**：论文构建了首个流式记忆评估基准，包含 1.4 万个 QA 对，涵盖三个维度：
    1.  **高保真感知（HFP）**：测试细节保留和抗干扰能力。
    2.  **结构逻辑推理（SLR）**：测试跨时间点的多跳推理。
    3.  **动态全局意识（DGA）**：测试频率追踪和时间排序。
*   **数据来源**：基于 LongDialQA，涵盖《生活大爆炸》、《老友记》、《办公室》三个剧集的对话，并合成语音模拟真实流场景。
*   **对比方法**：
    *   传统基准：RAG、Full Context、Rolling Summary。
    *   先进结构化方法：GraphRAG、HippoRAG、RAPTOR。
    *   智能体记忆系统：MemoRAG、A-Mem、MemGAS。

### 4. 资源与算力
*   **硬件环境**：文中提到实验在 **NVIDIA GPU** 上运行，并使用了 PyTorch 内存优化技术。
*   **具体细节**：论文**未明确列出**具体的 GPU 型号（如 A100 或 H100）及具体的训练/推理总时长。但强调了其方法在推理延迟（Latency）上的优势，证明了其在有限算力下实现实时部署的可行性。

### 5. 实验数量与充分性
*   **实验规模**：使用了 14,938 个 QA 对、55,673 条话语、6,053 个语义主题，规模较大。
*   **充分性**：
    *   **消融实验**：分别验证了短期缓冲区（B）、分层树（Mtree）和待处理缓冲区（Mpend）的必要性。
    *   **扩展性分析**：测试了不同参数规模（Qwen-3B, 7B, 14B）的底座模型。
    *   **复杂性分析**：探讨了上下文熵（复杂度）对模型表现的影响。
*   **客观性**：引入了 Gemini-2.5-Pro 作为专家评委，并结合了 BLEU、ROUGE、BERTScore、KEM（关键实体匹配）等多种自动化指标，评估维度全面。

### 6. 主要结论与发现
*   **打破困境**：ProStream 成功解决了保真度与效率的矛盾，在保持低延迟的同时，推理准确率甚至超过了全上下文（Oracle）模型。
*   **结构化优势**：主动的语义蒸馏比原始文本堆砌更能减少 LLM 的认知负荷，缓解“迷失在中间”（Lost-in-the-middle）现象。
*   **扩展规律**：模型规模越大，处理 ProStream 提供的结构化知识的能力越强，性能提升越明显。

### 7. 优点与亮点
*   **范式创新**：首次将流式对话记忆定义为“无限时界中的有界状态机”，从被动检索转向主动组织。
*   **理论支撑**：记忆优化算法有明确的数学建模（在线背包问题），并证明了其具有常数级的时间复杂度 $O(T_{max})$。
*   **实用性强**：通过自适应剪枝确保了推理延迟不随对话长度增加，非常适合实时交互系统。

### 8. 不足与局限
*   **指令-事实冲突**：在错误分析中发现，当用户提问包含错误前提时，模型倾向于顺从用户指令而非坚持记忆中的事实（合规性偏见）。
*   **语义噪声干扰**：在极高密度的对话中，模型有时会因关注显著的语义噪声而忽略细微但关键的上下文线索（如错过对话背景中的小细节）。
*   **计算开销**：虽然推理延迟低，但维护分层树（ASR、蒸馏、总结）需要持续的后台计算开销。

（完）
