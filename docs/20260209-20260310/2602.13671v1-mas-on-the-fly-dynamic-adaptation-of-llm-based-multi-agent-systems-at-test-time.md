---
title: "MAS-on-the-Fly: Dynamic Adaptation of LLM-based Multi-Agent Systems at Test Time"
title_zh: MAS-on-the-Fly：基于大语言模型的多智能体系统在测试时的动态自适应
authors: "Guangyi Liu, Haojun Lin, Huan Zeng, Heng Wang, Quanming Yao"
date: 2026-02-14
pdf: "https://arxiv.org/pdf/2602.13671v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 基于大模型的智能体系统动态适配
tldr: 针对现有大模型多智能体系统（MAS）缺乏部署后动态适应能力的问题，本文提出 MASFly 框架。该框架受生物系统启发，在测试阶段实现动态调整。它通过检索增强的 SOP 实例化机制为新查询定制协作模式，并利用经验引导的监督机制（Watcher 智能体）进行实时干预。实验表明，MASFly 在 TravelPlanner 等基准测试中达到 SOTA 水平，展现了极强的任务适应性和鲁棒性。
selection_source: fresh_fetch
motivation: 现有的多智能体系统多依赖手动设计或固定自动化流程，缺乏在测试阶段根据具体任务动态调整的能力。
method: 提出 MASFly 框架，结合检索增强的 SOP 实例化机制定制协作模式，并引入 Watcher 智能体进行经验引导的实时监督。
result: "在 TravelPlanner 基准测试中取得了 61.7% 的成功率，显著优于现有方法，并表现出优秀的适应性。"
conclusion: MASFly 证明了在测试阶段动态调整多智能体系统结构和执行过程能有效提升复杂任务的处理能力。
---

## 摘要
基于大语言模型（LLM）的多智能体系统（MAS）已成为解决复杂任务的一种极具前景的范式。然而，现有工作通常依赖于人工设计或“一刀切”的自动化方法，缺乏部署后的动态自适应能力。受生物系统适应方式的启发，我们提出了 MASFly，这是一种能够在测试时实现动态自适应的新型多智能体框架。为了实现系统生成的自适应，MASFly 采用了一种检索增强的标准作业程序（SOP）实例化机制，该机制利用自建的成功协作模式库，使 LLM 能够针对新查询组建定制化的 MAS。针对自适应执行，MASFly 引入了经验引导的监督机制，其中专门的 Watcher 智能体参考个性化经验池监控系统行为，并提供实时干预。大量实验表明，MASFly 达到了最先进的性能，特别是在 TravelPlanner 基准测试中取得了 61.7% 的成功率，同时展现出强大的任务适应性和鲁棒性。

## Abstract
Large Language Model (LLM)-based multi-agent systems (MAS) have emerged as a promising paradigm for solving complex tasks. However, existing works often rely on manual designs or "one-size-fits-all" automation, lacking dynamic adaptability after deployment. Inspired by how biological systems adapt, we introduce MASFly, a novel multi-agent framework enabling dynamic adaptation at test time. To adapt system generation, MASFly employs a retrieval-augmented SOP instantiation mechanism that leverages a self-constructed repository of successful collaboration patterns, enabling the LLM to assemble customized MASs for new queries. For adaptive execution, MASFly incorporates an experience-guided supervision mechanism, where a dedicated Watcher agent monitors system behaviors with reference to a personalized experience pool and provides real-time interventions. Extensive experiments demonstrate that MASFly achieves state-of-the-art performance, most notably a 61.7% success rate on the TravelPlanner benchmark, while exhibiting strong task adaptability and robustness.

---

## 论文详细总结（自动生成）

以下是对论文《MAS-on-the-Fly: Dynamic Adaptation of LLM-based Multi-Agent Systems at Test Time》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：现有的基于大语言模型（LLM）的多智能体系统（MAS）主要依赖于手动设计的标准作业程序（SOP）或“一刀切”的自动化构建方法。这些系统一旦部署，往往缺乏在**测试阶段（Test Time）**根据具体查询动态调整系统结构和执行逻辑的能力。
*   **核心问题**：如何在不进行额外模型训练的前提下，使 MAS 能够针对不同的任务需求动态构建系统，并在执行过程中实时纠偏，以应对复杂的长程规划和不可预见的反馈？
*   **整体含义**：论文受生物学启发，提出了 **MASFly** 框架，旨在实现 MAS 的“表型可塑性”（动态生成系统）和“稳态调节”（实时监督执行），从而提升系统的灵活性、鲁棒性和成功率。

### 2. 方法论：核心思想与关键技术
MASFly 包含三个阶段的闭环流程：
*   **SOP 引导的系统生成（SOP-Guided System Generation）**：
    *   **核心思想**：模拟生物的表型可塑性。
    *   **技术细节**：建立一个包含成功协作模式的 **SOP 仓库**。面对新查询时，通过**混合检索机制**（结合查询相似度和需求分析相似度）提取最相关的 SOP 模板，引导 LLM 实例化出特定任务的 Agent 角色、职责、工具及通信拓扑。
*   **过程监督执行（Process-Supervised Execution）**：
    *   **核心思想**：模拟生物的稳态调节。
    *   **技术细节**：引入一个全局 **Watcher（监督者）智能体** 和一个**个性化经验池（PEP）**。Watcher 实时监控 Agent 间的通信和环境交互，若发现异常（如工具误用、逻辑死循环），则参考 PEP 中的失败教训进行干预，甚至动态更换故障 Agent。
*   **反思经验蒸馏（Reflective Experience Distillation）**：
    *   **核心思想**：闭环学习与持续进化。
    *   **技术细节**：任务完成后，系统对结果进行反思。成功的案例被抽象为通用 SOP 存入仓库；失败的案例则分析原因，形成“智能体级经验”存入 PEP，供未来监督使用。

### 3. 实验设计
*   **数据集/场景**：
    1.  **长程规划**：TravelPlanner（包含 Two-Stage 和 Sole-Planning 模式）。
    2.  **通用问答**：GAIA（涵盖三个难度等级）。
    3.  **代码生成**：HumanEval、MBPP、MBPP Pro。
*   **对比方法**：
    *   **单模型基准**：CoT, ReAct, Reflexion。
    *   **手动/半自动 MAS**：MetaGPT, AgentVerse, EvoAgent, MegaAgent。
    *   **全自动/搜索类 MAS**：GPTSwarm, AFlow, AgentSquare, SwarmAgentic。

### 4. 资源与算力
*   **模型使用**：实验主要采用 **Qwen3-235B-A22B-Thinking-2507** 作为底层 LLM 骨干，通过 API 调用。
*   **算力说明**：论文**未明确说明**具体的 GPU 硬件型号、数量或训练时长。由于 MASFly 是一个**无需训练（Training-free）**的框架，其核心开销在于推理阶段的 API 调用成本和通信延迟。文中在效率分析部分对比了不同方法的运行时间（Clock Time）。

### 5. 实验数量与充分性
*   **实验规模**：在 5 个主流基准测试上进行了广泛评估，涵盖了从代码到复杂现实规划的多种任务。
*   **消融实验**：针对 SOP 检索、SOP 实例化、Watcher 监督机制、PEP 经验池分别做了消融，验证了各组件的必要性。
*   **鲁棒性测试**：专门设计了针对 MBPP Pro 的**对抗性提示攻击（Prompt Attack）**，验证系统在 Agent 被恶意干扰时的恢复能力。
*   **充分性评价**：实验设计较为全面且客观，通过多轮独立运行取平均值，并对比了最新的 SOTA 方法，证明了其在动态环境下的优越性。

### 6. 主要结论与发现
*   **性能突破**：MASFly 在所有基准测试中均优于现有方法。在最具挑战性的 TravelPlanner 任务中，其最终通过率（Final Pass Rate）达到了 **61.7%**，远超之前的 SOTA。
*   **动态适应性**：系统能根据问题难度自动调整 Agent 数量（简单问题 2 个，复杂问题 4 个以上），实现了性能与效率的平衡。
*   **极强鲁棒性**：得益于 Watcher 的实时干预，MASFly 在面对 Agent 故障或环境异常时表现出极高的恢复能力，而传统静态 MAS 在此类攻击下性能会大幅崩溃。

### 7. 优点（亮点）
*   **测试时自适应**：突破了 MAS 部署后结构固化的局限，实现了真正的“按需组建”。
*   **闭环经验学习**：无需微调模型参数，仅通过结构化的 SOP 和 PEP 积累，就能实现跨任务的经验复用。
*   **生物启发式设计**：将生物学的适应与调节原理成功转化为 MAS 的架构设计，逻辑自洽且效果显著。

### 8. 不足与局限
*   **启发式规则依赖**：Watcher 的干预频率（如每 M 轮检查一次）目前仍依赖于预设的启发式规则，而非完全自主触发，这可能在某些极端场景下导致监督滞后或过度干预。
*   **推理延迟**：由于引入了 Watcher 监控和多轮反思，系统的总推理时长和 API 消耗可能高于简单的单 Agent 或静态 MAS。
*   **经验池规模**：虽然文中提到 SOP 仓库较小，但随着任务种类的极度扩张，如何高效管理和检索海量的 PEP 经验仍是一个潜在的扩展性挑战。

（完）
