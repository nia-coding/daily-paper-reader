---
title: "IntentCUA: Learning Intent-level Representations for Skill Abstraction and Multi-Agent Planning in Computer-Use Agents"
title_zh: IntentCUA：面向计算机使用智能体中技能抽象与多智能体规划的意图级表示学习
authors: "Seoyoung Lee, Seobin Yoon, Seongbeen Lee, Yoojung Chun, Dayoung Park, Doyeon Kim, Joo Yong Sim"
date: 2026-02-19
pdf: "https://arxiv.org/pdf/2602.17049v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 用于长程执行和技能抽象的多智能体计算机使用框架
tldr: IntentCUA是一个针对计算机操作智能体的多智能体框架，旨在解决长程任务中的意图偏移和效率低下问题。该框架通过Planner、Plan-Optimizer和Critic的协作，将原始交互轨迹抽象为多视图意图表示和可重用技能，并存储在共享内存中。实验表明，IntentCUA在任务成功率和步骤效率上显著优于现有基准，证明了意图抽象和记忆驱动协作在复杂桌面自动化中的有效性。
selection_source: fresh_fetch
motivation: 现有的计算机操作智能体在长程任务中容易偏离用户意图，且因重复解决常规子问题导致效率低下和错误累积。
method: 提出IntentCUA框架，利用多智能体协作将交互轨迹抽象为意图对齐的技能，并通过共享内存和意图原型检索实现高效规划。
result: "在端到端评估中，IntentCUA实现了74.83%的任务成功率和0.91的步骤效率比，显著超越了基于强化学习和轨迹检索的基准方法。"
conclusion: 系统级的意图抽象和基于记忆的多智能体协作是提升动态桌面环境下自动化任务可靠性与效率的关键。
---

## 摘要
计算机使用智能体在噪声感知、多窗口上下文和不断演变的环境状态下进行长时程操作。现有方法（从基于强化学习的规划器到轨迹检索）往往会偏离用户意图，并重复解决常规子问题，导致误差累积和效率低下。我们提出了 IntentCUA，这是一个多智能体计算机使用框架，旨在通过意图对齐的规划记忆来稳定长时程执行。规划器（Planner）、规划优化器（Plan-Optimizer）和评论家（Critic）在共享记忆上进行协作，将原始交互轨迹抽象为多视图意图表示和可重用技能。在运行时，意图原型检索子组对齐的技能并将其注入部分规划中，从而减少冗余的重新规划并减轻跨桌面应用程序的误差传播。在端到端评估中，IntentCUA 实现了 74.83% 的任务成功率，步长效率比（Step Efficiency Ratio）为 0.91，优于基于强化学习和以轨迹为中心的基准模型。消融实验表明，多视图意图抽象和共享规划记忆共同提高了执行稳定性，其中协作式多智能体循环在长时程任务中带来的提升最大。这些结果强调，系统级意图抽象和基于记忆的协调是在大型动态环境中实现可靠且高效桌面自动化的关键。

## Abstract
Computer-use agents operate over long horizons under noisy perception, multi-window contexts, evolving environment states. Existing approaches, from RL-based planners to trajectory retrieval, often drift from user intent and repeatedly solve routine subproblems, leading to error accumulation and inefficiency. We present IntentCUA, a multi-agent computer-use framework designed to stabilize long-horizon execution through intent-aligned plan memory. A Planner, Plan-Optimizer, and Critic coordinate over shared memory that abstracts raw interaction traces into multi-view intent representations and reusable skills. At runtime, intent prototypes retrieve subgroup-aligned skills and inject them into partial plans, reducing redundant re-planning and mitigating error propagation across desktop applications. In end-to-end evaluations, IntentCUA achieved a 74.83% task success rate with a Step Efficiency Ratio of 0.91, outperforming RL-based and trajectory-centric baselines. Ablations show that multi-view intent abstraction and shared plan memory jointly improve execution stability, with the cooperative multi-agent loop providing the largest gains on long-horizon tasks. These results highlight that system-level intent abstraction and memory-grounded coordination are key to reliable and efficient desktop automation in large, dynamic environments.

---

## 论文详细总结（自动生成）

以下是对论文《IntentCUA: Learning Intent-level Representations for Skill Abstraction and Multi-Agent Planning in Computer-Use Agents》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的计算机使用智能体（Computer-Use Agents）在处理**长程（Long-horizon）任务**时面临三大挑战：
    1.  **意图偏移**：在执行多个子步骤时，智能体容易偏离原始用户目标。
    2.  **效率低下**：经常重复解决已经完成的常规子问题，导致冗余规划。
    3.  **误差累积**：局部感知错误会随步骤增加而传播，导致执行崩溃。
*   **研究背景**：虽然基于大模型的 GUI 智能体在 Web 和移动端有所进展，但在复杂的桌面环境（多窗口切换、OS 级快捷键、动态更新）中，保持长期的意图一致性和执行稳定性仍然是一个未解决的难题。

### 2. 方法论
IntentCUA 提出了一个多智能体协作框架，核心思想是将原始交互轨迹转化为**意图级表示**，并以此构建可重用的**技能抽象**。
*   **多视图意图表示学习**：
    *   从环境（E）、动作（A）、关键词（K）和描述（D）四个维度对用户轨迹进行标注。
    *   使用多视图编码器将这些特征映射到统一的共享嵌入空间。
    *   **损失函数**：结合了跨视图对比损失（$L_{con}$）、双向预测损失（$L_{pred}$）和视图内重构损失（$L_{rec}$），确保表示的语义一致性和区分度。
*   **技能抽象与分层索引**：
    *   利用 HDBSCAN 算法进行两级聚类：先按环境聚类为**意图组（IG）**，再按动作/描述聚类为**子组（SG）**。
    *   从子组中提取“技能提示（Skill Hints）”，即参数化的动作模式（Verb-Argument Structure），用于指导规划。
*   **多智能体协作规划循环**：
    *   **Planner（规划器）**：负责高层规划。采用“缓存优先”策略，根据意图原型从记忆中检索已有的全局规划或通过技能提示进行“填补缺口（Gap Filling）”。
    *   **Plan-Optimizer（规划优化器）**：将规划步骤落地为具体的 GUI 原子动作，并结合记忆中的相似轨迹进行动态调整。
    *   **Critic（评论家）**：监控执行后的屏幕状态，反馈“成功”、“可重试”或“阻塞”信号，触发局部重优化而非全局重规划。

### 3. 实验设计
*   **数据集/场景**：共 286 个真实世界 GUI 任务，涵盖 63 个领域。
    *   116 个来自 WebVoyager（Web 任务）。
    *   70 个来自 ScreenAgent（跨应用任务）。
    *   100 个内部自建任务（本地应用）。
*   **基准方法（Benchmarks）**：
    *   **UI-TARS-1.5**：基于强化学习的视觉规划执行器。
    *   **UFO 2**：以轨迹为中心的 Windows 自动化智能体。
*   **评估指标**：任务成功率（Success Rate）、步骤效率比（SER，成功步数/总步数）、延迟（Latency）。

### 4. 资源与算力
*   **模型使用**：所有智能体均基于 **GPT-4o** 模型。
*   **嵌入模型**：使用 OpenAI 的 `text-embedding-3-large` 生成 3072 维向量。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量或训练时长。仅提到编码器训练时的超参数（学习率 $1 \times 10^{-3}$，对比温度 $\tau=0.1$ 等）。

### 5. 实验数量与充分性
*   **实验规模**：测试了 286 个任务，并收集了 30 小时的交互轨迹（113 个文件）作为训练语料。
*   **消融实验**：设计了 5 组消融实验，分别验证了用户轨迹、意图表示学习、规划记忆（Plan-Memory）和技能提示（Skill Hint）的贡献。
*   **分布偏移测试**：训练轨迹仅覆盖 36 个领域，而测试集覆盖 63 个领域（65% 为未见领域），充分验证了系统的**泛化能力**。
*   **客观性**：通过步骤长度分桶（Binning）分析了成功率随任务复杂度增加的变化趋势，实验设计较为客观、公平。

### 6. 主要结论与发现
*   **性能领先**：IntentCUA 实现了 **74.83%** 的总成功率，远高于 UFO 2（51.2%）和 UI-TARS-1.5（38.8%）。
*   **长程稳定性**：在超过 30 步的极长任务中，IntentCUA 仍保持了 42.9% 的成功率，而基准方法均跌至 20% 以下。
*   **高效率与低延迟**：SER 达到 0.91，平均执行延迟比基准方法低 **4.5 倍**（1.46 分钟 vs 6.63/9.82 分钟），证明了记忆检索比重复规划更高效。

### 7. 优点
*   **意图对齐的记忆机制**：不只是简单的轨迹回放，而是通过抽象的意图原型进行检索，有效解决了长程任务中的意图漂移问题。
*   **技能参数化**：将原始操作转化为可重用的 Schema，增强了在不同应用间的迁移能力。
*   **多智能体闭环**：Planner-Optimizer-Critic 的协作模式减少了因感知噪声导致的全局崩溃，提高了系统的鲁棒性。

### 8. 不足与局限
*   **感知瓶颈**：系统仍依赖于脚本化的 GUI Grounding，对于突发弹窗（Pop-ups）或视觉遮挡的适应性有限。
*   **记忆检索开销**：随着规划记忆库的无限增长，检索效率可能会受到影响（虽然目前主要影响延迟而非准确性）。
*   **模型依赖**：高度依赖 GPT-4o 等强力大模型，对于轻量化部署或本地化运行的支持尚未讨论。

（完）
