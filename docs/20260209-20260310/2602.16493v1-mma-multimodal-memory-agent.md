---
title: "MMA: Multimodal Memory Agent"
title_zh: MMA：多模态记忆智能体
authors: "Yihao Lu, Wanru Cheng, Zeyu Zhang, Hao Tang"
date: 2026-02-18
pdf: "https://arxiv.org/pdf/2602.16493v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 具有动态可靠性评估的长程多模态记忆智能体
tldr: 针对长程多模态智能体在记忆检索中常遇到过时、低可信度或冲突信息的问题，本文提出多模态记忆智能体（MMA）。该框架通过结合来源可信度、时间衰减和冲突感知共识为检索项分配动态可靠性评分，从而重新加权证据或在支持不足时拒绝回答。研究还引入了MMA-Bench基准，揭示了视觉安慰剂效应。实验表明，MMA在减少预测方差、提升安全性和处理视觉冲突方面表现优异。
selection_source: fresh_fetch
motivation: 传统的相似度检索容易引入过时或冲突的记忆，导致多模态智能体产生过度自信的错误。
method: 提出MMA框架，利用动态可靠性评分机制对检索到的记忆进行加权，并根据证据强度决定是否采取行动。
result: "在FEVER上减少了35.2%的方差，在MMA-Bench的视觉模式下将准确率从基准的0%提升至41.18%。"
conclusion: MMA通过精细化的记忆可靠性管理，有效缓解了基础模型的视觉偏见并提升了长程任务的决策质量。
---

## 摘要
长程多模态智能体依赖于外部记忆；然而，基于相似性的检索往往会提取出陈旧、低可信度或冲突的项，这可能引发过度自信的错误。我们提出了多模态记忆智能体（MMA），它通过结合来源可信度、时间衰减和冲突感知的网络共识，为每个检索到的记忆项分配动态可靠性评分，并利用该信号重新加权证据，在支持不足时选择弃权。我们还引入了 MMA-Bench，这是一个通过程序生成的基准测试，用于评估具有受控说话者可靠性和结构化文本-视觉矛盾的信念动力学。利用该框架，我们揭示了“视觉安慰剂效应”（Visual Placebo Effect），展示了基于检索增强生成（RAG）的智能体如何从基础模型中继承潜在的视觉偏见。在 FEVER 数据集上，MMA 在保持与基线相当的准确率的同时，将方差降低了 35.2% 并提高了选择性效用；在 LoCoMo 上，面向安全的配置提高了可执行准确率并减少了错误回答；在 MMA-Bench 上，MMA 在视觉模式下达到了 41.18% 的 Type-B 准确率，而基线在相同协议下则崩溃至 0.0%。代码：https://github.com/AIGeeksGroup/MMA。

## Abstract
Long-horizon multimodal agents depend on external memory; however, similarity-based retrieval often surfaces stale, low-credibility, or conflicting items, which can trigger overconfident errors. We propose Multimodal Memory Agent (MMA), which assigns each retrieved memory item a dynamic reliability score by combining source credibility, temporal decay, and conflict-aware network consensus, and uses this signal to reweight evidence and abstain when support is insufficient. We also introduce MMA-Bench, a programmatically generated benchmark for belief dynamics with controlled speaker reliability and structured text-vision contradictions. Using this framework, we uncover the "Visual Placebo Effect", revealing how RAG-based agents inherit latent visual biases from foundation models. On FEVER, MMA matches baseline accuracy while reducing variance by 35.2% and improving selective utility; on LoCoMo, a safety-oriented configuration improves actionable accuracy and reduces wrong answers; on MMA-Bench, MMA reaches 41.18% Type-B accuracy in Vision mode, while the baseline collapses to 0.0% under the same protocol. Code: https://github.com/AIGeeksGroup/MMA.

---

## 论文详细总结（自动生成）

# 论文总结：MMA (Multimodal Memory Agent)

## 1. 核心问题与整体含义
在长程交互的多模态智能体中，外部记忆系统是维持上下文的关键。然而，现有的基于相似度检索（RAG）的记忆机制存在一个核心缺陷：**它默认所有检索到的记忆项都是同等可靠的**。在实际应用中，检索到的信息可能过时、来源不可信或相互冲突，导致智能体产生“过度自信的错误”或幻觉。

本文的研究动机在于：
*   **记忆层面的可靠性评估**：需要一种机制来区分可信信息与可疑内容。
*   **认识论上的谨慎（Epistemic Prudence）**：智能体应具备检测证据不足并选择“弃权（Abstain）”的能力，而非盲目给出错误答案。

## 2. 方法论
论文提出了 **MMA (Multimodal Memory Agent)** 框架，其核心是在推理阶段引入一个**元认知可靠性层（Confidence Module）**，为每个检索到的记忆项 $M_i$ 计算动态可靠性评分 $C(M_i)$。

### 核心技术细节：
可靠性评分由三个维度加权合成：
1.  **来源可信度 (Source Reliability, $S$)**：根据记忆来源（如不同用户或渠道）预设的信任先验进行打分。
2.  **时间衰减 (Temporal Decay, $T$)**：使用指数衰减函数模拟信息随时间的陈旧化，半衰期为 $T_{half}$。
3.  **网络共识 (Network Consensus, $C_{con}$)**：通过计算检索项与其语义邻域内其他记忆的一致性来过滤冲突。公式中引入了支持因子 $\sigma_{ij}$（余弦相似度），正值增强信心，负值（冲突）则惩罚信心。

### 决策流程：
智能体利用这些评分对证据进行重新加权。如果综合证据强度低于阈值，智能体将触发弃权机制，输出“未知”或“信息不足”，从而实现选择性预测。

## 3. 实验设计
论文在三个主要场景/基准上进行了评估：
*   **FEVER (事实核查)**：使用 500 个样本，评估智能体在处理需要证据验证的声明时的准确性与稳定性。
*   **LoCoMo (长程对话记忆)**：评估智能体在极长对话历史（跨越数月）中的信息检索和推理能力。
*   **MMA-Bench (本文提出)**：一个程序化生成的诊断性基准，专门设计了 10 个会话的叙事流，包含：
    *   **来源先验控制**：设置可靠用户 A 和不可靠用户 B。
    *   **逻辑矩阵**：分为四种冲突类型（标准、可靠性反转、歧义、不可知）。
    *   **多模态冲突**：故意设置文本与视觉证据之间的矛盾。
*   **对比方法**：主要对比了 **MIRIX**（先进的记忆智能体基线）、GPT-4.1-mini 和 Qwen-3-VL-Plus（作为基础模型参考）。

## 4. 资源与算力
论文中**未明确说明**具体的硬件配置（如 GPU 型号、数量）以及训练/推理的总时长。由于 MMA 主要是一个推理时（Inference-time）的框架，其核心开销在于计算记忆项之间的语义共识和可靠性评分，而非大规模模型训练。

## 5. 实验数量与充分性
实验设计较为全面且具有说服力：
*   **多维度评估**：涵盖了标准事实核查、长程对话和极端冲突诊断。
*   **统计稳定性**：在 FEVER 实验中使用了 3 个随机种子，并报告了标准差，证明了 MMA 在降低方差方面的有效性。
*   **消融实验**：针对 $S$（来源）、$T$（时间）、$C_{con}$（共识）三个组件分别进行了消融分析，验证了各模块在不同场景下的贡献。
*   **客观性**：引入了 CoRe (Confidence-and-Reserve) 评分机制，奖励合理的弃权并惩罚过度自信的错误，这比单纯的准确率指标更符合实际部署需求。

## 6 主要结论与发现
1.  **视觉安慰剂效应 (Visual Placebo Effect)**：研究发现，仅仅是视觉数据的存在（即使是模糊或无关的）就会绕过智能体的认识论过滤器，产生证据充足的错觉，导致智能体在原本应弃权的情况下给出错误答案。
2.  **提升稳定性**：在 FEVER 上，MMA 在保持准确率的同时，将性能方差降低了 **35.2%**。
3.  **解决可靠性反转**：在 MMA-Bench 的视觉模式下，当视觉证据支持不可靠来源时，基线模型完全崩溃（0% 准确率），而 MMA 达到了 **41.18%** 的准确率。
4.  **安全性提升**：在 LoCoMo 上，MMA 的安全配置显著减少了错误回答的数量，提高了可执行准确率。

## 7. 优点
*   **动态可靠性建模**：不只是简单的检索，而是对记忆质量进行了多维度的细粒度管理。
*   **MMA-Bench 的创新性**：填补了现有基准在“受控来源可靠性”和“跨模态冲突”评估上的空白。
*   **选择性预测**：通过弃权机制有效缓解了 RAG 系统中常见的幻觉问题。

## 8. 不足与局限
*   **依赖检索召回率**：MMA 作为一个后检索处理模块，如果底层的 RAG 系统根本没有检索到相关上下文，MMA 无法凭空修复缺失的信息。
*   **稀疏性-共识权衡**：在信息密度极低的场景（如 LoCoMo）中，严格的共识检查可能过于保守，导致智能体因找不到足够的邻域支持而错误地选择弃权。
*   **计算开销**：虽然论文未详述，但计算所有检索项之间的两两共识（Consensus）在检索量巨大时可能会增加推理延迟。

（完）
