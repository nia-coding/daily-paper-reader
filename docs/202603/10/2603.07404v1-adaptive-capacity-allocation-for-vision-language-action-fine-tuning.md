---
title: Adaptive Capacity Allocation for Vision Language Action Fine-tuning
title_zh: 视觉语言动作微调的自适应容量分配
authors: "Donghoon Kim, Minji Bae, Unghui Nam, Gyeonghun Kim, Suyun Lee, Kyuhong Shim, Byonghyo Shim"
date: 2026-03-08
pdf: "https://arxiv.org/pdf/2603.07404v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 视觉语言动作模型的秩自适应微调
tldr: 针对视觉语言动作模型（VLA）在机器人任务微调中固定秩难以适应不同任务复杂度的问题，本文提出LoRA-SP（Select-Prune）。该方法通过SVD风格的参数化和基于能量目标的动态路由，实现了输入和层级的自适应容量分配。在真实机器人实验中，LoRA-SP在显著减少参数量的同时，提升了多任务泛化性能，解决了传统LoRA在机器人领域表现不佳的痛点。
selection_source: fresh_fetch
motivation: 传统LoRA的固定秩难以满足机器人任务中高且多变的内在秩需求，导致多任务微调时性能受限且干扰严重。
method: 提出LoRA-SP，利用SVD参数化和能量目标驱动的路由器，动态选择和剪枝共享向量库中的活跃分量以实现自适应容量分配。
result: "在真实机器人操纵任务中，LoRA-SP比标准LoRA提升了高达31.6%的多任务成功率，且性能媲美甚至超过全量微调。"
conclusion: 通过动态秩分配和能量控制，LoRA-SP有效提升了VLA模型在复杂物理环境下的微调效率和跨任务泛化能力。
---

## 摘要
视觉语言动作模型（VLA）越来越多地应用于具身智能（Physical AI），但将预训练的 VLA 模型部署到未见过的环境、机器人本体或任务中仍需要进行适配。参数高效微调（PEFT），特别是 LoRA，在 VLA 策略中非常常用，然而其暴露的容量调节参数——秩（rank），在迁移过程中表现并不统一：机器人领域的迁移相比语言微调表现出更高且随任务变化的本征秩。对于大语言模型（LLM），较小的秩（如 $r \in \{4,

## Abstract
Vision language action models (VLAs) are increasingly used for Physical AI, but deploying a pre-trained VLA model to unseen environments, embodiments, or tasks still requires adaptation. Parameter-efficient fine-tuning (PEFT), especially LoRA, is common for VLA policies, yet the exposed capacity knob, the rank, does not transfer uniformly: robotics transfer exhibits a higher and task-varying intrinsic rank than language fine-tuning. Small ranks suffice for LLMs (e.g., $r \in \{4, 8\}$), while spectral analyses indicate VLAs may require much larger ranks (e.g., $r \approx 128$) or near-full rank, a mismatch that worsens in multi-task settings. We present LoRA-SP (Select-Prune), a rank-adaptive fine-tuning method that replaces fixed-rank updates with input- and layer-wise capacity. LoRA-SP uses an SVD-style parameterization with a small router whose nonnegative scores act as singular values over a shared vector bank. The active set is chosen by an energy target on the cumulative squared scores $E(k) \ge η$, providing a direct link to approximation error via our spectral analysis. During training, $η$ concentrates energy on a few directions and teaches the router to rely on fewer vectors while preserving accuracy. This yields compact adapters that reduce cross-task interference and improve generalization. On four real-robot manipulation tasks collected on an unseen AgileX PiPER arm, across two VLA backbones ($π_0$ and SmolVLA), LoRA-SP matches or exceeds full fine-tuning with far fewer trainable parameters, and improves multi-task success by up to 31.6% over standard LoRA while remaining robust to rank choice.

---

## 论文详细总结（自动生成）

以下是对论文《Adaptive Capacity Allocation for Vision Language Action Fine-tuning》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：视觉语言动作模型（VLA）在适配新环境或新机器人本体（Embodiment）时，传统的参数高效微调（PEFT）方法（如 LoRA）存在“秩（Rank）不匹配”问题。
*   **研究背景**：
    *   **本征秩差异**：大语言模型（LLM）微调通常只需要极小的秩（如 4 或 8），但 VLA 模型在处理物理交互任务时，其本征秩（Intrinsic Rank）显著更高且随任务复杂度剧烈波动（通常需要 $r \approx 128$ 甚至接近全量微调）。
    *   **多任务干扰**：在多任务微调中，固定秩的 LoRA 会强制不同复杂度的任务共享相同的子空间，导致严重的跨任务干扰和性能下降。
*   **整体含义**：本文旨在通过动态分配微调容量，解决 VLA 模型在面对未见过的机器人硬件和复杂多任务场景时的适配效率与泛化性问题。

### 2. 方法论：LoRA-SP (Select-Prune)
核心思想是将固定秩的更新替换为**基于输入和层级的自适应容量分配**。
*   **SVD 风格参数化**：将权重更新 $\Delta W$ 参数化为 $\Delta W(x) = U \text{diag}(s(x)) V$。其中 $U$ 和 $V$ 是共享的向量库（基底），$s(x)$ 是由一个轻量级路由器（Router）生成的非负得分，充当“奇异值”。
*   **选择机制 (Select)**：
    *   路由器根据当前输入 $x$ 为向量库中的每个方向打分。
    *   计算累积能量 $E(k) = \frac{\sum_{i=1}^k s_i^2}{\sum_{j=1}^r s_j^2}$。
    *   **动态秩选择**：选择最小的 $k$，使得 $E(k) \ge \eta$（$\eta$ 为预设的能量阈值，如 0.9）。这直接建立了低秩近似误差与任务需求之间的理论联系。
*   **剪枝机制 (Prune)**：
    *   引入**谱损失 (Spectral Loss)**：$\mathcal{L}_{spec} = 1 - E_k(x)$。
    *   该损失函数强制能量集中在少数关键方向上，训练过程中路由器会学会关闭不相关的向量，从而在保持精度的同时实现极高的参数压缩。

### 3. 实验设计
*   **机器人平台**：使用 **AgileX PiPER**（7 自由度机械臂）。这是一个预训练数据中未包含的“未见过本体（Unseen Embodiment）”。
*   **任务场景**：包含 4 个真实世界操纵任务：开锅盖（Open）、倒方块（Pour）、按按钮（Press）、抓放葡萄（Pick-Place）。
*   **数据集**：共收集 480 个真实演示视频（每个任务 120 个），包含侧视和腕部双摄像头 RGB 输入。
*   **基准模型 (Backbones)**：
    *   **$\pi_0$ (3.5B)**：基于 PaLIGemma 的高容量模型。
    *   **SmolVLA**：基于 SmolVLM-2 的轻量级模型。
*   **对比方法**：全量微调 (Full FT)、标准 LoRA ($r \in \{16, 32, 64, 128\}$)、AdaLoRA（基于重要性的秩分配）、LoRA-MoE（混合专家架构，包含 top-1 和加权求和路由）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。
*   **参数规模**：提到了微调参数占比。例如在 $\pi_0$ 上，LoRA-SP 的可训练参数约占总量的 9.2%；在 SmolVLA 上约占 17.1%。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了单任务与多任务的对比实验。
    *   针对两个不同规模的 VLA 主干网络进行了验证。
    *   包含详细的消融实验：验证了谱损失的必要性以及能量阈值 $\eta$ 对性能/秩的影响。
*   **充分性评价**：实验设计较为充分。通过在“未见过机器人本体”上进行测试，有力证明了方法的泛化能力。对比了多种 PEFT 变体，且使用了真实机器人而非纯仿真环境，增强了结果的说服力。

### 6. 主要结论与发现
*   **性能提升**：在多任务设置下，LoRA-SP 的成功率比标准 LoRA 提升了高达 **31.6%**，且在多个任务中达到或超过了全量微调的水平。
*   **秩的异质性**：实验发现视觉塔（Vision Tower）通常需要极高的秩，而动作专家（Action Expert）的秩需求随输入波动较大，语言模型层则相对较低。这证明了固定秩分配的不合理性。
*   **鲁棒性**：LoRA-SP 对初始秩的选择不敏感，能够自动通过能量目标 $\eta$ 调整到最优的活跃秩。

### 7. 优点
*   **理论支撑强**：利用 SVD 谱分析建立能量目标与近似误差的联系，比启发式的秩分配更具解释性。
*   **动态适配**：解决了 VLA 模型在不同模块（视觉 vs 动作）间容量需求极度不均的问题。
*   **抗干扰能力**：通过输入相关的向量激活，减少了多任务学习中的参数竞争（Interference）。

### 8. 不足与局限
*   **推理开销**：虽然活跃秩降低了，但引入了额外的路由器（Router）计算开销，文中未详细量化其对实时控制频率（Hz）的具体影响。
*   **本体覆盖**：实验仅在一种新机器人（PiPER）上完成，若能涵盖更多不同运动学结构的机器人（如双臂或移动底座）会更具普适性。
*   **数据依赖**：虽然在 480 个演示上表现良好，但在极小样本（如 <10 个演示）下的表现尚未讨论。

（完）
