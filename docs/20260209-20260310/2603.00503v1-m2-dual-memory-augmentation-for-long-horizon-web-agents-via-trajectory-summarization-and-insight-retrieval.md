---
title: "M$^2$: Dual-Memory Augmentation for Long-Horizon Web Agents via Trajectory Summarization and Insight Retrieval"
title_zh: M$^2$：通过轨迹摘要与见解检索实现的面向长程网络智能体的双重记忆增强
authors: "Dawei Yan, Haokui Zhang, Guangda Huzhang, Yang Li, Yibo Wang, Qing-Guo Chen, Zhao Xu, Weihua Luo, Ying Li, Wei Dong, Chunhua Shen"
date: 2026-02-28
pdf: "https://arxiv.org/pdf/2603.00503v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 长程网页智能体的双层记忆机制
tldr: 针对多模态大模型在长程网页任务中面临的计算开销大和推理能力不足的问题，本文提出了M$^2$框架。这是一种无需训练的双重记忆增强方法，通过动态轨迹总结（内部记忆）将冗余交互历史压缩为简洁的状态更新，并利用见解检索增强（外部记忆）从离线库中获取操作指南。该方法在WebVoyager等基准测试中显著提升了成功率并大幅降低了Token消耗，为构建高效网页智能体提供了新方案。
selection_source: fresh_fetch
motivation: 现有的网页智能体在处理长程任务时，面临计算成本高昂、上下文冗余以及复杂场景下推理能力不足的瓶颈。
method: 提出一种双层记忆机制，通过动态轨迹总结压缩交互历史为内部记忆，并从离线见解库中检索外部指南以增强决策。
result: "在多个基准测试中，该方法使模型成功率最高提升19.6%，同时显著降低了高达58.7%的Token计算开销。"
conclusion: M$^2$框架证明了通过高效的记忆管理和知识检索，可以在无需额外训练的情况下显著优化网页智能体的长程任务性能与效率。
---

## 摘要
基于多模态大语言模型（MLLMs）的智能体在自主网络导航中展现出巨大潜力。然而，处理长程任务仍然是一个关键瓶颈。现有的策略通常严重依赖大规模数据收集和模型训练，但在面对复杂、长程场景时，仍面临高昂的计算成本和推理能力不足的问题。为此，我们提出了 M$^2$，这是一个无需训练的记忆增强框架，旨在优化上下文效率和决策鲁棒性。我们的方法结合了双层记忆机制，协同利用动态轨迹摘要（内部记忆）将冗长的交互历史压缩为简洁的状态更新，以及见解检索增强（外部记忆）通过从离线见解库中检索可操作的指南来引导智能体。在 WebVoyager 和 OnlineMind2Web 上的广泛评估表明，M$^2$ 持续超越基准模型，使 Qwen3-VL-32B 的成功率提升高达 19.6%，Token 消耗减少 58.7%，而像 Claude 这样的闭源模型在显著降低计算开销的同时，准确率提升了高达 12.5%。

## Abstract
Multimodal Large Language Models (MLLMs) based agents have demonstrated remarkable potential in autonomous web navigation. However, handling long-horizon tasks remains a critical bottleneck. Prevailing strategies often rely heavily on extensive data collection and model training, yet still struggle with high computational costs and insufficient reasoning capabilities when facing complex, long-horizon scenarios. To address this, we propose M$^2$, a training-free, memory-augmented framework designed to optimize context efficiency and decision-making robustness. Our approach incorporates a dual-tier memory mechanism that synergizes Dynamic Trajectory Summarization (Internal Memory) to compress verbose interaction history into concise state updates, and Insight Retrieval Augmentation (External Memory) to guide the agent with actionable guidelines retrieved from an offline insight bank. Extensive evaluations across WebVoyager and OnlineMind2Web demonstrate that M$^2$ consistently surpasses baselines, yielding up to a 19.6% success rate increase and 58.7% token reduction for Qwen3-VL-32B, while proprietary models like Claude achieve accuracy gains up to 12.5% alongside significantly lower computational overhead.

---

## 论文详细总结（自动生成）

以下是对论文《M$^2$: Dual-Memory Augmentation for Long-Horizon Web Agents via Trajectory Summarization and Insight Retrieval》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：多模态大模型（MLLM）在执行长程（Long-Horizon）网页导航任务时，面临两大瓶颈：
    1.  **计算成本高昂**：传统的“全上下文”策略将所有历史截图和文本堆叠，导致 Token 消耗随步骤呈指数级或线性剧增。
    2.  **性能退化（Lost-in-the-Middle）**：冗长且充满噪声的历史信息（如广告、侧边栏）会干扰模型的推理，导致决策错误。
*   **研究背景**：现有改进方案通常依赖大规模的监督微调（SFT）或强化学习（RL），这不仅需要海量数据，还伴随着极高的训练成本。本文旨在提出一种**无需训练（Training-free）**、轻量化且高效的记忆增强框架。

### 2. 方法论：双重记忆增强框架（M$^2$）
M$^2$ 框架通过模拟人类认知，将上下文管理分为内部状态追踪和外部经验借鉴：
*   **内部记忆（Internal Memory, In-Mem）——动态轨迹摘要**：
    *   **核心思想**：用简洁的文本摘要取代冗余的原始历史截图。
    *   **技术细节**：在每一步交互中，要求智能体在输出动作的同时，生成一个三元组 `(Thought, Action, State Summary)`。
    *   **流程**：系统仅保留当前步骤的原始截图，而将历史步骤压缩为“状态 $\rightarrow$ 动作”的文本链。这种递归更新机制使上下文长度随任务深度仅呈亚线性增长。
*   **外部记忆（External Memory, Ex-Mem）——见解检索增强**：
    *   **核心思想**：从历史成功经验中提取通用导航策略。
    *   **技术细节**：构建一个离线“见解库（Insight Bank）”，包含从 5.5 万条成功轨迹中提取的 UI 交互规则（如搜索策略、点击逻辑、状态验证）。
    *   **流程**：推理时，利用 Sentence Transformer 计算当前用户查询与库中历史查询的语义相似度，检索 Top-5 相关见解并作为“防御性提示（Defensive Hints）”注入系统提示词。

### 3. 实验设计
*   **数据集/场景**：
    *   **WebVoyager**：涵盖 12 个代表性网站（如 Amazon, Apple, GitHub 等）。
    *   **OnlineMind2Web**：包含 291 个真实世界的在线任务，分为简单、中等、困难三个等级。
*   **基准（Benchmark）与对比方法**：
    *   **对比基准**：各模型的“Normal”模式（全上下文拼接策略）。
    *   **对比模型**：闭源前沿模型（Claude-3.7-Sonnet, Claude-Sonnet-4）和开源模型（Qwen3-VL-32B）。
    *   **间接对比**：与经过大量 SFT/RL 训练的 SOTA 模型（如 UI-TARS, OpenAI CUA）进行性能对标。

### 4. 资源与算力
*   **算力设施**：实验运行在基于 **Kubernetes (K8s)** 的高并发集群上，部署了 **64 个并发节点**。
*   **环境配置**：每个节点托管一个虚拟计算机环境，通过 `kubectl` 进行浏览器操作和视觉反馈捕获。
*   **训练时长**：由于该方法是 **Training-free（无需训练）** 的，因此不存在模型训练时长。主要的计算开销在于离线见解库的提取（使用 Ovis-80A3B 模型作为提取器）和推理时的向量检索（延迟约 6 毫秒，可忽略不计）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流基准上进行了详尽测试，涵盖了从电商到学术、地图等多种网页领域。
    *   **消融实验**：针对历史窗口大小 $k$、相似度函数（TF-IDF vs. Embedding）、检索见解数量 $i$、以及内部记忆的组成成分进行了 4 组专项对比。
*   **充分性与客观性**：实验设计较为充分。通过对比不同参数设置，验证了 5 步历史窗口和 Top-5 见解是最佳平衡点。使用 GPT-4o 作为第三方评判器（Judge）来评估任务成功率，确保了评估的客观性。

### 6. 主要结论与发现
*   **性能显著提升**：在 OnlineMind2Web 上，Qwen3-VL-32B 的成功率提升了 **19.6%**；在 WebVoyager 上，Claude 系列模型也有 **5.5% - 12.5%** 的增幅。
*   **大幅降低成本**：Qwen3-VL-32B 的 Token 消耗平均减少了 **58.7%**，Claude 模型的 Token 减少量也在 **30% - 55%** 之间。
*   **以弱胜强**：经过 M$^2$ 增强的开源模型（Qwen3-VL-32B, 74.0%）在 WebVoyager 上的表现甚至超过了未增强的闭源顶尖模型（Claude-3.7-Sonnet, 72.0%）。
*   **长程稳定性**：内部记忆有效解决了“上下文爆炸”问题，使智能体在长达 60 步的任务中依然能保持清晰的进度感。

### 7. 优点
*   **即插即用**：无需对模型进行任何参数更新，适用于任何具有多模态能力的 LLM。
*   **效率与精度双赢**：在提升导航成功率的同时，通过压缩历史信息极大地降低了推理延迟和 API 调用成本。
*   **跨任务泛化**：外部见解库提取的是通用的 UI 交互逻辑（如“若搜索无结果则简化关键词”），具有很强的跨领域迁移能力。

### 8. 不足与局限
*   **见解库依赖**：框架的上限受限于离线见解库的质量和覆盖范围。如果面对全新的、交互逻辑迥异的网页，检索到的见解可能失效。
*   **纯文本摘要的局限**：内部记忆目前完全依赖文本摘要，虽然节省了 Token，但在某些极度依赖视觉微小变化的场景下，可能会丢失关键的视觉线索。
*   **动态环境挑战**：对于实时变化极快的网页（如动态股票交易界面），静态的见解和摘要可能难以捕捉瞬时的交互陷阱。

（完）
