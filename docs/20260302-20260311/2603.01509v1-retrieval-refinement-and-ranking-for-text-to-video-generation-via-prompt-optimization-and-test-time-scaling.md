---
title: "Retrieval, Refinement, and Ranking for Text-to-Video Generation via Prompt Optimization and Test-Time Scaling"
title_zh: 通过提示词优化与测试时缩放实现文本到视频生成的检索、细化与排序
authors: "Zillur Rahman, Alex Sheng, Cristian Meo"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.01509v1"
tags: ["query:ai-ux"]
score: 9.0
evidence: 基于 RAG 的文本生成视频提示词优化框架
tldr: 针对文本生成视频（T2V）模型对提示词高度敏感且现有优化方法成本高昂的问题，本文提出了一种名为3R的检索、精炼与排序框架。该框架利用检索增强生成（RAG）提取修饰词以丰富上下文，结合扩散偏好优化对齐人类审美，并利用时间帧插值增强连贯性。3R无需对核心生成器进行任何训练，即可显著提升视频的静态保真度与动态一致性，为高质量视频生成提供了一种高效且可扩展的提示词优化方案。
selection_source: fresh_fetch
motivation: 现有的文本生成视频模型对提示词高度敏感，且现有的优化方法（如后期编辑或微调）存在复杂度高、成本昂贵或引入伪影的问题。
method: 提出3R框架，通过基于RAG的修饰词提取、基于扩散的偏好优化以及时间帧插值，在无需训练的情况下优化提示词并提升视频质量。
result: 实验证明3R框架显著提升了生成视频的静态保真度和动态连贯性，有效对齐了人类偏好。
conclusion: 3R框架为文本生成视频提供了一种高效、可扩展且无需训练的提示词优化方案，强调了提示词设计在生成质量中的核心作用。
---

## 摘要
尽管大规模数据集推动了文本到视频（T2V）生成模型的显著进展，但这些模型对输入提示词仍高度敏感，这表明提示词设计对生成质量至关重要。当前改进视频输出的方法往往存在不足：它们要么依赖复杂的后期编辑模型，面临引入伪影的风险；要么需要对核心生成器进行昂贵的微调，这严重限制了可扩展性和可访问性。在这项工作中，我们介绍了 3R，一种基于检索增强生成（RAG）的新型提示词优化框架。3R 利用了当前最先进的 T2V 扩散模型和视觉语言模型的能力。它可以在不进行任何模型训练的情况下与任何 T2V 模型配合使用。该框架利用了三个关键策略：基于 RAG 的修饰符提取以增强上下文基础，基于扩散的偏好优化以使输出符合人类偏好，以及时间帧插值以产生时间一致的视觉内容。这些组件共同实现了更准确、高效且上下文对齐的文本到视频生成。实验结果证明了 3R 在增强生成视频的静态保真度和动态连贯性方面的有效性，强调了优化用户提示词的重要性。

## Abstract
While large-scale datasets have driven significant progress in Text-to-Video (T2V) generative models, these models remain highly sensitive to input prompts, demonstrating that prompt design is critical to generation quality. Current methods for improving video output often fall short: they either depend on complex, post-editing models, risking the introduction of artifacts, or require expensive fine-tuning of the core generator, which severely limits both scalability and accessibility. In this work, we introduce 3R, a novel RAG based prompt optimization framework. 3R utilizes the power of current state-of-the-art T2V diffusion model and vision language model. It can be used with any T2V model without any kind of model training. The framework leverages three key strategies: RAG-based modifiers extraction for enriched contextual grounding, diffusion-based Preference Optimization for aligning outputs with human preferences, and temporal frame interpolation for producing temporally consistent visual contents. Together, these components enable more accurate, efficient, and contextually aligned text-to-video generation. Experimental results demonstrate the efficacy of 3R in enhancing the static fidelity and dynamic coherence of generated videos, underscoring the importance of optimizing user prompts.

---

## 论文详细总结（自动生成）

这是一份关于论文《Retrieval, Refinement, and Ranking for Text-to-Video Generation via Prompt Optimization and Test-Time Scaling》的结构化深入分析报告：

### 1. 核心问题与研究动机
*   **核心问题**：文本到视频（T2V）生成模型对输入提示词（Prompt）高度敏感。用户提供的短提示词往往缺乏细节，导致生成视频在视觉质量、物理合理性和时间连贯性上表现不佳。
*   **研究背景**：现有的改进方案通常分为两类：一是复杂的后期编辑（容易引入人工痕迹/伪影），二是昂贵的模型微调（扩展性差且计算成本高）。
*   **研究目标**：提出一种名为 **3R** 的框架，通过“推理时缩放”（Test-time scaling）策略，在不训练/不微调基础模型的前提下，通过优化提示词和增加推理计算量来提升视频生成质量。

### 2. 方法论：3R 框架
3R 框架由检索（Retrieval）、精炼（Refinement）和排序（Ranking）三个核心阶段组成，是一个“即插即用”的黑盒优化方案：
*   **检索（Retrieval）**：
    *   利用预训练的 Sentence Transformer 将用户原始提示词转化为向量。
    *   从关系数据库中检索相似场景的修饰词（Modifiers，包括主体、动作、环境等）。
    *   通过余弦相似度筛选出最相关的修饰词。
*   **精炼（Refinement）**：
    *   **合并阶段**：使用大语言模型（LLM，如 Mistral）将检索到的修饰词迭代地融入原始提示词，生成详细描述。
    *   **变体生成**：利用 LLM（如 GPT-4o）生成 $N$ 个不同的详细提示词变体，确保涵盖不同的视觉侧重点，同时保持原始意图。
*   **排序与增强（Ranking & Enhancement）**：
    *   **视频生成**：将 $N$ 个提示词输入 T2V 基础模型（如 LaVie），生成多个候选视频。
    *   **视频选择（Ranking）**：使用视觉问答（VQA）模型（VisionReward）对候选视频进行多维度打分（基于 29 个加权问题，如文本对齐、物理真实性等），选出得分最高的视频。
    *   **时间增强**：通过时间帧插值网络（Temporal Interpolation）将帧数从 16 帧提升至 61 帧，增强运动平滑度和时间一致性。

### 3. 实验设计
*   **数据集与基准**：主要使用 **EvalCrafter** 评估基准，该基准包含 17 个原始维度，最终聚合为四个核心指标：文本-视频对齐、视觉质量、运动质量和时间一致性。
*   **对比方法**：
    *   **Show-1**、**LaVie**（原始版本）、**Videocrafter2**。
    *   **IPO**（一种基于迭代偏好优化的模型）。
*   **实验场景**：涵盖了复杂语义对齐（如“蘑菇从人头上长出来”）、虚构角色（如“皮卡丘绝地武士”）以及文本生成等挑战性场景。

### 4. 资源与算力
*   **硬件环境**：实验使用了 **Nvidia H200 GPU**。
*   **推理效率**：使用 LaVie 作为基础模型时，在 H200 上生成单个视频大约需要 **5 秒**。
*   **模型配置**：
    *   LLM 模块：GPT-4o（精炼）、Mistral-7B（合并）。
    *   向量检索：all-MiniLM-L6-v2。
    *   视频生成基座：LaVie。
    *   奖励模型：VisionReward。

### 5. 实验数量与充分性
*   **实验规模**：在 EvalCrafter 基准上进行了全面测试，并运行了两次随机种子实验取平均值。
*   **消融实验**：设计了四组消融实验，分别验证了“单提示词优化”、“多提示词+视频选择”、“视频选择+VLM 反馈”以及“视频选择+时间插值”的效果。
*   **充分性评价**：实验设计较为充分，不仅有定量指标对比，还有针对特定失败模式（如文本生成、语义误解）的定性分析。通过消融实验清晰地展示了每个模块（RAG、排序、插值）的增益贡献。

### 6. 主要结论与发现
*   **性能领先**：3R 在 EvalCrafter 总分上达到了 245 分，超越了现有的开源模型（如 Videocrafter2 的 243 分）。
*   **推理时计算的价值**：证明了通过增加推理时的计算（如生成多个候选并排序）可以有效弥补基础模型能力的不足。
*   **提示词的重要性**：实验显示，仅通过 RAG 增强提示词，总分就提升了 7 分，说明很多生成失败源于用户提示词的“欠定义”而非模型架构问题。
*   **VLM 反馈的局限**：研究发现，引入视觉语言模型（VLM）的批评反馈（Critique）反而可能导致性能下降，因为目前的 VLM 容易产生“语义漂移”或过度纠错。

### 7. 优点
*   **无需训练（Training-free）**：完全基于推理逻辑，可直接应用于任何闭源（API）或开源 T2V 模型。
*   **对齐人类偏好**：通过加权 VQA 问题进行排序，使筛选出的视频更符合人类审美和物理直觉。
*   **模块化强**：检索、精炼、排序和增强四个模块可以根据需求独立替换或升级。

### 8. 不足与局限
*   **推理延迟**：由于需要生成多个候选视频并进行 VQA 评分和时间插值，整体推理时间显著增加，不适合实时性要求极高的场景。
*   **反馈瓶颈**：目前的 VLM 批评机制不够可靠，无法形成有效的闭环迭代优化。
*   **依赖外部知识库**：RAG 模块的效果高度依赖于预构建的关系数据库质量。

（完）
