---
title: Improving MLLMs in Embodied Exploration and Question Answering with Human-Inspired Memory Modeling
title_zh: 通过受人类启发的记忆建模提升多模态大语言模型在具身探索与问答中的表现
authors: "Ji Li, Jing Xia, Mingyi Li, Shiyan Hu"
date: 2026-02-17
pdf: "https://arxiv.org/pdf/2602.15513v1"
tags: ["query:sr-ai"]
score: 10.0
evidence: 受人类启发的具身智能体记忆建模
tldr: 针对具身智能体在长程观测和有限上下文下的挑战，本文提出一种受人类启发的非参数化存储框架。该框架显式解耦了情境记忆和语义记忆：通过语义检索与视觉推理结合的方式高效利用情境经验，并利用程序化规则提取构建结构化的语义记忆。实验证明该方法在具身问答和探索任务中达到SOTA水平，显著提升了智能体的探索效率和跨环境泛化能力。
selection_source: fresh_fetch
motivation: 现有的具身智能体存储方法多依赖文本摘要，容易丢失关键的视觉空间细节且在非平稳环境中表现脆弱。
method: 提出一种解耦情境与语义记忆的框架，结合检索优先的视觉推理机制和程序化规则提取技术来增强MLLM的决策能力。
result: 在A-EQA和GOAT-Bench等基准测试中取得SOTA性能，显著提升了LLM-Match得分、任务成功率及SPL指标。
conclusion: 情境记忆主要通过重用过去观测来提升探索效率，而语义记忆则通过结构化知识增强了智能体的复杂推理能力。
---

## 摘要
将多模态大语言模型（MLLMs）部署为具身智能体的大脑仍具挑战性，特别是在长时程观测和有限上下文预算的情况下。现有的记忆辅助方法通常依赖于文本摘要，这会丢弃丰富的视觉和空间细节，且在非平稳环境中表现脆弱。在这项工作中，我们提出了一个非参数化记忆框架，该框架显式地解耦了用于具身探索和问答的情节记忆（episodic memory）与语义记忆（semantic memory）。我们的检索优先、推理辅助范式通过语义相似性召回情节经验，并通过视觉推理进行验证，从而在无需严格几何对齐的情况下实现对过去观测结果的鲁棒重用。与此同时，我们引入了一种程序风格的规则提取机制，将经验转化为结构化的、可重用的语义记忆，从而促进跨环境的泛化。大量实验证明了该方法在具身问答和探索基准测试中达到了最先进的性能，在 A-EQA 上 LLM-Match 提升了 7.3%，LLM MatchXSPL 提升了 11.4%，在 GOAT-Bench 上成功率提升了 7.7%，SPL 提升了 6.8%。分析表明，我们的情节记忆主要提高了探索效率，而语义记忆则增强了具身智能体的复杂推理能力。

## Abstract
Deploying Multimodal Large Language Models as the brain of embodied agents remains challenging, particularly under long-horizon observations and limited context budgets. Existing memory assisted methods often rely on textual summaries, which discard rich visual and spatial details and remain brittle in non-stationary environments. In this work, we propose a non-parametric memory framework that explicitly disentangles episodic and semantic memory for embodied exploration and question answering. Our retrieval-first, reasoning-assisted paradigm recalls episodic experiences via semantic similarity and verifies them through visual reasoning, enabling robust reuse of past observations without rigid geometric alignment. In parallel, we introduce a program-style rule extraction mechanism that converts experiences into structured, reusable semantic memory, facilitating cross-environment generalization. Extensive experiments demonstrate state-of-the-art performance on embodied question answering and exploration benchmarks, yielding a 7.3% gain in LLM-Match and an 11.4% gain in LLM MatchXSPL on A-EQA, as well as +7.7% success rate and +6.8% SPL on GOAT-Bench. Analyses reveal that our episodic memory primarily improves exploration efficiency, while semantic memory strengthens complex reasoning of embodied agents.

---

## 论文详细总结（自动生成）

这是一份关于论文《Improving MLLMs in Embodied Exploration and Question Answering with Human-Inspired Memory Modeling》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
在具身智能（Embodied AI）领域，将多模态大语言模型（MLLM）作为智能体的“大脑”面临两大挑战：**长程观测（Long-horizon observations）**与**有限的上下文预算（Limited context budgets）**。
*   **现有方法的缺陷：** 现有的记忆辅助方法多依赖于文本摘要（丢弃了丰富的视觉和空间细节）或刚性的几何地图（在环境变化、传感器噪声和长程漂移面前表现脆弱）。
*   **研究动机：** 受人类记忆系统启发，论文旨在构建一种非参数化的记忆框架，不依赖刚性的几何对齐，而是通过“软性”的关联和推理来重用过去经验，使智能体能在非平稳环境中持续学习和泛化。

### 2. 方法论：核心思想与关键技术
论文提出了一个显式解耦**情节记忆（Episodic Memory）**和**语义记忆（Semantic Memory）**的框架：

*   **情节记忆（EM）：检索优先与推理辅助**
    *   **核心思想：** 记录详细的、具有时间戳的经验，通过语义相似性召回，而非硬性的几何融合。
    *   **流程：** 
        1.  使用 CLIP/SAM 构建分层嵌入场景图。
        2.  遇到新任务时，基于视觉相似性检索 Top-K 历史观测。
        3.  利用 MLLM 进行“系统 2”视觉推理，验证检索到的观测是否来自附近或具有任务相关性。
        4.  根据推理结果修剪边界点（Frontiers），指导高效探索。
*   **语义记忆（SM）：程序风格的规则提取**
    *   **核心思想：** 将经验转化为结构化、可重用的决策规则。
    *   **关键技术：**
        1.  **伪代码工作流：** 将推理日志抽象为伪代码（控制流和决策逻辑）。
        2.  **决策偏差检测（Decision Deviation Detection）：** 通过计算当前位置与真实轨迹的欧氏距离，识别关键错误点（Debug 模式）。
        3.  **规则提取：** MLLM 分析错误点并提取“if-then”、“情境-建议”等键值对规则，存入语义记忆库。
*   **元记忆（Meta Memory）：** 通过预定义的认知状态（探索、目标验证、二次检查、准备回答）实现行为的自动切换。

### 3. 实验设计
*   **数据集与场景：**
    *   **A-EQA (Active Embodied Question Answering)：** 基于 Open-EQA 框架，评估智能体在复杂环境中的探索和问答能力。
    *   **GOAT-Bench：** 一个多目标、长程的具身导航基准，涉及物体名称、描述和图像等多种模态的目标。
*   **对比方法：**
    *   包括基于 GPT-4o 和 Qwen2.5-VL 的多种 SOTA 模型，如 3D-Mem、ReExplore、Explore-EQA、ConceptGraphs 等。
*   **评估指标：**
    *   **LLM-Match：** 衡量回答的语义准确性。
    *   **SPL (Success weighted by Path Length)：** 衡量导航效率。
    *   **LLM-Match × SPL：** 综合衡量问答质量与探索效率。

### 4. 资源与算力
*   **模型后端：** 实验使用了商用模型 **GPT-4o** 和开源模型 **Qwen2.5-VL-7B-Instruct**。
*   **算力细节：** 论文**未明确说明**具体的 GPU 型号、数量及训练/推理的总时长。由于该框架是非参数化的（Non-parametric），其核心开销主要在于 MLLM 的 API 调用或本地推理，而非大规模模型训练。

### 5. 实验数量与充分性
*   **实验规模：** 
    *   在 A-EQA 上对比了超过 10 种基准方法，涵盖了 7 个细分问题类别（物体识别、定位、属性、空间关系等）。
    *   在 GOAT-Bench 上使用了 36 个场景的子集，包含 278 个导航子任务。
*   **消融实验：** 针对情节记忆和语义记忆分别做了消融，验证了各自对探索效率和推理准确性的贡献。
*   **客观性：** 实验跨越了不同能力的 MLLM 后端（GPT-4o vs Qwen），证明了框架的普适性。对比基准涵盖了最新的 2024/2025 年研究，具有较强的时效性和公平性。

### 6. 主要结论与发现
*   **性能提升：** 在 A-EQA 上，LLM-Match 提升了 7.3%，综合指标提升了 11.4%；在 GOAT-Bench 上成功率提升了 7.7%。
*   **功能分工：** 
    *   **情节记忆**主要通过重用过去观测来减少冗余路径，显著提升**探索效率**。
    *   **语义记忆**通过提供结构化知识，显著增强了智能体在**复杂推理**（如世界知识、功能推理）任务中的表现。
*   **模型扩展性：** 随着 MLLM 后端能力的增强，该框架表现出更好的正向收益，不易遇到性能瓶颈。

### 7. 优点（亮点）
*   **解耦设计：** 成功将复杂的几何融合问题转化为语义检索和视觉推理问题，降低了系统对精确建图的依赖。
*   **程序化抽象：** 引入伪代码风格的规则提取，比传统的纯文本摘要更具结构性，更易于被 LLM 理解和跨环境重用。
*   **人类启发：** 模拟人类“软性关联”而非“硬性匹配”的记忆模式，提高了在动态、噪声环境下的鲁棒性。

### 8. 不足与局限
*   **环境限制：** 目前主要在室内环境验证，室外大规模场景下的尺度差异和视觉复杂性可能给情节记忆检索带来挑战。
*   **参数化集成：** 作为一个非参数化框架，记忆的检索和利用与模型权重是分离的。未来若能将语义记忆参数化地集成到具身基础模型中，可能会进一步提升性能。
*   **实时性：** 依赖 MLLM 进行多次视觉推理和规则提取，在实时性要求极高的机器人任务中可能存在延迟压力。

（完）
