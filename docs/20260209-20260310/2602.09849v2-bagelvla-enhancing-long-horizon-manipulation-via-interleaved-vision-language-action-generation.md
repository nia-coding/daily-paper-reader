---
title: "BagelVLA: Enhancing Long-Horizon Manipulation via Interleaved Vision-Language-Action Generation"
title_zh: BagelVLA：通过交织的视觉-语言-动作生成增强长程操控
authors: "Yucheng Hu, Jianke Zhang, Yuanfei Luo, Yanjiang Guo, Xiaoyu Chen, Xinshu Sun, Kun Feng, Qingzhou Lu, Sheng Chen, Yangang Zhang, Wei Li, Jianyu Chen"
date: 2026-02-10
pdf: "https://arxiv.org/pdf/2602.09849v2"
tags: ["query:sr-ai"]
score: 10.0
evidence: 用于长程操作的视觉-语言-动作模型
tldr: BagelVLA是一个统一的视觉-语言-动作（VLA）模型，旨在解决复杂长程操控任务。它通过将语言规划、视觉预测和动作生成集成到单一框架中，克服了现有模型在推理和预测上的孤立性。该模型引入了残差流引导（RFG）技术，利用单步去噪提取预测性视觉特征，从而在保持低延迟的同时增强动作生成的准确性。实验证明其在多阶段推理任务中表现优异。
selection_source: fresh_fetch
motivation: 现有的VLA模型通常孤立地进行语言规划或视觉预测，难以在复杂长程操控任务中同时利用两者来指导动作生成。
method: 提出BagelVLA框架，通过交织文本推理与视觉预测，并引入残差流引导（RFG）技术来高效耦合多模态特征以指导动作执行。
result: 在多个模拟和真实世界基准测试中，BagelVLA显著优于现有基线模型，特别是在需要多阶段推理的任务中表现突出。
conclusion: 将语言规划与视觉预测深度集成到动作生成循环中，是提升具身智能体处理长程复杂任务能力的有效途径。
---

## 摘要
为具身智能体配备任务推理、预见物理结果并生成精确动作的能力，对于通用操控至关重要。虽然最近的视觉-语言-动作（VLA）模型利用了预训练的基础模型，但它们通常孤立地关注语言规划或视觉预测。这些方法很少同时整合这两种能力来指导动作生成，导致在复杂的长程操控任务中表现不佳。为了弥补这一差距，我们提出了 BagelVLA，这是一个在单一框架内整合了语言规划、视觉预测和动作生成的统一模型。BagelVLA 初始化自预训练的统一理解与生成模型，通过训练将文本推理和视觉预测直接交织到动作执行循环中。为了高效地耦合这些模态，我们引入了残差流引导（Residual Flow Guidance, RFG），它从当前观测值初始化，并利用单步去噪提取预测性视觉特征，以极低的延迟指导动作生成。大量实验表明，BagelVLA 在多个模拟和真实世界基准测试中显著优于现有基准模型，特别是在需要多阶段推理的任务中。

## Abstract
Equipping embodied agents with the ability to reason about tasks, foresee physical outcomes, and generate precise actions is essential for general-purpose manipulation. While recent Vision-Language-Action (VLA) models have leveraged pre-trained foundation models, they typically focus on either linguistic planning or visual forecasting in isolation. These methods rarely integrate both capabilities simultaneously to guide action generation, leading to suboptimal performance in complex, long-horizon manipulation tasks. To bridge this gap, we propose BagelVLA, a unified model that integrates linguistic planning, visual forecasting, and action generation within a single framework. Initialized from a pretrained unified understanding and generative model, BagelVLA is trained to interleave textual reasoning and visual prediction directly into the action execution loop. To efficiently couple these modalities, we introduce Residual Flow Guidance (RFG), which initializes from current observation and leverages single-step denoising to extract predictive visual features, guiding action generation with minimal latency. Extensive experiments demonstrate that BagelVLA outperforms existing baselines by a significant margin on multiple simulated and real-world benchmarks, particularly in tasks requiring multi-stage reasoning.

---

## 论文详细总结（自动生成）

以下是对论文《BagelVLA: Enhancing Long-Horizon Manipulation via Interleaved Vision-Language-Action Generation》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：通用的机器人操控需要智能体具备理解指令、预见物理结果并执行精确动作的能力。
*   **核心挑战**：现有的视觉-语言-动作（VLA）模型通常将语言规划（LLM）或视觉预测（扩散模型）孤立处理。这种分离导致模型在处理复杂的**长程（Long-Horizon）任务**时，难以同时利用逻辑推理和视觉动力学来指导底层控制。
*   **整体含义**：论文提出了 **BagelVLA**，这是一个统一的框架，首次在单一 Transformer 架构中实现了语言规划、视觉预测和动作生成的“交织”生成，使机器人能像人类一样“先思考步骤，再想象结果，最后执行动作”。

### 2. 方法论
*   **核心思想**：将操控任务建模为交织规划问题。给定全局指令和当前观测，模型联合建模子任务描述（$l_t$）、未来关键帧（$v_{t+k}$）和动作块（$a_t$）的分布。
*   **关键技术细节**：
    *   **混合专家架构（MoT）**：基于 Qwen2.5-7B 初始化，包含理解专家（LLM）、生成专家（视觉）和动作专家（2B 参数的小型 Transformer）。
    *   **交织推理链**：推理流程为：全局指令 + 图像 $\rightarrow$ 文本子任务规划 $\rightarrow$ 预测未来关键帧 $\rightarrow$ 生成动作。
    *   **残差流引导（RFG）**：为了解决视觉生成的高延迟问题，RFG 不从纯噪声生成图像，而是以当前帧为结构先验，通过**单步去噪**预测到下一关键帧的残差变化。这使得模型能以极低延迟提取预测性视觉特征。
    *   **两阶段训练**：第一阶段在大规模混合数据集（机器人+通用多模态）上预训练规划和预测能力；第二阶段引入动作专家，在特定机器人数据上进行全模型微调。

### 3. 实验设计
*   **数据集与场景**：
    *   **模拟环境**：Calvin（评估 OOD 泛化）和 Robotwin 2.0（包含 50 个复杂双臂任务）。
    *   **真实世界**：使用 Aloha-AgileX 双臂机器人，涵盖 9 类基础技能（如扫垃圾、倒薯条）和 2 类长程规划任务（按需叠方块、计算并摆放数字块）。
*   **Benchmark 与对比方法**：
    *   对比了 **$\pi_0$**（Flow-based VLA）、**RDT**、**UP-VLA** 和 **VPP**（视频预测策略）等前沿模型。
    *   评估指标包括任务成功率、平均完成长度和规划准确率。

### 4. 资源与算力
*   **训练算力**：
    *   **预训练阶段**：使用 **64 台 NVIDIA A800 GPU**，训练 20,000 步，Batch Size 约 1600。
    *   **微调阶段**：根据任务规模使用 **8 到 32 台 A800 GPU**。
*   **推理算力**：在单张 **RTX 5090 GPU** 上，每个动作块推理耗时 1.2 秒。通过异步执行机制，实际控制频率可达 **40Hz 至 72Hz**。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，包括模拟环境下的数百次测试和真实世界中每项任务 20 次的重复实验。
*   **消融实验**：针对双流匹配方案（完整去噪 vs 联合去噪 vs 单步去噪）、RFG 的有效性、预训练的作用以及各模态（语言/视觉）的贡献度进行了 4 组深度消融。
*   **客观性**：实验涵盖了从模拟到现实的迁移，并设置了随机初始位置和干扰物，验证了模型在 OOD（分布外）场景下的鲁棒性，实验设计科学且公平。

### 6. 主要结论与发现
*   **性能领先**：BagelVLA 在 Calvin 和 Robotwin 基准测试中均刷新了 SOTA 记录，尤其在长程任务中表现远超 $\pi_0$。
*   **交织规划的价值**：显式的语言规划和视觉预测能显著提升动作的准确性。在真实长程任务中，BagelVLA 的规划准确率接近 90%。
*   **RFG 的高效性**：证明了单步残差去噪足以提供高质量的视觉引导，同时将推理延迟降低到可实用的水平。

### 7. 优点
*   **高度统一**：将逻辑推理（LLM）与物理想象（扩散模型）完美融合在同一个 Transformer 序列中。
*   **推理高效**：RFG 技术巧妙地解决了视觉预测模型在实时控制中的瓶颈。
*   **泛化能力强**：得益于大规模多模态预训练，模型在面对未见过的物体（如 OOD 蔬菜、餐具）时表现出极强的语义理解和操作能力。

### 8. 不足与局限
*   **精细控制瓶颈**：尽管规划准确率高，但在某些极高精度的微操任务中（如插入细小孔洞），成功率仍受限于底层动作映射的精度。
*   **数据标注依赖**：交织规划依赖于高质量的子任务和关键帧标注，虽然论文使用了自动标注工具，但在极复杂场景下的标注质量可能影响性能。
*   **硬件要求**：虽然推理优化较好，但 7B+2B 的模型规模对于嵌入式边缘计算设备仍有较高要求。

（完）
