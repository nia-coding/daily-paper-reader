---
title: "MWM: Mobile World Models for Action-Conditioned Consistent Prediction"
title_zh: MWM：面向动作条件一致性预测的移动世界模型
authors: "Han Yan, Zishang Xiang, Zeyu Zhang, Hao Tang"
date: 2026-03-08
pdf: "https://arxiv.org/pdf/2603.07799v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 用于导航中动作条件一致性预测的世界模型
tldr: 本研究针对具身导航中世界模型多步预测易漂移及蒸馏推理不一致的问题，提出了移动世界模型（MWM）。MWM 采用两阶段训练框架，结合结构预训练与动作一致性（ACC）后训练，并引入推理一致性状态蒸馏（ICSD）以实现高效的少步扩散推理。实验证明，该方法在视觉保真度、轨迹精度及规划成功率上均有显著提升，为高效、准确的图像目标导航提供了新方案。
selection_source: fresh_fetch
motivation: 现有导航世界模型在多步预测中缺乏动作一致性，且蒸馏过程未考虑推理一致性，导致规划性能下降。
method: 提出两阶段训练框架（结构预训练+动作一致性后训练）及推理一致性状态蒸馏（ICSD）技术。
result: 在基准测试和真实世界任务中，该方法显著提升了视觉保真度、轨迹精度、规划成功率和推理效率。
conclusion: MWM 通过增强动作一致性，为基于规划的图像目标导航提供了一个高效且准确的移动世界模型。
---

## 摘要
世界模型允许在想象的未来预测空间中进行规划，为具身导航提供了一个极具前景的框架。然而，现有的导航世界模型往往缺乏动作条件一致性，导致视觉上合理的预测在多步展开（multi-step rollout）下仍可能发生漂移，从而降低规划性能。此外，高效部署需要少步扩散推理，但现有的蒸馏方法并未显式地保持展开一致性，造成了训练与推理之间的不匹配。为了应对这些挑战，我们提出了 MWM，一种用于基于规划的图像目标导航的移动世界模型。具体而言，我们引入了一个两阶段训练框架，将结构预训练与动作条件一致性（ACC）后训练相结合，以提高动作条件下的展开一致性。我们还进一步引入了推理一致性状态蒸馏（ICSD），用于具有改进展开一致性的少步扩散蒸馏。我们在基准测试和真实世界任务上的实验表明，在视觉保真度、轨迹准确性、规划成功率和推理效率方面均取得了持续的提升。代码：https://github.com/AIGeeksGroup/MWM。网站：https://aigeeksgroup.github.io/MWM。

## Abstract
World models enable planning in imagined future predicted space, offering a promising framework for embodied navigation. However, existing navigation world models often lack action-conditioned consistency, so visually plausible predictions can still drift under multi-step rollout and degrade planning. Moreover, efficient deployment requires few-step diffusion inference, but existing distillation methods do not explicitly preserve rollout consistency, creating a training-inference mismatch. To address these challenges, we propose MWM, a mobile world model for planning-based image-goal navigation. Specifically, we introduce a two-stage training framework that combines structure pretraining with Action-Conditioned Consistency (ACC) post-training to improve action-conditioned rollout consistency. We further introduce Inference-Consistent State Distillation (ICSD) for few-step diffusion distillation with improved rollout consistency. Our experiments on benchmark and real-world tasks demonstrate consistent gains in visual fidelity, trajectory accuracy, planning success, and inference efficiency. Code: https://github.com/AIGeeksGroup/MWM. Website: https://aigeeksgroup.github.io/MWM.

---

## 论文详细总结（自动生成）

这是一份关于论文《MWM: Mobile World Models for Action-Conditioned Consistent Prediction》的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：世界模型通过在预测的未来空间中进行规划，为具身导航提供了强大的框架。
*   **核心问题**：
    1.  **动作条件不一致性**：现有的导航世界模型在多步展开（rollout）预测时，虽然单帧视觉效果逼真，但预测轨迹会随时间漂移，偏离真实动作序列应导致的物理位置。
    2.  **训练与推理失配**：为了实时部署，通常需要对扩散模型进行少步（few-step）蒸馏，但现有的蒸馏方法只关注分布对齐，忽略了在加速推理时保持多步展开的一致性。
*   **研究目标**：开发一种既能保持高保真视觉生成，又能确保动作条件一致性，且支持高效少步推理的移动世界模型（MWM）。

### 2. 方法论
MWM 提出了一个“结构优先，一致性精炼”的两阶段训练框架：
*   **阶段 I：结构预训练（Structure Pretraining）**：
    *   在教师强制（Teacher-forcing）模式下训练动作条件扩散模型，学习场景的精细几何结构、光照和外观。
    *   使用 CDiT（条件扩散 Transformer）作为骨干网络，通过 AdaLN 注入动作和时间步信息。
*   **阶段 II：动作条件一致性（ACC）后训练**：
    *   **核心思想**：让模型在自身生成的预测上下文（Self-conditioned context）中进行训练，以减轻自回归预测中的误差累积。
    *   **关键技术**：冻结 CDiT 骨干网络，仅通过 LoRA 微调轻量级的 AdaLN 调制层，从而在不破坏结构信息的前提下优化动作对齐。
*   **推理一致性状态蒸馏（ICSD）**：
    *   在后训练中引入“推理一致性状态”，通过确定性 DDIM 更新，将训练时的截断估计状态与推理时的最终去噪状态对齐。
    *   这解决了少步采样时由于步长跳跃导致的训练-推理间隙，确保了高效推理下的预测一致性。
*   **规划算法**：采用基于交叉熵方法（CEM）的搜索，在预测的视觉空间中筛选最优动作序列。

### 3. 实验设计
*   **数据集/场景**：
    *   **SCAND**：大型人类操作导航数据集，包含 8.7 小时的室内外多模态数据。
    *   **MMK2-RealNav**：在大学建筑内采集的室内导航数据集，包含 1.3 公里的轨迹。
*   **Benchmark 与对比方法**：
    *   **对比方法**：包括 GNM、NoMaD（端到端策略）以及 NWM（现有的导航世界模型）。
    *   **评估指标**：轨迹精度（ATE, RPE）、任务成功率（SR）、导航误差（NE）、视觉保真度（FID）和感知相似度（DreamSim, LPIPS）。

### 4. 资源与算力
*   **硬件设备**：
    *   **预训练阶段**：使用 4 台 NVIDIA RTX PRO 6000 (96GB) GPU。
    *   **后训练阶段**：使用单台 RTX PRO 6000 GPU。
*   **训练细节**：
    *   预训练学习率为 $6 \times 10^{-5}$，总 Batch Size 为 192。
    *   ACC 后训练运行 1.5K 个优化步，学习率为 $2 \times 10^{-4}$，Batch Size 为 2（每个样本含 8 步展开）。
    *   推理效率：在 RTX PRO 6000 上，MWM（5 步 DDIM）的平均展开时间为 2.3 秒，比 NWM（25 步）快 4 倍以上。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了基准测试集评估、消融实验以及真实机器人部署。
*   **消融实验**：分别验证了 ACC 损失函数设计（L1/L2 vs LPIPS）、两阶段训练范式的必要性以及 ICSD 对蒸馏效果的提升。
*   **充分性评价**：实验设计较为全面，不仅在公开数据集上进行了定量对比，还在真实的 MMK2 机器人平台上进行了 4 种不同目标的导航测试。对比实验考虑了不同步数的 DDIM 采样，确保了对比的公平性。

### 6. 主要结论与发现
*   **性能提升**：MWM 在 DreamSim 指标上降低了 20.4%，FID 降低了 17.5%，同时实现了 4 倍以上的推理加速。
*   **轨迹精度**：在 ATE 和 RPE 指标上分别提升了 10.9% 和 8.5%，证明了预测与真实物理轨迹的高度一致。
*   **实机表现**：在真实世界部署中，成功率相对提升了 50%，导航误差减少了 32.1%。
*   **关键发现**：感知特征空间（LPIPS）的监督比像素级损失（L1/L2）更适合学习动作一致性；先学结构再学一致性的两阶段策略优于单一阶段训练。

### 7. 优点
*   **一致性增强**：通过 ACC 后训练显式解决了世界模型长期存在的“预测漂移”问题。
*   **高效且保真**：ICSD 技术使得模型在极少步数（如 5 步）采样下仍能保持极高的视觉质量和物理一致性。
*   **即插即用**：通过 LoRA 微调 AdaLN 层的方法非常轻量，易于在现有预训练模型上扩展。

### 8. 不足与局限
*   **开环规划**：目前的系统主要执行开环、单次规划，缺乏根据实时反馈进行在线重规划（Closed-loop replanning）的能力。
*   **任务局限**：实验主要集中在图像目标导航（Image-goal navigation），尚未广泛验证在复杂指令或长程逻辑推理任务中的表现。
*   **环境泛化**：虽然在室内外均有测试，但对于极端动态环境（如极度拥挤的人群）的鲁棒性仍有待进一步验证。

（完）
