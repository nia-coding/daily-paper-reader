---
title: "SGAgent: Suggestion-Guided LLM-Based Multi-Agent Framework for Repository-Level Software Repair"
title_zh: SGAgent：基于建议引导的 LLM 多智能体仓库级软件修复框架
authors: "Quanjun Zhang, Chengyu Gao, Yu Han, Ye Shang, Chunrong Fang, Zhenyu Chen, Liang Xiao"
date: 2026-02-27
pdf: "https://arxiv.org/pdf/2602.23647v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 用于软件修复的多智能体框架
tldr: SGAgent是一个基于大语言模型的多智能体框架，旨在解决仓库级软件修复中“定位后直接修复”导致的推理断层问题。该框架引入了“定位-建议-修复”范式，通过新增的建议阶段深入理解缺陷并提供可操作方案。结合知识图谱增强全局上下文感知，SGAgent在SWE-Bench等基准测试上取得了领先的修复率和成本效益，并展现出良好的跨任务泛化能力。
selection_source: fresh_fetch
motivation: 现有的软件修复方法直接从定位跳转到修复，缺乏对缺陷成因的深入推理，导致修复成功率受限。
method: 提出包含定位、建议、修复三个子智能体的框架，并利用知识图谱工具增强全局上下文检索与推理。
result: "在SWE-Bench上达到51.3%的修复准确率，且单次修复成本仅为1.48美元，显著优于同类基准模型。"
conclusion: SGAgent证明了引入建议阶段和知识图谱辅助能有效提升大模型在复杂仓库级软件修复任务中的性能与泛化性。
---

## 摘要
大语言模型（LLMs）的飞速发展促使了能够自主与环境交互并调用外部工具的智能体的出现。近年来，基于智能体的软件修复方法受到了广泛关注，因为修复智能体可以自动分析和定位漏洞、生成补丁，并在仓库级基准测试中达到最先进的性能。然而，现有方法通常采用“定位后修复”范式，直接从“漏洞在哪里”跳跃到“如何修复”，存在根本性的推理鸿沟。为此，我们提出了 SGAgent，一个用于仓库级软件修复的建议引导（Suggestion-Guided）多智能体框架，该框架遵循“定位-建议-修复”范式。SGAgent 引入了一个建议阶段，以加强从定位到修复的过渡。建议者（suggester）从漏洞位置出发，增量地检索相关上下文，直到完全理解漏洞，然后提供可操作的修复建议。此外，我们从目标仓库构建了一个知识图谱（Knowledge Graph），并开发了一个基于知识图谱的工具包，以增强 SGAgent 的全局上下文感知和仓库级推理能力。三个专门的子智能体（即定位者、建议者和修复者）协作实现自动化的端到端软件修复。在 SWE-Bench 上的实验结果表明，搭载 Claude-3.5 的 SGAgent 实现了 51.3% 的修复准确率、81.2% 的文件级定位准确率和 52.4% 的函数级定位准确率，平均每个实例成本为 1.48 美元，优于所有使用相同基础模型的基准方法。此外，SGAgent 在 VUL4J 和 VJBench 的漏洞修复任务上达到了 48% 的准确率，展示了在不同任务和编程语言上的强大泛化能力。

## Abstract
The rapid advancement of Large Language Models (LLMs) has led to the emergence of intelligent agents capable of autonomously interacting with environments and invoking external tools. Recently, agent-based software repair approaches have received widespread attention, as repair agents can automatically analyze and localize bugs, generate patches, and achieve state-of-the-art performance on repository-level benchmarks. However, existing approaches usually adopt a localize-then-fix paradigm, jumping directly from "where the bug is" to "how to fix it", leaving a fundamental reasoning gap. To this end, we propose SGAgent, a Suggestion-Guided multi-Agent framework for repository-level software repair, which follows a localize-suggest-fix paradigm. SGAgent introduces a suggestion phase to strengthen the transition from localization to repair. The suggester starts from the buggy locations and incrementally retrieves relevant context until it fully understands the bug, and then provides actionable repair suggestions. Moreover, we construct a Knowledge Graph from the target repository and develop a KG-based toolkit to enhance SGAgent's global contextual awareness and repository-level reasoning. Three specialized sub-agents (i.e., localizer, suggester, and fixer) collaborate to achieve automated end-to-end software repair. Experimental results on SWE-Bench show that SGAgent with Claude-3.5 achieves 51.3% repair accuracy, 81.2% file-level and 52.4% function-level localization accuracy with an average cost of $1.48 per instance, outperforming all baselines using the same base model. Furthermore, SGAgent attains 48% accuracy on VUL4J and VJBench for vulnerability repair, demonstrating strong generalization across tasks and programming languages.

---

## 论文详细总结（自动生成）

以下是对论文《SGAgent: Suggestion-Guided LLM-Based Multi-Agent Framework for Repository-Level Software Repair》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的仓库级（Repository-level）软件自动修复（APR）方法通常遵循“定位-修复”（Localize-then-Fix）范式。这种范式直接从确定漏洞位置跳转到生成补丁，忽略了中间的推理过程，导致 LLM 难以理解复杂的跨文件依赖和漏洞根因，容易产生表面修复或错误的补丁。
*   **研究动机**：模拟人类开发者的调试习惯——在定位漏洞后，通常会先分析代码间的交互并制定修复计划（建议），然后再动手修改。论文旨在通过引入“建议”阶段来弥补定位与修复之间的推理鸿沟。

### 2. 论文提出的方法论
SGAgent 采用 **“定位-建议-修复”（Locate-Suggest-Fix）** 范式，核心组件包括：
*   **多智能体协作框架**：
    *   **Localizer（定位者）**：利用问题描述和代码上下文，识别潜在的漏洞区域（行号范围）。
    *   **Suggester（建议者）**：核心创新点。从定位点出发，通过知识图谱增量检索相关上下文，分析函数调用和依赖关系，生成可操作的修复建议（Fix Plan）。
    *   **Fixer（修复者）**：根据定位结果和修复建议编写补丁，并进行解释性推理。
*   **知识图谱（KG）与工具包**：
    *   **构建**：通过静态分析（Tree-sitter, Pygments）提取类、方法、变量及其 7 种关系（如继承、调用、引用）。
    *   **工具包**：开发了 14 种专用工具（如 `get_code_relationships`, `find_variable_usage`），允许智能体精准检索仓库级信息。
*   **关键技术细节**：
    *   **动态内存总结**：当对话历史超过阈值时，调用 LLM 进行压缩总结，防止上下文稀释和幻觉。
    *   **补丁重排与验证**：结合回归测试、自动生成的复现测试以及多数投票（Majority Voting）来筛选最终补丁。

### 3. 实验设计
*   **数据集**：
    *   **SWE-Bench-Lite**：包含 300 个真实的 GitHub Python 漏洞实例，是目前衡量仓库级修复能力的主流基准。
    *   **VUL4J & VJBench**：用于评估 Java 漏洞修复的泛化能力。
*   **对比方法**：包括 DARS Agent, Lingxi, OpenHands, KGCompass, SWE-Agent, Agentless, AutoCodeRover 等 10 余种 SOTA 方法。
*   **评估指标**：修复成功率（% Resolved）、文件级/函数级定位准确率（File/Func Acc.）、平均修复成本（Avg Cost）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件算力（如 GPU 型号或数量）。
*   **成本分析**：由于该框架基于 LLM API（如 Claude, DeepSeek），作者重点分析了 **Token 成本**。SGAgent 在 Claude-3.5-Sonnet 上的平均修复成本为 **$1.48/实例**，在所有对比方法中排名第三低，具有较高的经济性。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 SWE-Bench-Lite 上进行了完整的主实验（RQ1）。
    *   **消融实验（RQ2）**：分别验证了“无建议阶段”、“无知识图谱”、“不同重排策略”的影响。
    *   **模型泛化实验（RQ3）**：测试了 Claude-3.5, DeepSeek-V3, Qwen3-235B 三种底座模型。
    *   **跨领域实验（RQ4）**：在 Java 漏洞修复任务上进行了验证。
*   **充分性评价**：实验设计全面，涵盖了性能对比、组件贡献分析、模型鲁棒性及跨语言泛化性，实验结果客观且具有统计说服力。

### 6. 论文的主要结论与发现
*   **性能领先**：SGAgent 在 Claude-3.5 下达到了 **51.3%** 的修复率，优于所有使用相同底座模型的基准方法。
*   **建议阶段至关重要**：消融实验显示，移除“建议”阶段后，修复率从 51.3% 骤降至 38.0%（下降 13.3%），证明了修复计划对复杂漏洞处理的决定性作用。
*   **知识图谱的价值**：KG 显著提升了定位准确率（文件级提升 3.1%），帮助智能体在海量代码中快速锁定目标。
*   **模型泛化性**：框架在 DeepSeek 和 Qwen 模型上同样有效，且在 Java 漏洞修复中也取得了 SOTA 性能（48% 准确率）。

### 7. 优点（亮点）
*   **范式创新**：打破了传统的“定位-修复”死板流程，引入“建议”阶段，更符合人类逻辑。
*   **结构化检索**：自建细粒度知识图谱并配套场景化工具包，解决了 LLM 在超长上下文中检索效率低的问题。
*   **高性价比**：在保持高修复率的同时，通过高效的工具调用和内存管理，显著降低了 API 调用成本。

### 8. 不足与局限
*   **模型依赖性**：虽然框架是模型无关的，但实验显示其性能高度依赖底座模型的推理能力（Claude-3.5 显著优于其他模型）。
*   **静态分析限制**：知识图谱主要依赖静态分析，对于某些高度动态的 Python 特性（如复杂的反射或动态注入）可能存在感知盲区。
*   **验证环节开销**：虽然修复成本低，但补丁验证阶段依赖于运行测试用例，这在实际工业大规模应用中可能面临环境配置和执行时间的挑战。

（完）
