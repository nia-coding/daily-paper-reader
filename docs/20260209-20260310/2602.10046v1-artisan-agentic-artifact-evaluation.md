---
title: "Artisan: Agentic Artifact Evaluation"
title_zh: Artisan：代理式制品评估
authors: "Doehyun Baek, Michael Pradel"
date: 2026-02-10
pdf: "https://arxiv.org/pdf/2602.10046v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 软件工程制品的自动化复现
tldr: 针对软件工程领域论文制品评估（AE）过程繁琐且耗时的问题，本文提出了Artisan，一个基于大语言模型的自动化智能体。Artisan将结果复现建模为代码生成任务，通过生成可独立运行的复现脚本来验证研究成果。研究还设计了自动评判机制以引导智能体并防止作弊，并推出了首个软件工程自动化制品评估基准Artisan-Bench。实验证明Artisan复现效率远超基准模型，并发现了多项现有论文中的错误。
selection_source: fresh_fetch
motivation: 现有的论文制品评估主要依赖人工，过程耗时费力且难以大规模推广。
method: 将复现过程转化为生成可执行脚本的任务，并结合自动评判机制引导LLM智能体完成复现。
result: 在包含60个任务的Artisan-Bench基准测试中，Artisan成功生成了44个复现脚本，性能是基准智能体的3.14倍。
conclusion: Artisan证明了利用自动化智能体进行大规模、细粒度的论文制品评估是可行且高效的，有助于提升研究的可复现性。
---

## 摘要
制品评估（Artifact evaluation）已成为软件工程界的标准实践，用以确保研究结果的可复现性。然而，目前的手动评估过程耗费人力，因此通常仅针对部分论文进行一次性评估。为了支持制品评估工作，我们提出了 Artisan，这是一个自动化的 LLM 智能体，旨在根据论文及其制品复现研究结果。该方法包含两个关键贡献：首先，我们将复现问题定义为代码生成任务，目标是生成一个复现脚本，执行该脚本即可复现论文中报告的结果。与以往在其他领域自动复现研究结果的工作不同，这种定义方式允许独立于智能体运行脚本，并能对复现过程进行细粒度评估。其次，我们设计了自动评判机制，在不泄露预期结果的前提下引导智能体达成目标，并防止其采取简单的作弊手段（如直接复制已有的结果）。为了评估 Artisan，我们引入了 Artisan-Bench，这是首个评估复现脚本生成能力的基准测试，也是软件工程领域首个自动化制品评估基准测试。Artisan-Bench 包含源自 23 篇软件工程论文的 60 个任务，涵盖了不同的研究领域和编程语言。我们验证了 Artisan-Bench 中所有任务的可复现性，以确保任务的可行性。实验结果显示，Artisan 表现出色，成功生成了 44/60 个复现脚本；在生成的复现脚本数量上，Artisan 比目前最强的基准方案（原生 LLM 智能体 mini-swe-agent）高出 3.14 倍，且每个任务平均仅需 0.45 美元和 48 分钟。此外，Artisan 还帮助发现了论文或制品中的 20 个新错误。

## Abstract
Artifact evaluation has become standard practice in the software engineering community to ensure the reproducibility of research results. However, the current manual process is labor-intensive, and hence, done only as a one-time assessment for a subset of all papers. To support the artifact evaluation effort, we present Artisan, an automated LLM agent for reproducing research results given a paper and its artifact. The approach is enabled by two key contributions: First, we frame the reproduction problem as a code generation task where the goal is to generate a reproduction script that, when executed, reproduces the results reported in a paper. Unlike prior work on automatically reproducing research results in other domains, this formulation allows for running the script independently of the agent and for assessing the reproduction process at a fine-grained level. Second, we design automated judging mechanism that guides the agent toward the expected results without revealing them and that prevent trivial solutions, such as simply copying checked-in results. To evaluate Artisan, we introduce Artisan-Bench, the first benchmark assessing the ability to generate reproduction scripts and the first benchmark for automated artifact evaluation in software engineering. Artisan-Bench comprises 60 tasks derived from 23 software engineering papers, covering different research areas and programming languages. We validate all tasks in Artisan-Bench for reproducibility to ensure that the tasks are feasible. Our experiments show that Artisan is effective, producing 44/60 reproduction scripts and outperforming the best available baseline, a vanilla LLM agent (mini-swe-agent), by 3.14$\times$ in terms of reproduction scripts generated while taking $0.45 and 48 minutes, on average per task. Artisan also helped uncover 20 new errors in either the paper or artifact.

---

## 论文详细总结（自动生成）

### 论文总结：Artisan: Agentic Artifact Evaluation

#### 1. 核心问题与整体含义（研究动机和背景）
在软件工程（SE）学术界，制品评估（Artifact Evaluation, AE）是确保研究可复现性的关键环节。然而，现有的 AE 流程存在三大痛点：
*   **人力成本极高**：评估单个制品通常需要 8-10 小时，甚至超过审稿时间。
*   **评估粒度粗糙**：现有的“徽章”制度仅提供二元信号，无法反映具体实验结果的复现程度。
*   **时效性差**：AE 通常是一次性的，随着环境演变，曾经通过评估的制品往往在一年后便无法运行。
**研究目标**：开发一个基于大语言模型（LLM）的自动化智能体，能够根据论文和制品自动生成复现脚本，实现低成本、大规模、细粒度的自动化制品评估。

#### 2. 方法论：核心思想与关键技术
Artisan 将复现问题建模为**代码生成任务**，其核心思想是让智能体生成一个可独立于智能体运行的 Bash 脚本。
*   **工作流程**：
    1.  **准备阶段**：自动从 GitHub/Zenodo 下载制品；对论文中的目标表格进行**混淆处理**（将数字替换为问号），防止智能体直接从文本中提取答案。
    2.  **工具集**：
        *   **Bash Tool**：用于文件操作、环境配置和命令执行。
        *   **Format Tool**：利用 LLM 将制品运行产生的杂乱日志或非结构化输出，填充进混淆后的表格模板中。
    3.  **两层自动评判机制（Judging Mechanism）**：
        *   **输出判别器（Output Judge）**：在隔离容器中运行生成的脚本，对比输出结果与论文原数据。若不匹配，将差异作为反馈传回智能体。
        *   **方法判别器（Method Judge）**：利用 LLM 检查智能体是否在“作弊”（例如直接复制制品中已有的结果文件或硬编码数字）。只有通过“完整复现”或“最后里程复现”的方法才被接受。

#### 3. 实验设计
*   **基准测试（Artisan-Bench）**：作者构建了首个 SE 自动化复现基准，包含来自 ICSE、FSE、ASE、ISSTA 等顶会的 **23 篇论文**，共 **60 个任务**。涵盖 Python, Java, Rust, Scala, OCaml 等多种语言。
*   **对比方法**：
    *   **mini-swe-agent**（Artisan 的基础框架）。
    *   **SWE-agent**（通用的软件工程智能体）。
    *   **OpenHands**（开源的 AI 开发者平台）。
*   **底层模型**：使用了 GPT-5.1、GPT-5-mini 和 DeepSeek V3.2 Reasoner 进行对比测试。

#### 4. 资源与算力
*   **算力消耗**：论文未提及模型训练算力（直接调用商业 API）。
*   **推理成本**：使用 GPT-5.1 时，每个任务的平均成本为 **0.45 美元**。
*   **运行时长**：每个任务平均耗时 **48 分钟**（包含运行制品实验的时间）。
*   **硬件环境**：实验在支持 Docker 的标准 Linux 环境中运行，排除了需要 GPU 或特殊硬件的论文。

#### 5. 实验数量与充分性
*   **实验规模**：针对 60 个任务进行了多轮测试，涵盖了 3 种主流智能体框架和 3 种不同的 LLM 后端。
*   **消融实验**：分别去除了“输出判别器”、“方法判别器”和“格式化工具”，验证了各组件对防止作弊和提高成功率的必要性。
*   **客观性**：所有任务均经过人工手动验证确保“可复现性”，并提供了人工编写的 Ground Truth 脚本作为参考。实验设计考虑了成本、时间限制（30 步/1 美元上限），具有较强的客观性。

#### 6. 主要结论与发现
*   **性能卓越**：Artisan（配合 GPT-5.1）成功复现了 **44/60** 个任务，性能是基准模型 mini-swe-agent 的 **3.14 倍**。
*   **发现现实错误**：Artisan 在复现过程中发现了 **20 处** 论文与制品之间的新不一致性（涉及 13 篇已获得 AE 徽章的论文），包括转录错误、舍入错误和制品代码 Bug。
*   **防作弊有效性**：实验证明，如果没有方法判别器，智能体极易倾向于通过复制制品中的缓存结果来“走捷径”。

#### 7. 优点
*   **独立性**：生成的复现脚本可脱离智能体运行，便于人类审计和后续持续集成。
*   **鲁棒的评判机制**：通过表格混淆和方法判别，有效解决了 LLM 智能体在复现任务中的“奖励黑客（Reward Hacking）”问题。
*   **跨语言与多领域**：不局限于 Python，展示了在复杂 SE 任务（如静态分析、形式化验证）中的通用性。

#### 8. 不足与局限
*   **环境依赖**：目前仅支持配置了 Docker 的制品，对于依赖复杂物理环境或非公开 API 的研究无法处理。
*   **修复能力有限**：当制品本身存在严重 Bug（如代码逻辑错误）时，Artisan 虽能发现不一致，但往往无法自动修复这些深层 Bug。
*   **成本波动**：虽然平均成本较低，但对于极其复杂的实验，可能会超出设定的步数或预算限制。

（完）
