---
title: "Memex(RL): Scaling Long-Horizon LLM Agents via Indexed Experience Memory"
title_zh: Memex(RL)：通过索引经验记忆扩展长程大语言模型智能体
authors: "Zhenting Wang, Huancheng Chen, Jiayun Wang, Wei Wei"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.04257v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 用于长程 LLM 智能体的索引经验记忆机制
tldr: 针对大语言模型智能体在长程任务中受限于上下文窗口的问题，本文提出 Memex 机制。该机制通过索引化经验记忆，将简洁摘要与稳定索引保留在工作上下文中，而将完整交互存入外部数据库。配合 MemexRL 强化学习框架，智能体能自主学习总结、归档与检索策略。实验证明，Memex 在显著减少上下文占用的同时，提升了长程任务的成功率。
selection_source: fresh_fetch
motivation: 现有的上下文压缩方法（如截断或摘要）会导致原始证据丢失，限制了智能体在长程任务中的表现。
method: 提出 Memex 索引记忆机制，结合外部数据库存储完整细节，并利用 MemexRL 框架优化智能体的读写与检索决策。
result: 在挑战性的长程任务中，Memex 智能体在保持较小工作上下文的同时，显著提高了任务成功率。
conclusion: 索引化经验记忆是一种比纯摘要更高效、低损耗的长程记忆方案，能有效扩展智能体的处理能力。
---

## 摘要
大语言模型（LLM）智能体在处理长程任务时，从根本上受到有限上下文窗口的限制。随着轨迹的增长，在上下文中保留工具输出和中间推理过程很快变得不可行：工作上下文变得过长，最终超出上下文预算，并且即使远端证据仍然存在，也变得难以利用。现有的解决方案通常通过截断或滚动摘要来缩短上下文，但这些方法从根本上是有损的，因为它们压缩或丢弃了过去的证据本身。我们引入了 Memex，这是一种索引经验记忆机制，它在不丢弃证据的情况下压缩上下文。Memex 维持一个由简洁结构化摘要和稳定索引组成的紧凑工作上下文，同时将高保真的底层交互存储在这些索引下的外部经验数据库中。智能体随后可以决定何时解引用（dereference）索引，并恢复当前子目标所需的精确历史证据。我们利用强化学习框架 MemexRL 优化了读写行为，采用针对上下文预算下索引记忆使用而定制的奖励塑造，使智能体学会总结什么、归档什么、如何索引以及何时检索。与仅使用摘要的方法相比，这产生了一种损耗显著更低的长程记忆形式。我们进一步提供了理论分析，展示了 Memex 循环在保持决策质量的同时，通过有限的解引用次数，使有效上下文计算量在历史增长时保持有界。实验表明，在具有挑战性的长程任务中，使用 MemexRL 训练的 Memex 智能体在显著减少工作上下文使用的同时，提高了任务成功率。

## Abstract
Large language model (LLM) agents are fundamentally bottlenecked by finite context windows on long-horizon tasks. As trajectories grow, retaining tool outputs and intermediate reasoning in-context quickly becomes infeasible: the working context becomes prohibitively long, eventually exceeds the context budget, and makes distant evidence harder to use even when it is still present. Existing solutions typically shorten context through truncation or running summaries, but these methods are fundamentally lossy because they compress or discard past evidence itself. We introduce Memex, an indexed experience memory mechanism that instead compresses context without discarding evidence. Memex maintains a compact working context consisting of concise structured summaries and stable indices, while storing full-fidelity underlying interactions in an external experience database under those indices. The agent can then decide when to dereference an index and recover the exact past evidence needed for the current subgoal. We optimize both write and read behaviors with our reinforcement learning framework MemexRL, using reward shaping tailored to indexed memory usage under a context budget, so the agent learns what to summarize, what to archive, how to index it, and when to retrieve it. This yields a substantially less lossy form of long-horizon memory than summary-only approaches. We further provide a theoretical analysis showing the potential of the Memex loop to preserve decision quality with bounded dereferencing while keeping effective in-context computation bounded as history grows. Empirically, on challenging long-horizon tasks, Memex agent trained with MemexRL improves task success while using a significantly smaller working context.

---

## 论文详细总结（自动生成）

这是一份关于论文《Memex(RL): Scaling Long-Horizon LLM Agents via Indexed Experience Memory》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心挑战**：LLM 智能体在处理长程任务（如复杂的科学研究、多步骤业务流程）时，受限于有限的上下文窗口。
*   **现有方案缺陷**：目前的截断（Truncation）或滚动摘要（Summarization）方法是“有损”的。它们会压缩或直接丢弃过去的原始证据（如工具输出、代码片段、日志），导致智能体在后续步骤中无法找回关键细节。
*   **研究动机**：借鉴人类管理长期工作的方式（保留简明笔记，同时通过索引/书签引用外部详细文档），提出一种“无损”的上下文压缩机制，使智能体能在极小的上下文占用下处理极长的任务轨迹。

### 2. 方法论：核心思想与关键技术
*   **核心思想（Memex）**：将记忆分为**工作上下文（Working Context）**和**外部经验存储（External Store）**。
    *   **工作上下文**：仅保留“索引摘要（Indexed Summary）”，包含当前进度描述和指向外部存储的稳定索引（Index）。
    *   **外部存储**：以键值对（Key-Value）形式存储高保真的原始交互内容。
*   **关键操作**：
    *   `CompressExperience`：将当前冗长的轨迹归档至外部存储，并在上下文中替换为简洁的索引摘要。支持手动编写内容或基于锚点（Anchor）的自动提取。
    *   `ReadExperience(index)`：通过解引用（Dereferencing）特定索引，将所需的原始证据重新注入当前上下文。
*   **MemexRL 强化学习框架**：
    *   **算法基础**：采用 GRPO（群体相对策略优化）风格的更新。
    *   **奖励塑造（Reward Shaping）**：$R = R_{task} - P_{context} - P_{redundancy} - P_{format}$。惩罚项包括上下文溢出、冗余工具调用（鼓励使用记忆而非重复执行）和格式错误。
    *   **分段轨迹处理**：将轨迹在压缩点处切分，各段独立计算概率但共享最终奖励，解决长程信用分配问题。
    *   **软触发机制**：系统实时反馈上下文占用状态，由智能体自主决定何时压缩，而非硬性规定。

### 3. 实验设计
*   **实验场景**：基于 **ALFWorld**（文字交互式家庭环境）进行了大幅强化难度修改：
    *   **隐藏命令**：不提供可用动作列表，需自主探索。
    *   **隐藏初始观察**：环境描述不固定在系统提示词中，需通过动作获取。
    *   **限制观察次数**：关键的“查看（look）”动作每集仅限一次，强制智能体必须依赖记忆。
    *   **强制摘要截断**：将摘要长度限制在 300 token，迫使智能体将细节存入外部索引。
*   **对比基准（Baseline）**：主要对比了经过 MemexRL 训练前后的智能体表现，验证强化学习对记忆管理能力的提升。

### 4. 资源与算力
*   **模型使用**：Qwen3-30B-A3B-Thinking-2507（一个拥有 30B 参数、活跃参数约 3B 的 MoE 模型）。
*   **训练框架**：基于开源的 LLM RL 框架 **Slime**。
*   **算力细节**：
    *   **量化技术**：推理阶段使用 **INT4** 量化；训练阶段使用量化感知训练（QAT），权重为 INT4，梯度累积使用 **BF16**。
    *   **具体配置**：Batch size 为 32，GRPO 组大小（Group size）为 8。
    *   **未明确说明**：文中未明确提及具体的 GPU 型号（如 A100/H100）及具体的训练总时长。

### 5. 实验数量与充分性
*   **实验规模**：训练集包含 **3553** 个任务。
*   **充分性评价**：
    *   **理论支撑**：提供了两个命题的理论证明，论证了 Memex 循环在理论上能保持决策质量且使计算量有界。
    *   **训练动态分析**：展示了成功率、惩罚项变化以及工具使用频率（压缩次数减少、检索次数增加）的演进曲线。
    *   **局限性**：实验主要集中在修改版的 ALFWorld 这一种基准上，虽然难度极高，但缺乏在代码生成、网络浏览等其他长程任务领域的横向对比。

### 6. 主要结论与发现
*   **性能飞跃**：MemexRL 将任务成功率从 **24.2% 提升至 85.6%**。
*   **效率提升**：在成功率大幅提升的同时，峰值工作上下文长度从约 1.7 万 token 降至约 9600 token（降幅约 **43%**）。
*   **行为转变**：RL 训练使智能体从“频繁盲目压缩”转变为“有选择性地压缩”并“频繁精准检索”。检索（ReadExperience）次数从场均 1 次增加到 6-7 次。

### 7. 优点
*   **低损耗性**：通过索引解引用机制，解决了传统摘要方法无法恢复精确细节（如长字符串 ID、代码）的问题。
*   **自主性**：将记忆管理视为一种“技能”而非硬编码规则，智能体能根据任务语义决定压缩时机。
*   **理论与实践结合**：不仅有显著的实验提升，还通过数学证明界定了该机制的有效性边界。

### 8. 不足与局限
*   **场景单一**：目前仅在 ALFWorld 变体上验证，其泛化到更开放、非结构化环境（如真实软件工程）的效果有待观察。
*   **冷启动依赖**：MemexRL 需要从高质量的演示数据中进行热启动（Warm-start），对初始数据的质量有一定要求。
*   **推理开销**：虽然节省了上下文 token，但引入了额外的外部数据库查询操作和多次推理步骤（解引用），可能增加端到端的延迟。

（完）
