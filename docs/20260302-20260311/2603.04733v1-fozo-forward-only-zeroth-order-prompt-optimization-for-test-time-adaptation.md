---
title: "FOZO: Forward-Only Zeroth-Order Prompt Optimization for Test-Time Adaptation"
title_zh: FOZO：用于测试时自适应的仅前向零阶提示优化
authors: "Xingyu Wang, Tao Wang"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.04733v1"
tags: ["query:ai-ux"]
score: 9.0
evidence: 用于测试时自适应的零阶提示词优化
tldr: 针对测试时自适应（TTA）中反向传播方法计算开销大且修改模型权重的问题，本文提出FOZO。这是一种仅前向传播的零阶提示优化方法，通过优化中间特征统计量和预测熵，并引入动态衰减扰动尺度，实现了在资源受限设备上的高效稳定自适应。实验证明FOZO在多个ImageNet变体数据集上超越了主流梯度方法，且在量化模型上表现出色。
selection_source: fresh_fetch
motivation: 现有的TTA方法要么因依赖反向传播而对硬件要求过高，要么因无需反向传播而导致自适应能力受限。
method: 提出一种基于零阶梯度的提示优化框架，结合特征统计与熵优化目标，并采用动态衰减的扰动尺度以保证收敛。
result: "在ImageNet-C等多个数据集上表现优异，Top-1准确率达到59.52%，优于主流梯度方法及SOTA前向方法。"
conclusion: FOZO为资源受限场景下的测试时自适应提供了一种高效、稳定且极具竞争力的无反向传播解决方案。
---

## 摘要
测试时自适应 (TTA) 对于使深度学习模型能够处理现实世界的数据分布偏移至关重要。然而，当前的方法面临显著的局限性：基于反向传播的方法由于其高计算和内存需求，以及在自适应过程中倾向于修改模型权重，不适用于低端部署设备；而传统的无反向传播技术则表现出受限的自适应能力。在这项工作中，我们提出了仅前向零阶优化 (FOZO)，这是一种用于 TTA 的新型且实用的无反向传播范式。FOZO 利用内存高效的零阶提示优化，该优化由同时优化中间特征统计量和预测熵的目标所引导。为了确保在分布外数据流上的高效且稳定的自适应，我们在零阶梯度估计过程中引入了动态衰减的扰动尺度，并在 TTA 数据流假设下理论证明了其收敛性。在 ImageNet-C、ImageNet-R 和 ImageNet-Sketch 上进行的广泛持续自适应实验证明了 FOZO 的卓越性能，在 ImageNet-C (5K, level 5) 上实现了 59.52% 的 Top-1 准确率，优于主要的基于梯度的方法和最先进的 (SOTA) 仅前向方法 FOA (58.13%)。此外，FOZO 在量化 (INT8) 模型上表现出强大的泛化能力。这些发现表明，FOZO 是资源受限场景下 TTA 部署的一个极具竞争力的解决方案。

## Abstract
Test-Time Adaptation (TTA) is essential for enabling deep learning models to handle real-world data distribution shifts. However, current approaches face significant limitations: backpropagation-based methods are not suitable for low-end deployment devices, due to their high computation and memory requirements, as well as their tendency to modify model weights during adaptation; while traditional backpropagation-free techniques exhibit constrained adaptation capabilities. In this work, we propose Forward-Only Zeroth-Order Optimization (FOZO), a novel and practical backpropagation-free paradigm for TTA. FOZO leverages a memory-efficient zeroth-order prompt optimization, which is led by objectives optimizing both intermediate feature statistics and prediction entropy. To ensure efficient and stable adaptation over the out-of-distribution data stream, we introduce a dynamically decaying perturbation scale during zeroth-order gradient estimation and theoretically prove its convergence under the TTA data stream assumption. Extensive continual adaptation experiments on ImageNet-C, ImageNet-R, and ImageNet-Sketch demonstrate FOZO's superior performance, achieving 59.52% Top-1 accuracy on ImageNet-C (5K, level 5) and outperforming main gradient-based methods and SOTA forward-only FOA (58.13%). Furthermore, FOZO exhibits strong generalization on quantized (INT8) models. These findings demonstrate that FOZO is a highly competitive solution for TTA deployment in resource-limited scenarios.

---

## 论文详细总结（自动生成）

### FOZO: 用于测试时自适应的仅前向零阶提示优化论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
在现实部署中，深度学习模型常面临训练与测试数据分布不一致（分布偏移）的问题。**测试时自适应（TTA）**旨在利用无标签测试数据动态调整模型。
*   **现有挑战**：
    *   **基于反向传播（BP）的方法**：计算和内存开销巨大，且需要修改模型权重，不适合低功耗边缘设备或权重固定的硬件。
    *   **现有的无梯度/仅前向方法**：如特征对齐或进化策略（CMA-ES），要么学习能力受限，要么在高维空间下收敛极慢，难以应对快速变化的数据流。
*   **研究目标**：开发一种既不需要反向传播、内存占用极低，又能实现快速、高性能自适应的 TTA 范式。

#### 2. 方法论：核心思想与技术细节
FOZO 提出了一种**仅前向传播的零阶提示优化（Zeroth-Order Prompt Optimization）**框架：
*   **核心思想**：在不更新模型权重的前提下，通过在输入层插入可学习的“提示词（Prompts）”，并利用零阶梯度估计（仅需前向计算损失值）来更新这些提示词。
*   **关键技术细节**：
    *   **零阶梯度估计（SPSA）**：采用同时扰动随机逼近（SPSA）算法。通过对提示词施加随机扰动 $Z$，计算扰动前后的损失差值来估计梯度方向，从而规避复杂的链式求导。
    *   **动态扰动方案（Dynamic Perturbation）**：为了平衡“探索”与“收敛”，设计了动态衰减的扰动尺度 $\epsilon_t$。当检测到损失波动大（暗示领域切换）时重置扰动，稳定时则按比例衰减。
    *   **深浅层对齐损失（Deep-Shallow Aligning Loss）**：
        *   **统计量对齐**：将模型分为深浅两组，分别计算 [CLS] token 的均值和方差，并与源域统计量对齐。
        *   **熵最小化**：通过最小化预测输出的熵来提高模型对目标域预测的置信度。
*   **理论保证**：论文在理论上证明了该方法在 TTA 数据流假设下的收敛性，指出其收敛速度与参数的“有效秩”相关，而非原始参数维度。

#### 3. 实验设计
*   **数据集与场景**：
    *   **ImageNet-C**：评估对常见图像损坏（如噪声、模糊）的鲁棒性。
    *   **ImageNet-R & ImageNet-Sketch**：评估对艺术风格、素描等语义偏移的泛化能力。
    *   **持续自适应（Continual TTA）**：模拟真实世界中领域不断切换且边界未知的场景。
    *   **量化模型自适应**：在 INT8 量化模型上验证方法的实用性。
*   **Benchmark 与对比方法**：
    *   **仅前向方法**：LAME, T3A, FOA (SOTA), ZOA。
    *   **基于反向传播的方法**：TENT, EATA, SAR, DeYO。

#### 4. 资源与算力
*   **硬件环境**：实验主要在 **NVIDIA RTX 4090 GPU** 上进行。
*   **算力指标**：
    *   论文详细记录了处理 ImageNet-C (5k) 的总耗时（如 FOZO 在特定设置下约为 179s，优于 FOA 的 224s）。
    *   **内存占用**：FOZO 的峰值显存约为 831 MiB，远低于基于 BP 方法的 5400+ MiB。
    *   **前向次数**：对比实验中设定了相同的前向传播次数（如 2 次或 28 次 FP）以确保公平。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 15 种损坏类型、5 个严重等级、3 个大型数据集变体。
*   **消融实验**：详细分析了零阶优化、特征对齐损失、动态扰动方案各自的贡献。
*   **超参数分析**：对提示词数量、Batch Size、衰减因子等进行了敏感度测试。
*   **公平性**：在相同的前向传播次数（FP）和硬件环境下对比了运行时间、内存和准确率，实验设计客观且全面。

#### 6. 主要结论与发现
*   **性能领先**：FOZO 在 ImageNet-C 上达到了 59.52% 的 Top-1 准确率，超越了现有的仅前向 SOTA 方法 FOA，甚至在某些指标上优于基于 BP 的方法。
*   **收敛极快**：相比于使用进化策略的 FOA，FOZO 达到相同准确率所需的时间缩短了约 34%。
*   **量化友好**：在 INT8 量化模型上，FOZO 依然能显著提升性能，证明了其在实际部署中的稳健性。
*   **内存极低**：由于无需存储中间层激活值用于反向传播，其内存需求仅为 BP 方法的 15% 左右。

#### 7. 优点（亮点）
*   **部署友好**：完全不依赖反向传播，非常适合 FPGA、ASIC 等对梯度支持较弱的硬件。
*   **理论支撑**：不仅有实验结果，还提供了严谨的收敛性证明，增强了算法的可信度。
*   **动态扰动创新**：巧妙地解决了零阶优化在非平稳数据流中容易陷入局部最优或不收敛的问题。

#### 8. 不足与局限
*   **前向开销**：虽然比 BP 省内存，但为了估计梯度，单次更新通常需要至少 2 次前向传播（正负扰动），在对延迟要求极高的实时任务中仍有优化空间。
*   **提示词依赖**：性能高度依赖于提示词的初始化和数量，虽然文中做了消融，但在极端未知的领域偏移下，固定长度的提示词可能存在表达瓶颈。
*   **应用范围**：目前主要在 Vision Transformer (ViT) 上验证，对于 CNN 等其他架构的普适性有待进一步大规模验证。

（完）
