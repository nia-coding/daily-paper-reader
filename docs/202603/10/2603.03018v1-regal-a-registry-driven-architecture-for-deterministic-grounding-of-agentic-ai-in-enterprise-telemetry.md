---
title: "REGAL: A Registry-Driven Architecture for Deterministic Grounding of Agentic AI in Enterprise Telemetry"
title_zh: REGAL：一种用于企业遥测中智能体 AI 确定性锚定的注册表驱动架构
authors: Yuvraj Agrawal
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.03018v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 企业遥测中的智能体自动化与接地
tldr: 本研究针对企业遥测数据在AI智能体应用中面临的上下文受限、语义定义模糊及接口演进挑战，提出了REGAL架构。该架构通过注册表驱动的确定性接地方法，结合Medallion ELT流水线生成压缩语义产物，并利用声明式定义合成MCP工具。REGAL将确定性计算视为核心原语，有效解决了工具漂移问题，提升了智能体在复杂企业环境中的运行效率与治理能力。
selection_source: fresh_fetch
motivation: 旨在解决大型语言模型在处理海量、异构且动态演进的企业私有遥测数据时，存在的上下文窗口限制、语义不一致及接口匹配难题。
method: 提出一种注册表驱动架构，结合Medallion ELT流水线生成语义产物，并通过声明式指标定义自动合成模型上下文协议（MCP）工具。
result: 原型验证表明，该架构实现了确定性接地，在降低延迟、提高Token效率以及增强操作治理方面具有显著优势。
conclusion: REGAL通过将确定性计算和语义编译提升为核心设计原语，为企业级AI智能体的系统化接地提供了一套标准化的架构模式。
---

## 摘要
企业工程组织从版本控制系统、CI/CD 流水线、问题追踪器和可观测性平台中产生大量异构遥测数据。大语言模型 (LLM) 实现了新型的智能体自动化，但将此类智能体锚定在私有遥测数据上会面临三个实际挑战：有限的模型上下文、本地定义的语义概念以及不断演进的指标接口。我们提出了 REGAL，这是一种用于企业遥测中智能体 AI 系统确定性锚定的注册表驱动架构。REGAL 采用了一种明确的架构方法：将确定性遥测计算视为一等原语 (first-class primitive)，且 LLM 在受限且版本控制的操作空间上运行，而非直接处理原始事件流。该架构结合了：(1) 一个产生可重放、语义压缩的 Gold 级产物的奖章式 (Medallion) ELT 流水线；(2) 一个从声明式指标定义中合成模型上下文协议 (MCP) 工具的注册表驱动编译层。注册表充当“接口即代码”层，确保工具规范与执行之间的一致性，缓解工具漂移，并直接在语义边界嵌入治理策略。原型实现和案例研究验证了确定性锚定的可行性，并阐明了其对延迟、Token 效率和运营治理的影响。本研究系统化了企业级 LLM 锚定的架构模式；它没有提出新的学习算法，而是将确定性计算和语义编译提升为智能体系统的一等设计原语。

## Abstract
Enterprise engineering organizations produce high-volume, heterogeneous telemetry from version control systems, CI/CD pipelines, issue trackers, and observability platforms. Large Language Models (LLMs) enable new forms of agentic automation, but grounding such agents on private telemetry raises three practical challenges: limited model context, locally defined semantic concepts, and evolving metric interfaces.   We present REGAL, a registry-driven architecture for deterministic grounding of agentic AI systems in enterprise telemetry. REGAL adopts an explicitly architectural approach: deterministic telemetry computation is treated as a first-class primitive, and LLMs operate over a bounded, version-controlled action space rather than raw event streams.   The architecture combines (1) a Medallion ELT pipeline that produces replayable, semantically compressed Gold artifacts, and (2) a registry-driven compilation layer that synthesizes Model Context Protocol (MCP) tools from declarative metric definitions. The registry functions as an "interface-as-code" layer, ensuring alignment between tool specification and execution, mitigating tool drift, and embedding governance policies directly at the semantic boundary.   A prototype implementation and case study validate the feasibility of deterministic grounding and illustrate its implications for latency, token efficiency, and operational governance. This work systematizes an architectural pattern for enterprise LLM grounding; it does not propose new learning algorithms, but rather elevates deterministic computation and semantic compilation to first-class design primitives for agentic systems.

---

## 论文详细总结（自动生成）

这是一份关于论文《REGAL: A Registry-Driven Architecture for Deterministic Grounding of Agentic AI in Enterprise Telemetry》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：在企业环境中，如何让 AI 智能体（Agent）可靠地利用海量、异构且动态演进的遥测数据（如日志、CI/CD 流水线、问题追踪器等）进行决策。
*   **研究动机**：传统的概率性锚定（如直接将原始日志输入 RAG 系统）面临三大挑战：
    1.  **上下文限制**：遥测数据量远超 LLM 的上下文窗口，且 Token 成本极高。
    2.  **语义歧义**：企业内部概念（如“P1 级故障”、“回归测试”）具有局部定义，模型易产生幻觉。
    3.  **接口漂移（Tool Drift）**：手动编写的工具接口与底层不断演进的数据架构容易脱节，导致治理风险。

### 2. 方法论
REGAL 提出了一种**注册表驱动的确定性锚定架构**，其核心思想是将确定性计算与概率性推理分离。
*   **核心思想**：LLM 不应直接处理原始事件流，而应作为“消费者”调用受限的、版本化的语义工具。
*   **关键技术细节**：
    *   **Medallion ELT 流水线**：采用奖章式架构，将原始数据（Bronze）经过模式协调（Silver）转化为语义压缩的、可重放的 Gold 级产物。
    *   **注册表驱动的语义编译**：这是核心创新。通过一个声明式的指标注册表（Registry），自动编译生成符合 **模型上下文协议 (MCP)** 的工具接口。
    *   **接口即代码 (Interface-as-Code)**：工具的 Schema、检索逻辑和治理策略均由注册表统一生成，确保模型看到的工具描述与后端执行逻辑严格一致。
    *   **确定性-概率性非干扰属性**：定义了公式 $\frac{\partial D}{\partial P} = 0$，即概率性推理（P）的改变（如更换模型或提示词）绝不能影响确定性计算（D）的结果。
    *   **混合推拉模型**：支持用户触发的工具调用（Pull）和基于事件流的实时预警（Push）。

### 3. 实验设计
*   **场景与案例研究**：论文通过一个原型实现和具体的**事件调查工作流**（例如：“为什么昨天 iOS 崩溃率激增？”）进行验证。
*   **对比基准**：
    *   **传统手动工作流**：工程师手动切换多个平台（仪表盘、CI/CD、Jira）进行关联分析。
    *   **REGAL 辅助工作流**：智能体通过调用编译好的 MCP 工具，在 Gold 级产物上进行合成推理。
*   **对比维度**：跨系统调查开销、交互延迟、Token 缩放行为。

### 4. 资源与算力
*   **算力说明**：论文未明确提及具体的 GPU 型号、数量或训练时长。
*   **原因**：REGAL 侧重于**系统架构设计**而非训练新的学习算法。原型部署在通用服务器硬件上，利用标准的任务编排引擎（如 Airflow）和数据库（如 MongoDB）运行。

### 5. 实验数量与充分性
*   **实验规模**：论文主要展示了一个原型系统的实现和代表性的案例研究，而非大规模的基准测试（Benchmark）。
*   **充分性评价**：
    *   作为一篇**系统化（Systematization）**论文，其通过原型验证了架构的可行性和操作连贯性。
    *   **客观性**：实验客观地展示了在多小时遥测窗口下，预聚合的 Gold 产物比原始日志序列化能显著减少 Token 消耗。
    *   **局限性**：缺乏与 Text-to-SQL 或 GraphRAG 等其他先进锚定技术的定量对比实验，样本量较小。

### 6. 主要结论与发现
*   **确定性锚定可行性**：通过将计算上移至确定性层，可以有效消除工具漂移并降低模型幻觉。
*   **Token 效率**：Token 使用量随指标数量而非原始日志条数增长，显著提升了经济性。
*   **治理增强**：通过在 MCP 边界嵌入访问控制（ACL）和缓存策略，实现了“接口即治理”。
*   **延迟表现**：在原型中，数据检索延迟远低于模型推理延迟，证明架构不会成为性能瓶颈。

### 7. 优点
*   **架构严谨性**：明确区分了确定性计算和概率性推理，解决了企业级 AI 应用中最棘手的可靠性问题。
*   **自动化程度高**：注册表驱动的编译机制减少了手动维护工具接口的工作量。
*   **解耦设计**：模型无关（Model-agnostic）的设计允许企业在不改变数据流水线的情况下更换底层 LLM。

### 8. 不足与局限
*   **实验覆盖面**：缺乏大规模、多行业的基准测试数据支持。
*   **表达能力受限**：为了安全性限制了智能体的操作空间，可能无法处理注册表定义之外的复杂、非结构化查询（相比于 Text-to-SQL）。
*   **依赖性**：系统的有效性高度依赖于前期对 Gold 级产物和指标注册表的精心设计。
*   **模型变异性**：虽然架构是确定的，但最终的解释质量仍受限于 LLM 本身的推理能力。

（完）
