---
title: Learning from Synthetic Data Improves Multi-hop Reasoning
title_zh: 从合成数据中学习提升了多步推理能力
authors: "Anmol Kabra, Yilun Yin, Albert Gong, Kamilė Stankevičiūtė, Dongyoung Go, Johann Lee, Katie Z. Luo, Carla P. Gomes, Kilian Q. Weinberger"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.02091v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 利用规则生成的合成数据进行强化学习微调以提升推理能力
tldr: 本研究探讨了利用规则生成的合成数据进行强化学习（RL）微调，以提升大语言模型的多跳推理能力。针对现有RL微调依赖昂贵的人工标注或易出错的LLM生成数据的问题，作者发现即使合成数据仅包含虚构知识，也能显著提高模型在真实世界问答基准上的表现。研究表明，合成数据能教会模型“知识组合”这一通用的推理技能，为提升模型推理能力提供了一种免费且可扩展的新途径。
selection_source: fresh_fetch
motivation: 传统的强化学习微调受限于高质量标注数据的稀缺性、高成本以及大模型生成数据的幻觉问题。
method: 研究者采用基于规则生成的虚构知识合成数据，对大语言模型进行强化学习微调以训练其多跳推理。
result: 实验证明，在虚构合成数据上训练的模型在真实问答任务中表现更优，展现出更强的知识组合与推理迁移能力。
conclusion: 规则生成的合成推理数据是提升大语言模型通用推理能力的有效且低成本的资源。
---

## 摘要
强化学习 (RL) 已被证明能显著提升大语言模型 (LLMs) 在数学、编程和多步推理任务中的推理能力。然而，RL 微调需要大量高质量的可验证数据，这些数据通常源自人工标注、由前沿 LLM 生成或由基于 LLM 的验证器评分。这三种方式都有明显的局限性：人工标注的数据集规模较小且整理成本高昂，LLM 生成的数据容易产生幻觉且成本较高，而基于 LLM 的验证器则不够准确且速度较慢。在这项工作中，我们研究了一种更廉价的替代方案：在规则生成的合成数据上进行多步推理任务的 RL 微调。我们发现，尽管合成数据仅包含虚构知识，但在合成数据上微调的 LLM 在流行的真实世界问答基准测试中表现显著更好。通过按问题难度对性能进行分层分析，我们发现合成数据教会了 LLM 组合知识——这是一种基础且可泛化的推理技能。我们的工作强调了规则生成的合成推理数据是一种免费且可扩展的资源，可用于提升 LLM 的推理能力。

## Abstract
Reinforcement Learning (RL) has been shown to significantly boost reasoning capabilities of large language models (LLMs) in math, coding, and multi-hop reasoning tasks. However, RL fine-tuning requires abundant high-quality verifiable data, often sourced from human annotations, generated from frontier LLMs, or scored by LLM-based verifiers. All three have considerable limitations: human-annotated datasets are small and expensive to curate, LLM-generated data is hallucination-prone and costly, and LLM-based verifiers are inaccurate and slow. In this work, we investigate a cheaper alternative: RL fine-tuning on rule-generated synthetic data for multi-hop reasoning tasks. We discover that LLMs fine-tuned on synthetic data perform significantly better on popular real-world question-answering benchmarks, despite the synthetic data containing only fictional knowledge. On stratifying performance by question difficulty, we find that synthetic data teaches LLMs to compose knowledge -- a fundamental and generalizable reasoning skill. Our work highlights rule-generated synthetic reasoning data as a free and scalable resource to improve LLM reasoning capabilities.

---

## 论文详细总结（自动生成）

这篇论文探讨了如何利用规则生成的合成数据来突破大语言模型（LLM）在强化学习（RL）微调过程中的数据瓶颈。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：强化学习（尤其是带有可验证奖励的 RLVR）能显著提升模型推理能力，但其高度依赖高质量、可验证的数据。目前这类数据主要来自人工标注（昂贵且量少）、前沿模型生成（易幻觉且成本高）或 LLM 验证器（慢且不准）。
*   **研究动机**：作者提出了一种“激进”的替代方案——使用**基于规则生成的合成数据**（如基于模板、上下文无关文法和逻辑程序生成的虚构知识）。
*   **整体含义**：研究旨在验证一个核心假设：在完全虚构、语义简单的合成数据上训练，是否能让模型习得一种通用的、可迁移到真实世界的“知识组合”（Knowledge Composition）推理技能。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：利用规则生成器产生大规模、零成本、完全可验证的虚构世界问答对，通过强化学习强制模型在这些虚构事实间进行多步逻辑跳转。
*   **关键技术细节**：
    *   **合成数据生成**：使用了四类数据集：
        1.  **PhantomWiki**：生成虚构人物及其关系的维基百科式文档，涉及多跳查询。
        2.  **GSM-∞**：基于数学计算图生成的无限算术应用题。
        3.  **ReasoningGym (Family/Knights)**：家庭关系推理和逻辑悖论（骑士与无赖）问题。
    *   **算法流程**：采用 **GRPO（Group Relative Policy Optimization）** 算法。这是一种策略梯度算法的变体，通过一组输出的相对奖励来估计优势函数（Advantage），无需额外的价值模型（Value Model），节省了显存。
    *   **奖励设计**：采用结果导向的奖励（Outcome-only Reward）。对于数学和逻辑题给予二元奖励（对/错），对于多答案的 PhantomWiki 给予 F1 分数奖励。

### 3. 实验设计
*   **实验对象（模型）**：涵盖了不同规模和家族的模型，包括 Qwen3 (0.6B, 1.7B, 4B)、Qwen2.5 (1.5B, 7B) 和 Phi-4-mini-reasoning。
*   **训练数据集**：上述四种规则生成的合成数据集（各约 10K 样本）。
*   **评估基准（Benchmark）**：5 个真实的知识密集型多跳推理数据集：**HotpotQA、2WikiMultihopQA、MuSiQue、CofCA、SynthWorlds-RM**。
*   **对比方法**：
    *   **Base 模型**：未经过额外 RL 微调的原始模型。
    *   **格式奖励消融**：仅奖励输出格式（`<answer>` 标签）而不奖励正确性。
    *   **SFT vs RL**：对比有监督微调（使用推理轨迹）与强化学习的效果。
    *   **真实数据 RL**：在真实数据集（如 HotpotQA 训练集）上直接进行 RL。

### 4. 资源与算力
*   **算力设备**：使用了 **4 台 NVIDIA H100 (80GB VRAM)** GPU。
*   **训练时长**：
    *   对于 1B 到 4B 规模的模型，在 10K 样本上进行 RL 微调大约需要 **1 天**（约 100 H100 小时）。
    *   较小的模型（如 Qwen2.5-1.5B）由于生成的思维链（CoT）较短，训练约需 **20 小时**。
*   **软件框架**：使用了 Hugging Face TRL 库、vLLM 加速推理以及 FlashAttention-2。

### 5. 实验数量与充分性
*   **实验规模**：作者进行了跨模型家族（Qwen, Phi）、跨模型参数量（0.6B 到 7B）、跨合成任务（数学、逻辑、多跳）以及跨真实基准的广泛实验。
*   **充分性与客观性**：
    *   实验设计非常充分，不仅验证了性能提升，还通过**难度分层分析**证明了模型在未见过的难度（OOD）上也能泛化。
    *   通过**虚构世界测试**排除了模型仅是“激活了预训练记忆”的可能性。
    *   设置了随机种子并报告了标准差，确保了结果的可重复性和公平性。

### 6. 主要结论与发现
*   **显著提升**：在合成数据上微调后，Qwen3-0.6B 在真实多跳任务上的 F1 分数提升了 **56% 到 131%**。
*   **技能迁移**：尽管合成数据中没有真实的地理、历史知识，但模型学会了“如何将 A->B 和 B->C 串联起来”这一通用技能。
*   **RL 优于 SFT**：在合成数据上，SFT 容易导致任务过拟合，而 RL 能更好地诱导推理能力的迁移。
*   **过程自发改进**：虽然只奖励最终答案，但观察发现模型生成的中间推理步骤（思维链）的准确性也随之提高。
*   **可扩展性**：性能随合成数据量的增加单调提升，没有观察到过拟合现象。

### 7. 优点
*   **成本极低**：规则生成的合成数据几乎是免费的，且可以无限扩展。
*   **无污染风险**：由于知识是完全虚构的，不存在测试集泄露或预训练数据污染的问题。
*   **科学性强**：成功将“推理能力”与“知识储备”解耦，证明了推理是一种可以独立训练的元技能。

### 8. 不足与局限
*   **大模型边际效应**：在 4B 和 7B 等较大模型上，提升效果不如小模型显著（作者推测可能受限于算力导致的超参数设置不佳，如 Rollout 数量不足）。
*   **语言多样性缺失**：规则生成的模板语言非常单一，虽然推理逻辑迁移了，但模型在处理复杂、多变的真实语言表达时仍存在上限。
*   **仍逊于在域训练**：虽然合成数据效果很好，但其表现仍略低于直接在真实多跳数据上进行 RL 的效果。

（完）
