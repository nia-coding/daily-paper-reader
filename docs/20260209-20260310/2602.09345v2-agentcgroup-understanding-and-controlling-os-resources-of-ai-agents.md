---
title: "AgentCgroup: Understanding and Controlling OS Resources of AI Agents"
title_zh: AgentCgroup：理解与控制 AI Agent 的操作系统资源
authors: "Yusheng Zheng, Jiakun Fan, Quanzhi Fu, Yiwei Yang, Wei Zhang, Andi Quinn"
date: 2026-02-10
pdf: "https://arxiv.org/pdf/2602.09345v2"
tags: ["query:sr-ai"]
score: 10.0
evidence: SWE-rebench任务中操作系统资源动态的特征分析
tldr: "本研究针对多租户云环境下AI Agent资源消耗波动大且难以预测的问题，通过分析144个软件工程任务，揭示了OS级执行占总延迟的56-74%以及内存峰值显著等特征。针对现有资源控制机制在粒度、响应速度和适应性上的不足，提出了AgentCgroup。该系统利用eBPF技术和分层cgroup结构，实现了基于意图的内核级资源调度与隔离，有效提升了多租户环境下的资源利用率和隔离性。"
selection_source: fresh_fetch
motivation: 现有的资源控制机制无法应对AI Agent在执行工具调用时产生的高动态、不可预测且以内存为瓶颈的资源需求。
method: 提出AgentCgroup，一种基于eBPF的意图驱动控制器，通过分层cgroup结构和内核态策略实现细粒度的资源管理。
result: 初步评估表明，AgentCgroup显著改善了多租户环境下的资源隔离效果，并有效减少了资源浪费。
conclusion: AgentCgroup通过将Agent的意图与内核级资源控制相结合，为解决AI Agent在云端部署中的资源管理难题提供了高效方案。
---

## 摘要
AI Agent 越来越多地部署在多租户云环境中，它们在沙箱容器内执行各种工具调用（tool calls），每次调用都有不同的资源需求和剧烈的波动。我们对沙箱化 AI 编程 Agent 的操作系统级资源动态进行了系统性表征，分析了来自 SWE-rebench 基准测试中跨两个大语言模型（LLM）的 144 个软件工程任务。我们的测量结果表明：(1) 操作系统级执行（工具调用、容器和 Agent 初始化）占端到端任务延迟的 56-74%；(2) 内存而非 CPU 是并发瓶颈；(3) 内存峰值由工具调用驱动，峰均比高达 15.4 倍；(4) 资源需求在不同任务、运行和模型之间具有高度不可预测性。通过将这些特性与无服务器（serverless）、微服务和批处理工作负载进行对比，我们发现了现有资源控制中的三个不匹配：粒度不匹配（容器级策略与工具调用级动态）、响应性不匹配（用户空间反应与亚秒级不可预测突发）以及适应性不匹配（基于历史的预测与非确定性有状态执行）。我们提出了 AgentCgroup，这是一种基于意图驱动和 eBPF 的资源控制器。它利用 Agent 声明资源需求和重构执行策略的能力，采用与工具调用边界对齐的分层 cgroup 结构，通过 sched_ext 和 memcg_bpf_ops 进行内核级强制执行，并应用运行时自适应策略。初步评估表明，该方案改善了多租户隔离并减少了资源浪费。AgentCgroup 已在 https://github.com/eunomia-bpf/agentcgroup 开源。

## Abstract
AI agents are increasingly deployed in multi-tenant cloud environments, where they execute diverse tool calls within sandboxed containers, each call with distinct resource demands and rapid fluctuations. We present a systematic characterization of OS-level resource dynamics in sandboxed AI coding agents, analyzing 144 software engineering tasks from the SWE-rebench benchmark across two LLM models. Our measurements reveal that (1) OS-level execution (tool calls, container and agent initialization) accounts for 56-74% of end-to-end task latency; (2) memory, not CPU, is the concurrency bottleneck; (3) memory spikes are tool-call-driven with a up to 15.4x peak-to-average ratio; and (4) resource demands are highly unpredictable across tasks, runs, and models. Comparing these characteristics against serverless, microservice, and batch workloads, we identify three mismatches in existing resource controls: a granularity mismatch (container-level policies vs. tool-call-level dynamics), a responsiveness mismatch (user-space reaction vs. sub-second unpredictable bursts), and an adaptability mismatch (history-based prediction vs. non-deterministic stateful execution). We propose AgentCgroup, an intent-driven eBPF-based resource controller that exploits agents ability to declare resource needs and reconstruct execution strategies, using hierarchical cgroup structures aligned with tool-call boundaries, in-kernel enforcement via sched_ext and memcg_bpf_ops, and runtime-adaptive policies. Preliminary evaluation demonstrates improved multi-tenant isolation and reduced resource waste. AgentCgroup is open-source at https://github.com/eunomia-bpf/agentcgroup

---

## 论文详细总结（自动生成）

这篇论文《AgentCgroup: Understanding and Controlling OS Resources of AI Agents》针对 AI Agent 在云端部署中的资源管理难题提出了创新的解决方案。以下是详细总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着 AI 编程 Agent（如 Claude Code, OpenHands）在多租户云环境中的普及，它们频繁调用各种工具（如编译器、测试运行器）的行为给操作系统资源管理带来了巨大挑战。
*   **研究动机**：现有的资源控制机制（如 Kubernetes QoS、标准 cgroup）是为相对稳定的微服务或批处理任务设计的。而 AI Agent 的资源需求具有**极高的动态性、不可预测性和有状态性**。
*   **核心矛盾**：如果限制太死，Agent 会因 OOM（内存溢出）崩溃并丢失昂贵的 LLM 上下文；如果限制太松，则会造成严重的资源浪费，降低多租户环境的承载密度。

### 2. 方法论：核心思想与关键技术
论文提出了 **AgentCgroup**，一个基于 eBPF 的意图驱动资源控制器，核心思想是将 Agent 的语义意图与内核级的精确控制相结合：
*   **细粒度资源域（Fine-grained Domains）**：不再以容器为最小单位，而是为每个“工具调用”（Tool Call）动态创建子 cgroup。通过透明的 Bash 包装器拦截调用，实现工具级的资源隔离。
*   **内核级 eBPF 强制执行**：
    *   **CPU 调度**：利用 `sched_ext` 在内核态实现自定义调度策略，优先保障高优先级任务。
    *   **内存管理**：利用 `memcg_bpf_ops` 钩子，在内存触及阈值时实现微秒级的延迟（Throttling）或冻结（Freezing），而非直接杀死进程。
*   **意图驱动的协同（Intent-driven Coordination）**：
    *   **上行意图**：Agent 在执行前声明资源需求（如“高内存需求”）。
    *   **下行反馈**：当资源受限时，系统向 Agent 返回自然语言反馈（如“内存不足，请减小测试范围”），让 Agent 自主调整策略。

### 3. 实验设计
*   **数据集/场景**：使用了 **SWE-rebench** 基准测试，包含 144 个真实的软件工程任务。
*   **模型背景**：对比了 Claude Haiku 4.5（云端 API）和 GLM-4.7-Flash（本地 GPU 推理）两种 Agent 后端。
*   **对比方法**：
    *   **特征分析阶段**：对比了 Serverless、微服务和批处理工作负载的资源特征。
    *   **系统评估阶段**：对比了“无隔离（No Isolation）”基准与“AgentCgroup (BPF)”在多租户压力下的表现。

### 4. 资源与算力
*   **硬件环境**：
    *   特征表征实验：Intel Core Ultra 9 285K (24核), 128 GB DDR5 RAM。
    *   原型评估实验：Intel Core Ultra 7 258V (4核), 16 GB RAM。
*   **算力说明**：论文侧重于操作系统层面的资源消耗（CPU/内存），而非 LLM 的训练算力。实验中使用了本地 GPU 运行 GLM 模型，但未详细列出 GPU 型号和训练时长，因为研究重点是 Agent 执行工具时的宿主机资源开销。

### 5. 实验数量与充分性
*   **实验规模**：分析了 144 个任务的完整生命周期，采样频率为 1 秒，涵盖了数千次工具调用。
*   **充分性**：特征分析部分非常详尽，揭示了 15.4 倍的峰均比等关键数据。
*   **局限性**：系统评估部分属于“初步评估（Preliminary Evaluation）”，主要通过回放（Replay）真实轨迹的方式进行，并发规模较小（3 个并发 Session），尚未在大规模生产集群中进行实测。

### 6. 主要结论与发现
*   **OS 开销巨大**：工具调用和初始化占总延迟的 56-74%，LLM 推理仅占 26-44%。
*   **内存是瓶颈**：内存需求波动剧烈（峰均比 15.4x），且不同任务间差异达 20 倍。
*   **不可预测性**：同一任务多次运行，执行时间差异可达 1.8 倍，且 LLM 令牌数与内存峰值几乎无相关性。
*   **AgentCgroup 效果**：在内存压力下，AgentCgroup 将高优先级任务的 P95 延迟降低了 29%，并将 OOM 生存率从 66% 提升至 100%。

### 7. 优点
*   **视角独特**：首次系统性地表征了 AI Agent 在 OS 层面的资源动态。
*   **技术先进**：巧妙利用了 Linux 内核最新的 eBPF 特性（如 `sched_ext` 和 `memcg_bpf_ops`），实现了比传统用户态工具更快的响应速度。
*   **闭环设计**：提出的“意图-反馈”机制充分利用了 AI Agent 能够理解自然语言反馈的特性，打破了 OS 与应用之间的隔阂。

### 8. 不足与局限
*   **评估场景有限**：目前的评估主要基于轨迹回放，缺乏在真实高并发云环境下的长周期测试。
*   **框架依赖**：虽然声称不修改 Agent 框架，但“意图声明”仍需要 Agent 开发者主动适配环境变量。
*   **内核版本要求高**：依赖非常新的 Linux 内核特性（如 6.15+），在旧版企业级系统中难以部署。
*   **初始化开销**：虽然解决了执行期的资源控制，但对于容器镜像过大导致的初始化延迟（占任务时间 31-48%）尚未提出根本性的优化方案。

（完）
