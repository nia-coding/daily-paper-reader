---
title: "AgentConductor: Topology Evolution for Multi-Agent Competition-Level Code Generation"
title_zh: AgentConductor：面向竞赛级代码生成的拓扑演化多智能体系统
authors: "Siyu Wang, Ruotian Lu, Zhihao Yang, Yuchao Wang, Yanzhou Zhang, Lei Xu, Qimin Xu, Guojun Yin, Cailian Chen, Xinping Guan"
date: 2026-02-19
pdf: "https://arxiv.org/pdf/2602.17100v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 多智能体系统的拓扑演化与协作推理
tldr: 针对现有大模型多智能体系统在代码生成中拓扑结构固定、无法根据任务难度动态调整的问题，本文提出 AgentConductor。该系统以强化学习优化的编排智能体为核心，通过拓扑密度函数和难度区间划分，动态生成任务自适应的层级有向无环图拓扑。实验表明，该方法在竞赛级代码生成任务中显著提升了准确率，并大幅降低了通信冗余和 Token 成本。
selection_source: fresh_fetch
motivation: 现有多智能体系统缺乏对任务难度的自适应能力和基于执行反馈的拓扑动态优化，导致通信冗余和性能瓶颈。
method: 提出一种由强化学习驱动的编排智能体，通过拓扑密度函数和难度区间划分，动态构建任务自适应的层级 DAG 交互拓扑。
result: "在五个代码数据集上达到 SOTA，相比最强基准，pass@1 准确率提升高达 14.6%，同时降低了 13% 的拓扑密度和 68% 的 Token 成本。"
conclusion: 动态演化的多智能体拓扑结构能有效平衡协作效率与资源消耗，是提升复杂代码生成任务性能的关键。
---

## 摘要
大语言模型（LLM）驱动的多智能体系统（MAS）通过预定义的交互拓扑协调专业化智能体，在竞赛级代码生成等复杂任务中展现出巨大潜力。最近的研究表明，精心设计的多智能体工作流和通信图可以通过利用协作推理显著提高代码生成性能。然而，现有方法既不能根据任务难度调整拓扑密度，也无法在实例内部利用执行反馈迭代优化拓扑，这导致了冗余通信和性能瓶颈。为了解决这些问题，我们提出了 AgentConductor：一个以基于 LLM 的编排智能体为核心、经强化学习优化的 MAS，它能够实现端到端的反馈驱动动态交互拓扑生成。对于每个查询，AgentConductor 推断智能体角色和任务难度，然后构建一个任务自适应、密度感知的分层有向无环图（DAG）拓扑，这由两项关键创新支撑。首先，我们设计了一种新型拓扑密度函数，捕捉了多智能体交互中感知通信的数学特征。其次，我们采用难度区间划分，以避免过度剪枝，从而实现每个难度级别的精确拓扑密度上限测量和更细粒度的控制。实验表明，在三个竞赛级和两个基础代码数据集上，AgentConductor 达到了最先进的准确率，在 pass@1 准确率上比最强基准提高了高达 14.6%，密度降低了 13%，Token 成本降低了 68%。

## Abstract
Large language model(LLM)-driven multi-agent systems(MAS) coordinate specialized agents through predefined interaction topologies and have shown promise for complex tasks such as competition-level code generation. Recent studies demonstrate that carefully designed multi-agent workflows and communication graphs can significantly improve code generation performance by leveraging collaborative reasoning. However, existing methods neither adapt topology density to task difficulty nor iteratively refine the topology within an instance using execution feedback, which leads to redundant communication and performance bottlenecks. To address these issues, we propose AgentConductor: a reinforcement learning-optimized MAS with an LLM-based orchestrator agent as its core, which enables end-to-end feedback-driven dynamic generation of interaction topologies. For each query, AgentConductor infers agent roles and task difficulty, then constructs a task-adapted, density-aware layered directed acyclic graph (DAG) topology, underpinned by two key innovations. First, we design a novel topological density function that captures communication-aware mathematical characterizations of multi-agent interactions. Second, we adopt difficulty interval partitioning to avoid excessive pruning for precise topological density upper bound measurement per difficulty level and finer-grained control. Empirically, across three competition-level and two foundational code datasets, AgentConductor achieves state-of-the-art accuracy, outperforming the strongest baseline by up to 14.6% in pass@1 accuracy, 13% in density reduction, and 68% in token cost reduction.

---

## 论文详细总结（自动生成）

### AgentConductor：面向竞赛级代码生成的拓扑演化多智能体系统论文总结

#### 1. 核心问题与研究动机
竞赛级编程任务（如 APPS、CodeContests）对逻辑推理和算法实现要求极高。现有的基于大语言模型（LLM）的多智能体系统（MAS）虽然通过预设的交互拓扑（如链式、树式或全连接图）提升了性能，但存在以下痛点：
*   **拓扑固定：** 无法根据任务难度动态调整。简单任务使用复杂拓扑会导致 Token 浪费，复杂任务使用简单拓扑则性能受限。
*   **缺乏反馈演化：** 现有系统通常在执行前确定结构，无法根据代码运行的报错信息（执行反馈）在实例内部实时优化交互拓扑。
*   **通信冗余：** 缺乏对拓扑密度的量化控制，导致不必要的智能体间通信。

#### 2. 方法论：AgentConductor 架构
AgentConductor 的核心是一个经过强化学习优化的**编排智能体（Orchestrator Agent）**，其核心思想是“按需构建拓扑”：
*   **三阶段训练流程：**
    1.  **SFT（监督微调）：** 使用 GPT-4o 生成的 4500 条高质量拓扑数据，赋予编排智能体关于交互图结构的先验知识。
    2.  **RL（强化学习）：** 采用 **GRPO（组相对策略优化）** 算法。通过与代码执行环境交互，根据代码正确性、拓扑合法性和拓扑密度给予奖励，训练智能体生成最优拓扑。
    3.  **动态推理：** 在推理阶段，编排智能体先推断任务难度，生成初始拓扑；若执行失败，则结合错误日志演化出新的拓扑进行重试。
*   **关键技术细节：**
    *   **层级 DAG 拓扑：** 支持层内并行和跨层连接，通过人类可读的 **YAML** 格式表示。
    *   **拓扑密度函数 ($S_{complex}$)：** 综合考虑节点数（成本）、边密度（通信开销）和图深度（并行度），并根据任务难度（入门、面试、竞赛）设置不同的密度上限。
    *   **多目标奖励函数：** 结合了 YAML 格式正确性、代码执行结果（Pass/Fail/Error 类型）以及难度感知的密度惩罚。

#### 3. 实验设计
*   **数据集：**
    *   **竞赛级：** APPS、LiveCodeBench (V4)、CodeContests。
    *   **基础级：** HumanEval、MBPP。
*   **基准测试（Benchmark）：** 对比了 4 类共 11 种方法：
    *   **Vanilla：** GPT-4o-mini 单模型。
    *   **经典 MAS：** AutoGen、MetaGPT、MapCoder。
    *   **工作流优化：** AFlow、FlowReasoner、Chain-of-Agents。
    *   **拓扑优化：** GPTSwarm、AgentPrune、MacNet、G-Designer。

#### 4. 资源与算力
*   **模型骨干：** 使用 **Qwen2.5-3B-Instruct** 作为编排智能体的基础模型。
*   **算力资源：** 实验在拥有 **4 张 NVIDIA A800 GPU** 的集群上完成。
*   **训练细节：** SFT 阶段学习率为 $1 \times 10^{-4}$；RL 阶段使用 Verl 框架，组大小 $G=8$，学习率为 $1 \times 10^{-6}$。

#### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 5 个主流数据集，进行了多次重复实验（均值±标准差）。
*   **消融实验：** 详细验证了 SFT、RL 以及奖励函数中各个组件（如节点奖励、边奖励、深度奖励）对最终性能的影响。
*   **跨领域验证：** 在 GAIA、HLE 和 PopQA 等非代码数据集上进行了零样本（Zero-shot）和微调迁移实验，验证了编排智能体的泛化能力。
*   **评价：** 实验设计全面，对比基线涵盖了从经典框架到最新的 RL 驱动方法，对比维度包括准确率、Token 成本和拓扑密度，具有较高的客观性和说服力。

#### 6. 主要结论与发现
*   **性能领先：** AgentConductor 在所有数据集上均达到 SOTA。在 APPS 上 pass@1 达到 58.8%，比最强基准高出 14.6%。
*   **高性价比：** 在提升准确率的同时，Token 成本降低了约 68%，拓扑密度降低了 13%。
*   **难度自适应：** 编排智能体成功学会了为简单任务分配稀疏拓扑，为困难任务分配稠密拓扑，实现了精度与成本的平衡。
*   **反馈驱动有效：** 多轮演化机制允许系统在遇到 Runtime Error 等错误时，通过调整拓扑（如增加 Debugger 角色）来修复问题。

#### 7. 优点与亮点
*   **创新的拓扑表示：** 使用 YAML 使得复杂的图结构变得可生成且易于人类理解。
*   **端到端优化：** 不同于以往的剪枝方法，AgentConductor 实现了从任务理解到拓扑生成的全流程 RL 优化。
*   **细粒度控制：** 引入数学化的拓扑密度函数，并与任务难度挂钩，解决了 MAS 中常见的“通信爆炸”问题。

#### 8. 不足与局限
*   **模型依赖：** 编排智能体的性能高度依赖于基础模型（如 Qwen-3B）的指令遵循能力，极小规模模型可能难以处理复杂的 YAML 逻辑。
*   **推理延迟：** 虽然 Token 成本降低了，但多轮演化和多智能体协作本身会带来比单模型更高的端到端响应延迟。
*   **环境依赖：** 系统的核心优势在于利用执行反馈，对于缺乏自动评测环境（Sandbox）的非代码任务，其演化效率可能会下降。

（完）
