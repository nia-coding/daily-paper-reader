---
title: "RLAR: An Agentic Reward System for Multi-task Reinforcement Learning on Large Language Models"
title_zh: RLAR：一种用于大语言模型多任务强化学习的智能体化奖励系统
authors: "Andrew Zhuoer Feng, Cunxiang Wang, Bosi Wen, Yidong Wang, Yu Luo, Hongning Wang, Minlie Huang"
date: 2026-02-28
pdf: "https://arxiv.org/pdf/2603.00724v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 通过工具合成和代码生成实现自我演进奖励系统的智能体驱动框架
tldr: 针对大语言模型强化学习中静态奖励模型成本高且泛化性差的问题，本文提出RLAR框架。该框架将奖励获取转化为动态工具合成任务，利用智能体自主从互联网检索最优奖励模型并生成代码验证器。RLAR能随训练数据分布变化而自我演进，在数学、编程等多种任务上显著提升了模型性能，为多任务RL对齐提供了高效的动态奖励方案。
selection_source: fresh_fetch
motivation: 传统的静态奖励模型在强化学习迭代过程中难以应对分布外数据，且针对特定领域的训练成本极高。
method: 提出一种智能体驱动的框架，通过动态检索互联网奖励模型和合成程序化验证器，为每个查询定制奖励函数。
result: 在数学、代码、翻译和对话任务中实现了10到60分的性能提升，并在RewardBench-V2上显著优于静态基准模型。
conclusion: RLAR证明了通过动态奖励编排可以有效解决奖励模型的泛化难题，显著增强了大语言模型在多任务场景下的对齐效果。
---

## 摘要
通过强化学习进行的大语言模型对齐关键取决于奖励函数的质量。然而，静态且特定领域的奖励模型通常训练成本高昂，并且在强化学习迭代过程中遇到的分布外场景中表现出较差的泛化能力。我们提出了 RLAR（Reinforcement Learning from Agent Rewards），这是一个由智能体驱动的框架，能够为每个查询动态分配定制化的奖励函数。具体而言，RLAR 将奖励获取转化为动态的工具合成与调用任务。它利用大语言模型智能体从互联网上自主检索最优奖励模型，并通过代码生成合成程序化验证器。这使得奖励系统能够随着训练过程中不断变化的数据分布而自我演进。实验结果表明，RLAR 在数学、编程、翻译和对话任务中带来了 10 到 60 不等的持续性能提升。在 RewardBench-V2 上，RLAR 显著优于静态基准模型，并接近性能上限，通过动态奖励编排展示了卓越的泛化能力。数据和代码可在以下链接获取：https://github.com/ZhuoerFeng/RLAR。

## Abstract
Large language model alignment via reinforcement learning depends critically on reward function quality. However, static, domain-specific reward models are often costly to train and exhibit poor generalization in out-of-distribution scenarios encountered during RL iterations. We present RLAR (Reinforcement Learning from Agent Rewards), an agent-driven framework that dynamically assigns tailored reward functions to individual queries. Specifically, RLAR transforms reward acquisition into a dynamic tool synthesis and invocation task. It leverages LLM agents to autonomously retrieve optimal reward models from the Internet and synthesize programmatic verifiers through code generation. This allows the reward system to self-evolve with the shifting data distributions during training. Experimental results demonstrate that RLAR yields consistent performance gains ranging from 10 to 60 across mathematics, coding, translation, and dialogue tasks. On RewardBench-V2, RLAR significantly outperforms static baselines and approaches the performance upper bound, demonstrating superior generalization through dynamic reward orchestration. The data and code are available on this link: https://github.com/ZhuoerFeng/RLAR.

---

## 论文详细总结（自动生成）

### RLAR：一种用于大语言模型多任务强化学习的智能体化奖励系统

#### 1. 论文的核心问题与整体含义
在通过强化学习（RL）对齐大语言模型（LLM）的过程中，**奖励函数（Reward Function）的质量**是决定模型性能天花板的关键。然而，传统的静态奖励模型面临三大挑战：
*   **泛化性差**：静态模型在面对训练分布之外（OOD）的数据时表现不佳。
*   **成本高昂**：针对特定领域标注数据并训练专门的奖励模型耗时耗力。
*   **奖励作弊（Reward Hacking）**：模型容易利用奖励模型的漏洞（如冗长偏见、格式偏见）来获取高分，而非真正解决问题。

**RLAR** 的核心动机是：将奖励获取从“静态评分”转变为“**动态工具合成与调用**”，利用 LLM 智能体的能力，为每一个查询（Query）实时定制最合适的奖励机制。

#### 2. 论文提出的方法论
RLAR 提出了一个自演进的智能体框架，其核心流程如下：
*   **策略路由（Policy Router）**：对于输入的查询，智能体首先判断现有工具库（Library）是否能准确评估。若能，则选择现有工具；若不能，则触发“合成”动作。
*   **智能体化工具合成（Agentic Tool Synthesis）**：
    *   **WrapLLM**：针对需要语义理解的任务（如对话、翻译），智能体在互联网（如 HuggingFace）上检索最相关的轻量级奖励模型，自动编写部署代码并封装为 API 工具。
    *   **CodeVerify**：针对客观性强的任务（如数学、编程），智能体通过“计划-执行”策略生成 Python 脚本，作为确定性的程序化验证器（如执行单元测试或数值匹配）。
*   **验证与入库**：新合成的工具需通过 `EvalTool` 模块的验证（检查代码可执行性或文档一致性）后，才会被加入工具库。
*   **强化学习优化**：采用 **GRPO（群组相对策略优化）** 算法，利用动态分配的奖励信号更新策略模型。

#### 3. 实验设计
*   **数据集与场景**：
    *   **数学推理**：GSM8K, Hendrycks-MATH。
    *   **代码生成**：LeetCode-Dataset。
    *   **翻译**：FLORES-200（涵盖中、德、英、法、日）。
    *   **通用对话**：UltraChat。
    *   **OOD 泛化评估**：AIME-24/25, MBPP, WMT-24。
*   **基准测试（Benchmark）**：使用 RewardBench-V2 评估奖励路由的准确性。
*   **对比方法**：
    *   **分类奖励模型**：Skywork-Reward-V2 (Llama/Qwen), SeedX。
    *   **生成式奖励模型**：Skywork-Critic。
    *   **AI 反馈（RLAIF）**：GPT-5-as-a-judge。
    *   **人类启发式基准**：基于规则的手写奖励函数。

#### 4. 资源与算力
*   **硬件环境**：
    *   **训练集群**：8 × NVIDIA H100 (80GB) GPU，用于策略模型训练。
    *   **辅助集群**：8 × NVIDIA A100 (80GB) GPU，用于奖励模型的部署与推理。
*   **训练细节**：基于 VERL 框架，进行 80 步 GRPO 迭代。
*   **效率对比**：相比于 GPT-5-as-a-judge，RLAR 减少了约 **80% 的 API Token 消耗**和 **75% 的 GPU 训练时长**。

#### 5. 实验数量与充分性
论文进行了多维度的实验：
*   **主实验**：在 Llama-3.1-8B 和 Qwen3-8B 两个底座上验证了多任务性能。
*   **泛化性实验**：在多个未见过的 OOD 数据集上进行了测试。
*   **消融实验**：分别移除了 CodeVerify、WrapLLM 和 Selector，验证了各模块的必要性。
*   **奖励路由分析**：在 RewardBench-V2 上对比了不同底座（GPT-4o, GLM-4.6, GPT-5.1）的路由准确率。
*   **充分性评价**：实验设计涵盖了从性能提升到成本控制、再到抗作弊能力的全面评估，对比基准包含了目前开源界最强的奖励模型，实验结果具有较高的客观性和说服力。

#### 6. 主要结论与发现
*   **性能显著提升**：RLAR 在数学、代码等任务上相比静态基准提升了 10% 至 60%。
*   **解决奖励作弊**：相比于分类奖励模型，RLAR 训练出的模型在格式和冗长性上表现更稳健，有效避免了“格式作弊”。
*   **路由准确性**：利用 GPT-5.1 作为路由器的 RLAR 在 RewardBench-V2 上达到了 **90.44%** 的准确率，超越了所有单一模型和集成方法。
*   **成本效益**：通过调用轻量级开源模型和代码验证器，大幅降低了对昂贵商业模型 API 的依赖。

#### 7. 优点
*   **动态演进**：奖励系统不再是静态的，而是能随训练数据分布的变化而自动扩展工具库。
*   **异构奖励融合**：成功将“神经网络评分”与“程序化硬约束”结合，兼顾了语义理解和逻辑严谨性。
*   **高泛化能力**：通过动态路由，模型能更好地适应 OOD 场景，避免了单一奖励模型在未知领域的失效。

#### 8. 不足与局限
*   **推理延迟**：动态检索和部署奖励模型可能会引入额外的训练前置时间。
*   **依赖外部质量**：WrapLLM 模块依赖于开源社区（如 HuggingFace）的模型文档质量，若 README 存在误导，可能导致工具封装失败。
*   **模态限制**：目前仅验证了文本任务，尚未扩展到多模态（如图像、音频）领域。
*   **复杂性**：系统架构比单一奖励模型复杂，对智能体编排能力有较高要求。

（完）
