---
title: Rollout-Training Co-Design for Efficient LLM-Based Multi-Agent Reinforcement Learning
title_zh: 基于大语言模型的高效多智能体强化学习的采样-训练协同设计
authors: "Zhida Jiang, Zhaolong Xing, Jiawei Lu, Yipei Niu, Qingyuan Sang, Liangxu Zhang, Wenquan Dai, Junhua Shu, Jiaxing Wang, Qiangyu Pei, Qiong Chen, Xinyu Liu, Fangming Liu, Ai Han, Zhen Chen, Ke Zhang"
date: 2026-02-10
pdf: "https://arxiv.org/pdf/2602.09578v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 大规模基于大语言模型的多智能体强化学习训练框架
tldr: 针对大规模基于大语言模型的多智能体强化学习（MARL）训练中存在的同步瓶颈、负载不均衡及资源利用率低等系统级挑战，本文提出了首个端到端优化框架 FlexMARL。该框架通过解耦采样与训练架构、引入微批次驱动的异步流水线以及智能资源调度，显著提升了训练效率和硬件利用率，为大规模 MARL 提供了高效的基础设施支持。
selection_source: fresh_fetch
motivation: 现有的强化学习框架主要针对单智能体优化，难以应对大规模多智能体场景下的同步延迟和资源分配不均等系统性难题。
method: 提出了 FlexMARL 框架，采用采样-训练解耦架构，结合微批次异步流水线、层级负载均衡及以智能体为中心的资源动态分配技术。
result: 在大规模生产集群上的实验表明，FlexMARL 相比现有框架实现了高达 7.3 倍的加速，并将硬件利用率提升了 5.6 倍。
conclusion: FlexMARL 通过软硬件协同设计，有效解决了大规模 LLM-MARL 的系统瓶颈，是实现高效多智能体训练的关键基础设施。
---

## 摘要
尽管多智能体强化学习（MARL）在算法层面取得了创新，但用于大规模 MARL 训练的底层网络基础设施仍未得到充分探索。现有的训练框架主要针对单智能体场景进行优化，未能解决 MARL 特有的系统级挑战，包括采样-训练同步屏障、采样负载不均衡以及训练资源利用率低。为了填补这一空白，我们提出了 FlexMARL，这是首个针对基于大语言模型的大规模 MARL 全面优化采样、训练及其编排的端到端训练框架。具体而言，FlexMARL 引入了联合编排器，在采样-训练解耦架构下管理数据流。基于经验存储，一种新型的微批次驱动异步流水线消除了同步屏障，同时提供了强一致性保证。采样引擎采用并行采样方案并结合分层负载均衡，能够适应智能体间及智能体内部倾斜的请求模式。训练引擎通过以智能体为中心的资源分配实现按需硬件绑定。不同智能体的训练状态通过统一且位置无关的通信进行交换。在大规模生产集群上的实验结果表明，与现有框架相比，FlexMARL 实现了高达 7.3 倍的加速，并将硬件利用率提高了 5.6 倍。

## Abstract
Despite algorithm-level innovations for multi-agent reinforcement learning (MARL), the underlying networked infrastructure for large-scale MARL training remains underexplored. Existing training frameworks primarily optimize for single-agent scenarios and fail to address the unique system-level challenges of MARL, including rollout-training synchronization barriers, rollout load imbalance, and training resource underutilization. To bridge this gap, we propose FlexMARL, the first end-to-end training framework that holistically optimizes rollout, training, and their orchestration for large-scale LLM-based MARL. Specifically, FlexMARL introduces the joint orchestrator to manage data flow under the rollout-training disaggregated architecture. Building upon the experience store, a novel micro-batch driven asynchronous pipeline eliminates the synchronization barriers while providing strong consistency guarantees. Rollout engine adopts a parallel sampling scheme combined with hierarchical load balancing, which adapts to skewed inter/intra-agent request patterns. Training engine achieves on-demand hardware binding through agent-centric resource allocation. The training states of different agents are swapped via unified and location-agnostic communication. Empirical results on a large-scale production cluster demonstrate that FlexMARL achieves up to 7.3x speedup and improves hardware utilization by up to 5.6x compared to existing frameworks.

---

## 论文详细总结（自动生成）

这篇论文提出了 **FlexMARL**，这是首个针对大规模基于大语言模型（LLM）的多智能体强化学习（MARL）设计的端到端训练框架。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的强化学习（RL）框架（如 OpenRLHF, veRL）主要针对单智能体优化，无法应对大规模 MARL 带来的独特系统级挑战。
*   **研究动机**：
    1.  **同步瓶颈**：多智能体交互存在严重的“长尾效应”，训练必须等待最慢的采样完成。
    2.  **采样负载不均**：不同角色的智能体（如核心智能体与辅助智能体）请求量差异巨大，导致部分资源排队而部分资源闲置。
    3.  **资源利用率低**：静态资源分配无法适应训练过程中智能体激活状态的动态变化。

### 2. 方法论：核心思想与关键技术
FlexMARL 通过**采样-训练协同设计**，提出了三个核心组件：
*   **联合编排器 (Joint Orchestrator)**：
    *   **解耦架构**：将采样和训练资源池物理分离，消除资源竞争。
    *   **经验存储 (Experience Store)**：采用多表结构管理不同智能体的数据，支持类型感知存储。
    *   **微批次异步流水线**：将全局批次拆分为微批次，在采样进行时即触发梯度计算，通过解耦梯度计算与参数更新，在消除长尾延迟的同时保持同步训练的数学等效性。
*   **采样引擎 (Rollout Engine)**：
    *   **并行采样**：支持查询间（Inter-query）和查询内（Intra-query）的并行生成。
    *   **层级负载均衡**：实时监控各智能体请求队列，通过 D2D 通信动态迁移推理实例，平衡智能体间和智能体内的负载。
*   **训练引擎 (Training Engine)**：
    *   **以智能体为中心的资源分配**：利用进程组（Process Group）抽象，仅在智能体需要更新时动态绑定硬件资源。
    *   **状态交换机制**：设计了统一的 `Set/Get API`，实现训练状态（权重、优化器状态）在显存与主机内存间的高效换入换出。

### 3. 实验设计
*   **数据集/场景**：使用了两个真实的工业级电商数据集：**商家助手 (MA)** 和 **类目助手 (CA)**，模拟复杂的多智能体协作任务。
*   **Benchmark (对比方法)**：
    *   **MAS-RL**：将单智能体 RL 框架直接迁移至多智能体场景的朴素实现。
    *   **DistRL**：采用解耦架构但保持同步流水线的框架。
    *   **MARTI (ICLR 2026)**：当时最先进的 MARL 框架，支持异步采样。
*   **模型规模**：使用了 Qwen2.5-14B 和 Qwen2.5-32B 作为骨干模型。

### 4. 资源与算力
*   **硬件环境**：在一个拥有 **48 个节点** 的生产集群上进行。
*   **算力细节**：每个节点配备 **16 张商用 NPU**（64GB 显存），节点间通过 HCCS 互联。
*   **训练配置**：使用了 DeepSpeed ZeRO-3 优化，GRPO 算法，学习率 1e-6，全局 Batch Size 为 64。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的评估，包括端到端性能对比、采样负载分布分析、硬件利用率监测、状态交换开销测试、消融实验以及大规模扩展性测试（最高支持 15 个智能体协作）。
*   **充分性与公平性**：实验覆盖了从系统吞吐量到底层硬件指标的全面对比，所有框架在相同超参数下运行，并使用了固定随机种子以保证可重复性。消融实验清晰地展示了负载均衡和异步流水线各自的贡献，实验设计较为客观、充分。

### 6. 主要结论与发现
*   **性能提升**：FlexMARL 在 MA 和 CA 数据集上分别实现了 **7.3 倍** 和 **5.6 倍** 的端到端加速。
*   **吞吐量**：相比 MARTI，FlexMARL 的吞吐量提升了约 1.3-1.4 倍。
*   **资源利用率**：硬件利用率从基准框架的极低水平提升至平均 **32.4%**（MA）和 **19.8%**（CA），有效解决了资源闲置问题。
*   **扩展性**：成功支持了跨节点的复杂异构配置（如 3 个 32B 模型 + 7 个 14B 模型同时训练），而传统框架在此配置下常因 OOM 崩溃。

### 7. 优点与亮点
*   **系统级协同**：不只是简单的算法改进，而是从底层基础设施层面解决了 MARL 的通信和调度瓶颈。
*   **微批次流水线**：巧妙地平衡了“异步带来的效率”与“同步带来的模型一致性”。
*   **工业级验证**：在 48 节点的大规模 NPU 集群和真实电商业务数据上验证，具有极强的实用价值。
*   **弹性调度**：层级负载均衡和状态交换机制使得框架能够灵活应对极度倾斜的工作负载。

### 8. 不足与局限
*   **硬件依赖性**：实验主要基于特定的商用 NPU 集群，虽然提出了通用 API，但在不同硬件架构（如 NVIDIA GPU）上的移植表现未详细展示。
*   **数据隐私限制**：由于商业机密，电商数据集的具体细节（如 Prompt 内容、具体任务逻辑）被隐藏，外部研究者难以完全复现相同的数据分布。
*   **复杂度**：引入了复杂的进程管理和状态交换逻辑，对底层驱动和通信栈的稳定性要求较高。

（完）
