---
title: "AgentSkiller: Scaling Generalist Agent Intelligence through Semantically Integrated Cross-Domain Data Synthesis"
title_zh: AgentSkiller：通过语义集成的跨域数据合成扩展通用智能体能力
authors: "Zexu Sun, Bokai Ji, Hengyi Cai, Shuaiqiang Wang, Lei Wang, Guangxia Li, Xu Chen"
date: 2026-02-10
pdf: "https://arxiv.org/pdf/2602.09372v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 跨领域数据合成用于扩展通用智能体能力
tldr: AgentSkiller是一个全自动框架，旨在通过合成语义关联的跨领域多轮交互数据，解决通用智能体高质量长程数据稀缺的问题。该框架利用DAG架构、领域本体和实体图构建一致的环境，并通过跨领域融合机制模拟复杂任务。实验表明，利用该框架合成的1.1万条数据训练的模型在函数调用能力上显著优于基线，尤其在大参数模型上表现突出。
selection_source: fresh_fetch
motivation: 现有的智能体训练数据存在隐私受限、多样性不足及长程高质量数据稀缺等瓶颈，限制了通用智能体能力的扩展。
method: 提出AgentSkiller框架，通过构建领域本体、实体图及服务蓝图，结合跨领域融合机制和基于角色的模拟器，自动化生成可验证的多轮交互数据。
result: 成功合成约1.1万个高质量交互样本，实验证明该数据集能显著提升模型在函数调用任务中的表现。
conclusion: 语义集成的跨领域数据合成是提升通用智能体智能的有效途径，为大规模扩展智能体能力提供了可靠的数据生产管线。
---

## 摘要
大语言模型（LLM）智能体在通过工具解决现实世界问题方面展现出巨大潜力，但通用智能的发展受限于高质量、长程（long-horizon）数据的匮乏。现有方法通常收集受隐私限制的 API 日志，或生成缺乏多样性的脚本化交互，难以产生扩展能力所需的数据。我们提出了 AgentSkiller，这是一个全自动框架，用于在真实的、语义关联的领域中合成多轮交互数据。该框架采用基于有向无环图（DAG）的架构，具有显式的状态转换，以确保确定性和可恢复性。该流水线构建了领域本体和以人为中心的实体图（Person-Centric Entity Graph），通过针对模型上下文协议（Model Context Protocol）服务器的服务蓝图（Service Blueprints）定义工具接口，并使用一致的数据库和严格的领域策略（Domain Policies）填充环境。一种跨域融合机制将各项服务链接起来以模拟复杂任务。最后，该流水线通过验证解决方案路径、进行基于执行的验证过滤，并使用基于角色（Persona）的模拟器生成查询以实现自动化部署，从而创建用户任务。这产生了具有清晰状态变化的可靠环境。为了证明其有效性，我们合成了约 1.1 万个交互样本；实验结果表明，在该数据集上训练的模型在函数调用（function calling）方面比基准模型有显著提升，特别是在参数规模较大的情况下。

## Abstract
Large Language Model agents demonstrate potential in solving real-world problems via tools, yet generalist intelligence is bottlenecked by scarce high-quality, long-horizon data. Existing methods collect privacy-constrained API logs or generate scripted interactions lacking diversity, which struggle to produce data requisite for scaling capabilities. We propose AgentSkiller, a fully automated framework synthesizing multi-turn interaction data across realistic, semantically linked domains. It employs a DAG-based architecture with explicit state transitions to ensure determinism and recoverability. The pipeline builds a domain ontology and Person-Centric Entity Graph, defines tool interfaces via Service Blueprints for Model Context Protocol servers, and populates environments with consistent databases and strict Domain Policies. A cross-domain fusion mechanism links services to simulate complex tasks. Finally, the pipeline creates user tasks by verifying solution paths, filtering via execution-based validation, and generating queries using a Persona-based Simulator for automated rollout. This produces reliable environments with clear state changes. To demonstrate effectiveness, we synthesized $\approx$ 11K interaction samples; experimental results indicate that models trained on this dataset achieve significant improvements on function calling over baselines, particularly in larger parameter regimes.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **AgentSkiller** 的全自动框架，旨在通过合成高质量、跨领域的交互数据来提升通用智能体（Generalist Agent）的性能。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **核心瓶颈**：通用智能体的发展受限于高质量、长程（Long-horizon）训练数据的稀缺。
*   **现有方法缺陷**：
    1.  **真实API日志**：受限于隐私保护且覆盖范围有限。
    2.  **脚本化合成数据**：缺乏对话多样性，且往往需要大量人工验证，难以规模化。
*   **研究目标**：构建一个全自动、可扩展的框架，生成具有语义关联、逻辑一致且可执行的跨领域多轮交互数据，以突破数据规模和质量的瓶颈。

### 2. 方法论：AgentSkiller 框架
AgentSkiller 采用基于 **DAG（有向无环图）** 的状态机架构，确保生成过程的确定性和可恢复性。其核心流程分为五个阶段：
*   **阶段 1：领域本体与拓扑构建**：从种子话题扩展出 300 个领域，构建“以人为中心”的实体图（Person-Centric Entity Graph），模拟现实中人与系统资源的交互。
*   **阶段 2：服务规范定义**：将抽象实体转化为服务蓝图（Service Blueprints），对接 MCP（Model Context Protocol）标准，定义工具接口和数据模式。
*   **阶段 3：语义落地与实现**：利用 Coding LLM 生成符合约束的数据库（使用 Faker 等工具）和领域策略（Domain Policy），并实现 Python 编写的 MCP 服务器。
*   **阶段 4：跨领域融合**：通过共享实体将不同领域的服务链接（如医疗+保险），进行策略协调（Policy Harmonization），模拟复杂的跨系统任务。
*   **阶段 5：任务合成与实例化**：
    *   **轨迹枚举**：在工具图中搜索有效路径。
    *   **执行过滤**：在沙盒中运行生成的代码，剔除逻辑不通或运行报错的样本。
    *   **角色模拟器**：基于 Persona 生成自然语言查询，并故意引入信息不对称（要求智能体主动询问缺失信息）。
*   **关键技术**：**双模型架构**（推理用 Textual LLM，写代码用 Coding LLM）、**测试驱动的自我修正**（基于错误回溯自动修复代码）。

### 3. 实验设计
*   **数据集**：合成了约 **1.1万条** 高质量交互轨迹（5,941 条单领域，5,581 条跨领域）。
*   **评估基准（Benchmarks）**：
    *   **$\tau$-Bench**：评估真实领域工具使用。
    *   **$\tau^2$-Bench**：评估双控制环境下的对话智能体。
    *   **ACEBench-Agent**：评估函数调用能力。
*   **对比方法**：
    *   **闭源模型**：GPT-5-think, GPT-o3, Claude-Sonnet-4, Gemini-2.5-pro 等。
    *   **开源模型/基线**：DeepSeek-V3.1, xLAM-2, AgentScaler, ToolMind, EnvScaler, MUA 等。
*   **训练设置**：对 Qwen3-4B/8B/14B 进行全参数微调。

### 4. 资源与算力
*   **训练框架**：使用了 `verl` 强化学习/微调框架。
*   **超参数**：序列长度 128k，学习率 1e-5，Warmup 比例 0.1，Cosine 调度器。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号（如 A100/H100）及数量，也未提及具体的训练总时长。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了从 4B 到 14B 不同参数规模的模型。
    *   进行了单领域数据 vs. 全量数据（含跨领域）的对比实验。
    *   针对不同任务复杂度（工具调用次数）进行了性能衰减分析。
*   **充分性评价**：实验设计较为充分。通过在三个主流 Benchmark 上与数十个最先进模型对比，证明了其数据的有效性。特别是针对 $\tau^2$-Bench 的跨领域分析，揭示了数据多样性对长程任务的关键作用。

### 6. 主要结论与发现
*   **小模型逆袭**：经过 AgentSkiller 数据微调的 **14B 模型** 在 $\tau^2$-Bench 上达到了 **79.1%** 的得分，超越了 GPT-o3 (68.4%) 和许多参数量大得多的模型（如 70B 或 235B 级别）。
*   **跨领域数据的必要性**：仅用单领域数据训练的模型在复杂任务上表现受限，加入跨领域合成数据后，性能在 $\tau$-Bench 上从 45.4% 跃升至 67.2%。
*   **质量胜过规模**：实验证明，通用智能体的能力提升更多取决于训练数据的逻辑深度和结构化连接，而非单纯的参数堆叠。

### 7. 优点（亮点）
*   **全自动闭环**：从想法到环境构建再到任务生成，全程无需人工干预，且具备自我修复能力。
*   **高保真度**：通过“执行验证”确保每一条合成数据在逻辑上是可通的，解决了合成数据中常见的“幻觉轨迹”问题。
*   **语义集成**：不同于随机组合工具，该框架强调领域间的语义关联，生成的跨领域任务更符合现实逻辑。

### 8. 不足与局限
*   **教师模型依赖**：生成过程高度依赖高性能模型（如 GPT-5.2 或 Claude Opus），合成成本虽然比人工低，但仍有一定门槛。
*   **长程衰减**：实验显示，当工具调用超过 14-20 步时，模型准确率仍会出现显著下降，表明极长程任务仍是挑战。
*   **领域偏差**：虽然扩展了 300 个领域，但数据分布仍集中在物流、医疗、金融等特定行业，对更广泛的开放域任务覆盖可能存在偏差。

（完）
