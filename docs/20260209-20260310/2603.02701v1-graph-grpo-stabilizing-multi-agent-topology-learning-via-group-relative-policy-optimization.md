---
title: "Graph-GRPO: Stabilizing Multi-Agent Topology Learning via Group Relative Policy Optimization"
title_zh: Graph-GRPO：通过组相对策略优化稳定多智能体拓扑学习
authors: "Yueyang Cang, Xiaoteng Zhang, Erlu Zhao, Zehua Ji, Yuhang Liu, Yuchen He, Zhiyuan Ning, Chen Yijun, Wenge Que, Li Shi"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.02701v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 优化多智能体系统中的通信拓扑
tldr: 本研究针对大语言模型多智能体系统中的通信拓扑优化问题，提出了 Graph-GRPO 框架。针对传统强化学习方法在拓扑学习中面临的高梯度方差和信用分配难题，该方法通过引入组相对策略优化（GRPO），对每个查询采样多组拓扑并计算相对优势，从而降低奖励噪声并增强训练稳定性。实验表明，该方法在推理和代码生成任务上显著优于现有基准，能有效识别关键的智能体协作路径。
selection_source: fresh_fetch
motivation: 现有的多智能体拓扑学习方法依赖单样本绝对奖励，导致在面对不同难度的任务时存在严重的梯度方差和信用分配困难。
method: 提出 Graph-GRPO 框架，通过为每个任务采样一组多样化的通信图，并利用组内相对表现归一化奖励来优化边缘权重。
result: 在推理和代码生成基准测试中，Graph-GRPO 显著提升了训练稳定性，并超越了现有的最先进拓扑优化基准方法。
conclusion: Graph-GRPO 通过组相对评价机制有效解决了多智能体协作中的奖励噪声问题，为构建高效通信拓扑提供了稳定的优化路径。
---

## 摘要
优化通信拓扑对于基于大语言模型（LLM）的多智能体系统（MAS）的效率和有效性至关重要。虽然最近的方法利用强化学习来动态构建特定任务的图，但它们通常依赖于具有绝对奖励（如二元正确性）的单样本策略梯度。这种范式面临严重的梯度方差和信用分配问题：简单的查询会为次优结构产生无信息的正向奖励，而困难的查询通常会导致失败，从而无法提供学习信号。为了应对这些挑战，我们提出了 Graph-GRPO，这是一个集成组相对策略优化（Group Relative Policy Optimization）的新型拓扑优化框架。Graph-GRPO 不是孤立地评估单个拓扑，而是为每个查询采样一组多样的通信图，并根据它们在组内的相对表现来计算特定边的优势。通过在采样组内对奖励进行归一化，我们的方法有效地减轻了源自任务难度差异的噪声，并实现了细粒度的信用分配。在推理和代码生成基准测试上的广泛实验表明，Graph-GRPO 显著优于最先进的基准方法，实现了卓越的训练稳定性，并识别出了此前被奖励噪声掩盖的关键通信路径。

## Abstract
Optimizing communication topology is fundamental to the efficiency and effectiveness of Large Language Model (LLM)-based Multi-Agent Systems (MAS). While recent approaches utilize reinforcement learning to dynamically construct task-specific graphs, they typically rely on single-sample policy gradients with absolute rewards (e.g., binary correctness). This paradigm suffers from severe gradient variance and the credit assignment problem: simple queries yield non-informative positive rewards for suboptimal structures, while difficult queries often result in failures that provide no learning signal. To address these challenges, we propose Graph-GRPO, a novel topology optimization framework that integrates Group Relative Policy Optimization. Instead of evaluating a single topology in isolation, Graph-GRPO samples a group of diverse communication graphs for each query and computes the advantage of specific edges based on their relative performance within the group. By normalizing rewards across the sampled group, our method effectively mitigates the noise derived from task difficulty variance and enables fine-grained credit assignment. Extensive experiments on reasoning and code generation benchmarks demonstrate that Graph-GRPO significantly outperforms state-of-the-art baselines, achieving superior training stability and identifying critical communication pathways previously obscured by reward noise.

---

## 论文详细总结（自动生成）

这篇论文提出了 **Graph-GRPO**，旨在通过引入组相对策略优化（GRPO）机制，解决大语言模型多智能体系统（MAS）中通信拓扑学习的不稳定性和信用分配难题。

以下是对该论文的结构化总结：

### 1. 研究动机与核心问题
*   **核心问题**：在多智能体协作中，通信拓扑（谁和谁说话）决定了效率。现有方法多采用强化学习（如 REINFORCE）来动态生成拓扑，但面临两大瓶颈：
    1.  **高梯度方差**：任务难度不一。简单任务下，即使是冗余或错误的拓扑也能得到正向奖励（Reward=1）；困难任务下，即使拓扑合理也可能因模型能力限制而失败（Reward=0），导致学习信号充满噪声。
    2.  **信用分配难题**：绝对奖励通常施加于整个图。当系统成功时，所有边（包括无用的边）都会被强化，无法识别真正起关键作用的通信路径。
*   **研究背景**：从静态拓扑（链式、全连接）向动态、任务自适应拓扑演进，但优化算法仍停留在粗粒度的单样本阶段。

### 2. 方法论
*   **核心思想**：借鉴 DeepSeekMath 提出的 GRPO 算法，将“绝对奖励”转变为“组内相对优势”。通过对同一个查询采样一组不同的拓扑，利用组内表现的平均值作为基准，从而抵消任务难度带来的偏差。
*   **关键技术细节**：
    *   **策略网络架构**：使用图注意力网络（GAT）作为骨干，结合节点编码器（MiniLM）处理智能体角色和查询。
    *   **DAG 约束**：在生成拓扑概率矩阵时引入有向无环图（DAG）掩码，确保信息流向的逻辑性（从前序智能体流向后续智能体）。
    *   **组采样（Group Sampling）**：针对每个查询采样 $K$ 个（实验中 $K=16$）不同的拓扑结构。
    *   **边缘级信用分配**：计算每条边 $e_{ij}$ 的“边特定得分” $S_{ij}$（即包含该边的拓扑在组内的条件成功率）。
    *   **优势计算与优化**：对 $S_{ij}$ 进行组内归一化处理得到优势值 $A_{ij}$。只有表现优于组平均水平的边才会被正向强化。该方法无需 Critic 网络，降低了显存开销。

### 3. 实验设计
*   **数据集/场景**：涵盖三大领域共 6 个基准测试：
    *   **通用推理**：MMLU。
    *   **数学推理**：GSM8K、MultiArith、SVAMP、AQuA。
    *   **代码生成**：HumanEval。
*   **对比方法（Baselines）**：
    *   **单智能体**：Vanilla、CoT、Self-Consistency。
    *   **固定拓扑**：Chain、Tree、Complete Graph、LLM-Debate。
    *   **拓扑优化 SOTA**：AgentPrune、AgentDropout、G-Designer、EIB-LEARNER（此前最强基准）。
*   **实验设置**：使用 GPT-3.5-Turbo 作为底层 LLM，智能体数量根据任务设为 4 到 6 个。

### 4. 资源与算力
*   **算力设备**：论文提到使用了 **NVIDIA A100 GPU** 进行训练。
*   **模型细节**：策略网络使用 `all-MiniLM-L6-v2` 编码器和 3 层 GAT。
*   **训练时长**：文中未明确给出具体的训练总时长，但提到 GRPO 机制由于省去了 Critic 网络，在内存效率和训练稳定性上优于传统的 PPO。

### 5. 实验数量与充分性
*   **实验规模**：在 6 个主流数据集上进行了全面测试，结果显示 Graph-GRPO 在所有指标上均达到 SOTA。
*   **消融实验**：对比了“边缘级（Edge-Level）”与“图级（Graph-Level）”的 GRPO 优化。结果证明，细粒度的边缘级评估对性能提升至关重要（平均提升 1.82%）。
*   **效率分析**：进行了 Token 消耗与准确率的 Pareto 前沿分析，证明该方法在保持高精度的同时，能自发收敛到稀疏拓扑，节省通信成本。
*   **评价**：实验设计较为充分，对比了从基础到最前沿的各类方法，客观证明了其在稳定性、准确率和成本控制上的优势。

### 6. 主要结论与发现
*   **性能突破**：Graph-GRPO 在 6 个任务上的平均准确率达到 92.45%，显著超过 EIB-LEARNER（91.38%）。
*   **稳定性提升**：通过组相对优化，模型能有效过滤简单任务中的“易得奖励”噪声，避免学习到冗余边。
*   **自发稀疏性**：模型不仅提高了准确率，还通过惩罚无信息边，识别出了关键的语义通信路径，实现了极高的 Token 效率。

### 7. 优点
*   **创新性地引入 GRPO**：首次将组相对优化应用于离散结构搜索（拓扑学习），解决了 MAS 中的信用分配顽疾。
*   **无需 Critic 网络**：简化了强化学习的训练流程，降低了显存占用，提高了收敛速度。
*   **细粒度评估**：边缘级的得分机制比传统的图级奖励更能精准刻画智能体间的协作价值。

### 8. 不足与局限
*   **可扩展性限制**：策略网络基于 GAT，其复杂度为 $O(N^2)$。对于超大规模智能体集群（如 $N > 100$），计算开销可能成为瓶颈。
*   **静态拓扑局限**：目前为每个查询生成一个静态拓扑。对于多轮对话中动态变化的协作需求，可能需要更细粒度的“轮次级（Turn-level）”动态调整机制。
*   **底层模型依赖**：实验主要基于 GPT-3.5，在更强（如 GPT-4）或更弱的开源模型上的泛化表现仍有待进一步验证。

（完）
