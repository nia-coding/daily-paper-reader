---
title: "AgentDropoutV2: Optimizing Information Flow in Multi-Agent Systems via Test-Time Rectify-or-Reject Pruning"
title_zh: AgentDropoutV2：通过测试时“纠正或拒绝”剪枝优化多智能体系统中的信息流
authors: "Yutong Wang, Siyuan Xiong, Xuebo Liu, Wenkang Zhou, Liang Ding, Miao Zhang, Min Zhang"
date: 2026-02-26
pdf: "https://arxiv.org/pdf/2602.23258v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 优化多智能体系统中的信息流与错误纠正
tldr: "针对多智能体系统（MAS）中错误信息级联传播的问题，本文提出AgentDropoutV2框架。该框架无需重新训练，在推理阶段通过“纠正或拒绝”剪枝机制优化信息流。它利用检索增强的纠正器识别并修复错误，对无法修复的输出进行剪枝，从而防止错误扩散。实验显示其在数学基准测试上平均提升了6.3%的准确率，展现了极强的适应性和泛化能力。"
selection_source: fresh_fetch
motivation: 旨在解决多智能体系统中单个智能体产生的错误信息会随信息流级联放大，且现有结构工程或微调方法成本高、灵活性差的问题。
method: 提出一种测试时纠正或拒绝剪枝框架，利用检索增强的纠正器和故障驱动指标池动态拦截、修复或剔除错误输出。
result: 在广泛的数学基准测试中，该方法使多智能体系统的平均任务准确率显著提升了6.3个百分点。
conclusion: AgentDropoutV2通过动态优化信息流，在不增加训练成本的前提下，显著增强了多智能体系统的鲁棒性、准确性和任务适应能力。
---

## 摘要
虽然多智能体系统（MAS）在复杂推理方面表现出色，但它们常受到单个参与者产生的错误信息所带来的级联影响。目前的解决方案通常诉诸于僵化的结构工程或昂贵的微调，限制了它们的可部署性和适应性。我们提出了 AgentDropoutV2，这是一个测试时“纠正或拒绝”剪枝框架，旨在无需重新训练即可动态优化 MAS 的信息流。我们的方法充当主动防火墙，拦截智能体输出，并采用检索增强纠正器，根据故障驱动的指标池迭代纠正错误。该机制允许利用提炼出的故障模式作为先验知识，精确识别潜在错误。随后，无法修复的输出将被剪枝以防止错误传播，同时回退策略可保持系统完整性。在广泛数学基准测试上的实证结果表明，AgentDropoutV2 显著提升了 MAS 的任务性能，在数学基准测试上实现了平均 6.3 个百分点的准确率提升。此外，该系统表现出强大的泛化能力和适应性，能够根据任务难度动态调节纠正力度，同时利用上下文感知指标解决广泛的错误模式。我们的代码和数据集已发布在 https://github.com/TonySY2/AgentDropoutV2。

## Abstract
While Multi-Agent Systems (MAS) excel in complex reasoning, they suffer from the cascading impact of erroneous information generated by individual participants. Current solutions often resort to rigid structural engineering or expensive fine-tuning, limiting their deployability and adaptability. We propose AgentDropoutV2, a test-time rectify-or-reject pruning framework designed to dynamically optimize MAS information flow without retraining. Our approach acts as an active firewall, intercepting agent outputs and employing a retrieval-augmented rectifier to iteratively correct errors based on a failure-driven indicator pool. This mechanism allows for the precise identification of potential errors using distilled failure patterns as prior knowledge. Irreparable outputs are subsequently pruned to prevent error propagation, while a fallback strategy preserves system integrity. Empirical results on extensive math benchmarks show that AgentDropoutV2 significantly boosts the MAS's task performance, achieving an average accuracy gain of 6.3 percentage points on math benchmarks. Furthermore, the system exhibits robust generalization and adaptivity, dynamically modulating rectification efforts based on task difficulty while leveraging context-aware indicators to resolve a wide spectrum of error patterns. Our code and dataset are released at https://github.com/TonySY2/AgentDropoutV2.

---

## 论文详细总结（自动生成）

### AgentDropoutV2：通过测试时“纠正或拒绝”剪枝优化多智能体系统信息流

#### 1. 核心问题与研究动机
多智能体系统（MAS）虽然在处理复杂推理任务上具有潜力，但面临一个致命缺陷：**错误级联传播**。在多步协作中，单个智能体产生的错误信息会像病毒一样扩散到下游，导致整个任务失败。
*   **现有痛点**：目前的解决方法要么依赖僵化的结构设计（如优化拓扑图），要么依赖昂贵的模型微调。这些方法缺乏测试时的灵活性，无法在推理过程中动态识别并修复突发错误。
*   **研究目标**：提出一种无需重新训练、在推理阶段动态拦截并优化信息流的框架，实现对错误信息的“主动防御”。

#### 2. 方法论
AgentDropoutV2 核心思想是建立一个**测试时的“纠正或拒绝”剪枝机制**，其工作流程如下：
*   **故障驱动指标池（Indicator Pool）**：通过离线挖掘 MAS 历史失败轨迹，利用“老师模型”总结出具体的错误模式（如：平方根处理错误、整数条件遗漏等），并进行去重处理，形成一个结构化的先验知识库。
*   **测试时拦截与检索**：在 MAS 运行中，拦截每个智能体的输出。纠正器（Rectifier）根据当前任务上下文，从指标池中检索最相关的 $K$ 个对抗性指标。
*   **迭代纠正（Rectify）**：纠正器根据指标对输出进行审查。若发现违规，则生成具体反馈，引导智能体重新生成。这一过程是迭代的（默认最多 3 轮）。
*   **三态门控机制**：
    1.  **通过（Pass）**：无错误，信息传给下游。
    2.  **重试（Retry）**：有错误且未达迭代上限，重新生成。
    3.  **拒绝（Reject/Pruning）**：多次纠正仍失败，直接剔除该输出，防止污染后续环节。
*   **全局回退（Global Fallback）**：若剪枝导致有效信息过少（低于阈值 $\gamma$），系统会触发重置，从头开始执行，以保证推理的完整性。

#### 3. 实验设计
*   **数据集/场景**：
    *   **数学推理（9个基准）**：GSM8K, MATH-500, AQuA, AMC23, OlympiadBench, OlymMATH (Easy/Hard), AIME24, AIME25。
    *   **代码生成（4个基准）**：MBPP, HumanEval, CodeContests, LiveCodeBenchV1。
*   **对比方法**：
    *   **Single**：单模型推理。
    *   **AutoGen**：标准多智能体协作框架。
    *   **Generic Indicators**：使用通用逻辑检查（无特定指标池）的本框架版本。
    *   **Retrieved Indicators**：使用检索增强指标池的完整版 AgentDropoutV2。
*   **Backbone**：主要使用 Qwen3-8B 和 Qwen3-4B 作为推理引擎，GPT-4.1-mini 作为选择器。

#### 4. 资源与算力
*   **算力说明**：论文未明确给出具体的 GPU 数量或总训练时长。
*   **推理成本**：由于该方法是**测试时（Test-time）**框架，不涉及大规模参数训练。主要的算力消耗在于推理阶段的多次迭代调用（纠正过程）以及离线构建指标池时的教师模型调用。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从基础数学到奥数级别的 13 个主流数据集，实验覆盖面广。
*   **消融实验**：非常充分。作者研究了迭代轮数（$T_{max}$）、检索指标数量（$K_{act}$）、检索机制的有效性以及指标池去重的必要性。
*   **客观性**：通过跨模型（8B 到 4B 的指标迁移）和跨领域（数学到代码）的实验，验证了框架的泛化能力和可移植性，实验设计较为公平、客观。

#### 6. 主要结论与发现
*   **性能显著提升**：在数学任务上平均提升了 **6.3 个百分点**，在极难的 AIME25 任务上表现尤为突出。
*   **动态适应性**：系统能根据任务难度自动调节纠正深度。简单任务（如 GSM8K）多为一次通过，复杂任务（如 AIME）则频繁触发多轮纠正和拒绝机制。
*   **指标有效性**：实验证明，随机指标甚至会降低性能，只有通过检索匹配的精准指标才能有效引导纠正。
*   **难度评估器潜力**：剪枝率和纠正轮数与任务难度高度相关，可作为衡量任务复杂度的量化指标。

#### 7. 优点
*   **即插即用**：作为模型无关的模块，可适配多种 MAS 框架，无需昂贵的微调。
*   **精准纠错**：引入“指标池”将模糊的自我修正转变为基于先验知识的精准审计，有效解决了 LLM 盲目自纠可能引入新幻觉的问题。
*   **鲁棒性强**：通过“拒绝”机制充当语义断路器，强制切断错误传播路径。

#### 8. 不足与局限
*   **推理延迟**：迭代纠正机制必然增加推理时间（Latency）和 Token 消耗，在实时性要求极高的场景下可能受限。
*   **纠正器依赖**：系统的上限受限于纠正器模型（Rectifier）的判断能力。如果纠正器本身无法识别高级逻辑错误，框架效果会打折扣。
*   **指标池构建**：虽然是离线构建，但仍需要一定规模的失败轨迹数据，对于全新领域的冷启动可能存在挑战。

（完）
