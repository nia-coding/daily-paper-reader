---
title: "STRUCTUREDAGENT: Planning with AND/OR Trees for Long-Horizon Web Tasks"
title_zh: STRUCTUREDAGENT：基于与或树的长程网络任务规划
authors: "ELita Lobo, Xu Chen, Jingjing Meng, Nan Xi, Yang Jiao, Chirag Agarwal, Yair Zick, Yan Gao"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.05294v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 为长程任务提出了分层规划框架和结构化记忆模块。
tldr: 针对大语言模型智能体在处理长程 Web 任务时存在的规划能力弱、记忆受限及贪婪行为等问题，本文提出 STRUCTUREDAGENT 框架。该框架结合动态 AND/OR 树的在线分层规划器与结构化记忆模块，实现了高效搜索与约束满足。实验证明，该方法在 WebVoyager 和 WebArena 等基准测试中显著提升了长程任务性能，并提供了可解释的规划过程。
selection_source: fresh_fetch
motivation: 现有 Web 智能体在复杂长程任务中因缺乏有效规划和历史追踪能力，容易陷入局部最优或提前终止。
method: 提出一种基于动态 AND/OR 树的分层规划框架，并结合结构化记忆模块来维护候选方案和约束条件。
result: 在 WebVoyager、WebArena 及购物基准测试中，STRUCTUREDAGENT 的表现均优于标准的基于 LLM 的智能体。
conclusion: 通过分层规划和结构化记忆，可以有效提升 LLM 智能体在复杂 Web 环境下的长程决策能力和可解释性。
---

## 摘要
大语言模型（LLMs）的最新进展使得用于顺序决策的智能体系统成为可能。这类智能体必须感知其环境，跨多个时间步进行推理，并采取能够优化长期目标的行动。然而，由于追踪历史的上下文记忆有限、规划能力薄弱以及导致过早终止的贪婪行为，现有的网络智能体在复杂的长程任务中面临挑战。为了应对这些挑战，我们提出了 STRUCTUREDAGENT，这是一个分层规划框架，包含两个核心组件：（1）一个利用动态与或树进行高效搜索的在线分层规划器；（2）一个结构化记忆模块，用于追踪和维护候选解决方案，以提高信息寻求任务中的约束满足度。该框架还能生成可解释的分层计划，从而简化调试并在需要时便于人工干预。我们在 WebVoyager、WebArena 和自定义购物基准测试上的实验结果表明，与标准的基于 LLM 的智能体相比，STRUCTUREDAGENT 提升了在长程网络浏览任务中的性能。

## Abstract
Recent advances in large language models (LLMs) have enabled agentic systems for sequential decision-making. Such agents must perceive their environment, reason across multiple time steps, and take actions that optimize long-term objectives. However, existing web agents struggle on complex, long-horizon tasks due to limited in-context memory for tracking history, weak planning abilities, and greedy behaviors that lead to premature termination. To address these challenges, we propose STRUCTUREDAGENT, a hierarchical planning framework with two core components: (1) an online hierarchical planner that uses dynamic AND/OR trees for efficient search and (2) a structured memory module that tracks and maintains candidate solutions to improve constraint satisfaction in information-seeking tasks. The framework also produces interpretable hierarchical plans, enabling easier debugging and facilitating human intervention when needed. Our results on WebVoyager, WebArena, and custom shopping benchmarks show that STRUCTUREDAGENT improves performance on long-horizon web-browsing tasks compared to standard LLM-based agents.

---

## 论文详细总结（自动生成）

### 论文总结：STRUCTUREDAGENT —— 基于与或树的长程网络任务规划

#### 1. 核心问题与整体含义（研究动机和背景）
现有的 LLM 网页智能体在处理复杂、长程（Long-Horizon）任务时表现不佳，主要归结为四个挑战：
*   **上下文记忆有限**：难以在多个页面间追踪备选方案。
*   **规划能力薄弱**：无法构建和执行跨越长路径的复杂计划。
*   **缺乏错误恢复机制**：一旦执行失败，难以回溯或探索替代路径。
*   **贪婪决策**：容易在未完全满足用户约束的情况下过早终止任务。
本文受认知科学中人类解决问题的启发，提出将复杂任务递归分解为子问题，通过分层规划来提升智能体的鲁棒性和可解释性。

#### 2. 方法论：核心思想与技术细节
STRUCTUREDAGENT 结合了**在线分层规划器**和**结构化记忆模块**：
*   **动态 AND/OR 树结构**：
    *   **AND 节点**：代表必须全部完成的子目标。
    *   **OR 节点**：代表实现目标的多种备选策略。
    *   **ACTION 节点**：叶子节点，对应具体的浏览器操作（点击、输入、滚动等）。
*   **改进的贪婪深度优先搜索（DFS）**：
    *   引入三种执行状态：**进入（ENTERING）**、**退出（EXITING）**和**失败（FAILED）**。
    *   支持**动态计划修订**：当某个节点失败时，触发“节点修复（Node Repair）”或“剪枝（Pruning）”，并将错误向上回传，触发父节点的重新规划。
*   **结构化记忆模块（Structured Memory）**：
    *   针对信息寻求任务，建立动态表格追踪候选实体及其满足的约束条件。
    *   在决策时检索 Top-K 候选方案，减少重复探索。
*   **核心操作器**：由 LLM 驱动的局部操作，包括节点扩展、修复、完成检查、全局树更新和观察摘要。

#### 3. 实验设计
*   **基准测试（Benchmarks）**：
    1.  **Custom Complex Shopping (Amazon)**：60 个长程购物任务（10-30 步），包含复杂约束。
    2.  **WebVoyager**：129 个真实世界网站任务。
    3.  **WebArena**：630 个任务，涵盖地图、购物、Reddit 和 GitLab。
*   **对比方法**：
    *   **AgentOccam**：基于动作树的强力基线。
    *   **BasicClaudeAction**：带完整历史记录的 Claude 智能体。
    *   **SteP**：基于堆栈和人工编写策略的智能体。
    *   **WebarenaReplication**：官方 CoT 推理基线。
*   **评估指标**：任务成功率（Success Rate），采用 LLM-as-a-Judge（GPT-4, Gemini, Kimi）和人工评估。

#### 4. 资源与算力
*   论文**未明确说明**具体的训练算力（如 GPU 型号或数量），因为该框架主要是在**推理侧（Inference-time）**通过调用闭源模型 API 实现。
*   使用的骨干模型包括：**Claude 3.5 Sonnet (v2)**、**Claude 3.7 Sonnet** 和 **Kimi-k2**。

#### 5. 实验数量与充分性
*   **实验规模**：在三个大型基准测试上进行了超过 800 个任务的测试，涵盖了从简单到极高难度的不同梯度。
*   **充分性**：实验设计非常充分。不仅对比了多种 SOTA 基线，还跨越了不同的模型家族（Claude, Kimi）验证泛化性。
*   **客观性**：引入了多模型交叉评审和人工评估，并详细分析了不同评估者之间的分歧率（Disagreement Rate），确保了结果的客观公平。

#### 6. 主要结论与发现
*   **性能领先**：在 Amazon Hard 任务上，STRUCTUREDAGENT 显著优于 AgentOccam（人工评估领先 7% 以上）；在 WebArena 综合评分中达到 52.6%，位居第一。
*   **长程任务优势**：任务越复杂、约束越多，该框架的优势越明显。
*   **模型规模效应**：即使使用更强的模型（如 Claude 3.7），分层规划依然能带来显著的性能增益。
*   **结构化记忆的作用**：在多约束检索任务中，结构化记忆能有效提升约束满足度，但在简单任务中可能引入噪声。

#### 7. 优点
*   **高可解释性**：生成的 AND/OR 树清晰展示了智能体的决策逻辑，便于调试和人工干预。
*   **鲁棒的错误处理**：通过回溯和修复机制，智能体能从执行失败中恢复，而不是直接崩溃。
*   **约束追踪**：结构化记忆解决了 LLM 在长对话中容易遗忘细节的问题。
*   **即插即用**：作为一个框架，可以适配不同的骨干 LLM。

#### 8. 不足与局限
*   **效率与成本**：由于需要进行多次树操作和节点检查，其轨迹长度（Action 数量）通常比基线长，导致推理成本和响应延迟增加。
*   **简单任务的冗余**：在非常简单的任务中，分层规划可能显得过于繁琐，甚至因过度约束导致性能轻微下降。
*   **依赖模型能力**：框架的效果高度依赖于骨干模型对节点扩展和修复指令的遵循能力。

（完）
