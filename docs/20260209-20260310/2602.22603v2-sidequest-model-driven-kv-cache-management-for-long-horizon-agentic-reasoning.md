---
title: "SideQuest: Model-Driven KV Cache Management for Long-Horizon Agentic Reasoning"
title_zh: SideQuest：面向长程智能体推理的模型驱动 KV 缓存管理
authors: "Sanjay Kariyappa, G. Edward Suh"
date: 2026-02-26
pdf: "https://arxiv.org/pdf/2602.22603v2"
tags: ["query:sr-ai"]
score: 9.0
evidence: 长程智能体推理中的 KV 缓存管理
tldr: SideQuest 针对长程智能体任务中 KV 缓存占用过高的问题，提出了一种由模型驱动的压缩方法。不同于传统的启发式策略，它利用大推理模型（LRM）自身来评估上下文中各 token 的重要性，并将其作为并行辅助任务执行，以避免干扰主推理过程。实验表明，该方法在仅需少量训练数据的情况下，能显著降低峰值显存占用并保持高准确度，有效解决了长上下文推理中的资源瓶颈。
selection_source: fresh_fetch
motivation: 现有的 KV 缓存压缩启发式方法难以有效支持长程智能体任务中的多步推理，导致内存压力大且性能受限。
method: 提出 SideQuest 框架，将 KV 缓存管理作为并行的辅助任务，利用推理模型自身判断 token 的有用性并进行压缩。
result: "在仅使用 215 个样本训练的情况下，SideQuest 将智能体任务的峰值 token 使用量减少了高达 65%，且准确率几乎无损。"
conclusion: 通过让推理模型参与自身的缓存管理，可以比传统启发式方法更高效地处理长上下文推理任务。
---

## 摘要
长期运行的智能体任务（如深度研究）需要对分布在多个网页和文档中的信息进行多跳推理。在此类任务中，大语言模型（LLM）的上下文主要由来自外部检索的 token 占据，导致内存使用量迅速增长并限制了解码性能。尽管针对长上下文输入已存在多种 KV 缓存压缩技术，但我们发现现有的启发式方法无法有效支持多步推理模型。我们通过 SideQuest 解决了这一挑战——这是一种利用大推理模型（LRM）自身通过推理上下文中 token 的有用性来执行 KV 缓存压缩的新颖方法。为了防止与该管理过程相关的 token 污染模型内存，我们将 KV 缓存压缩构建为与主推理任务并行执行的辅助任务。我们的评估结果显示，使用仅包含 215 个样本训练的模型，SideQuest 在智能体任务中将峰值 token 使用量减少了高达 65%，且准确率下降极小，性能优于基于启发式的 KV 缓存压缩技术。

## Abstract
Long-running agentic tasks, such as deep research, require multi-hop reasoning over information distributed across multiple webpages and documents. In such tasks, the LLM context is dominated by tokens from external retrieval, causing memory usage to grow rapidly and limiting decode performance. While several KV cache compression techniques exist for long-context inputs, we find that existing heuristics fail to support multi-step reasoning models effectively. We address this challenge with SideQuest -- a novel approach that leverages the Large Reasoning Model (LRM) itself to perform KV cache compression by reasoning about the usefulness of tokens in its context. To prevent the tokens associated with this management process from polluting the model's memory, we frame KV cache compression as an auxiliary task executed in parallel to the main reasoning task. Our evaluations, using a model trained with just 215 samples, show that SideQuest reduces peak token usage by up to 65% on agentic tasks with minimal degradation in accuracy, outperforming heuristic-based KV cache compression techniques.

---

## 论文详细总结（自动生成）

### 论文总结：SideQuest —— 面向长程智能体推理的模型驱动 KV 缓存管理

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）演变为能够进行深度研究和多步推理的自主智能体，其上下文窗口往往被大量的外部检索文档和中间思维链（Thought Traces）占据。这导致 **KV 缓存（Key-Value Cache）** 呈线性增长，带来了两大瓶颈：
*   **内存压力：** 巨大的 KV 缓存限制了并发请求的批处理大小（Batch Size）。
*   **计算延迟：** 注意力机制变成内存带宽受限（Memory-bandwidth bound），生成每个新 token 都需要加载海量张量。
现有的启发式压缩方法（如 H2O、SnapKV）依赖静态重要性指标（如注意力分数），但在智能体任务中，token 的重要性是**动态且非单调**的（例如，一个早期看似无关的搜索结果可能在十步之后的总结阶段变得至关重要）。因此，论文提出了 SideQuest，旨在让模型“自我管理”内存。

#### 2. 方法论：核心思想与关键技术
SideQuest 的核心思想是将 KV 缓存管理从“固定启发式”转变为“模型驱动的推理任务”。
*   **并行辅助线程（Auxiliary Thread）：** 系统定期派生一个与主推理线程共享上下文的辅助线程。该线程专门负责分析当前上下文，识别哪些工具调用（Tool Calls）或响应已过时。
*   **触发机制：** 通过添加特定的触发词（如 `** Memory management mode **`）引导模型进入管理模式，输出结构化的删除指令（如 `{del_cursors: [0]}`）。
*   **事后分析标注（Hindsight Annotation）：** 训练数据通过分析成功的推理轨迹生成。如果某个工具响应在后续步骤中再未被引用，则将其标记为“可过期”。
*   **训练策略：** 
    *   **联合优化：** 使用交叉熵损失训练辅助任务，同时使用**逻辑值蒸馏（Logit Distillation）**确保模型在主推理任务上的能力不退化。
    *   **参数高效微调：** 采用 LoRA 对混合专家模型（MoE）的部分层进行微调。

#### 3. 实验设计
*   **数据集/场景：**
    *   **FRAMES：** 考察模型在维基百科文章上的多跳推理能力。
    *   **BrowseComp：** 考察模型在复杂网页导航中寻找特定信息的能力。
*   **基准模型：** `gpt-oss-20b`。
*   **对比方法：** 
    *   **Uncompressed Baseline：** 全量注意力（上限）。
    *   **H2O：** 基于“重击者”（Heavy Hitter）的频率剪枝。
    *   **SnapKV：** 基于聚类的重要信息保留。
    *   **R-KV：** 针对推理模型的冗余感知压缩。
*   **评估指标：** 峰值 Token 使用量、KV 缓存内存读取量、准确率（Accuracy）以及系统吞吐量。

#### 4. 资源与算力
*   **训练：** 使用了 LoRA 微调，仅针对 MoE 层的部分投影层（第 7、15、23 层）。训练样本量极小，仅使用了 **215 个高质量推理轨迹**。
*   **推理/服务：** 实验在单张 **NVIDIA H100 GPU** 上运行，并集成在 SGLang 推理框架中。
*   **时长：** 文中未明确给出具体的训练总时长，但提到微调进行了 3 个 Epoch。

#### 5. 实验数量与充分性
*   **实验规模：** 在 FRAMES 上测试了 424 个样本，在 BrowseComp 上测试了 500 个样本。
*   **维度覆盖：** 实验涵盖了“中等”和“高”两种推理强度设置，并对比了不同 token 预算（16k, 24k）下的表现。
*   **客观性：** 实验通过本地服务器模拟搜索 API，确保了评估的可重复性和确定性。对比了多种主流的启发式基准，并提供了端到端的系统性能指标（如吞吐量和运行时间），实验设计较为全面且客观。

#### 6. 主要结论与发现
*   **高效压缩：** SideQuest 将峰值 token 使用量减少了 **56% - 65%**，KV 缓存内存读取量减少了 **53% - 71%**。
*   **性能保持：** 在大幅减小内存占用的同时，准确率损失极小（FRAMES 下降约 2%，分布外数据集 BrowseComp 下降约 5%），远优于启发式方法。
*   **系统增益：** 在实际服务中，SideQuest 使峰值吞吐量提升了 **83.9%**，并将总运行时间缩短了 **36.8%**。
*   **鲁棒性：** 启发式方法在低预算下常导致模型崩溃（输出不可解析），而 SideQuest 保持了与全量模型相当的逻辑连贯性。

#### 7. 优点（亮点）
*   **语义感知：** 相比于计算注意力分数，利用模型自身的语义理解能力来判断 token 价值更加精准。
*   **零污染并行：** 辅助线程在并行分支运行，管理 token 不会进入主线程的上下文，避免了“管理开销”本身占用内存。
*   **自适应预算：** 抛弃了固定的 token 窗口限制，模型根据任务难度动态决定保留多少内存。
*   **极低数据依赖：** 仅需 200 余个样本即可训练出有效的管理能力，展示了强大的泛化潜力。

#### 8. 不足与局限
*   **覆盖范围有限：** 目前仅针对“工具响应”（Tool Responses）进行清理，尚未涉及模型自身中间推理步骤（Thought tokens）的压缩。
*   **分布外性能：** 在 OOD 数据集（BrowseComp）上仍有 5% 的准确率下降，说明管理逻辑的泛化性仍有提升空间。
*   **计算开销：** 虽然减少了内存带宽压力，但派生辅助线程会带来额外的计算量（FLOPs），在非内存受限的场景下可能优势不明显。
*   **数据规模：** 训练集规模较小，可能限制了模型处理更复杂、更极端长上下文场景的能力。

（完）
