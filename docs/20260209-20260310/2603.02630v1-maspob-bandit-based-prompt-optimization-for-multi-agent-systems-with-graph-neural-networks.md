---
title: "MASPOB: Bandit-Based Prompt Optimization for Multi-Agent Systems with Graph Neural Networks"
title_zh: MASPOB：基于多臂老虎机与图神经网络的多智能体系统提示词优化
authors: "Zhi Hong, Qian Zhang, Jiahang Sun, Zhiwei Shang, Mingze Kong, Xiangyi Wang, Yao Shu, Zhongxiang Dai"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.02630v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 多智能体系统编排的提示词优化
tldr: 针对多智能体系统（MAS）中提示词优化面临的样本效率低、拓扑耦合及搜索空间爆炸等挑战，本文提出MASPOB框架。该框架结合老虎机算法（Bandit）与置信上限（UCB）实现高效探索，利用图神经网络（GNN）捕捉智能体间的拓扑结构，并通过坐标上升法将指数级搜索复杂度降至线性，在多个基准测试中达到SOTA性能。
selection_source: fresh_fetch
motivation: 多智能体系统提示词优化面临评估成本高昂、智能体间拓扑耦合复杂以及搜索空间呈指数级爆炸的难题。
method: 提出基于老虎机算法的MASPOB框架，利用GNN捕捉结构先验并结合坐标上升法将复杂优化分解为单变量子问题。
result: 实验表明，MASPOB在多个基准测试中均优于现有基准方法，实现了最先进的性能表现。
conclusion: MASPOB通过融合不确定性建模与拓扑感知表示，为受限预算下的多智能体系统提示词优化提供了高效且可扩展的方案。
---

## 摘要
大语言模型（LLMs）在许多现实应用中取得了巨大成功，特别是作为多智能体系统（MAS）的认知骨干，在实践中编排复杂的工作流。由于许多部署场景不允许修改 MAS 工作流，且其性能对输入提示词高度敏感，提示词优化成为提升其性能的一种更自然的方法。然而，现实世界中 MAS 的提示词优化面临三个关键挑战：(1) 由于评估成本极高，对样本效率有极高要求；(2) 提示词之间存在拓扑诱导的耦合；(3) 搜索空间的组合爆炸。为了应对这些挑战，我们提出了 MASPOB（基于多臂老虎机的多智能体系统提示词优化），这是一个基于多臂老虎机的新型样本高效框架。通过利用置信上限（UCB）来量化不确定性，该老虎机框架平衡了探索与利用，在严格受限的预算内实现了收益最大化。为了处理拓扑诱导的耦合，MASPOB 集成了图神经网络（GNNs）以捕获结构先验，学习提示词语义的拓扑感知表示。此外，它采用坐标上升法将优化分解为单变量子问题，将搜索复杂度从指数级降低到线性级。在多个基准测试上的广泛实验表明，MASPOB 达到了最先进的性能，始终优于现有的基准方法。

## Abstract
Large Language Models (LLMs) have achieved great success in many real-world applications, especially the one serving as the cognitive backbone of Multi-Agent Systems (MAS) to orchestrate complex workflows in practice. Since many deployment scenarios preclude MAS workflow modifications and its performance is highly sensitive to the input prompts, prompt optimization emerges as a more natural approach to improve its performance. However, real-world prompt optimization for MAS is impeded by three key challenges: (1) the need of sample efficiency due to prohibitive evaluation costs, (2) topology-induced coupling among prompts, and (3) the combinatorial explosion of the search space. To address these challenges, we introduce MASPOB (Multi-Agent System Prompt Optimization via Bandits), a novel sample-efficient framework based on bandits. By leveraging Upper Confidence Bound (UCB) to quantify uncertainty, the bandit framework balances exploration and exploitation, maximizing gains within a strictly limited budget. To handle topology-induced coupling, MASPOB integrates Graph Neural Networks (GNNs) to capture structural priors, learning topology-aware representations of prompt semantics. Furthermore, it employs coordinate ascent to decompose the optimization into univariate sub-problems, reducing search complexity from exponential to linear. Extensive experiments across diverse benchmarks demonstrate that MASPOB achieves state-of-the-art performance, consistently outperforming existing baselines.

---

## 论文详细总结（自动生成）

这是一份关于论文《MASPOB: Bandit-Based Prompt Optimization for Multi-Agent Systems with Graph Neural Networks》的结构化深入分析总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于**多智能体系统（MAS）中的提示词（Prompt）优化**。在复杂的实际应用中，MAS 通过多个专门的智能体协作完成任务，其性能高度依赖于每个智能体的提示词。然而，优化 MAS 提示词面临三大挑战：
*   **样本效率低下**：评估一个提示词组合需要运行整个 MAS 工作流，涉及多次 LLM 调用，成本极高。
*   **拓扑诱导的耦合**：上游智能体提示词的改变会影响下游的输入分布，导致各智能体间的优化目标非独立。
*   **组合爆炸**：搜索空间随智能体数量呈指数级增长，穷举搜索不可行。
**研究动机**：在不修改专家验证过的工作流拓扑的前提下，如何高效、感知拓扑地优化各智能体的提示词。

### 2. 方法论：MASPOB 框架
MASPOB 结合了不确定性引导的探索、拓扑感知建模和可扩展的组合搜索，核心组件包括：
*   **拓扑感知代理模型（GNN Surrogate）**：使用**图注意力网络（GAT）**对 MAS 工作流（有向无环图 DAG）进行建模。将智能体视为节点，提示词嵌入作为节点特征，通过消息传递机制捕捉智能体间的结构依赖和影响传播。
*   **基于老虎机的探索-利用权衡（LinUCB）**：将优化问题表述为上下文老虎机（Contextual Bandit）。利用**置信上限（UCB）**准则：
    *   **利用**：由 GNN 预测的性能得分。
    *   **探索**：基于信息矩阵计算的认识不确定性（Epistemic Uncertainty）。
    *   这种机制确保在有限的评估预算（如 50 次）内优先测试最有潜力或信息量最大的提示词组合。
*   **坐标上升搜索（Coordinate Ascent）**：为了解决组合爆炸，将全局搜索分解为一系列单变量子问题。每次只优化一个智能体的提示词，而保持其他智能体固定，将搜索复杂度从指数级降低为线性级。

### 3. 实验设计
*   **数据集/场景**：涵盖三大类共 6 个基准测试：
    *   **问答（QA）**：HotpotQA, DROP。
    *   **代码生成**：HumanEval, MBPP。
    *   **数学推理**：GSM8K, MATH (Level 5)。
*   **对比方法（Baselines）**：
    *   **基础方法**：IO（直接调用）、CoT（思维链）、ReAct。
    *   **单智能体优化器**：PromptBreeder, Instinct。
    *   **多智能体/多阶段优化器**：AFlow, MIPRO（基于 DSPy 框架的强基准）。
*   **实验设置**：所有优化方法统一给定 **50 次验证评估预算**，以确保公平性。

### 4. 资源与算力
*   **模型使用**：
    *   **骨干 LLM**：主要使用 `GPT-4o-mini`，泛化性测试中使用了 `Qwen3-32B`。
    *   **嵌入模型**：`Qwen3-Embedding-8B` 用于生成提示词的 1024 维向量表示。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量或训练总时长。但提到 GNN 代理模型的训练和坐标上升搜索的计算开销相对于 LLM API 调用而言是“微不足道”的。

### 5. 实验数量与充分性
*   **实验规模**：在 6 个不同领域的基准上进行了测试，每组实验重复 3 次并报告均值和标准差。
*   **消融实验**：
    *   **GNN 的有效性**：对比了将 GNN 替换为 MLP 的效果（性能下降 2.31%）。
    *   **不确定性类型**：对比了线性 UCB 与神经 UCB 的收敛速度。
    *   **搜索策略**：对比了坐标上升法与全局穷举搜索的运行时间与性能（坐标上升法在损失极小性能的情况下提速 98% 以上）。
*   **充分性评价**：实验设计非常充分且客观。通过统一评估预算、跨模型验证和深度消融，有力证明了 MASPOB 在受限资源下的优越性。

### 6. 主要结论与发现
*   **性能领先**：MASPOB 在所有基准上均达到 SOTA，平均性能比 IO 提升 12.02%，比强基准 MIPRO 提升 1.71%。
*   **拓扑建模至关重要**：显式建模 MAS 的 DAG 结构能显著提升复杂任务（如多步推理）的优化效果。
*   **样本效率高**：在仅 50 次评估内，MASPOB 就能识别出高度协调的提示词组合，且在 35 轮左右趋于稳定。
*   **结构复杂性适应**：在更复杂的 MAS 结构（更多智能体）中，MASPOB 的领先优势依然稳固。

### 7. 优点
*   **样本效率极佳**：非常适合 LLM API 调用成本昂贵的现实场景。
*   **拓扑感知**：不同于以往将 MAS 视为黑盒的方法，MASPOB 利用 GNN 提取了工作流的结构先验。
*   **算法优雅且实用**：结合了经典的 Bandit 理论与现代 GNN，通过坐标上升解决了离散组合空间的搜索难题。

### 8. 不足与局限
*   **固定拓扑限制**：该方法假设 MAS 的工作流拓扑是预先定义且不可更改的，无法同时优化提示词和工作流结构（虽然这是其设计初衷，但在某些场景下可能限制了性能上限）。
*   **提示词池依赖**：优化效果仍受限于初始生成的提示词候选池（20 个变体）的质量。
*   **计算开销**：虽然搜索过程快，但维护信息矩阵和频繁重训 GNN 在智能体数量极多或提示词池极大时可能存在扩展性挑战。

（完）
