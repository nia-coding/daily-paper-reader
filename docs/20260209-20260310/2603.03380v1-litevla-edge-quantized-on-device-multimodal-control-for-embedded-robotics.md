---
title: "LiteVLA-Edge: Quantized On-Device Multimodal Control for Embedded Robotics"
title_zh: LiteVLA-Edge：面向嵌入式机器人的量化设备端多模态控制
authors: "Justin Williams, Kishor Datta Gupta, Roy George, Mrinmoy Sarkar"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.03380v1"
tags: ["query:sr-ai"]
score: 10.0
evidence: 用于嵌入式机器人控制的视觉-语言-动作模型
tldr: LiteVLA-Edge 针对嵌入式机器人部署，提出了一种高效的视觉-语言-动作（VLA）模型流水线。通过在 Jetson Orin 硬件上结合 FP32 监督微调、4位 GGUF 量化以及 llama.cpp 加速推理，实现了完全离线的端到端控制。该系统在 ROS 2 环境下达到了 6.6Hz 的推理频率，为在资源受限的嵌入式设备上运行多模态控制模型提供了可行的实践路径和基准。
selection_source: fresh_fetch
motivation: 现有的 VLA 模型由于计算需求高和推理延迟大，难以直接部署在资源受限的嵌入式机器人硬件上。
method: 结合了 FP32 监督微调、4位 GGUF 量化技术，并利用 llama.cpp 运行时在 Jetson Orin 上实现 GPU 加速推理。
result: 在 ROS 2 集成环境下实现了 150.5 毫秒（约 6.6Hz）的平均端到端延迟，且支持完全离线运行。
conclusion: 该研究验证了在嵌入式硬件上执行紧凑型多模态控制模型的技术可行性，并为端侧 VLA 的未来评估提供了可复现的基准。
---

## 摘要
视觉-语言-动作 (VLA) 模型为感知、语言调节和动作生成提供了一个统一的框架，但由于计算需求和推理延迟，许多现有系统仍难以部署在嵌入式机器人环境中。在本文中，我们提出了 LiteVLA-Edge，这是一个面向部署的 VLA 流水线，旨在 Jetson Orin 级硬件上实现完全的设备端推理。我们的方法结合了 FP32 精度下的监督式图像到动作微调、训练后 4-bit GGUF 量化，以及通过 llama.cpp 运行时的 GPU 加速推理。在我们的部署配置下，LiteVLA-Edge 在完全离线运行于集成 ROS 2 的感知-推理-动作流水线中时，实现了 150.5 ms（约 6.6 Hz）的平均端到端延迟。我们的贡献并非引入新的策略目标，而是提供了一条在嵌入式硬件上本地执行紧凑型多模态控制模型的实际系统路径，同时保留了感知、推理和执行之间的模块化接口。这些结果确立了反应式语言调节控制的时间可行性，并为未来机器人设备端 VLA 的任务级评估提供了可复现的基准。

## Abstract
Vision-Language-Action (VLA) models provide a unified framework for perception, language conditioning, and action generation, but many existing systems remain difficult to deploy in embedded robotic settings because of their computational requirements and inference latency. In this paper, we present LiteVLA-Edge, a deployment-oriented VLA pipeline for fully on-device inference on Jetson Orin-class hardware. Our approach combines supervised image-to-action fine-tuning in FP32 with post-training 4-bit GGUF quantization and GPU-accelerated inference through the \texttt{llama.cpp} runtime. Under our deployment configuration, LiteVLA-Edge achieves a mean end-to-end latency of 150.5\,ms (approximately 6.6\,Hz) while operating entirely offline within a ROS~2-integrated perception--reasoning--action pipeline. Rather than introducing a new policy objective, our contribution is a practical systems path for executing compact multimodal control models locally on embedded hardware while preserving modular interfaces between perception, reasoning, and actuation. These results establish timing feasibility for reactive language-conditioned control and provide a reproducible baseline for future task-level evaluation of on-device VLAs in robotics.

---

## 论文详细总结（自动生成）

### LiteVLA-Edge：面向嵌入式机器人的量化设备端多模态控制总结

#### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉-语言-动作（VLA）模型（如 RT-2、OpenVLA）参数量巨大（通常 >7B），依赖云端计算或高性能桌面 GPU，无法在功耗受限、低延迟要求的嵌入式机器人（如无人机、战术防御机器人）上本地运行。
*   **研究动机**：早期的轻量化尝试（如 LiteVLA）虽然实现了端侧运行，但推理延迟高达数秒，只能进行“开环”的慢速决策。本研究旨在通过系统级优化，在嵌入式硬件上实现高频（>6Hz）的“闭环”视觉运动控制。

#### 2. 方法论
*   **核心思想**：采用“先全精度微调，后量化部署”的策略，利用极小规模的多模态骨干网络，结合高效的推理引擎实现实时控制。
*   **关键技术细节**：
    *   **骨干网络**：选用蒸馏后的 **SmolVLM-256M**（仅 2.56 亿参数），具备语义推理能力且内存占用极低。
    *   **微调策略**：在 FP32 精度下使用 **LoRA**（秩 $r=8$，缩放因子 $\alpha=8$）进行监督式图像到动作（Image-to-Action）微调，以保证动作指令的精确度。
    *   **量化压缩**：训练后将模型转换为 **GGUF 格式**，并应用 **4-bit (Q4_K_M) 量化**。
    *   **推理加速**：利用 **llama.cpp** 库，将全部 42 层 Transformer 算子卸载（Offload）到 Jetson Orin 的 GPU 上，通过 CUDA 后端加速。
    *   **系统集成**：通过 **ROS 2** 节点集成，将模型输出的 Token 解码为标准速度指令（`geometry_msgs/Twist`），实现感知-推理-动作的模块化解耦。

#### 3. 实验设计
*   **硬件平台**：主要在 **NVIDIA Jetson AGX Orin (64GB)** 上进行评估，对比参考了 Raspberry Pi 4。
*   **对比方法**：
    *   **通用 VLA**：OpenVLA (7B)。
    *   **高效 VLA**：EdgeVLA、Efficient VLA。
    *   **紧凑型 VLM（仅视觉语言推理）**：Moondream2、TinyLLaVA、PaliGemma、Qwen2-VL。
*   **评估指标**：端到端推理延迟（Latency）、推理频率（Hz）、延迟抖动（Jitter/Std Deviation）。

#### 4. 资源与算力
*   **推理算力**：使用了 **NVIDIA Jetson AGX Orin**（生产级嵌入式 GPU 模块），功耗配置在 25W-40W 之间。
*   **训练算力**：文中提到在 FP32 下进行 LoRA 微调，但未明确说明训练所用的具体 GPU 型号（推测为桌面级 GPU）及训练总时长。
*   **部署配置**：设置上下文窗口 $n_{ctx} = 512$，限制输出最大为 12 个 Token，以最小化 KV 缓存开销。

#### 5. 实验数量与充分性
*   **实验规模**：进行了 **300 次** 连续推理运行以测量统计学意义上的延迟数据。
*   **充分性评价**：
    *   **系统性能评估充分**：对于延迟、抖动和硬件占用率的测量非常详尽，证明了其在嵌入式设备上的运行可行性。
    *   **任务级评估略显不足**：论文重点在于“系统路径”和“时序可行性”，缺乏在复杂机器人操作任务（如具体的抓取成功率、导航精度）上的大规模基准测试。

#### 6. 主要结论与发现
*   **性能突破**：LiteVLA-Edge 实现了 **150.5 ms** 的平均端到端延迟（约 **6.64 Hz**），相比前代基准提升了约 **220%**。
*   **闭环可行性**：6.6 Hz 的频率跨越了机器人控制的阈值，使 VLA 模型能够从“停顿-思考-执行”模式转变为“实时修正”的闭环视觉伺服控制。
*   **稳定性**：延迟标准差仅为 **0.13 ms**，极低的抖动确保了 ROS 2 控制心跳的确定性，避免了高延迟 AI 控制器常见的震荡风险。

#### 7. 优点
*   **实用性强**：提供了一套完整的、基于开源工具（llama.cpp, ROS 2）的部署流水线，具有很高的复现价值。
*   **平衡性佳**：在极小的参数量（256M）下保留了多模态推理能力，同时满足了嵌入式设备的功耗和实时性要求。
*   **模块化设计**：通过 ROS 2 接口解耦，允许加入确定性的安全覆盖（Safety Overrides），解决了端到端模型“黑盒”难以调试的问题。

#### 8. 不足与局限
*   **任务泛化性验证缺失**：虽然证明了“跑得快”，但未通过 Open X-Embodiment 等大型数据集验证其在复杂任务上的“聪明程度”是否优于其他轻量化模型。
*   **量化损失风险**：尽管文中提到动作稳定，但 4-bit 量化在极端精确的操作任务中仍可能引入数值漂移，需进一步的长时漂移测试。
*   **应用场景限制**：目前主要针对 Jetson Orin 等高端嵌入式平台，对于更低端的单片机或低功耗 CPU 仍有挑战。

（完）
