---
title: "MemPO: Self-Memory Policy Optimization for Long-Horizon Agents"
title_zh: MemPO：面向长时程智能体的自记忆策略优化
authors: "Ruoran Li, Xinghua Zhang, Haiyang Yu, Shitong Duan, Xiang Li, Wenxin Xiang, Chonghua Liao, Xudong Guo, Yongbin Li, Jinli Suo"
date: 2026-02-28
pdf: "https://arxiv.org/pdf/2603.00680v1"
tags: ["query:sr-ai"]
score: 10.0
evidence: 长程智能体的自记忆策略优化
tldr: 针对长程智能体在交互中上下文增长导致的性能下降问题，本文提出 MemPO 算法。该方法通过自记忆策略优化，使智能体能自主总结和管理记忆。通过改进基于记忆有效性的信用分配机制，智能体可选择性保留关键信息，在显著降低 Token 消耗的同时提升任务表现，在 F1 分数和效率上均优于现有 SOTA 模型。
selection_source: fresh_fetch
motivation: 现有的外部记忆模块缺乏主动管理能力，且不断增长的上下文会损害长程智能体的性能与稳定性。
method: 提出 MemPO 算法，通过改进基于记忆有效性的信用分配机制，使策略模型能够自主总结并选择性保留关键记忆信息。
result: "实验显示 MemPO 相比基准模型 F1 分数提升 25.98%，相比 SOTA 提升 7.1%，同时减少了约 70% 的 Token 使用量。"
conclusion: MemPO 有效解决了长程任务中的上下文冗余问题，实现了记忆管理与任务目标的对齐，显著提升了智能体的效率与性能。
---

## 摘要
长时程智能体在与环境交互过程中面临上下文规模不断增长的挑战，这会导致性能和稳定性下降。现有方法通常引入外部记忆模块并从存储的记忆中检索相关信息，这阻碍了模型自身主动管理其记忆内容并使其与智能体的总体任务目标保持一致。为了解决这些局限性，我们提出了自记忆策略优化算法（MemPO），该算法使智能体（策略模型）能够在与环境交互期间自主地总结和管理其记忆。通过改进基于记忆有效性的信用分配机制，策略模型可以有选择地保留关键信息，在保持任务性能的同时显著降低 Token 消耗。大量的实验和分析证实，MemPO 相比基础模型实现了 25.98% 的 F1 分数绝对增益，相比之前的 SOTA 基准实现了 7.1% 的增益，同时将 Token 使用量分别减少了 67.58% 和 73.12%。

## Abstract
Long-horizon agents face the challenge of growing context size during interaction with environment, which degrades the performance and stability. Existing methods typically introduce the external memory module and look up the relevant information from the stored memory, which prevents the model itself from proactively managing its memory content and aligning with the agent's overarching task objectives. To address these limitations, we propose the self-memory policy optimization algorithm (MemPO), which enables the agent (policy model) to autonomously summarize and manage their memory during interaction with environment. By improving the credit assignment mechanism based on memory effectiveness, the policy model can selectively retain crucial information, significantly reducing token consumption while preserving task performance. Extensive experiments and analyses confirm that MemPO achieves absolute F1 score gains of 25.98% over the base model and 7.1% over the previous SOTA baseline, while reducing token usage by 67.58% and 73.12%.

---

## 论文详细总结（自动生成）

这是一份关于论文《MemPO: Self-Memory Policy Optimization for Long-Horizon Agents》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
长时程（Long-Horizon）智能体在解决复杂任务时，通常需要与环境进行多轮交互。传统的 **ReAct** 范式会导致上下文随交互轮数线性增长，带来三大问题：
*   **上下文窗口限制：** 触及 LLM 的最大 Token 上限。
*   **高昂成本：** 冗长的上下文导致推理 Token 消耗巨大。
*   **性能下降：** 产生“迷失在中间”（Lost in the Middle）现象，干扰模型提取关键信息。
现有的外部记忆（如 RAG）通常是**被动检索**，无法与智能体的任务目标主动对齐。本文旨在让智能体具备**自主总结和管理记忆**的能力，使其在只保留极短上下文的情况下仍能高效完成任务。

### 2. 方法论：核心思想与关键技术
论文提出了 **MemPO（自记忆策略优化）** 算法，将记忆管理转化为智能体的内在动作。

*   **交互范式：** 智能体每一步输出包含三个动作：`<mem>`（总结历史关键信息）、`<think>`（推理）、`<tool_call>`（调用工具）。在推理下一轮时，仅保留上一轮的 `<mem>` 内容作为输入，丢弃更早的原始历史。
*   **双层信用分配机制（Credit Assignment）：**
    1.  **轨迹级优势（Trajectory-level Advantage, $A_T$）：** 基于最终答案的正确性和格式规范性给予奖励，使用 GRPO 算法进行组内相对评分。
    2.  **记忆级优势（Memory-level Advantage, $A_M$）：** 针对 `<mem>` 动作设计的密集奖励。其核心指标是**条件概率**：计算在给定当前生成的记忆内容下，模型生成正确答案的后验概率。如果记忆保留了关键信息，该概率应显著提高。
*   **策略优化：** 最终的 Token 优势值是 $A_T$ 和 $A_M$ 的加权组合。通过强化学习（GRPO 变体），引导模型在 `<mem>` 标签内生成最有助于解决任务的精简信息。

### 3. 实验设计
*   **数据集/场景：**
    *   **多目标任务（Multi-objective）：** 基于 HotpotQA 和 NQ 合成的 2 到 10 个目标的复杂查询（需多步搜索）。
    *   **单目标 QA：** NQ, TriviaQA, PopQA, Bamboogle, Musique 等。
    *   **深度研究任务（Deep Research）：** GAIA, Frames, WebWalkerQA（测试 OOD 泛化能力）。
*   **对比方法（Baselines）：**
    *   基础模型：Qwen2.5-7B (ReAct)。
    *   强化学习基准：ReSearch, DeepResearcher, GRPO (无记忆版)。
    *   记忆相关基准：A-MEM (基于 RAG), MEM1 (基于 RL)。

### 4. 资源与算力
*   **模型规模：** 统一使用 **Qwen2.5-7B** 作为基础模型。
*   **训练细节：** 
    *   **SFT 阶段：** 使用 GPT-4 生成约 10k 条包含记忆动作的轨迹进行微调。
    *   **RL 阶段：** 组大小（Group Size）为 16，Batch Size 为 128，学习率 1e-6。
*   **算力说明：** 论文**未明确列出**具体的 GPU 型号（如 A100/H100）数量及总训练时长，但提到了使用组相对策略优化（GRPO）以减少显存开销。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了从简单 QA 到极其复杂的 10 目标搜索任务，并在本地 Wiki 和真实 Web 环境下均进行了测试。
*   **消融实验：** 针对“记忆奖励设计”和“上下文保留策略（1步 vs 3步 vs 全文）”做了专门对比。
*   **充分性评价：** 实验设计较为充分。通过统计记忆内容的条件概率分布，客观证明了 MemPO 确实提升了记忆的信息密度。对比实验涵盖了当前主流的 Agent RL 框架，具有较强的说服力。

### 6. 主要结论与发现
*   **性能领先：** 在多目标任务中，MemPO 的 F1 分数比基础模型提升 25.98%，比之前的 SOTA (MEM1) 提升 7.1%。
*   **极致减耗：** 相比 ReSearch 等方法，Token 消耗降低了 **67.58% - 73.12%**，且任务越复杂，减耗效果越明显。
*   **克服注意力稀释：** 实验发现，在长时程任务中，保留“全上下文”反而不如保留“精简记忆”效果好，验证了长上下文对模型推理的负面干扰。
*   **泛化性：** 在未见过的 Web 搜索和深度研究任务（GAIA）中，MemPO 表现出良好的零样本迁移能力。

### 7. 优点
*   **主动性：** 将记忆管理从“外部插件”变为“内在能力”，实现了记忆与任务目标的端到端对齐。
*   **高效性：** 显著降低了长程 Agent 的运行成本，使其在有限窗口内处理无限轮次交互成为可能。
*   **奖励设计巧妙：** 利用条件概率作为记忆有效性的度量，解决了长程任务中稀疏奖励难以指导中间步骤的问题。

### 8. 不足与局限
*   **动作格式依赖：** 性能高度依赖于模型遵循 `<mem>` 等特定格式的能力，若格式崩溃则系统失效。
*   **计算偏差：** 论文提到，由于不同步骤调用的工具不同，记忆的信息含量天然存在差异，这可能在组内优势计算时引入偏差（虽然引入了偏置项 $\epsilon$ 缓解）。
*   **算力细节缺失：** 未披露具体训练资源消耗，难以评估该方法的训练成本。
*   **实时性：** 增加 `<mem>` 总结步骤会带来额外的推理延迟（Time-to-First-Token 之后的部分）。

（完）
