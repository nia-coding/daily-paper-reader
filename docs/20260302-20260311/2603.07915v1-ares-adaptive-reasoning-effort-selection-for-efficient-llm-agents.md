---
title: "Ares: Adaptive Reasoning Effort Selection for Efficient LLM Agents"
title_zh: Ares：面向高效大语言模型智能体的自适应推理开销选择
authors: "Jingbo Yang, Bairu Hou, Wei Wei, Yujia Bao, Shiyu Chang"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.07915v1"
tags: ["query:ai-ux"]
score: 9.0
evidence: LLM 智能体的动态推理努力选择
tldr: Ares 针对 LLM 智能体推理成本高昂的问题，提出了一种动态推理力度选择框架。通过轻量级路由预测每一步任务所需的最低推理级别（如高/中/低），在保证任务成功率的同时显著降低推理 token 消耗。该方法通过自动化的数据生成流水线训练路由，实现了在多种复杂智能体任务中的高效推理。
selection_source: fresh_fetch
motivation: 现有的 LLM 智能体在所有步骤均使用高强度推理会导致巨大的计算成本，而静态或随机的推理力度选择无法兼顾效率与准确性。
method: 提出 Ares 框架，利用轻量级路由器根据交互历史动态预测每步所需的最低推理级别，并配套开发了用于识别最小推理力度的训练数据生成流水线。
result: "在 TAU-Bench、BrowseComp-Plus 和 WebArena 等多个基准测试中，Ares 在保持任务成功率基本不变的前提下，将推理 token 使用量降低了高达 52.7%。"
conclusion: Ares 证明了按需分配推理资源是提升 LLM 智能体效率的有效途径，为构建高性能且低成本的自主智能体提供了可插拔的解决方案。
---

## 摘要
由具备思考能力的大语言模型（LLM）驱动的现代智能体通过长链条思维推理实现了高准确率，但也产生了巨大的推理成本。虽然许多 LLM 现在支持可配置的推理级别（例如：高/中/低），但静态策略通常效果不佳：在每一步都使用低开销模式会导致性能显著下降，而随机选择则无法保证准确率或提供实质性的成本降低。然而，智能体应当将高推理开销保留给困难步骤（如导航复杂的网站结构），而在简单步骤（如打开目标 URL）中使用低开销模式。在本文中，我们提出了 Ares，这是一个专为多步智能体任务量身定制的逐步动态推理开销选择框架。Ares 采用轻量级路由，根据交互历史预测每一步最合适的最低推理级别。为了训练该路由，我们开发了一个数据生成流水线，用于识别成功完成步骤所需的最小推理开销。随后，我们对路由进行微调以预测这些级别，从而实现对任何 LLM 智能体的即插即用集成。我们在多种智能体任务上评估了 Ares，包括用于工具使用智能体的 TAU-Bench、用于深度研究智能体的 BrowseComp-Plus 以及用于 Web 智能体的 WebArena。实验结果表明，与固定的高开销推理相比，Ares 将推理 Token 使用量减少了高达 52.7%，同时对任务成功率的影响微乎其微。

## Abstract
Modern agents powered by thinking LLMs achieve high accuracy through long chain-of-thought reasoning but incur substantial inference costs. While many LLMs now support configurable reasoning levels (e.g., high/medium/low), static strategies are often ineffective: using low-effort modes at every step leads to significant performance degradation, while random selection fails to preserve accuracy or provide meaningful cost reduction. However, agents should reserve high reasoning effort for difficult steps like navigating complex website structures, while using lower-effort modes for simpler steps like opening a target URL. In this paper, we propose Ares, a framework for per-step dynamic reasoning effort selection tailored for multi-step agent tasks. Ares employs a lightweight router to predict the lowest appropriate reasoning level for each step based on the interaction history. To train this router, we develop a data generation pipeline that identifies the minimum reasoning effort required for successful step completion. We then fine-tune the router to predict these levels, enabling plug-and-play integration for any LLM agents. We evaluate Ares on a diverse set of agent tasks, including TAU-Bench for tool use agents, BrowseComp-Plus for deep-research agents, and WebArena for web agents. Experimental results show that Ares reduces reasoning token usage by up to 52.7% compared to fixed high-effort reasoning, while introducing minimal degradation in task success rates.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **Ares** 的框架，旨在解决大语言模型（LLM）智能体在执行多步任务时推理成本过高的问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
现代“思考型”大语言模型（如 GPT-5、Gemini-3、DeepSeek-R1 等）通过长链条思维（CoT）推理显著提升了智能体在复杂任务中的表现。然而，这种能力的提升伴随着巨大的推理成本（Token 消耗）。
*   **现状：** 现有的模型通常提供可配置的推理级别（如高/中/低），但用户往往只能采取静态策略（如每步都用高推理或每步都用低推理）。
*   **痛点：** 静态策略效率低下。简单步骤（如打开 URL）使用高推理是浪费，而复杂步骤（如导航复杂网页）使用低推理会导致任务失败。
*   **目标：** 实现**逐步动态推理开销选择**，在保证任务成功率的前提下，最大限度地降低计算成本。

### 2. 方法论
Ares 引入了一个轻量级的**路由器（Router）**，在智能体执行每一步操作前，预测所需的最低推理级别。
*   **核心思想：** 将推理力度的选择建模为一个序列决策问题，通过识别每一步任务的“最小充分推理力度”来训练路由器。
*   **关键技术细节：**
    1.  **数据生成流水线（三阶段）：**
        *   **轨迹收集：** 使用高推理模式收集成功的任务轨迹，筛选出步骤最精简的路径作为基准。
        *   **推理力度标注：** 对基准轨迹的每一步，分别测试不同推理级别（高/中/低），通过多次采样和 LLM 判定，找出能稳定产生正确动作的最低级别。
        *   **理由（Rationale）生成：** 使用强大的教师模型（如 GPT-5）为标注的标签生成解释，说明为何该步骤需要特定的推理力度。
    2.  **训练策略：**
        *   **监督微调（SFT）：** 训练轻量级模型（如 Qwen3-1.7B）学习“环境观察 -> 理由 -> 推理级别”的映射。
        *   **强化学习（RL）：** 使用 **GRPO 算法**进一步优化。奖励函数综合考虑了任务成功率（高权重）、推理成本（负向奖励）和格式规范。
    3.  **KV 缓存复用：** 由于是在同一模型的不同模式间切换，Ares 允许复用 KV Cache，避免了多模型路由带来的额外编码开销。

### 3. 实验设计
*   **数据集/场景：**
    *   **TAU-Bench：** 评估工具使用能力（零售和航空领域对话）。
    *   **BrowseComp-Plus：** 评估深度研究和信息检索能力。
    *   **WebArena：** 评估复杂的网页导航和真实环境交互能力。
*   **基准模型（Backbone）：** 主要使用 `gpt-oss-20b` 作为智能体骨干模型。
*   **对比方法：**
    *   **规则基准：** 固定低、中、高推理力度。
    *   **随机策略：** 每步随机选择推理级别。
    *   **提示词路由（Prompting-based）：** 使用 GPT-5 或 Gemini-3 Pro 作为路由器直接进行预测。

### 4. 资源与算力
*   **模型规模：** 路由器采用 Qwen3-1.7B，属于极轻量级模型。
*   **训练细节：** SFT 阶段训练 3 个 epoch，学习率 5e-6；RL 阶段训练 5 个 epoch，学习率 1.5e-6。
*   **算力说明：** 论文**未明确说明**具体的 GPU 型号、数量或总训练时长。但从模型规模（1.7B）和训练参数来看，其训练成本相对较低。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了三大类完全不同的智能体任务，每类任务都有独立的测试集。
*   **消融实验：** 针对“是否有 SFT”、“是否有理由生成”、“RL 奖励函数是否归一化”等关键变量进行了详细对比。
*   **泛化性测试：** 将在 20B 模型上训练的路由器直接应用于 120B 模型，验证了跨规模的泛化能力。
*   **评价：** 实验设计较为全面，对比了从静态到动态、从轻量级到闭源大模型的多种方案，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **显著降本：** 与固定高推理策略相比，Ares 在 TAU-Bench 上减少了 **52.7%** 的推理 Token 消耗。
*   **性能保持：** 在大幅降本的同时，任务成功率几乎没有下降，甚至在 WebArena 等任务中因减少了“过度思考（Overthinking）”而略有提升。
*   **RL 的必要性：** 强化学习阶段能有效纠正 SFT 阶段的贪婪偏差，帮助路由器发现更优的效率-准确率平衡点。
*   **动作相关性：** 实验发现，涉及“回退（Go back）”或“分支（Branch）”等纠错/重规划动作时，路由器会自发选择高推理力度。

### 7. 优点
*   **即插即用：** 作为一个独立的轻量级路由器，可以轻松集成到现有的智能体架构中。
*   **效率极高：** 路由器本身开销极小（1.7B），且支持 KV Cache 复用，实际部署延迟低。
*   **解决过度思考：** 发现并证明了动态调整推理力度可以避免智能体在简单任务中因逻辑漂移导致的失败。

### 8. 不足与局限
*   **依赖离线标注：** 训练数据的获取依赖于高昂的多次采样和 LLM 判定，对于新领域的迁移可能存在数据准备成本。
*   **离散级别限制：** 目前仅支持模型预设的离散推理级别（高/中/低），无法实现更细粒度的 Token 预算控制。
*   **误差传递风险：** 如果路由器在早期步骤错误地选择了低推理，可能会导致不可逆的错误累积，虽然 RL 缓解了此问题，但风险依然存在。

（完）
