---
title: "ST4VLA: Spatially Guided Training for Vision-Language-Action Models"
title_zh: ST4VLA：视觉-语言-动作模型的空间引导训练
authors: "Jinhui Ye, Fangjing Wang, Ning Gao, Junqiu Yu, Yangkun Zhu, Bin Wang, Jinyu Zhang, Weiyang Jin, Yanwei Fu, Feng Zheng, Yilun Chen, Jiangmiao Pang"
date: 2026-02-10
pdf: "https://arxiv.org/pdf/2602.10109v1"
tags: ["query:sr-ai"]
score: 10.0
evidence: 用于具身任务和电机动作的视觉-语言-动作框架
tldr: 针对大视觉语言模型（VLM）在具身智能任务中动作生成能力不足的问题，本文提出了ST4VLA框架。该框架通过空间引导训练将动作学习与空间先验对齐，包含空间定位预训练和空间引导动作后训练两个阶段，利用点、框、轨迹预测增强模型的空间感知并引导动作生成。实验表明，ST4VLA在多个机器人基准测试中刷新了SOTA，显著提升了泛化性和鲁棒性。
selection_source: fresh_fetch
motivation: 现有的视觉语言模型在处理需要将指令转化为底层电机动作的具身任务时，缺乏足够的空间感知和动作对齐能力。
method: 提出一种双系统框架，通过空间定位预训练获取空间先验，并利用空间引导的动作后训练，通过空间提示词引导模型生成更精准的动作。
result: 在SimplerEnv基准测试中，Google Robot和WidowX的性能分别提升至84.6和73.2，刷新了SOTA记录并展现出极强的泛化性。
conclusion: 研究证明了可扩展的空间引导训练是实现稳健、通用机器人学习的一条极具前景的路径。
---

## 摘要
大型视觉-语言模型（VLMs）在多模态理解方面表现出色，但在扩展到具身任务时表现不佳，因为这些任务需要将指令转化为低级运动动作。我们提出了 ST4VLA，这是一个双系统视觉-语言-动作框架，利用空间引导训练将动作学习与 VLM 中的空间先验对齐。ST4VLA 包含两个阶段：(i) 空间定位预训练，通过来自网络规模和机器人特定数据的可扩展点、框和轨迹预测，使 VLM 具备可迁移的先验知识；(ii) 空间引导动作后训练，通过空间提示鼓励模型产生更丰富的空间先验，以引导动作生成。这种设计在策略学习过程中保留了空间定位能力，并促进了空间和动作目标之间的一致性优化。实验表明，ST4VLA 相比于原始 VLA 取得了显著改进，在 Google Robot 上的性能从 66.1 提升至 84.6，在 WidowX Robot 上从 54.7 提升至 73.2，在 SimplerEnv 上创下了新的最先进结果。它还展示了对未见物体和改写指令的更强泛化能力，以及在现实环境设置中对长程扰动的鲁棒性。这些结果凸显了可扩展的空间引导训练是实现鲁棒、泛化机器人学习的一个极具前景的方向。源代码、数据和模型已发布在 https://internrobotics.github.io/internvla-m1.github.io/。

## Abstract
Large vision-language models (VLMs) excel at multimodal understanding but fall short when extended to embodied tasks, where instructions must be transformed into low-level motor actions. We introduce ST4VLA, a dual-system Vision-Language-Action framework that leverages Spatial Guided Training to align action learning with spatial priors in VLMs. ST4VLA includes two stages: (i) spatial grounding pre-training, which equips the VLM with transferable priors via scalable point, box, and trajectory prediction from both web-scale and robot-specific data, and (ii) spatially guided action post-training, which encourages the model to produce richer spatial priors to guide action generation via spatial prompting. This design preserves spatial grounding during policy learning and promotes consistent optimization across spatial and action objectives. Empirically, ST4VLA achieves substantial improvements over vanilla VLA, with performance increasing from 66.1 -> 84.6 on Google Robot and from 54.7 -> 73.2 on WidowX Robot, establishing new state-of-the-art results on SimplerEnv. It also demonstrates stronger generalization to unseen objects and paraphrased instructions, as well as robustness to long-horizon perturbations in real-world settings. These results highlight scalable spatially guided training as a promising direction for robust, generalizable robot learning. Source code, data and models are released at https://internrobotics.github.io/internvla-m1.github.io/

---

## 论文详细总结（自动生成）

### ST4VLA: 空间引导训练的视觉-语言-动作模型深度总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的视觉-语言模型（VLM）虽然在多模态理解上表现卓越，但在转化为具体的机器人电机动作（VLA）时存在巨大鸿沟。
*   **研究背景**：
    *   **空间先验缺失**：机器人不仅要理解指令，还需知道在3D世界中“在哪里”以及“如何”行动。
    *   **训练冲突**：直接对 VLM 进行动作微调会导致其原有的空间感知能力崩溃（灾难性遗忘）；而简单的联合训练（Co-training）则会产生梯度冲突，导致感知与控制目标难以对齐。
*   **研究动机**：通过引入“空间引导训练”，将空间先验（点、框、轨迹）显式地整合进动作学习中，实现“理解-定位-执行”的解耦与协同。

#### 2. 论文提出的方法论
ST4VLA 采用了一种**双系统（System 1 & System 2）**架构和**两阶段训练**策略：
*   **核心思想**：将任务分解为“System 2（VLM 规划器）负责空间推理”和“System 1（动作专家）负责低级控制”。
*   **关键技术细节**：
    1.  **阶段一：空间定位预训练（Spatial Grounding Pre-training）**：在 Web 规模的多模态数据和机器人特定数据上，训练 VLM 预测物体的点（Point）、边界框（Box）和视觉轨迹（Trajectory）。
    2.  **阶段二：空间引导动作后训练（Spatially Guided Action Post-training）**：
        *   **空间提示（Spatial Prompting）**：在动作训练时，通过提示词（如“找出如何执行并定位关键物体”）激活 VLM 的空间推理能力。
        *   **双系统连接**：使用轻量级查询 Transformer（Querying Transformer）连接 VLM 规划器（Qwen2.5-VL）和动作专家（基于 DiT 的扩散模型）。
        *   **梯度衰减因子**：在反向传播时对传回 VLM 的梯度进行衰减（如 0.5），以保护 VLM 的语义推理能力不被动作优化破坏。

#### 3. 实验设计
*   **数据集与场景**：
    *   **仿真环境**：SimplerEnv（包含 Google Robot 和 WidowX 平台）、LIBERO 任务集。
    *   **大规模仿真**：在 Isaac-Sim 中构建了包含 200 个任务、3000 多个物体的抓取放置基准。
    *   **真实世界**：使用 Franka FR3 机械臂和 ARX LIFT2 双臂机器人，涵盖短程抓取、长程任务（如做三明治、整理抽屉）及物理干扰测试。
*   **对比方法**：对比了 RT-1, RT-2, OpenVLA, π0, GR00T N1.5, CogACT, Magma 等当前最先进的 VLA 模型。

#### 4. 资源与算力
*   **训练算力**：
    *   使用了 **16 张 NVIDIA A100 GPU**。
    *   训练步数为 **50k 步**（约 2.5 个 Epoch）。
    *   训练时长：LIBERO 任务集微调约需 20 小时。
*   **推理算力**：在配备 **NVIDIA RTX 4080 (16GB VRAM)** 的工作站上即可运行推理。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了 SimplerEnv 的全量测试（VM/VA 轨道）。
    *   大规模仿真包含 200 个任务，每个任务 5 条随机轨迹。
    *   真实世界评估包含 300 次 Rollout，涵盖 5 种泛化维度。
    *   长程任务包含 22 小时的遥操作演示数据。
*   **充分性与公平性**：实验设计非常全面，不仅有标准 Benchmark，还包含了大量的消融实验（如空间数据规模的缩放定律、提示词格式影响、梯度相似度分析 PSS 等），对比基准均采用了官方报告或严格复现的数据，体现了高度的客观性和公平性。

#### 6. 论文的主要结论与发现
*   **性能突破**：ST4VLA 在 SimplerEnv 上刷新了 SOTA，Google Robot 成功率从 66.1% 升至 84.6%，WidowX 从 54.7% 升至 73.2%。
*   **空间先验的重要性**：实验证明，显式的空间引导能显著提升模型对未见物体、新指令和复杂环境的泛化能力。
*   **缩放定律**：发现空间定位数据存在“门槛效应”，当数据量超过 200 万对时，模型性能会出现爆发式增长。
*   **优化一致性**：通过 PSS 指标证明，空间引导训练能显著提高感知目标与动作目标之间的梯度对齐度（从 0.25 提升至 0.42）。

#### 7. 优点：亮点总结
*   **双系统架构**：巧妙地结合了 VLM 的高层推理能力和 DiT 的底层控制精度。
*   **空间引导策略**：通过简单的空间提示（Spatial Prompting）而非复杂的硬编码，实现了感知与动作的深度融合。
*   **强大的泛化性**：在处理“未见物体”和“改写指令”时表现出极强的鲁棒性，尤其在长程任务和物理干扰下依然稳定。
*   **开源贡献**：提供了完整的代码、数据和模型，具有很高的社区价值。

#### 8. 不足与局限
*   **感知局限**：在极端拥挤的场景下，模型仍可能出现误识别或错误的抓取位姿预测。
*   **模态依赖**：目前主要依赖 RGB 图像，未充分利用深度信息或触觉反馈，这在处理透明物体或精细操作时可能受限。
*   **数据成本**：虽然使用了大量合成数据，但高质量的空间标注数据（尤其是轨迹标注）的获取成本依然较高。

（完）
