---
title: "ADM-DP: Adaptive Dynamic Modality Diffusion Policy through Vision-Tactile-Graph Fusion for Multi-Agent Manipulation"
title_zh: ADM-DP：基于视觉-触觉-图融合的多智能体操作自适应动态模态扩散策略
authors: "Enyi Wang, Wen Fan, Dandan Zhang"
date: 2026-02-25
pdf: "https://arxiv.org/pdf/2602.21622v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 多智能体机器人操纵及用于协调控制的扩散策略
tldr: 针对多智能体协作操纵中协调、抓取稳定性和避障的挑战，本文提出自适应动态模态扩散策略（ADM-DP）。该框架融合了视觉（RGB与点云）、触觉（FSR反馈）和图结构（多智能体位姿）信息。通过自适应模态注意力机制动态调整权重，并采用解耦训练范式，在保持智能体独立性的同时实现高效协作。实验表明，该方法在多项任务中显著优于现有基准，提升了复杂环境下的操纵鲁棒性。
selection_source: fresh_fetch
motivation: 解决多智能体协作操纵中存在的协调困难、抓取不稳定以及共享工作空间内的碰撞规避问题。
method: 提出一种融合视觉、触觉和图编码的扩散策略，利用自适应注意力机制动态分配模态权重，并结合解耦训练实现多智能体协作。
result: "在七项多智能体任务中，ADM-DP 相比现有最先进基准实现了 12% 至 25% 的性能提升。"
conclusion: 通过多模态信息的动态融合与解耦协作，ADM-DP 有效增强了多智能体系统在复杂操纵场景下的感知能力和任务成功率。
---

## 摘要
由于在共享工作空间中对协调性、抓取稳定性和避障的综合需求，多智能体机器人操作仍然具有挑战性。为了应对这些挑战，我们提出了自适应动态模态扩散策略（ADM-DP），这是一个集成视觉、触觉和基于图（多智能体姿态）模态以实现协调控制的框架。ADM-DP 引入了四项关键创新：首先，增强型视觉编码器通过特征线性调制（FiLM）融合 RGB 和点云特征以丰富感知；其次，触觉引导的抓取策略利用力敏电阻（FSR）反馈来检测接触不足并触发纠正性抓取优化，从而提高抓取稳定性；第三，基于图的碰撞编码器利用多个智能体的共享工具中心点（TCP）位置作为结构化运动学上下文，以维持空间意识并减少智能体间的干扰；第四，自适应模态注意力机制（AMAM）根据任务上下文动态重新加权模态，实现灵活融合。为了提高可扩展性和模块化，该框架采用了解耦训练范式，使智能体在共享空间信息的同时学习独立策略，从而在保持低相互依赖性的同时保留集体意识。在七个多智能体任务中，ADM-DP 相比最先进的基准模型实现了 12-25% 的性能提升。消融研究表明，在需要多种感知模态的任务中改进最为显著，验证了我们的自适应融合策略，并证明了其在多种操作场景下的鲁棒性。

## Abstract
Multi-agent robotic manipulation remains challenging due to the combined demands of coordination, grasp stability, and collision avoidance in shared workspaces. To address these challenges, we propose the Adaptive Dynamic Modality Diffusion Policy (ADM-DP), a framework that integrates vision, tactile, and graph-based (multi-agent pose) modalities for coordinated control. ADM-DP introduces four key innovations. First, an enhanced visual encoder merges RGB and point-cloud features via Feature-wise Linear Modulation (FiLM) modulation to enrich perception. Second, a tactile-guided grasping strategy uses Force-Sensitive Resistor (FSR) feedback to detect insufficient contact and trigger corrective grasp refinement, improving grasp stability. Third, a graph-based collision encoder leverages shared tool center point (TCP) positions of multiple agents as structured kinematic context to maintain spatial awareness and reduce inter-agent interference. Fourth, an Adaptive Modality Attention Mechanism (AMAM) dynamically re-weights modalities according to task context, enabling flexible fusion. For scalability and modularity, a decoupled training paradigm is employed in which agents learn independent policies while sharing spatial information. This maintains low interdependence between agents while retaining collective awareness. Across seven multi-agent tasks, ADM-DP achieves 12-25% performance gains over state-of-the-art baselines. Ablation studies show the greatest improvements in tasks requiring multiple sensory modalities, validating our adaptive fusion strategy and demonstrating its robustness for diverse manipulation scenarios.

---

## 论文详细总结（自动生成）

以下是对论文《ADM-DP: Adaptive Dynamic Modality Diffusion Policy through Vision-Tactile-Graph Fusion for Multi-Agent Manipulation》的深度结构化总结：

### 1. 核心问题与研究背景
多智能体机器人操纵面临三大核心挑战：**协调性（Coordination）**、**抓取稳定性（Grasp Stability）**以及共享空间内的**碰撞规避（Collision Avoidance）**。
*   **研究动机：** 现有的基于扩散策略（Diffusion Policy）的方法多针对单臂或视觉中心化场景。在多臂协作中，简单的视觉反馈难以应对遮挡和精细接触需求；而传统的“静态融合”策略（如直接拼接所有传感器数据）会导致在任务不同阶段引入大量噪声（例如：接近物体时触觉信号为零，却仍占用计算资源并干扰决策）。
*   **核心目标：** 开发一种能够根据任务上下文动态调整感知重点、具备空间避障意识且能通过触觉自我修正抓取的多智能体协作框架。

### 2. 方法论与关键技术
论文提出了 **ADM-DP** 框架，其核心包含四个创新组件：
*   **增强型视觉编码器：** 使用 **FiLM（特征线性调制）** 融合 RGB 图像（提取语义）和点云（提取几何空间信息）。通过点云特征生成的仿射变换参数来调制图像特征，增强了对遮挡环境的鲁棒性。
*   **触觉引导抓取策略：** 引入了一种主动修正机制。在数据采集阶段，特意包含 30% 的“从浅抓取到深抓取”的修正轨迹。策略学习将 FSR（力敏电阻）的弱信号与接触不足关联，从而触发纠正性的加深动作。
*   **基于图的碰撞编码器：** 利用 **GAT（图注意力网络）** 处理所有智能体的共享 TCP（工具中心点）位置。将每个末端执行器视为图节点，通过距离反比权重计算空间关系，实现轻量化的避障感知，无需建模复杂的完整运动链。
*   **自适应模态注意力机制 (AMAM)：** 这是本文的核心。通过一个带有**熵正则化（Entropy Regularization）**的门控注意力模块，动态分配视觉、触觉和图特征的权重。例如：接近阶段视觉权重高，接触阶段触觉权重上升，近距离协作时图特征权重加强。
*   **解耦训练范式：** 智能体学习独立的策略，但通过共享 TCP 信息保持集体意识。这种方式降低了状态空间的维度，提高了系统的可扩展性。

### 3. 实验设计
*   **实验场景：** 在 ManiSkill 和 RoboFactory 基础上设计了 7 项任务。
    *   **双臂任务：** 抬起障碍物 (Lift Barrier)、传递销钉 (Pass Peg)、抬起机械臂 (Lift Arm)、双机叠方块。
    *   **三臂任务：** 传递鞋子 (Pass Shoe)、纸巾拍照 (Take Photo of Tissue)、三机叠方块。
*   **基准模型 (Benchmarks)：** 
    *   **DP (Diffusion Policy):** 基于图像的扩散策略。
    *   **DP3 (3D Diffusion Policy):** 基于点云的扩散策略。
    *   **Flow Policy:** 基于流匹配（Flow Matching）的策略。
*   **数据规模：** 分别在 50 组和 150 组演示数据两种制度下进行评估。

### 4. 资源与算力
*   **算力说明：** 论文中**未明确说明**具体的 GPU 型号、数量及训练总时长。
*   **实现细节：** 提到使用了 DDIM 采样加速推理（仅需 20 步），观察窗口为 3 帧，预测动作序列长度为 8，执行前 6 个动作后重新规划。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了从双臂到三臂的跨度，任务类型包括协作传递、精密堆叠和力控抬升。
*   **消融实验：** 进行了四组详尽的消融实验（移除点云、移除触觉、移除图编码、移除 AMAM 动态融合），验证了每个模块的必要性。
*   **客观性：** 实验在不同数据量（50 vs 150）下均显示出一致的领先优势，且通过可视化注意力权重的变化证明了 AMAM 的有效性，实验设计较为全面、客观。

### 6. 主要结论与发现
*   **性能提升：** ADM-DP 在所有任务中均优于基准模型，平均成功率比最强基准高出 **12% - 25%**。
*   **数据效率：** 在 50 组演示的低数据量下，ADM-DP 的领先优势更明显（平均提升 13.3%），说明多模态融合提高了学习效率。
*   **动态适应：** AMAM 成功实现了感知焦点的自动切换（如：抓取瞬间触觉权重从接近 0 跃升至 0.35），有效抑制了无关模态的噪声。
*   **避障效果：** 引入图编码后，在 Pass Peg 等近距离协作任务中，碰撞率显著降低。

### 7. 优点与亮点
*   **动态融合思想：** 解决了多模态学习中“始终开启所有传感器”导致的噪声干扰问题，模仿了人类在不同阶段关注不同感官的行为。
*   **触觉主动化：** 将触觉从被动观察提升为主动修正信号（通过特定的数据采集协议），显著增强了抓取鲁棒性。
*   **轻量化协作：** 通过 TCP 图编码而非全量运动学建模，实现了高效的避障，具有良好的扩展性。

### 8. 不足与局限
*   **硬件细节缺失：** 缺乏训练算力和推理延迟的具体数据，难以评估其实际部署的计算成本。
*   **传感器局限：** 实验基于 FSR 阵列，虽然空间分辨率尚可，但相比于高分辨率视觉触觉传感器（如 GelSight），其捕获精细纹理和滑动细节的能力有限。
*   **仿真与现实差距：** 实验主要在仿真环境（ManiSkill）中完成，虽然提到了未来工作会部署到真机，但目前尚未展示真机在复杂动态环境下的表现。
*   **智能体规模：** 虽然扩展到了三臂，但对于更多智能体（如 5 臂以上）的极度拥挤空间，图编码的有效性和策略收敛性仍待验证。

（完）
