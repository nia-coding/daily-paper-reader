---
title: "PRECEPT: Planning Resilience via Experience, Context Engineering & Probing Trajectories A Unified Framework for Test-Time Adaptation with Compositional Rule Learning and Pareto-Guided Prompt Evolution"
title_zh: PRECEPT：通过经验、上下文工程与探测轨迹实现规划韧性——一种结合组合式规则学习与帕累托引导提示词演化的测试时自适应统一框架
authors: Arash Shahmansoori
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09641v1"
tags: ["query:ai-ux"]
score: 9.0
evidence: 大模型智能体的帕累托引导提示词演化与上下文工程
tldr: 针对LLM智能体在处理多条件规则检索、规则组合及应对陈旧知识时的局限性，本文提出PRECEPT框架。该框架结合了确定性精确匹配检索、基于贝叶斯可靠性的冲突感知记忆以及Pareto引导的提示词演化算法（COMPASS）。实验表明，PRECEPT在组合泛化、持续学习和对抗鲁棒性方面表现优异，显著提升了智能体在复杂动态环境下的规划韧性。
selection_source: fresh_fetch
motivation: 解决LLM智能体在规则检索退化、规则组合不可靠以及缺乏对陈旧或对抗性知识检测机制的问题。
method: 构建了一个集成确定性规则检索、冲突感知记忆管理和Pareto引导提示词演化的统一测试时自适应框架。
result: "在首试成功率上领先Full Reflexion 41.1个百分点，实现100%的组合任务成功率，并减少了61%的执行步骤。"
conclusion: PRECEPT通过结构化知识管理与动态演化机制，显著增强了LLM智能体在复杂任务中的适应性与鲁棒性。
---

## 摘要
以自然语言存储知识的 LLM 智能体在条件数量增加时会遭遇严重的检索性能下降，通常难以可靠地组合已学习的规则，且往往缺乏检测陈旧或对抗性知识的显式机制。我们提出了 PRECEPT，这是一个用于测试时自适应的统一框架，包含三个紧密耦合的组件：(1) 基于结构化条件键的确定性精确匹配规则检索；(2) 具有贝叶斯来源可靠性和基于阈值的规则失效机制的冲突感知记忆；(3) COMPASS，一个帕累托引导的提示词演化外环。精确检索消除了确定性路径上的部分匹配解释错误（构造上为 0%，而根据定理 B.6 的独立性模型在 N=10 时为 94.4%），并通过语义层级结构支持组合式堆叠；冲突感知记忆解决了静态与动态知识间的不一致，并支持漂移自适应；COMPASS 通过相同的端到端执行流水线评估提示词。实验结果（9-10 个种子）显示：PRECEPT 相比 Full Reflexion 取得了 +41.1pp 的首试优势 (d>1.9)，+33.3pp 的组合泛化提升 (d=1.55)，在双向物流组合上达到 100% 的 $P_1$ (d=2.64)，+40-55pp 的持续学习增益，在对抗性静态知识下表现出强大的最终鲁棒性（在对抗性 SK 激活时物流任务达到 100%；集成任务部分恢复），+55.0pp 的漂移恢复 (d=0.95, p=0.031)，且步骤减少了 61%。核心对比具有统计学显著性，通常 p < 0.001。

## Abstract
LLM agents that store knowledge as natural language suffer steep retrieval degradation as condition count grows, often struggle to compose learned rules reliably, and typically lack explicit mechanisms to detect stale or adversarial knowledge. We introduce PRECEPT, a unified framework for test-time adaptation with three tightly coupled components: (1) deterministic exact-match rule retrieval over structured condition keys, (2) conflict-aware memory with Bayesian source reliability and threshold-based rule invalidation, and (3) COMPASS, a Pareto-guided prompt-evolution outer loop. Exact retrieval eliminates partial-match interpretation errors on the deterministic path (0% by construction, vs 94.4% under Theorem~B.6's independence model at N=10) and supports compositional stacking through a semantic tier hierarchy; conflict-aware memory resolves static--dynamic disagreements and supports drift adaptation; COMPASS evaluates prompts through the same end-to-end execution pipeline.   Results (9--10 seeds): PRECEPT achieves a +41.1pp first-try advantage over Full Reflexion (d>1.9), +33.3pp compositional generalization (d=1.55), 100% $P_1$ on 2-way logistics compositions (d=2.64), +40--55pp continuous learning gains, strong eventual robustness under adversarial static knowledge (100% logistics with adversarial SK active; partial recovery on integration), +55.0pp drift recovery (d=0.95, p=0.031), and 61% fewer steps. Core comparisons are statistically significant, often at p<0.001.

---

## 论文详细总结（自动生成）

### 论文总结：PRECEPT 统一框架

#### 1. 核心问题与整体含义（研究动机和背景）
论文针对当前大语言模型（LLM）智能体在复杂规划任务中的四个核心局限性（L1-L4）：
*   **组合爆炸（Compositional Explosion）：** 面对 $2^N$ 种条件组合，传统方法难以通过有限训练覆盖所有情况。
*   **解释退化（Interpretation Degradation）：** 以自然语言存储的规则在检索时，随着条件数量增加，LLM 的理解准确率呈指数级下降。
*   **漂移盲区（Drift Blindness）：** 智能体难以识别陈旧或过时的规则，且强化学习（RL）在环境变化时需要重新训练。
*   **样本效率低下：** RL 需要数百次尝试，而实际部署要求在极少数尝试（$\beta \le 3$）内完成学习。

**研究背景：** 现有的“语言反思”（Verbal Reflection）方法依赖模糊的语义匹配，缺乏确定性保障。PRECEPT 旨在通过结构化规则学习和测试时自适应（Test-Time Adaptation），构建一个具有规划韧性的统一框架。

#### 2. 方法论：核心思想与关键技术
PRECEPT 框架由三个紧密耦合的组件组成：
*   **确定性规则检索与组合学习：**
    *   **核心思想：** 放弃纯自然语言检索，采用基于结构化条件键（Condition Keys）的 $O(1)$ 精确匹配检索。
    *   **语义层级结构（Semantic Tier Hierarchy）：** 将规则分为安全（Safety）、合规（Compliance）和偏好（Preferences）三个层级。当多个原子规则冲突时，高层级规则自动覆盖低层级规则，实现 $N$ 个原子规则对 $2^N-1$ 种组合场景的覆盖。
*   **冲突感知记忆与漂移自适应（Evo-Memory）：**
    *   **类型 I 冲突（静态 vs 动态）：** 使用 6 信号集成检测器识别文档知识与实际经验的不一致。采用**贝叶斯来源可靠性模型**（Beta 分布）和 **Thompson 采样**来决定信任哪种来源。
    *   **类型 II 冲突（环境漂移）：** 引入基于阈值的规则失效机制。规则失败时置信度减半，连续失败 $2$ 次则彻底删除规则并触发重新学习。
*   **COMPASS 提示词演化外环：**
    *   **双频控制：** 高频监控层（每步执行）进行轻量级约束检查；低频架构层（事件触发）进行提示词演化。
    *   **帕累托引导（Pareto-Guided）：** 基于成功率和步骤效率两个维度，利用 MAP-Elites 保持策略多样性，筛选非支配最优提示词。

#### 3. 实验设计
*   **实验场景/数据集：** 构建了三个具有“黑天鹅”属性的复杂域：
    1.  **物流（Logistics）：** 涉及港口关闭、海关延迟，具有严重的组件重叠陷阱。
    2.  **预订（Booking）：** 涉及航班/酒店超订，具有大规模稀疏键空间。
    3.  **集成（Integration）：** 模拟 API 故障，故意设置具有误导性的品牌名称（如 `salesforce` vs `salesforce-backup`）来测试 LLM 的先验偏差。
*   **对比方法（Baselines）：** 
    *   **Full Reflexion（增强版）：** 增加了向量数据库和 BM25 检索。
    *   **ExpeL（增强版）：** 增加了条件感知过滤和结构化洞察提取。
*   **评估指标：** 首试成功率（$P_1$）、最终成功率（$P_t$）、平均执行步骤。

#### 4. 资源与算力
*   **模型使用：** 实验统一使用 `gpt-4o-mini` 作为推理引擎，`text-embedding-3-small` 用于向量嵌入。
*   **算力说明：** 论文**未明确说明**具体的 GPU 型号、数量或总训练时长。由于该框架主要基于 API 调用和测试时自适应，其核心开销在于 LLM 的 Token 消耗而非本地模型训练。

#### 5. 实验数量与充分性
*   **实验规模：** 论文共设计了 9 组核心实验，涵盖主域对比、组合泛化、训练规模消融、持续学习、规则持久性、静态知识对抗及漂移自适应等。
*   **充分性与客观性：** 
    *   每组实验运行 9-10 个独立种子，并提供 95% 置信区间。
    *   使用了 Cohen's d 效应量和配对 t 检验（Bonferroni 校正）来验证统计显著性。
    *   实验设计考虑了对抗性设置（如注入错误的静态知识），能够客观反映系统在极端情况下的恢复能力。

#### 6. 主要结论与发现
*   **性能优势：** PRECEPT 在首试成功率上比 Full Reflexion 高出 **41.1 个百分点**，在集成域（最难任务）中优势尤为明显。
*   **组合泛化：** 仅通过学习原子规则，PRECEPT 在 2 路物流组合任务中达到了 **100% 的首试成功率**。
*   **效率提升：** 相比基准方法，PRECEPT 减少了 **61% 的执行步骤**，平均每项任务仅需约 2.3 步（接近理论最小值 2.0）。
*   **鲁棒性：** 在环境发生剧烈漂移（规则完全改变）后，PRECEPT 的恢复速度显著快于基准方法，展现了强大的“规则覆盖”能力。

#### 7. 优点
*   **结构化与确定性的结合：** 通过 $O(1)$ 检索绕过了 LLM 对复杂文本检索的解释错误，解决了 RAG 系统常见的模糊匹配问题。
*   **贝叶斯冲突处理：** 引入 Thompson 采样处理知识冲突，比简单的启发式规则更具数学严谨性。
*   **自净化机制：** 规则失效后能自动从演化后的提示词中剔除，保证了长效运行的可靠性。
*   **样本效率极高：** 在 $\beta=3$（每个错误仅见 3 次）的情况下即可达到近乎完美的表现。

#### 8. 不足与局限
*   **离散化假设：** 当前框架假设条件可以被规范化为离散 Token（如 `A+B`），对于连续型特征（如重量、温度）需要预处理。
*   **合成基准限制：** 实验主要在作者设计的合成域上进行，虽然难度高于标准 Benchmark，但在真实世界超大规模规则库（如 $E > 1000$）中的扩展性仍待验证。
*   **单智能体局限：** 目前仅关注单智能体自适应，尚未涉及多智能体间的约束传播。
*   **COMPASS 增益场景：** 实验 8-9 显示，在确定性检索已经解决大部分问题时，外环提示词演化的边际增益在某些匹配模式下可能有限。

（完）
