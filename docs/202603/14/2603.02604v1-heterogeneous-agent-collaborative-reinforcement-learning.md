---
title: Heterogeneous Agent Collaborative Reinforcement Learning
title_zh: 异构智能体协作强化学习
authors: "Zhixia Zhang, Zixuan Huang, Xin Xia, Deqing Wang, Fuzhen Zhuang, Shuai Ma, Ning Ding, Yaodong Yang, Jianxin Li, Yikun Ban"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.02604v1"
tags: ["query:ai-ux"]
score: 8.0
evidence: 异构智能体协作强化学习
tldr: 本研究针对独立在线策略优化效率低下的问题，提出了异构智能体协作强化学习（HACRL）范式。该范式允许异构智能体在训练期间共享经过验证的采样数据以实现双向互助学习，而在推理时保持独立运行。基于此，作者开发了HACPO算法，通过四种专门机制解决能力差异和策略偏移问题，在推理基准测试中以一半的采样成本实现了显著的性能提升，为多智能体协同进化提供了高效方案。
selection_source: carryover_cache
motivation: 旨在解决传统强化学习中异构智能体独立训练导致的采样效率低下以及单向知识蒸馏无法实现双向互助的问题。
method: 提出了HACPO算法，通过共享采样数据并引入四种针对性机制，在理论保证下实现异构智能体间的双向协作优化。
result: "实验显示HACPO在多种推理任务中均能提升所有参与智能体的性能，相比GSPO平均提升3.3%且采样成本降低50%。"
conclusion: HACRL范式证明了异构智能体间通过双向知识共享可以显著提升强化学习的训练效率和最终性能。
---

## 摘要
我们引入了异构智能体协作强化学习（HACRL），这是一种旨在解决孤立同策略（on-policy）优化效率低下问题的新型学习范式。HACRL 实现了独立执行下的协作优化：异构智能体在训练期间共享经过验证的展开（rollouts）以实现共同提升，而在推理时则独立运行。与基于大语言模型（LLM）的多智能体强化学习（MARL）不同，HACRL 不需要协同部署；与同/异策略蒸馏不同，它实现了异构智能体之间的双向互助学习，而非单向的师生传递。基于这一范式，我们提出了 HACPO，这是一种协作强化学习算法，通过原则性的展开共享来最大化样本利用率和跨智能体知识迁移。为了缓解能力差异和策略分布偏移，HACPO 引入了四种定制机制，并在无偏优势估计和优化正确性方面提供了理论保证。在多种异构模型组合和推理基准测试上的广泛实验表明，HACPO 持续提升了所有参与智能体的性能，在仅使用一半展开成本的情况下，平均超越 GSPO 3.3%。

## Abstract
We introduce Heterogeneous Agent Collaborative Reinforcement Learning (HACRL), a new learning paradigm that addresses the inefficiencies of isolated on-policy optimization. HACRL enables collaborative optimization with independent execution: heterogeneous agents share verified rollouts during training to mutually improve, while operating independently at inference time. Unlike LLM-based multi-agent reinforcement learning (MARL), HACRL does not require coordinated deployment, and unlike on-/off-policy distillation, it enables bidirectional mutual learning among heterogeneous agents rather than one-directional teacher-to-student transfer. Building on this paradigm, we propose HACPO, a collaborative RL algorithm that enables principled rollout sharing to maximize sample utilization and cross-agent knowledge transfer. To mitigate capability discrepancies and policy distribution shifts, HACPO introduces four tailored mechanisms with theoretical guarantees on unbiased advantage estimation and optimization correctness. Extensive experiments across diverse heterogeneous model combinations and reasoning benchmarks show that HACPO consistently improves all participating agents, outperforming GSPO by an average of 3.3\% while using only half the rollout cost.

---

## 论文详细总结（自动生成）

以下是对论文《Heterogeneous Agent Collaborative Reinforcement Learning》（HACRL）的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：在带验证奖励的强化学习（RLVR）中，如训练数学推理模型，昂贵的“在线采样”和“结果验证”是主要瓶颈。
*   **核心问题**：现有的 RL 优化（如 GRPO、GSPO）通常让不同模型独立训练，导致采样资源浪费。此外，现有的知识迁移方法（如蒸馏）多为单向（老师到学生），且难以处理架构、分词器或规模完全不同的“异构”模型。
*   **整体含义**：论文提出了 **HACRL** 范式，旨在让多个异构智能体在训练时共享彼此生成的采样数据（Rollouts），实现双向互助学习，而在推理时依然保持独立运行，从而在降低采样成本的同时突破单一模型的性能瓶颈。

### 2. 方法论：HACPO 算法
核心思想是**“训练时协作优化，推理时独立执行”**。为了解决异构模型间的能力差异和分布偏移，提出了 **HACPO** 算法，包含四个关键技术：
*   **智能体能力感知优势估计（Agent-Capability-Aware Advantage Estimation）**：针对异构智能体能力不同的现状，通过滑动窗口计算各智能体的实时表现比率（$\omega$），动态调整优势估计的基准（Baseline），确保优势计算在统计上对所有参与者是无偏的。
*   **模型能力差异系数（Model Capabilities Discrepancy Coefficient）**：在梯度更新时，利用能力比率作为调制因子。这使得模型会更积极地学习比自己强的智能体生成的样本，而保守地对待较弱智能体的样本。
*   **指数重要性采样（Exponential Importance Sampling）**：引入非梯度的指数重缩放因子（$\alpha$），修正异构模型间巨大的策略分布差异，防止跨智能体更新过于激进。
*   **步进式裁剪（Stepwise Clipping）**：采用非对称裁剪边界，并随训练步数动态收紧。这能防止跨智能体样本在训练后期主导梯度更新，增强训练稳定性。

### 3. 实验设计
*   **数据集与场景**：主要针对数学推理任务。使用 MATH 数据集（7.5k 题目）进行训练。
*   **评估基准（Benchmarks）**：涵盖 7 个挑战性指标：MATH-500、MATH、GSM8K、AIME2025、AMC23、Minerva 和 Olympiad。
*   **异构设置**：
    1.  **异构状态**：相同架构不同微调阶段（如 Qwen3-4B vs 4B-Instruct）。
    2.  **异构规模**：同系列不同参数量（如 Qwen3-1.7B vs 4B）。
    3.  **异构模型**：完全不同的家族（如 Qwen3-4B vs Llama3.2-3B）。
*   **对比方法**：
    *   **GRPO/GSPO**：标准的单智能体同策略 RL。
    *   **GSPO ×2**：资源对等基准（双倍采样和更新次数），用于排除数据量增加带来的干扰。
    *   **Naive**：简单的多智能体共享采样，但不使用 HACPO 的四种优化机制。

### 4. 资源与算力
*   **硬件环境**：论文明确提到实验是在 **8 张 GPU** 上进行的。
*   **软件框架**：使用了开源的 `verl` 强化学习框架。
*   **训练细节**：训练通常进行 1 个 epoch，学习率设为 $1 \times 10^{-6}$。
*   **未明确说明**：文中未具体指明 GPU 的型号（如 A100 或 H100）以及具体的总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：论文在正文和附录中展示了 **6 组** 不同的异构模型组合实验，涵盖了从 1B 到 8B 的多种模型。
*   **消融实验**：针对四个核心组件（优势估计、能力系数、重要性采样、步进裁剪）分别做了详尽的消融实验，验证了每个模块的必要性。
*   **充分性与公平性**：实验设计非常严谨，特别是引入了“资源对等基准（GSPO ×2）”，证明了性能提升不仅仅是因为看了更多数据，而是因为异构模型间的“知识互补”。实验结果在多个不同难度的基准测试上保持了一致性，具有较高的客观性。

### 6. 主要结论与发现
*   **双向互助**：HACPO 不仅能让弱模型变强，强模型也能从弱模型的“错误路径”或“独特探索”中获益。
*   **高效性**：相比于最先进的 GSPO，HACPO 在**仅使用一半采样成本**的情况下，平均准确率提升了 **3.3%**。
*   **稳定性**：通过步进式裁剪和指数 IS，成功解决了异构模型间因分词器和架构不同导致的训练不稳定问题。
*   **理论验证**：论文从理论上证明了其优势估计的无偏性以及异构梯度方向与同策略优化方向的一致性。

### 7. 优点（亮点）
*   **范式创新**：打破了单向蒸馏的局限，提出了异构智能体“协同进化”的新模式。
*   **极高的实用价值**：在 LLM 训练成本极高的背景下，通过共享 Rollouts 显著提升了样本效率。
*   **兼容性强**：能够处理跨家族模型（如 Qwen 和 Llama）的协作，这在以往的研究中较少见。
*   **理论与实践结合**：不仅有显著的实验提升，还提供了完备的数学证明。

### 8. 不足与局限
*   **超参数敏感性**：消融实验显示，指数因子 $\alpha$ 的最优值在不同模型组合间存在差异，可能需要针对特定组合进行调优。
*   **任务局限**：目前实验主要集中在具有“可验证奖励”的数学推理任务上，对于缺乏自动验证器的通用对话或创意写作任务，该方法的适用性尚未验证。
*   **通信开销**：虽然节省了采样算力，但在多智能体协作训练时，模型间的 Rollouts 传输和分词转换（Retokenization）会带来额外的通信和计算开销，文中未详细量化这一成本。

（完）
