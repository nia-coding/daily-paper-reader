---
title: Structure-aware Prompt Adaptation from Seen to Unseen for Open-Vocabulary Compositional Zero-Shot Learning
title_zh: 面向开放词汇组合零样本学习的从已见向未见的结构感知提示自适应
authors: "Yihang Duan, Jiong Wang, Pengpeng Zeng, Ji Zhang, Lei Zhao, Chong Wang, Jingkuan Song, Lianli Gao"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.03815v1"
tags: ["query:ai-ux"]
score: 8.0
evidence: 零样本学习中的提示词适配与微调
tldr: 本文针对开放词汇组合零样本学习（OV-CZSL）中模型难以泛化到未见属性和对象的问题，提出了一种结构感知提示适配（SPA）方法。该方法受人类类比学习启发，利用语义相关概念在嵌入空间中形成的局部结构一致性，通过训练阶段的结构一致性损失（SCL）和推理阶段的结构引导适配策略（SAS），实现了从已知到未知概念的有效迁移。SPA 是一种即插即用的方法，在保持闭集性能的同时显著提升了开集识别效果。
selection_source: carryover_cache
motivation: 现有的提示微调方法在处理开放词汇场景下未见的属性和对象时泛化能力不足，而人类通常能通过类比相似语义的已知概念来推断未知概念。
method: 提出 SPA 方法，包含在训练阶段通过结构一致性损失（SCL）约束局部结构，以及在推理阶段利用结构引导适配策略（SAS）将未见概念与已知概念的结构进行对齐。
result: 在 OV-CZSL 基准测试上的实验表明，SPA 在保持竞争力的闭集性能的同时，显著提升了模型在开放词汇设置下的表现。
conclusion: SPA 是一种通用的即插即用方法，通过利用嵌入空间的局部结构一致性，有效解决了组合零样本学习向开放词汇场景迁移的挑战。
---

## 摘要
开放词汇组合零样本学习（OV-CZSL）的目标是在开放词汇设置下识别属性-物体组合，其中会对已见和未见属性及物体的组合进行评估。最近，提示微调（prompt tuning）方法在封闭设置（即组合零样本学习 CZSL，仅评估已见属性和物体的组合）中展示了强大的泛化能力。然而，直接将这些方法应用于 OV-CZSL 可能不足以泛化到未见的属性、物体及其组合，因为它受限于已见的属性和物体。通常，当面对未见概念时，人类会利用具有相似语义的已见概念进行类比，从而推断其含义（例如，“湿的”和“潮湿的”，“衬衫”和“夹克”）。在本文中，我们通过实验证明，语义相关的属性或物体在嵌入空间中的分布倾向于形成一致的局部结构。基于上述结构，我们提出了结构感知提示自适应（SPA）方法，使模型能够从已见属性和物体泛化到未见属性和物体。具体而言，在训练阶段，我们设计了一种结构感知一致性损失（SCL），在每次迭代中鼓励已见属性和物体的局部结构一致性。在推理阶段，我们设计了一种结构引导自适应策略（SAS），将未见属性和物体的结构与具有相似语义的已训练已见属性和物体的结构进行自适应对齐。值得注意的是，SPA 是一种即插即用的方法，可以无缝集成到现有的 CZSL 提示微调方法中。在 OV-CZSL 基准测试上的大量实验表明，SPA 在取得具有竞争力的封闭集性能的同时，显著提升了开放词汇结果。

## Abstract
The goal of Open-Vocabulary Compositional Zero-Shot Learning (OV-CZSL) is to recognize attribute-object compositions in the open-vocabulary setting, where compositions of both seen and unseen attributes and objects are evaluated. Recently, prompt tuning methods have demonstrated strong generalization capabilities in the closed setting, where only compositions of seen attributes and objects are evaluated, i.e., Compositional Zero-Shot Learning (CZSL). However, directly applying these methods to OV-CZSL may not be sufficient to generalize to unseen attributes, objects and their compositions, as it is limited to seen attributes and objects. Normally, when faced with unseen concepts, humans adopt analogies with seen concepts that have the similar semantics thereby inferring their meaning (e.g., "wet" and "damp", "shirt" and "jacket"). In this paper, we experimentally show that the distribution of semantically related attributes or objects tends to form consistent local structures in the embedding space. Based on the above structures, we propose Structure-aware Prompt Adaptation (SPA) method, which enables models to generalize from seen to unseen attributes and objects. Specifically, in the training stage, we design a Structure-aware Consistency Loss (SCL) that encourages the local structure's consistency of seen attributes and objects in each iteration. In the inference stage, we devise a Structure-guided Adaptation Strategy (SAS) that adaptively aligns the structures of unseen attributes and objects with those of trained seen attributes and objects with similar semantics. Notably, SPA is a plug-and-play method that can be seamlessly integrated into existing CZSL prompt tuning methods. Extensive experiments on OV-CZSL benchmarks demonstrate that SPA achieves competitive closed-set performance while significantly improving open-vocabulary results.

---

## 论文详细总结（自动生成）

以下是对论文《Structure-aware Prompt Adaptation from Seen to Unseen for Open-Vocabulary Compositional Zero-Shot Learning》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的组合零样本学习（CZSL）主要关注“已见属性+已见物体”的新组合，而**开放词汇组合零样本学习（OV-CZSL）**挑战更大，要求模型识别包含**未见属性**或**未见物体**的组合。
*   **研究动机**：
    *   虽然基于 CLIP 的提示微调（Prompt Tuning）在传统 CZSL 中表现优异，但直接迁移到 OV-CZSL 时，模型往往会过度拟合已见概念，导致对未见概念的泛化能力差。
    *   **人类认知启发**：人类通过类比来理解新概念（如通过“湿的”理解“潮湿的”）。作者发现，语义相关的概念在 CLIP 嵌入空间中具有稳定的**局部结构一致性**，这种结构在微调前后应得到保留并用于引导未见概念的学习。

### 2. 方法论：结构感知提示自适应（SPA）
SPA 是一种即插即用的框架，包含两个核心组件：
*   **结构感知一致性损失（SCL，训练阶段）**：
    *   **核心思想**：防止微调过程破坏 CLIP 原有的语义空间结构。
    *   **技术细节**：首先计算已见属性/物体在原始 CLIP 空间中的 Top-K 近邻。在训练迭代中，通过 **KL 散度**约束微调后的嵌入向量与其原始近邻之间的相似度分布，确保局部语义邻域的稳定性。
*   **结构引导自适应策略（SAS，推理阶段）**：
    *   **核心思想**：利用已见概念的“学习经验”来校准未见概念。
    *   **技术细节**：对于一个未见的属性或物体，找到其在已见集合中的 Top-K 语义近邻。计算这些近邻在训练过程中产生的**参数偏移量（Shift）**，并根据语义相似度进行加权聚合，将该偏移量补偿给未见概念的提示词（Prompt），从而实现从已见向未见的知识迁移。

### 3. 实验设计
*   **数据集**：在四个主流基准上进行了评估：**MIT-States**、**C-GQA**、**VAW-CZSL**（大规模）和 **UT-Zappos**（细粒度鞋类）。
*   **Benchmark 设置**：采用了 Saini 等人提出的 OV-CZSL 划分协议，将测试集分为已见组合（AO）、已见概念新组合（(AO)*）、含未见属性组合（A*O）、含未见物体组合（AO*）以及全未见组合（A*O*）。
*   **对比方法**：
    *   **传统范式**：LE, CompCos, OADis, SCEN, CANet 等。
    *   **VLM 提示微调范式（作为 Baseline）**：CSP, HPL, DFSP, Troika。SPA 被集成到这些方法中以验证其通用性。

### 4. 资源与算力
*   **模型骨干**：统一使用 **CLIP ViT-B/32**。
*   **超参数**：学习率设为 $5 \times 10^{-5}$ 或 $1 \times 10^{-5}$，使用 AdamW 优化器，训练 20 个 epoch。
*   **算力说明**：论文**未明确提及**具体的 GPU 型号和数量。但作者提供了计算开销分析，指出 SPA 增加的训练时间仅约 5.2%（约 1 分钟），推理延迟增加极低（0.02ms/图），表明该方法对算力需求非常友好。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从细粒度到大规模的 4 个数据集，对比了 4 种不同的 CLIP 提示微调基准方法。
*   **消融实验**：详细分析了 SCL 和 SAS 各自的贡献、近邻数 $K$ 的敏感性、损失权重 $\lambda$ 和温度系数 $\tau$ 的影响。
*   **客观性**：通过在多个基准上取得一致的提升（尤其是最难的 A*O* 任务），证明了方法的有效性。实验对比遵循了相同的骨干网络和评估协议，确保了公平性。

### 6. 主要结论与发现
*   **性能提升**：SPA 在所有数据集上均显著提升了 OV-CZSL 的性能。例如在 C-GQA 的全未见组合（A*O*）上，相对提升高达 **55.1%**。
*   **即插即用**：SPA 可以无缝集成到现有的各种提示微调方法中，且均能带来增益。
*   **结构价值**：实验证实了保留 CLIP 原始局部结构对于零样本泛化的重要性，简单的类比机制（SAS）能有效缓解未见概念的表示偏差。

### 7. 优点
*   **高效性**：计算开销极小，不改变模型架构，仅通过损失函数和推理时的参数补偿实现。
*   **泛化性强**：不仅在传统的 CZSL 任务上保持竞争力，在更具挑战性的开放词汇场景下表现卓越。
*   **理论动机直观**：利用类比学习和流形一致性，符合人类认知逻辑且易于实现。

### 8. 不足与局限
*   **语义鸿沟限制**：如果未见概念与所有已见概念的语义距离都非常远（即在嵌入空间中极度孤立），SAS 的类比效果会打折扣。
*   **近邻搜索开销**：虽然目前在万级词汇下表现良好，但如果未来扩展到数百万级别的超大规模词汇表，Brute-force 的近邻搜索可能成为瓶颈（尽管作者建议可用 Faiss 等加速）。
*   **视觉-语义对齐风险**：方法依赖于 CLIP 预训练空间的质量，如果原始空间中存在严重的语义偏见（如反义词聚在一起），可能会引入错误引导。

（完）
