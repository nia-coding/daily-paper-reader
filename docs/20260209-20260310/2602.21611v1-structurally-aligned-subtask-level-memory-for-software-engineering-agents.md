---
title: Structurally Aligned Subtask-Level Memory for Software Engineering Agents
title_zh: 面向软件工程智能体的结构化对齐子任务级记忆
authors: "Kangning Shen, Jingyuan Zhang, Chenxi Sun, Wencong Zeng, Yang Yue"
date: 2026-02-25
pdf: "https://arxiv.org/pdf/2602.21611v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 软件工程智能体在长程任务中的子任务级记忆
tldr: "本研究针对软件工程（SWE）智能体在长程推理中记忆粒度过粗的问题，指出传统的实例级记忆会导致检索误导。为此，提出了一种“结构化对齐的子任务级记忆”方法，通过将记忆的存取与智能体的功能分解对齐，实现更精准的经验复用。在 SWE-bench Verified 上的实验表明，该方法显著提升了不同大模型底座的性能，平均 Pass@1 提高 4.7%，证明了细粒度记忆对复杂软件开发任务的有效性。"
selection_source: fresh_fetch
motivation: 现有的实例级记忆机制由于粒度不匹配，在处理描述相似但逻辑不同的任务阶段时容易产生错误的检索引导。
method: 提出结构化对齐的子任务级记忆方法，将记忆的存储、检索和更新过程与智能体的功能分解结构相匹配。
result: "在 SWE-bench Verified 基准测试中，该方法使平均 Pass@1 提升了 4.7 个百分点，在 Gemini 2.5 Pro 上提升达 6.8 个百分点。"
conclusion: 将记忆管理细化至子任务级别，能更有效地利用过去经验来辅助软件工程智能体处理复杂的长程推理任务。
---

## 摘要
大语言模型（LLMs）在作为自主软件工程（SWE）智能体方面展现出了巨大潜力。近期的研究进一步探索了通过记忆机制增强这些智能体，以支持长程推理。然而，这些方法通常在粗粒度的实例级别运行，将整个问题解决过程视为存储和检索的原子单位。我们通过实证研究证明，实例级记忆存在根本性的粒度不匹配问题，当具有相似表面描述的任务在特定阶段需要不同的推理逻辑时，会导致错误的检索。为解决这一问题，我们提出了结构化对齐的子任务级记忆（Structurally Aligned Subtask-Level Memory），该方法将记忆的存储、检索和更新与智能体的功能分解相对齐。在 SWE-bench Verified 上的广泛实验表明，我们的方法在多种骨干模型上均一致优于原始智能体和强大的实例级记忆基线，平均 Pass@1 较原始智能体提升了 4.7 个百分点（例如，在 Gemini 2.5 Pro 上提升了 6.8 个百分点）。性能增益随交互步骤的增加而增长，表明利用过往经验有助于复杂软件工程任务中的长程推理。

## Abstract
Large Language Models (LLMs) have demonstrated significant potential as autonomous software engineering (SWE) agents. Recent work has further explored augmenting these agents with memory mechanisms to support long-horizon reasoning. However, these approaches typically operate at a coarse instance granularity, treating the entire problem-solving episode as the atomic unit of storage and retrieval. We empirically demonstrate that instance-level memory suffers from a fundamental granularity mismatch, resulting in misguided retrieval when tasks with similar surface descriptions require distinct reasoning logic at specific stages. To address this, we propose Structurally Aligned Subtask-Level Memory, a method that aligns memory storage, retrieval, and updating with the agent's functional decomposition. Extensive experiments on SWE-bench Verified demonstrate that our method consistently outperforms both vanilla agents and strong instance-level memory baselines across diverse backbones, improving mean Pass@1 over the vanilla agent by +4.7 pp on average (e.g., +6.8 pp on Gemini 2.5 Pro). Performance gains grow with more interaction steps, showing that leveraging past experience benefits long-horizon reasoning in complex software engineering tasks.

---

## 论文详细总结（自动生成）

### 论文总结：面向软件工程智能体的结构化对齐子任务级记忆

#### 1. 核心问题与整体含义（研究动机和背景）
大语言模型（LLM）在处理复杂的软件工程（SWE）任务时，通常需要多轮交互和长程推理。现有的记忆增强机制多采用**实例级（Instance-level）**粒度，即将整个问题的解决过程作为一个原子单位进行存储和检索。
*   **研究动机**：作者发现实例级记忆存在“粒度不匹配”问题。
    *   **误导性检索**：表面描述相似的任务（如“修复登录按钮”与“修复登录超时”）在具体执行阶段（如前端 UI vs 后端服务）所需的推理逻辑截然不同，全局相似性会导致引入无关干扰。
    *   **经验浪费**：表面不相关的任务可能在某个子任务（如“更新事件监听器”）上共享相同的操作逻辑，但实例级检索无法捕获这种局部可复用的技能。
*   **核心目标**：通过将记忆操作与智能体的功能分解（子任务）相对齐，实现更精准的经验复用。

#### 2. 方法论：核心思想与关键技术
论文提出了**结构化对齐的子任务级记忆（Structurally Aligned Subtask-Level Memory）**，其核心流程如下：
*   **子任务建模与动态分割**：将推理过程划分为四个功能类别：`ANALYZE`（分析）、`REPRODUCE`（复现）、`EDIT`（编辑）、`VERIFY`（验证）。智能体在交互过程中自主预测下一个子任务的类别和意图描述。
*   **结构化记忆状态**：记忆条目存储为三元组 $m = (z, d, e)$：
    *   $z$：功能类别（如 EDIT）。
    *   $d$：局部意图描述（包含目标和关键词）。
    *   $e$：抽象经验（从轨迹中提取的可迁移见解，而非原始日志）。
*   **两阶段检索策略**：
    1.  **类别过滤（硬约束）**：仅在相同功能类别的记忆中搜索，消除跨阶段噪声。
    2.  **语义匹配**：在过滤后的集合中，根据当前意图描述 $d$ 进行向量相似度检索。
*   **经验积累与更新**：子任务完成后，利用 LLM 算子从原始轨迹中提取抽象经验，过滤掉特定仓库的噪声（如具体文件路径），保留通用策略，并更新至记忆库。

#### 3. 实验设计
*   **数据集/基准测试**：使用 **SWE-bench Verified**，包含 500 个真实的 GitHub issue。
*   **底座模型**：评估了四种主流模型：Gemini 2.5 Flash、Gemini 2.5 Pro、Claude 3.7 Sonnet 和 Claude 4.0 Sonnet。
*   **对比方法**：
    1.  **Vanilla Agent**：不带记忆的原始 Mini SWE Agent。
    2.  **Instance-level Memory**：复现了 Reasoning-Bank 方法，基于全局相似性检索实例级摘要。
*   **实验协议**：采用测试时流式处理（Streaming），记忆从零开始随任务执行实时积累。

#### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的硬件型号（如 GPU 数量）或训练时长。
*   **实现细节**：由于实验基于闭源模型 API（Gemini 和 Claude 系列），主要的计算开销在于 API 调用。作者强调，为了公平对比，子任务记忆的提取开销也被计入每个实例的总步数预算内（Budget-neutral）。

#### 5. 实验数量与充分性
*   **实验规模**：在 500 个 issue 上进行了测试，并针对每个模型进行了 **3 次独立运行**（使用不同随机种子打乱任务顺序），报告平均 Pass@1 和标准差。
*   **消融实验**：非常充分，涵盖了：
    *   结构化提示的影响（证明增益非仅来自 Prompt 结构）。
    *   类别隔离的必要性（对比全局检索）。
    *   抽象经验 vs 原始轨迹（证明去噪的重要性）。
*   **深度分析**：包括在线学习的动态曲线、任务复杂度分析、记忆分布的长尾效应分析以及功能专业化分析。实验设计客观、公平，充分验证了方法的鲁棒性。

#### 6. 主要结论与发现
*   **性能提升**：该方法在所有底座模型上均取得显著提升，平均 Pass@1 提高 **4.7 个百分点**。其中 Gemini 2.5 Pro 提升最明显（+6.8 pp）。
*   **缓解干扰**：相比实例级记忆在 Claude 模型上出现的性能波动或下降，子任务级记忆表现出极高的稳定性，证明了细粒度过滤能有效减少噪声。
*   **长程推理优势**：分析表明，性能增益随交互步数增加而扩大，尤其在**高难度任务（Hard tasks）**上提升达 8.7%，说明记忆能有效作为“计算捷径”，防止智能体在长路径中迷失。
*   **在线进化**：随着记忆库密度的增加，智能体表现出明显的学习曲线，后期任务受益于前期积累的经验。

#### 7. 优点
*   **粒度精准**：解决了 SWE 任务中“表面相似但逻辑不同”的根本矛盾。
*   **结构化对齐**：将记忆与软件工程的自然工作流（分析-复现-编辑-验证）结合，符合人类开发者的逻辑。
*   **低噪声**：通过 LLM 抽象提取和类别过滤，确保检索到的内容具有高度的可迁移性。
*   **即插即用**：无需参数微调，适用于各种底座模型，具有良好的通用性。

#### 8. 不足与局限
*   **依赖底座能力**：子任务的分割和经验的提取高度依赖底座 LLM 的规划和总结能力，对于极弱的模型可能效果受限。
*   **检索开销**：虽然在步数上是预算中立的，但频繁的子任务级检索和提取会增加总体的 Token 消耗和推理延迟。
*   **领域局限**：目前仅在软件工程（SWE）领域验证，虽然该领域具有明显的阶段性，但在其他非结构化任务中的适用性有待探索。
*   **累积误差风险**：如果前期子任务提取了错误的“经验”，可能会在后续任务中产生负面迁移。

（完）
