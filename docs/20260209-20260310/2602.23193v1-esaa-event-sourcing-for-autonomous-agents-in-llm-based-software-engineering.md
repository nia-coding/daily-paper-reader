---
title: "ESAA: Event Sourcing for Autonomous Agents in LLM-Based Software Engineering"
title_zh: ESAA：基于大语言模型软件工程中自主智能体的事件溯源架构
authors: Elzo Brito dos Santos Filho
date: 2026-02-26
pdf: "https://arxiv.org/pdf/2602.23193v1"
tags: ["query:sr-ai"]
score: 10.0
evidence: 基于大语言模型的软件工程自主智能体
tldr: 本研究针对大语言模型智能体在软件工程中面临的状态缺失、长上下文退化及生成不确定性等问题，提出了ESAA架构。该架构借鉴事件溯源模式，将智能体的认知意图与项目状态变更分离，通过确定性编排器验证并记录结构化事件流。通过边界契约和回放验证机制，确保了任务的不可变性与可追溯性。实验证明，该架构在单智能体及多智能体协作场景下均表现出良好的可靠性与可扩展性。
selection_source: fresh_fetch
motivation: 旨在解决LLM智能体在复杂软件工程任务中存在的状态管理困难、上下文退化以及概率性生成与确定性执行需求之间的鸿沟。
method: 提出ESAA架构，通过事件溯源模式分离意图与执行，利用追加式日志记录事件，并结合边界契约与回放机制进行状态验证。
result: 在单智能体落地页项目和多智能体临床仪表盘系统的案例研究中，均成功完成了所有任务并通过了状态验证。
conclusion: ESAA架构为自主智能体提供了稳健的状态管理和取证追溯能力，有效提升了多智能体协作在软件工程中的可扩展性与可靠性。
---

## 摘要
基于大语言模型（LLM）的自主智能体已从反应式助手演变为能够进行规划、通过工具执行操作并根据环境观察进行迭代的系统。然而，它们仍受限于结构性缺陷：缺乏原生状态、长程上下文退化，以及概率性生成与确定性执行需求之间的差距。本文提出了 ESAA（自主智能体事件溯源）架构，受事件溯源（Event Sourcing）模式启发，该架构将智能体的认知意图与项目的状态变更分离。在 ESAA 中，智能体仅以经过验证的 JSON 格式（agent.result 或 issue.report）发出结构化意图；确定性编排器负责验证、将事件持久化到仅追加日志（activity.jsonl）中、应用文件写入效果，并投影出可验证的物化视图（roadmap.json）。该方案结合了边界契约（AGENT_CONTRACT.yaml）、元提示配置文件（PARCER）以及带哈希的重放验证（esaa verify），确保了已完成任务的不可变性和取证可追溯性。两项案例研究验证了该架构：(i) 一个落地页项目（9 个任务，49 个事件，单智能体组合）和 (ii) 一个临床仪表盘系统（50 个任务，86 个事件，跨 8 个阶段的 4 个并发智能体），两者均以 run.status=success 和 verify_status=ok 结束。多智能体案例研究展示了异构 LLM（Claude Sonnet 4.6、Codex GPT-5、Antigravity/Gemini 3 Pro 和 Claude Opus 4.6）的真实并发编排，为该架构在单智能体场景之外的可扩展性提供了经验证据。

## Abstract
Autonomous agents based on Large Language Models (LLMs) have evolved from reactive assistants to systems capable of planning, executing actions via tools, and iterating over environment observations. However, they remain vulnerable to structural limitations: lack of native state, context degradation over long horizons, and the gap between probabilistic generation and deterministic execution requirements. This paper presents the ESAA (Event Sourcing for Autonomous Agents) architecture, which separates the agent's cognitive intention from the project's state mutation, inspired by the Event Sourcing pattern. In ESAA, agents emit only structured intentions in validated JSON (agent.result or issue.report); a deterministic orchestrator validates, persists events in an append-only log (activity.jsonl), applies file-writing effects, and projects a verifiable materialized view (roadmap.json). The proposal incorporates boundary contracts (AGENT_CONTRACT.yaml), metaprompting profiles (PARCER), and replay verification with hashing (esaa verify), ensuring the immutability of completed tasks and forensic traceability. Two case studies validate the architecture: (i) a landing page project (9 tasks, 49 events, single-agent composition) and (ii) a clinical dashboard system (50 tasks, 86 events, 4 concurrent agents across 8 phases), both concluding with run.status=success and verify_status=ok. The multi-agent case study demonstrates real concurrent orchestration with heterogeneous LLMs (Claude Sonnet 4.6, Codex GPT-5, Antigravity/Gemini 3 Pro, and Claude Opus 4.6), providing empirical evidence of the architecture's scalability beyond single-agent scenarios.

---

## 论文详细总结（自动生成）

这是一份关于论文《ESAA: Event Sourcing for Autonomous Agents in LLM-Based Software Engineering》的结构化深入总结：

### 1. 核心问题与研究背景
*   **核心问题**：在基于大语言模型（LLM）的软件工程中，自主智能体面临三大结构性挑战：
    1.  **缺乏原生状态管理**：智能体难以在长周期任务中保持目标、契约与效果的一致性。
    2.  **上下文退化**：长程对话会导致信息丢失（如“Lost-in-the-middle”现象），引发状态漂移。
    3.  **概率性与确定性的鸿沟**：LLM 生成的概率性文本与软件工程要求的确定性执行（如文件写入、编译）之间存在冲突。
*   **研究背景**：现有的多智能体框架（如 AutoGen、MetaGPT、LangGraph）虽然在协作上有所进步，但通常依赖内存快照，缺乏生产级软件工程所需的不可变审计追踪和确定性重放机制。

### 2. 方法论：ESAA 架构
ESAA 借鉴了软件架构中的**事件溯源（Event Sourcing）**和 **CQRS（命令查询职责分离）**原则，核心思想是将智能体的“认知意图”与系统的“状态变更”彻底分离。
*   **核心流程**：
    1.  **意图发射**：智能体不直接修改代码，而是发出受约束的 JSON 格式意图（如 `agent.result`）。
    2.  **确定性编排**：编排器根据 **JSON Schema** 和**边界契约**（`AGENT_CONTRACT.yaml`）验证意图。
    3.  **事件持久化**：验证通过后，将事件追加到不可变日志 `activity.jsonl` 中。
    4.  **状态投影**：编排器根据日志计算出当前的物化视图 `roadmap.json`（只读模型），并应用文件写入效果。
*   **关键技术细节**：
    *   **PARCER 配置文件**：通过元提示（Metaprompting）强制智能体遵循严格的 JSON 封装，抑制自由文本输出。
    *   **不可变性规则**：已完成的任务不可回溯，任何修复必须通过新的 `issue.report` 事件产生新路径。
    *   **哈希验证**：使用 SHA-256 对投影状态进行规范化哈希计算，确保状态可重放且未被篡改。

### 3. 实验设计
论文通过两个复杂度递增的案例研究进行验证：
*   **案例 1：落地页项目（单智能体组合）**
    *   **场景**：9 个顺序任务，涵盖规范、实现和 QA。
    *   **目标**：验证智能体遵守约束性输出契约的能力。
*   **案例 2：临床仪表盘系统（多智能体并发）**
    *   **场景**：50 个任务，涉及数据库、API、UI、安全等 7 个组件。
    *   **Benchmark**：与 AutoGen、MetaGPT、LangGraph 等框架在功能特性（如不可变日志、确定性重放、边界契约等）上进行了定性对比。
    *   **协作模式**：4 个异构智能体（Claude Sonnet 4.6, Codex GPT-5, Gemini 3 Pro, Claude Opus 4.6）并发执行。

### 4. 资源与算力
*   **算力资源**：论文未提及具体的本地 GPU 硬件（如型号或数量），因为实验主要依赖于商业 LLM 的 API 接口。
*   **模型使用**：使用了当时（设定为 2026 年）最先进的模型，包括 **Codex GPT-5**、**Claude 4.6 系列**和 **Gemini 3 Pro**。
*   **执行时长**：案例 2 的执行跨度约为 15 小时。
*   **存储开销**：案例 2 的 86 个事件日志仅占用约 15 KB 空间。

### 5. 实验数量与充分性
*   **实验数量**：共进行了 2 组核心案例研究（n=2）。
*   **充分性评价**：
    *   **深度较好**：案例 2 模拟了真实世界的复杂性（并发、异构模型、依赖图）。
    *   **广度有限**：仅涵盖了两个特定领域的 POC 项目，样本量较小，不足以进行大规模统计推断。
    *   **客观性**：通过 `verify_status=ok` 和 `output.rejected=0` 等量化指标证明了架构的合规性，实验设计较为严谨。

### 6. 主要结论与发现
*   **结构合规性**：通过强制 JSON 约束，实验中未出现解析失败或违约输出（拒绝率为 0）。
*   **并发处理能力**：事件溯源的追加语义天然地序列化了并发智能体的活动，解决了多智能体冲突问题。
*   **审计与调试**：通过重放事件流，可以精确重建任何时间点的项目状态，实现了“时空旅行调试”。
*   **上下文效率**：编排器仅向智能体提供“纯净视图”（Purified View），有效缓解了长上下文导致的性能退化。

### 7. 优点
*   **高可靠性**：将 LLM 的概率性行为限制在受控的沙盒内，确保了系统状态的确定性。
*   **取证溯源**：提供了完整的审计轨迹，这对于生产环境中的责任归属和错误回滚至关重要。
*   **异构兼容**：能够无缝集成不同厂商的 LLM，利用各自的专业化优势。

### 8. 不足与局限
*   **泛化风险**：尚未在超大规模企业级仓库（如单体大仓库 monorepo）或复杂的 CI/CD 流水中进行测试。
*   **Token 开销**：JSON 封装和验证前导码增加了约 200-500 token 的额外开销。
*   **评价维度**：目前的验证侧重于“合规性”和“可重放性”，尚未直接衡量生成代码的“设计质量”或“业务价值”。
*   **内部有效性**：实验在受控环境下进行，且使用了 Temperature=0.0，可能无法完全反映真实开发中的多样性。

（完）
