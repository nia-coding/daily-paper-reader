---
title: "LAP: Language-Action Pre-Training Enables Zero-shot Cross-Embodiment Transfer"
title_zh: LAP：语言-动作预训练实现零样本跨具身迁移
authors: "Lihan Zha, Asher J. Hancock, Mingtong Zhang, Tenny Yin, Yixuan Huang, Dhruv Shah, Allen Z. Ren, Anirudha Majumdar"
date: 2026-02-11
pdf: "https://arxiv.org/pdf/2602.10556v2"
tags: ["query:sr-ai"]
score: 10.0
evidence: 用于零样本跨具身迁移的语言-动作预训练
tldr: "针对现有视觉-语言-动作模型（VLA）难以在不同机器人形态间实现零样本迁移的问题，本文提出了语言-动作预训练（LAP）方法。该方法直接将底层机器人动作表示为自然语言，使动作监督与预训练视觉语言模型的输入输出分布对齐。基于此开发的LAP-3B模型无需特定形态的微调，在多种未知机器人和任务中实现了超过50%的零样本成功率，比现有最强模型提升了约2倍，展示了强大的跨形态迁移和扩展能力。"
selection_source: fresh_fetch
motivation: 现有的VLA模型与训练时的机器人形态耦合过紧，导致在部署到新形态机器人时需要昂贵的微调。
method: 提出LAP方法，将底层机器人动作直接映射为自然语言描述，无需学习分词器或特定形态的架构设计。
result: "LAP-3B在未见过的机器人形态上实现了超过50%的平均零样本成功率，性能是先前最强VLA模型的两倍。"
conclusion: LAP通过统一动作预测与视觉问答格式，实现了高效的跨形态零样本迁移和良好的扩展性。
---

## 摘要
机器人学的一个长期目标是开发一种通用策略，能够在无需针对每个具身进行适配的情况下，零样本部署到新的机器人具身（embodiments）上。尽管进行了大规模的多具身预训练，现有的视觉-语言-动作模型（VLAs）仍然与其训练时的具身紧密耦合，通常需要昂贵的微调。我们引入了语言-动作预训练（LAP），这是一种简单的方案，直接用自然语言表示底层机器人动作，从而将动作监督与预训练视觉-语言模型的输入输出分布对齐。LAP 不需要学习分词器（tokenizer），不需要昂贵的标注，也不需要针对特定具身的架构设计。基于 LAP，我们提出了 LAP-3B。据我们所知，这是首个在没有任何特定具身微调的情况下，实现对先前未见过的机器人具身进行实质性零样本迁移的 VLA。在多个新型机器人和操作任务中，LAP-3B 实现了超过 50% 的平均零样本成功率，比之前最强的 VLA 提升了约 2 倍。我们进一步证明，LAP 能够实现高效的适配和良好的扩展性，同时将动作预测和视觉问答（VQA）统一在共享的语言-动作格式中，通过协同训练获得额外收益。

## Abstract
A long-standing goal in robotics is a generalist policy that can be deployed zero-shot on new robot embodiments without per-embodiment adaptation. Despite large-scale multi-embodiment pre-training, existing Vision-Language-Action models (VLAs) remain tightly coupled to their training embodiments and typically require costly fine-tuning. We introduce Language-Action Pre-training (LAP), a simple recipe that represents low-level robot actions directly in natural language, aligning action supervision with the pre-trained vision-language model's input-output distribution. LAP requires no learned tokenizer, no costly annotation, and no embodiment-specific architectural design. Based on LAP, we present LAP-3B, which to the best of our knowledge is the first VLA to achieve substantial zero-shot transfer to previously unseen robot embodiments without any embodiment-specific fine-tuning. Across multiple novel robots and manipulation tasks, LAP-3B attains over 50% average zero-shot success, delivering roughly a 2x improvement over the strongest prior VLAs. We further show that LAP enables efficient adaptation and favorable scaling, while unifying action prediction and VQA in a shared language-action format that yields additional gains through co-training.

---

## 论文详细总结（自动生成）

### LAP：语言-动作预训练实现零样本跨具身迁移 —— 论文深度总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的视觉-语言-动作模型（VLA）虽然在多具身数据上进行了大规模训练，但在面对未见过的机器人硬件（新具身，如不同的夹持器、相机位置或机械臂型号）时，往往无法直接运行（零样本迁移能力差），通常需要昂贵的针对性微调。
*   **研究背景**：这种局限性源于“分布失配”。预训练的视觉语言模型（VLM）是在互联网规模的文本上训练的，而传统的 VLA 将动作表示为抽象的离散 Token 或连续数值，这破坏了 VLM 原有的语义表示。
*   **整体含义**：本文提出 **LAP (Language-Action Pre-training)**，核心逻辑是将底层机器人动作直接表示为自然语言（如“向左移动 5 厘米”），使动作预测任务在形式上与 VLM 的原始文本生成任务完全对齐，从而保留并利用 VLM 强大的泛化能力。

#### 2. 论文提出的方法论
*   **核心思想**：将动作监督信号转化为自然语言描述（Language-Actions），利用 VLM 的语言先验来统一不同机器人的动作空间。
*   **关键技术细节**：
    *   **语言-动作表示**：通过确定性的模板将末端执行器的位移（Delta Pose）转换为文本。例如：`“move forward 5cm, tilt forward 30 degrees”`。这种方式无需学习分词器（Tokenizer），且具有语义可解释性。
    *   **混合架构（Mixture-of-Transformers）**：
        *   **VLM 主干**：负责预测低频的语言-动作 Token，提取高级语义和空间特征。
        *   **动作专家（Action Expert）**：一个轻量级的基于流匹配（Flow-matching）的扩散模型，负责高频（25Hz）的连续动作输出。
    *   **知识隔离（Knowledge Insulation）**：在训练时，阻断动作专家到 VLM 主干的梯度回传，确保 VLM 仅通过语言监督进行学习，防止预训练表示退化。
    *   **坐标系随机化**：在训练中随机切换机器人基座坐标系和末端执行器坐标系，增强具身无关的推理能力。

#### 3. 实验设计
*   **数据集**：使用了 Open X-Embodiment (OXE)、DROID 和 MolmoAct 等大规模机器人数据集。
*   **实验场景**：
    *   **真实机器人**：涵盖 4 种具身（1 种已见：DROID；3 种完全未见：Custom Franka、YAM、Kinova）。
    *   **任务类型**：包括取放、排序、抽纸巾、放毛巾、倒水等 10 种操作任务。
    *   **仿真环境**：LIBERO 基准测试。
*   **对比方法（Benchmark）**：
    *   **开源 VLA**：π0.5、OpenVLA、X-VLA、MolmoAct、VLA-0。
    *   **消融对比**：使用相同架构但不同动作表示（如 FAST Token、数字 Token、无 VLM 监督）的模型。

#### 4. 资源与算力
*   **硬件型号**：使用了 Google 的 **TPU v6e** 芯片。
*   **训练规模**：
    *   主模型（LAP-3B）在 64 个 TPU v6e 核心上训练了约 **10 小时**（1.5 万个梯度步）。
    *   总算力消耗：整个项目（含原型开发、消融实验等）累计消耗超过 **4000 个 TPU v6e-64 小时**。
*   **推理性能**：在单张 **NVIDIA RTX 4090 GPU** 上可实现 25Hz 的实时控制。

#### 5. 实验数量与充分性
*   **实验规模**：进行了超过 **1300 次真实机器人评估试验**，这在机器人论文中属于非常大规模的验证。
*   **充分性与公平性**：
    *   **跨具身验证**：在三种完全不同的未见硬件上测试，验证了真正的零样本能力。
    *   **消融实验**：对比了多种动作表示方式，证明了语言表示是性能提升的关键。
    *   **扩展性分析**：测试了从 4B 到 27B 不同参数规模的模型（基于 Gemma 3），验证了 Scaling Law。
    *   **统计严谨性**：使用了 95% 置信区间来报告成功率，而非简单的平均值，实验设计客观公平。

#### 6. 论文的主要结论与发现
*   **零样本突破**：LAP-3B 在未见过的机器人具身上实现了 **52% 的平均成功率**，比之前最强的模型（约 20% 左右）提升了 2 倍以上。
*   **微调效率**：在需要适配复杂任务时，LAP-3B 比基准模型节省了约 **2.5 倍的数据量**。
*   **协同效应**：将动作预测与视觉问答（VQA，如运动预测）任务协同训练，能进一步提升空间推理能力。
*   **扩展性**：随着模型参数增加，LAP 的验证损失持续下降，且未出现基准模型在大规模下的性能饱和现象。

#### 7. 优点
*   **简单高效**：无需复杂的动作分词器学习，直接利用自然语言这一最通用的接口。
*   **极强的泛化性**：首次在真实世界中展示了 VLA 模型对全新机器人形态的强大零样本适应力。
*   **架构解耦**：通过 VLM 处理语义/空间逻辑，扩散模型处理底层控制，兼顾了泛化性与实时性。

#### 8. 不足与局限
*   **具身多样性限制**：目前主要集中在单臂操作任务，尚未验证在双臂、足式或更异构的机器人（如无人机）上的效果。
*   **精度瓶颈**：对于需要极端精密控制（如缝纫、微小零件组装）或极高反应速度的任务，语言抽象可能存在信息损失。
*   **坐标系依赖**：虽然引入了随机化，但仍依赖于末端执行器的位姿定义，对于非末端控制类型的机器人适配性有待研究。

（完）
