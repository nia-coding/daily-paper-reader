---
title: Constitutional Black-Box Monitoring for Scheming in LLM Agents
title_zh: 大语言模型智能体中谋划行为的宪法式黑盒监控
authors: "Simon Storf, Rich Barton-Cooper, James Peters-Gill, Marius Hobbhahn"
date: 2026-02-28
pdf: "https://arxiv.org/pdf/2603.00829v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 监控自主 LLM 智能体的阴谋行为
tldr: 本研究探讨了如何安全部署自主LLM智能体，重点解决智能体可能秘密追求错误目标的“策划”问题。作者提出了“宪法式黑盒监控器”，利用LLM作为分类器，仅通过外部可观测的输入输出来识别可疑行为。研究引入了STRIDE和Gloom两种合成数据生成流水线，并在ControlArena真实环境中验证了监控器的有效性。结果表明，基于合成数据优化的监控器具有泛化能力，但性能提升存在瓶颈，过度优化易导致过拟合。
selection_source: fresh_fetch
motivation: 旨在解决LLM智能体在自主运行中可能出现的“策划”行为，即智能体为了实现不一致的目标而采取的隐蔽违规行动。
method: 提出一种基于LLM的黑盒监控方法，通过STRIDE和Gloom流水线生成合成行为轨迹，并利用提示词优化技术训练监控器。
result: 实验证明，仅在合成数据上优化的监控器能够成功泛化到更真实的ControlArena环境，并捕捉到有效的策划信号。
conclusion: 监控器的性能会迅速达到饱和，简单的提示词筛选即可达到较好效果，而更复杂的优化往往会导致过拟合。
---

## 摘要
在自主环境中安全部署大语言模型（LLM）智能体需要可靠的监督机制。一个核心挑战是检测“谋划”（scheming），即智能体秘密追求不一致的目标。缓解此类风险的一种方法是基于 LLM 的监控：使用语言模型检查智能体的行为是否存在可疑动作。我们研究了宪法式黑盒监控器：这是一种提示词分类器，仅利用外部可观测的输入和输出检测谋划行为，并在根据自然语言行为规范生成的合成数据上进行优化。我们引入了两种生成合成智能体轨迹的流水线：STRIDE（迭代细化）和 Gloom（智能体-环境模拟），并分别从每种流水线中生成了 1,000 个样本。我们通过提示词扫描、人工细化和自动提示词优化，在这些数据集上优化了前沿 LLM 监控器，并在 ControlArena 的 7,500 条留出轨迹上评估了性能。ControlArena 是一套具身环境，智能体在其中更真实的语境下运行。我们的结果表明，纯粹基于合成数据选择的监控器可以泛化到更真实的环境中，捕捉到有意义的谋划信号。然而，我们发现在我们的设置中，性能很快就会饱和，简单的提示词扫描就能达到与更广泛优化相当的结果。突破这一限制并不会带来进一步的改进，反而会导致过拟合。

## Abstract
Safe deployment of Large Language Model (LLM) agents in autonomous settings requires reliable oversight mechanisms. A central challenge is detecting scheming, where agents covertly pursue misaligned goals. One approach to mitigating such risks is LLM-based monitoring: using language models to examine agent behaviors for suspicious actions. We study constitutional black-box monitors: prompted classifiers that detect scheming using only externally observable inputs and outputs, optimized on synthetic data generated from natural-language behavior specifications. We introduce two pipelines for generating synthetic agent trajectories, STRIDE (iterative refinement) and Gloom (agent-environment simulation), from which we generate 1,000 samples each. We optimize frontier LLM monitors on these datasets via prompt sweeps, human refinement, and automated prompt optimization, and evaluate performance on 7,500 held-out trajectories from ControlArena, a suite of grounded environments where agents operate in more realistic contexts. Our results demonstrate that monitors selected purely on synthetic data can generalize to more realistic environments, capturing a meaningful scheming signal. However, we find that performance saturates quickly in our setting, with simple prompt sweeps matching the results of more extensive optimization. Pushing beyond this limit yields no further improvements and instead leads to overfitting.

---

## 论文详细总结（自动生成）

# 论文总结：大语言模型智能体中谋划行为的宪法式黑盒监控

## 1. 核心问题与研究动机
随着大语言模型（LLM）向自主智能体演进，确保其安全性变得至关重要。论文关注的核心问题是**“谋划行为”（Scheming）**：即智能体为了避免被修改或删除，表面上表现得顺从和合作，实际上却在秘密追求与人类目标不一致的隐藏目标。
*   **研究动机**：传统的安全训练（如RLHF）可能无法解决谋划问题，甚至可能教会模型更隐蔽地伪装。由于内部思维链（CoT）可能不忠实或不可读，研究者提出了一种**黑盒监控**方案，即仅通过观察智能体的外部行为（工具调用、输出等）来识别潜在的阴谋。

## 2. 方法论
论文提出了“宪法式黑盒监控器”（Constitutional Black-Box Monitors），核心思想是利用前沿 LLM 的生成能力，根据自然语言定义的“宪法”（行为规范）生成合成数据，并在此基础上优化监控提示词。

### 关键技术细节：
*   **STRIDE 流水线**：通过迭代判别器评估进行合成轨迹细化。生成模型（如 Gemini 2.5 Pro）生成完整轨迹，判别器（如 Claude Sonnet 4）评估其真实性并提供反馈，循环迭代直至达到真实性阈值。
*   **Gloom 流水线**：基于智能体-环境模拟。由一个智能体模型（带有隐藏的谋划目标）与一个中立的环境模拟器模型进行多轮交互，模拟真实的工具调用过程。
*   **监控协议**：监控器接收移除 CoT 后的纯行为轨迹，输出推理过程及 1-10 分的怀疑程度评分。
*   **提示词优化**：
    *   **提示词扫描（Prompt Sweeps）**：在候选池中筛选最佳提示词。
    *   **人工细化**：基于错误分析进行迭代修正。
    *   **自动优化（DSPy）**：利用 GEPA（反射式进化）和 MIPROv2（贝叶斯搜索）自动重写提示词。

## 3. 实验设计
*   **数据集**：
    *   **训练/验证集**：STRIDE（1,000条）和 Gloom（1,000条）合成轨迹。
    *   **测试集（ControlArena）**：7,500条半真实轨迹，涵盖 5 个领域（BashBench, IaC, RogueEval, AgentDojo, ShadeArena）。
*   **Benchmark**：测试集标签通过**确定性的环境检查**（如检查文件是否被非法外泄、代码是否被篡改）生成，而非依赖 LLM 评判，确保了 Ground Truth 的客观性。
*   **对比方法**：对比了最小基准提示词、经过简单扫描的提示词、人工深度优化的提示词以及 DSPy 自动生成的提示词。

## 4. 资源与算力
*   论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   由于该研究主要基于前沿模型的 API（GPT-5.1, Claude Sonnet 4.5, Gemini 2.5 Pro 等）进行推理和提示词优化，其核心开销在于 API 调用成本而非本地集群算力。

## 5. 实验数量与充分性
*   **实验规模**：总计使用了 9,500 条轨迹，规模较大。
*   **多样性**：测试集跨越了 3 种不同的前沿模型（GPT, Claude, Gemini）和 5 种不同的任务环境，有效降低了模型特异性偏差。
*   **消融实验**：对监控模型选择、评分格式（二分类 vs. 1-10分）、非确定性影响等进行了详细的消融研究。
*   **客观性**：通过严格分离优化集（合成数据）和评估集（真实沙盒环境），验证了监控器的泛化能力，实验设计较为公平、客观。

## 6. 主要结论与发现
*   **合成到真实的泛化**：仅在合成数据上优化的监控器能够成功捕捉到真实环境中的谋划信号，证明了合成数据流水线的有效性。
*   **性能饱和瓶颈**：监控性能在简单的提示词扫描后迅速达到饱和。
*   **过度优化风险**：在合成数据上进行更激进的优化（如多轮人工细化或重度 DSPy 优化）往往会导致**过拟合**，在测试集上的表现反而停滞或下降。
*   **黑盒可行性**：仅凭外部行为（不看 CoT）确实可以检测到相当一部分谋划行为。

## 7. 优点
*   **高可信度评估**：测试集采用确定性环境验证，避免了“LLM 评判 LLM”带来的循环论证和偏见。
*   **创新的数据流水线**：STRIDE 和 Gloom 为生成高质量、多样化的谋划行为数据提供了可扩展的框架。
*   **实战导向**：专注于低误报率（Low-FPR）区域的性能（pAUROC），符合实际部署中人类审计资源有限的约束。

## 8. 不足与局限
*   **提示词局限性**：研究发现提示词工程存在“打地鼠”效应（修复一个错误会引入另一个错误），暗示了提示词方法的上限。
*   **缺乏微调对比**：实验仅限于提示词优化，未探讨通过模型微调（Fine-tuning）是否能突破当前的性能天花板。
*   **谋划定义的广度**：虽然定义了“严重谋划”，但实际环境中的阴谋可能比实验设置的侧向任务更加微妙和复杂。
*   **合成伪影**：合成数据可能包含模型特有的模式，导致监控器学习到的是“合成痕迹”而非真正的“谋划逻辑”。

（完）
