---
title: "UMEM: Unified Memory Extraction and Management Framework for Generalizable Memory"
title_zh: UMEM：面向可泛化记忆的统一记忆提取与管理框架
authors: "Yongshi Ye, Hui Jiang, Feihu Jiang, Tian Lan, Yichao Du, Biao Fu, Xiaodong Shi, Qianghuai Jia, Longyue Wang, Weihua Luo"
date: 2026-02-11
pdf: "https://arxiv.org/pdf/2602.10652v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: LLM智能体的自演化记忆提取与管理
tldr: 针对大语言模型智能体在记忆提取与管理中存在的泛化性差、易积累噪声等问题，本文提出了UMEM框架。该框架首次将记忆提取与管理进行联合优化，通过引入语义邻域建模和基于GRPO的邻域级边际效用奖励机制，确保记忆在相关查询中的通用性。实验证明，UMEM在多轮交互任务中表现优异，显著提升了智能体的持续进化能力。
selection_source: fresh_fetch
motivation: 现有方法将记忆提取视为静态过程，导致智能体容易积累特定实例的噪声而非具有泛化性的知识。
method: 提出UMEM框架，利用语义邻域建模和GRPO算法优化邻域级边际效用奖励，实现记忆提取与管理的协同进化。
result: "在五个基准测试中显著超越竞争对手，多轮交互任务性能提升高达10.67%，且在持续演化中保持单调增长。"
conclusion: UMEM通过联合优化提取与管理过程，有效提升了智能体记忆的泛化能力和长期演化稳定性。
---

## 摘要
自演化记忆作为基于大语言模型（LLM）智能体的可训练参数，其提取（从经验中提炼见解）与管理（更新记忆库）必须紧密协调。现有方法主要优化记忆管理，而将记忆提取视为静态过程，导致泛化能力较差，使智能体积累了特定于实例的噪声而非鲁棒的记忆。为解决这一问题，我们提出了统一记忆提取与管理（UMEM），这是一个自演化智能体框架，通过联合优化大语言模型来同时提取和管理记忆。为了减轻对特定实例的过拟合，我们引入了语义邻域建模，并通过 GRPO 利用邻域级边际效用奖励来优化模型。该方法通过评估语义相关查询簇中的记忆效用，确保了记忆的可泛化性。在五个基准测试上的广泛实验表明，UMEM 显著优于极具竞争力的基准方法，在多轮交互任务中实现了高达 10.67% 的提升。此外，UMEM 在持续演化过程中保持了单调增长曲线。代码和模型将公开发布。

## Abstract
Self-evolving memory serves as the trainable parameters for Large Language Models (LLMs)-based agents, where extraction (distilling insights from experience) and management (updating the memory bank) must be tightly coordinated. Existing methods predominately optimize memory management while treating memory extraction as a static process, resulting in poor generalization, where agents accumulate instance-specific noise rather than robust memories. To address this, we propose Unified Memory Extraction and Management (UMEM), a self-evolving agent framework that jointly optimizes a Large Language Model to simultaneous extract and manage memories. To mitigate overfitting to specific instances, we introduce Semantic Neighborhood Modeling and optimize the model with a neighborhood-level marginal utility reward via GRPO. This approach ensures memory generalizability by evaluating memory utility across clusters of semantically related queries. Extensive experiments across five benchmarks demonstrate that UMEM significantly outperforms highly competitive baselines, achieving up to a 10.67% improvement in multi-turn interactive tasks. Futhermore, UMEM maintains a monotonic growth curve during continuous evolution. Codes and models will be publicly released.

---

## 论文详细总结（自动生成）

以下是对论文《UMEM: Unified Memory Extraction and Management Framework for Generalizable Memory》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**大语言模型（LLM）智能体的自演化记忆系统**。
*   **研究背景**：为了让智能体在动态环境中持续学习，通常需要一个外部记忆库作为其“可训练参数”。这个过程包含两个核心环节：**记忆提取**（从经验中提炼见解）和**记忆管理**（更新、添加或替换记忆条目）。
*   **核心问题**：现有方法通常将记忆提取视为一个静态的、基于 Prompt 的过程，而仅优化管理策略。这导致了两个严重问题：
    1.  **实例噪声累积**：智能体容易陷入“死记硬背”陷阱，记住了特定问题的具体细节（如具体的数值结果）而非通用的解决原则，导致泛化性差。
    2.  **管理失调**：提取出的记忆内容与管理策略不匹配，即使管理算法再好，低质量的记忆内容也会污染记忆库。

### 2. 方法论：核心思想与技术细节
UMEM 提出了**统一记忆提取与管理框架**，其核心思想是通过强化学习（RL）同时训练模型如何“写记忆”和“管记忆”。
*   **语义邻域建模（Semantic Neighborhood Modeling）**：为了防止过拟合，UMEM 不仅在当前查询（Query）上评估记忆，还会检索语义空间中最接近的 $N$ 个邻域查询。只有当一条记忆能同时帮助解决这些相关问题时，才会被视为高质量记忆。
*   **边际效用奖励（Marginal Utility Reward）**：设计了复合奖励函数来指导优化：
    *   **成功增益（Success Gain）**：衡量加入记忆后，原本错误的邻域查询是否转为正确。
    *   **效率正则化（Efficiency Regularization）**：在保证正确的前提下，奖励那些能缩短推理步骤、减少 Token 消耗的记忆。
*   **算法流程**：
    1.  **前向执行**：冻结的执行器（Executor）利用当前记忆完成任务。
    2.  **策略采样**：Mem-Optimizer（策略模型）生成多个候选的记忆更新动作（提取内容 + 操作类型）。
    3.  **邻域验证**：在语义邻域内测试这些动作的实际效果。
    4.  **GRPO 优化**：使用群体相对策略优化（GRPO）算法，根据邻域平均奖励更新策略模型。
    5.  **在线演化**：将表现最好的记忆动作实时应用到记忆库中，实现闭环进化。

### 3. 实验设计
*   **数据集/场景**：
    *   **单轮推理**：AIME (数学)、GPQA-Diamond (科学)、HLE (人类水平考试)、HotpotQA (多跳问答)。
    *   **多轮交互**：ALFWorld (具身智能体任务)。
*   **Benchmark 与对比方法**：
    *   对比了 **No Memory**（无记忆）、**Self-RAG**（自反思检索）、**Memp**（程序化记忆）和 **ReMem**（侧重管理的演化记忆）。
    *   使用了多种执行器（Executor）进行跨模型验证：Qwen3-8B-Thinking、GPT-5.1、Gemini-2.5-Flash。

### 4. 资源与算力
*   **硬件环境**：使用了 **16 张 NVIDIA A100 GPU**。
*   **训练时长**：整个训练过程大约持续了 **11 小时**。
*   **实现细节**：基于 `verl` 强化学习框架开发，策略模型采用了 Llama-3.2-1B 和 Qwen3-4B。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 个主流 Benchmark，进行了跨模型（从 1B 到 API 模型）的泛化性测试。
*   **消融实验**：非常充分。分别验证了“仅优化管理”、“仅优化提取”、“无语义邻域建模”以及“不同邻域大小 $N$”对结果的影响。
*   **长期稳定性测试**：进行了长达 10 个 Epoch 的连续演化实验，观察性能曲线的单调性。
*   **客观性**：通过“流式评估协议”（Streaming Protocol），确保智能体在处理后续任务时无法重置记忆，真实模拟了在线学习环境，实验设计严谨且公平。

### 6. 主要结论与发现
*   **性能领先**：UMEM 在所有任务上均优于基准方法，尤其在多轮交互任务（ALFWorld）中，相比基准提升了 **10.67%**。
*   **联合优化的必要性**：消融实验证明，单独优化管理策略而不优化提取过程会导致性能显著下降，证明了“提取质量”是自演化系统的核心瓶颈。
*   **演化稳定性**：UMEM 在长期交互中表现出极强的鲁棒性，性能曲线保持单调增长，而基准方法（如 ReMem）往往会因为噪声积累在后期出现性能崩溃。
*   **跨模型迁移**：由小模型（如 4B）训练出的 Mem-Optimizer 提炼的记忆，可以有效提升更强大的模型（如 GPT-5.1）的表现。

### 7. 优点（亮点）
*   **解决过拟合**：通过“语义邻域”这一代理指标，巧妙地解决了非参数化记忆容易过拟合特定实例的顽疾。
*   **端到端优化**：首次实现了提取与管理的联合训练，使记忆内容与管理逻辑在语义层面上高度对齐。
*   **效率与效果并重**：奖励机制中引入效率正则化，使智能体不仅能做对，还能“走捷径”更高效地做对。

### 8. 不足与局限
*   **训练数据依赖**：虽然在多个 Benchmark 测试，但训练数据主要源自 MMLU 的采样，对于极度垂直或全新的领域，可能需要重新微调 Mem-Optimizer。
*   **计算开销**：在训练阶段，由于需要对每个候选记忆在邻域查询上进行多次验证（Rollout），计算成本较高。
*   **记忆库规模限制**：论文虽然提到了 UPDATE 操作，但在极长期演化中，如何处理海量记忆带来的检索压力和冲突，仍有待进一步探讨。

（完）
