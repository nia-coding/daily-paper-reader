---
title: "SWE-AGI: Benchmarking Specification-Driven Software Construction with MoonBit in the Era of Autonomous Agents"
title_zh: SWE-AGI：在自主智能体时代使用 MoonBit 进行规范驱动软件构建的基准测试
authors: "Zhirui Zhang, Hongbo Zhang, Haoxiang Fei, Zhiyuan Bao, Yubin Chen, Zhengyu Lei, Ziyue Liu, Yixuan Sun, Mingkun Xiao, Zihang Ye, Yu Zhang, Hongcheng Zhu, Yuxiang Wen, Heung-Yeung Shum"
date: 2026-02-10
pdf: "https://arxiv.org/pdf/2602.09447v2"
tags: ["query:sr-ai"]
score: 9.0
evidence: 在SWE场景下对自主软件构建进行基准测试
tldr: 本研究针对大语言模型在自主构建生产级软件方面的能力，提出了开源基准测试 SWE-AGI。该基准要求智能体根据权威规范（如RFC）使用 MoonBit 语言实现解析器、解释器等复杂系统，代码量达数千行。通过利用新兴的 MoonBit 生态减少数据泄露，研究评估了前沿模型在长程架构推理上的表现，揭示了代码阅读是当前 AI 辅助开发的主要瓶颈。
selection_source: fresh_fetch
motivation: 旨在评估大语言模型在缺乏现有代码参考的情况下，仅依据技术规范自主构建大规模软件系统的能力。
method: "提出了基于 MoonBit 语言的 SWE-AGI 基准，包含 22 个要求实现 1,000 至 10,000 行核心逻辑的复杂软件工程任务。"
result: "gpt-5.3-codex 以 86.4% 的胜率领先，而研究发现随着代码规模增加，代码阅读能力成为制约智能体表现的关键瓶颈。"
conclusion: 尽管规范驱动的自主软件工程正变得可行，但在可靠支持生产级开发方面仍面临严峻挑战。
---

## 摘要
尽管大语言模型（LLMs）已展现出令人印象深刻的代码编写能力，但它们根据明确规范自主构建生产级软件的能力仍是一个悬而未决的问题。我们推出了 SWE-AGI，这是一个开源基准测试，用于评估使用 MoonBit 编写的软件系统的端到端、规范驱动构建。SWE-AGI 任务要求基于 LLM 的智能体在固定的 API 支架下，严格根据权威标准和 RFC 实现解析器、解释器、二进制解码器和 SAT 求解器。每个任务涉及实现 1,000 至 10,000 行核心逻辑，这对应于经验丰富的人类开发人员数周或数月的工程量。通过利用新兴的 MoonBit 生态系统，SWE-AGI 最大限度地减少了数据泄漏，迫使智能体依赖长程架构推理而非代码检索。在前沿模型中，gpt-5.3-codex 取得了最佳的整体性能（解决了 19/22 个任务，占比 86.4%），优于 claude-opus-4.6（15/22，68.2%），而 kimi-2.5 在开源模型中表现最强。随着任务难度的增加，性能急剧下降，特别是在复杂的、规范密集型系统中。行为分析进一步表明，随着代码库规模的扩大，代码阅读而非编写成为了 AI 辅助开发中的主要瓶颈。总的来说，虽然规范驱动的自主软件工程正变得日益可行，但在其能够可靠地支持生产级开发之前，仍面临着巨大的挑战。

## Abstract
Although large language models (LLMs) have demonstrated impressive coding capabilities, their ability to autonomously build production-scale software from explicit specifications remains an open question. We introduce SWE-AGI, an open-source benchmark for evaluating end-to-end, specification-driven construction of software systems written in MoonBit. SWE-AGI tasks require LLM-based agents to implement parsers, interpreters, binary decoders, and SAT solvers strictly from authoritative standards and RFCs under a fixed API scaffold. Each task involves implementing 1,000-10,000 lines of core logic, corresponding to weeks or months of engineering effort for an experienced human developer. By leveraging the nascent MoonBit ecosystem, SWE-AGI minimizes data leakage, forcing agents to rely on long-horizon architectural reasoning rather than code retrieval. Across frontier models, gpt-5.3-codex achieves the best overall performance (solving 19/22 tasks, 86.4%), outperforming claude-opus-4.6 (15/22, 68.2%), and kimi-2.5 exhibits the strongest performance among open-source models. Performance degrades sharply with increasing task difficulty, particularly on hard, specification-intensive systems. Behavioral analysis further reveals that as codebases scale, code reading, rather than writing, becomes the dominant bottleneck in AI-assisted development. Overall, while specification-driven autonomous software engineering is increasingly viable, substantial challenges remain before it can reliably support production-scale development.

---

## 论文详细总结（自动生成）

# 论文总结：SWE-AGI 基准测试分析

### 1. 论文的核心问题与整体含义
本文探讨的核心问题是：**在缺乏现有代码参考的情况下，大语言模型（LLM）驱动的智能体能否仅依据技术规范（如 RFC、国际标准）自主构建生产级规模的软件系统？**

研究背景在于，现有的代码基准测试存在两大缺陷：一是任务过于碎片化（如 HumanEval 仅关注函数级实现）；二是存在严重的数据泄露风险（如 SWE-bench 基于 GitHub 现有仓库，模型可能在训练中见过相关代码）。为此，作者提出了 **SWE-AGI**，旨在通过新兴的编程语言和严格的规范驱动流程，评估智能体在长程架构推理、系统设计及复杂逻辑实现上的真实能力。

### 2. 论文提出的方法论
核心思想是构建一个**“规范驱动、从零开始、抗检索”**的端到端软件工程基准。

*   **关键技术细节：**
    *   **语言选择（MoonBit）：** 选用国产新兴语言 MoonBit。由于其生态尚在起步阶段，互联网上几乎没有现成的复杂系统实现，强制模型必须通过理解规范而非检索记忆来编写代码。
    *   **规范驱动流程：** 为智能体提供权威的规范文档（`specs/`）、明确的任务说明（`TASK.md`）和固定的 API 支架。
    *   **声明先行（Declaration-first）：** 利用 MoonBit 的 `declare` 关键字预定义函数签名和类型，固定公共接口，确保智能体在受控的架构约束下进行开发。
    *   **迭代反馈循环：** 智能体在沙盒环境中运行，可利用公开测试集进行本地迭代，最终提交至隐藏的私有测试集进行验证。

### 3. 实验设计
*   **数据集/场景：** 包含 22 个复杂的软件工程任务，涵盖 7 大类别：
    1.  模板与领域特定语言（如 Pug, JQ）。
    2.  数据序列化格式（如 YAML, TOML）。
    3.  标记语言（如 HTML5, XML）。
    4.  编程语言前端（如 C99, Lua, Python 子集）。
    5.  二进制格式与解码器（如 WASM, Protobuf）。
    6.  网络协议（如 URI, HPACK）。
    7.  自动化推理（如 CDCL SAT 求解器）。
*   **难度分级：** 分为 Easy（6个）、Medium（8个）、Hard（8个）三个等级，核心逻辑量在 $10^3$ 至 $10^4$ 行之间。
*   **对比方法：** 评估了多款前沿模型，包括 OpenAI 的 **gpt-5.3-codex**、**gpt-5.2-codex**，Anthropic 的 **claude-opus-4.6/4.5**，以及国产模型 **kimi-2.5**、**deepseek-v3.2**、**glm-4.7**、**qwen3-max** 等。

### 4. 资源与算力
论文**未明确说明**训练这些模型所使用的具体算力（如 GPU 型号或数量），因为该研究侧重于对已有模型的推理评估。
*   **评估成本：** 论文详细记录了推理过程中的 **Token 消耗**和**墙钟时间（Wall-clock time）**。例如，gpt-5.2-codex 在处理某些 Hard 任务时运行时间超过 42 小时，单项任务的 Token 消耗达到数亿级别。

### 5. 实验数量与充分性
*   **实验规模：** 针对 22 个任务进行了详尽的端到端测试，并对智能体的行为（如阅读规范、写代码、调试、查阅文档）进行了分类统计。
*   **充分性与公平性：** 实验设计较为客观。通过隐藏私有测试集防止过拟合；通过 MoonBit 语言特性规避了数据泄露偏差；对不同梯队的模型采取了分级评估（前沿模型全量测试，二线模型进行 Easy 级快速筛选），实验覆盖面较广，具有较强的说服力。

### 6. 论文的主要结论与发现
*   **性能梯队：** **gpt-5.3-codex** 表现最强，总胜率 86.4%（19/22）；**claude-opus-4.6** 紧随其后（68.2%）。国产模型中 **kimi-2.5** 在 Easy 任务中表现突出。
*   **难度瓶颈：** 随着任务从 Easy 向 Hard 跨越，所有模型的成功率均大幅下降。Hard 任务（如 HTML5 解析器、C99 编译器）仍是当前 AI 的巨大挑战。
*   **核心瓶颈转移：** 行为分析发现，在构建大规模系统时，**“代码阅读与理解”**而非“代码编写”成为了主要瓶颈。随着代码库增长，智能体需花费 40%-60% 的操作进行代码巡检以维持架构一致性。
*   **近乎成功：** 许多失败任务的测试通过率极高（>95%），说明模型已能构建核心系统，但在处理规范中的极端边缘情况（Edge Cases）时仍欠缺严谨性。

### 7. 优点
*   **创新性基准：** 填补了从“代码片段生成”到“完整系统构建”评估的空白。
*   **防污染设计：** 巧妙利用 MoonBit 语言特性，解决了代码基准测试中长期存在的“背题”现象。
*   **深度行为分析：** 不仅关注结果（Pass/Fail），还通过工具调用日志分析了 AI 工程师的作业模式，提出了“阅读是瓶颈”的深刻洞察。

### 8. 不足与局限
*   **样本量限制：** 22 个任务虽然规模大，但样本绝对数量较少，可能存在一定的偶然性。
*   **语言局限性：** 结论高度依赖于 MoonBit 语言，虽然防泄露效果好，但模型在更通用的语言（如 Rust, C++）上的表现可能因训练数据丰富度不同而有所差异。
*   **非功能性评估缺失：** 目前仅关注功能正确性，尚未对生成代码的运行性能（内存、速度）、安全性及可维护性进行量化评估。

（完）
