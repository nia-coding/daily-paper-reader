---
title: Unifying Language-Action Understanding and Generation for Autonomous Driving
title_zh: 统一自动驾驶中的语言-动作理解与生成
authors: "Xinyang Wang, Qian Liu, Wenjie Ding, Zhao Yang, Wei Li, Chang Liu, Bailin Li, Kun Zhan, Xianpeng Lang, Wei Chen"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.01441v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 统一自动驾驶VLA的语言与动作
tldr: "LinkVLA 针对自动驾驶中视觉-语言-动作（VLA）模型的指令-动作对齐差和生成效率低的问题，提出了一种统一架构。通过将语言与动作 Token 映射至共享离散码本，并引入轨迹描述生成的辅助任务，增强了跨模态一致性。同时，采用两步由粗到精的生成策略，在提升驾驶性能的同时，将推理耗时降低了 86%。"
selection_source: fresh_fetch
motivation: 现有的自动驾驶 VLA 模型存在语言指令与动作输出失配，以及自回归生成动作效率低下的关键缺陷。
method: 提出 LinkVLA 架构，通过共享码本统一语言与动作 Token，并结合轨迹理解辅助任务及两步式由粗到精的生成方法。
result: 在闭环驾驶基准测试中，该方法显著提升了指令遵循准确率和驾驶性能，并大幅减少了推理延迟。
conclusion: LinkVLA 通过结构与语义的双重对齐以及高效的生成策略，为构建高性能、低延迟的自动驾驶 VLA 模型提供了有效方案。
---

## 摘要
视觉-语言-动作（VLA）模型正成为端到端自动驾驶中一种极具前景的范式，因其利用世界知识并对复杂驾驶场景进行推理的潜力而备受重视。然而，现有方法存在两个关键局限：语言指令与动作输出之间持续存在的不对齐问题，以及典型自回归动作生成固有的低效率。在本文中，我们提出了 LinkVLA，这是一种直接应对这些挑战以增强对齐性和效率的新型架构。首先，我们通过将语言和动作 token 统一到一个共享的离散码本（codebook）中，并在单个多模态模型中进行处理，从而建立结构化链接。这从根本上在结构上强制实现了跨模态的一致性。其次，为了创建深层语义链接，我们引入了一个辅助动作理解目标，训练模型根据轨迹生成描述性说明，从而促进双向语言-动作映射。最后，我们用一种两步式由粗到精（coarse-to-fine）的生成方法 C2F 取代了缓慢的逐步生成，该方法能高效解码动作序列，节省了 86% 的推理时间。在闭环驾驶基准测试上的实验表明，指令遵循准确性和驾驶性能均有持续提升，同时推理延迟也得到了降低。

## Abstract
Vision-Language-Action (VLA) models are emerging as a promising paradigm for end-to-end autonomous driving, valued for their potential to leverage world knowledge and reason about complex driving scenes. However, existing methods suffer from two critical limitations: a persistent misalignment between language instructions and action outputs, and the inherent inefficiency of typical auto-regressive action generation. In this paper, we introduce LinkVLA, a novel architecture that directly addresses these challenges to enhance both alignment and efficiency. First, we establish a structural link by unifying language and action tokens into a shared discrete codebook, processed within a single multi-modal model. This structurally enforces cross-modal consistency from the ground up. Second, to create a deep semantic link, we introduce an auxiliary action understanding objective that trains the model to generate descriptive captions from trajectories, fostering a bidirectional language-action mapping. Finally, we replace the slow, step-by-step generation with a two-step coarse-to-fine generation method C2F that efficiently decodes the action sequence, saving 86% inference time. Experiments on closed-loop driving benchmarks show consistent gains in instruction following accuracy and driving performance, alongside reduced inference latency.

---

## 论文详细总结（自动生成）

以下是对论文《Unifying Language-Action Understanding and Generation for Autonomous Driving》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：视觉-语言-动作（VLA）模型虽然在自动驾驶中展现出强大的推理和泛化潜力，但面临两大瓶颈：
    1.  **语义失配（Misalignment）**：模型可能正确理解了“向左变道”的指令，却输出了“直行”的轨迹，即语言指令与物理动作之间缺乏深层一致性。
    2.  **推理效率低下**：传统的自回归（Auto-regressive）动作生成需要逐个 token 预测，导致推理延迟极高，难以满足自动驾驶的实时性要求。
*   **核心目标**：通过结构化和语义的双重对齐，以及高效的生成策略，构建一个既能精准遵循指令又能快速响应的端到端驾驶模型。

### 2. 方法论
LinkVLA 架构基于 **InternVL2-1B**（包含 InternViT-300M 和 Qwen2-0.5B），核心创新包括：
*   **统一 Token 空间**：将连续的驾驶轨迹通过空间量化转化为离散 Token，并将其并入 LLM 的现有文本词表。通过共享码本（Shared Codebook），迫使模型在同一表征空间内处理语言和空间概念。
*   **动作 Token 化优化**：
    *   **对数坐标变换（Log Transformation）**：对坐标进行非线性缩放，使靠近车辆的区域分辨率更高，提升近场控制精度。
    *   **空间软标签（Spatial Soft-labeling）**：在训练时使用以真值为中心的 2D 高斯分布作为目标，而非 one-hot 标签，使模型学习到动作空间的物理连续性。
*   **双向对齐学习目标**：
    *   **动作生成**：$p(Action | Vision, Language)$。
    *   **动作理解（辅助任务）**：$p(Language | Vision, Action)$。要求模型根据观察到的轨迹生成描述性文本，通过这种“互逆”任务强化语义对齐。
*   **由粗到精（C2F）生成流程**：
    1.  **终点预测**：模型首先预测轨迹的终点。
    2.  **粗略初始化**：通过线性插值生成初始路径。
    3.  **并行细化**：利用 Transformer 的并行能力，一次性将粗略路径细化为符合交通规则和指令的平滑轨迹，避免了逐点自回归的延迟。

### 3. 实验设计
*   **数据集与场景**：
    *   **Bench2Drive**：基于 CARLA 模拟器的闭环驾驶基准，包含 44 种交互场景和 220 条官方路线。
    *   **Action Dreaming**：用于评估指令遵循能力（如加速、变道、避障等）。
    *   **DriveLM-hard**：用于评估 VQA（视觉问答）和驾驶评论生成能力。
*   **对比方法**：包括传统的端到端模型（UniAD, VAD, TCP）、基于 VLM 的模型（Orion, SimLingo）以及最新的 VLA 模型（AutoVLA）。
*   **评估指标**：驾驶得分（DS）、成功率（SR）、推理延迟（Latency）、指令遵循成功率等。

### 4. 资源与算力
*   **硬件环境**：使用了 **32 张 NVIDIA H20 GPU**。
*   **训练配置**：
    *   训练时长：30 个 Epoch。
    *   Batch Size：48。
    *   优化器：AdamW，配合余弦学习率调度。
    *   微调技术：LoRA（Rank=32, Alpha=64）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的闭环测试（220 条路线）和多维度的能力评估。
*   **消融实验**：非常详尽，涵盖了 Token 化、C2F 策略、对齐任务、软标签、码本大小（K）、对数缩放因子（k）以及高斯分布标准差（σ）等多个变量。
*   **公平性与客观性**：对比了多种不同架构的 SOTA 方法，并在统一的 Bench2Drive 框架下运行，实验设计符合学术规范，结果具有说服力。

### 6. 主要结论与发现
*   **性能突破**：LinkVLA 在 Bench2Drive 上取得了 **91.01** 的驾驶得分，显著超过了之前的 SOTA 模型 SimLingo（85.07）。
*   **效率提升**：相比于纯自回归生成方式，C2F 策略将推理延迟从 361ms 降低至 **48ms**，节省了约 **86%** 的时间，且性能不降反升。
*   **对齐效果**：引入“动作理解”辅助任务后，模型在复杂指令（如特定目标变道）下的遵循成功率大幅提升，证明了双向学习对语义对齐的有效性。

### 7. 优点
*   **架构优雅**：通过共享码本从底层消除了模态隔阂，而非仅仅在特征层做融合。
*   **实用性强**：C2F 生成策略解决了 VLA 模型落地最核心的实时性痛点。
*   **数学严谨**：引入对数变换和空间软标签，充分考虑了自动驾驶对近场精度和空间拓扑的需求。

### 8. 不足与局限
*   **模拟器依赖**：所有实验均在 CARLA 模拟器中完成，虽然 Bench2Drive 具有挑战性，但与真实世界的复杂光影、传感器噪声及长尾场景仍有差距。
*   **算力门槛**：尽管推理高效，但训练过程仍依赖大量高性能 GPU（32x H20），对于资源受限的团队具有一定门槛。
*   **安全保障机制**：论文主要关注性能和对齐，对于 VLM 可能产生的“幻觉”如何影响驾驶安全，缺乏专门的硬性约束或安全兜底机制讨论。

（完）
