---
title: "AdaptOrch: Task-Adaptive Multi-Agent Orchestration in the Era of LLM Performance Convergence"
title_zh: AdaptOrch：大语言模型性能趋同时代下的任务自适应多智能体编排
authors: Geunbin Yu
date: 2026-02-18
pdf: "https://arxiv.org/pdf/2602.16873v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 任务自适应多智能体编排与拓扑结构
tldr: 随着大模型性能趋于一致，单模型选择的收益递减。本文提出 AdaptOrch 框架，强调多智能体编排拓扑（并行、串行、层级、混合）对系统性能的决定性作用。通过任务自适应的拓扑路由算法和合成协议，AdaptOrch 在多个基准测试中显著提升了性能，证明了编排设计是独立于模型规模的关键优化方向。
selection_source: fresh_fetch
motivation: 在大模型性能趋同的背景下，研究重点应从选择单一最佳模型转向优化多智能体之间的协作拓扑结构。
method: 提出 AdaptOrch 框架，利用拓扑路由算法根据任务依赖图动态选择最优编排模式，并结合自适应合成协议处理输出。
result: "在编程、推理和检索增强生成任务中，该方法比静态单拓扑基准提升了 12-23% 的性能。"
conclusion: 编排拓扑设计已成为独立于模型缩放的顶级优化目标，能够有效提升多智能体系统的整体效能。
---

## 摘要
随着来自不同供应商的大语言模型在基准测试性能上趋于一致，为每个任务选择单一最佳模型的传统范式所带来的收益正在递减。我们认为，编排拓扑（即多个智能体如何协调、并行和综合的结构化组合）现在对系统级性能的影响已超过了单个模型的能力。我们提出了 AdaptOrch，这是一个用于任务自适应多智能体编排的形式化框架，它根据任务依赖图和经验推导的领域特征，在四种规范拓扑（并行、顺序、层级和混合）中进行动态选择。我们的框架引入了三项关键贡献：(1) 性能趋同缩放定律，形式化了编排选择优于模型选择的条件；(2) 拓扑路由算法，在 O(|V| + |E|) 时间内将任务分解的有向无环图（DAG）映射到最优编排模式；(3) 自适应综合协议，具有可证明的终止保证和针对并行智能体输出的启发式一致性评分。我们在代码编写 (SWE-bench)、推理 (GPQA) 和检索增强生成任务中验证了 AdaptOrch，结果表明，即使使用相同的基础模型，拓扑感知编排也比静态单一拓扑基准提升了 12-23%。我们的研究结果确立了编排设计是独立于模型缩放的一等优化目标。

## Abstract
As large language models from diverse providers converge toward comparable benchmark performance, the traditional paradigm of selecting a single best model per task yields diminishing returns. We argue that orchestration topology -- the structural composition of how multiple agents are coordinated, parallelized, and synthesized -- now dominates system-level performance over individual model capability. We present AdaptOrch, a formal framework for task-adaptive multi-agent orchestration that dynamically selects among four canonical topologies (parallel, sequential, hierarchical, and hybrid) based on task dependency graphs and empirically derived domain characteristics. Our framework introduces three key contributions: (1) a Performance Convergence Scaling Law, formalizing conditions under which orchestration selection outweighs model selection; (2) a Topology Routing Algorithm that maps task decomposition DAGs to optimal orchestration patterns in O(|V| + |E|) time; and (3) an Adaptive Synthesis Protocol with provable termination guarantees and heuristic consistency scoring for parallel agent outputs. We validate AdaptOrch across coding (SWE-bench), reasoning (GPQA), and retrieval-augmented generation tasks, demonstrating that topology-aware orchestration achieves 12-23% improvement over static single-topology baselines, even when using identical underlying models. Our results establish orchestration design as a first-class optimization target independent of model scaling.

---

## 论文详细总结（自动生成）

这篇论文由 Geunbin Yu 撰写，发表于 2026 年 2 月，探讨了在大语言模型（LLM）性能趋同的背景下，如何通过优化多智能体编排拓扑结构来提升系统整体性能。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：到 2026 年初，顶级 LLM（如 GPT-4o, Claude 3.5, Gemini 2.0 等）在主流基准测试上的得分差距已缩小至 2-5% 以内。
*   **核心问题**：当模型个体能力达到平台期时，传统的“选择最强模型”策略收益递减。
*   **核心假设**：系统级性能的优化重点已从“模型选择”转向“编排设计”，即如何组织多个智能体的协作结构（拓扑结构）。
*   **研究意义**：提出了 AdaptOrch 框架，旨在根据任务特征动态调整智能体之间的协作模式，以实现性能的最优增长。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：将任务分解为有向无环图（DAG），通过分析 DAG 的结构属性（宽度、深度、耦合度），从四种规范拓扑中选择最优的一种。
*   **关键技术细节**：
    *   **性能趋同缩放定律**：从理论上证明了当模型性能差距 $\epsilon$ 趋于 0 时，编排拓扑带来的性能方差将远超模型选择带来的方差。
    *   **任务分解与 DAG 构建**：利用 LLM 将复杂任务拆解为子任务，并标注依赖关系、计算成本和耦合强度（$c \in [0, 1]$）。
    *   **拓扑路由算法**：一个时间复杂度为 $O(|V| + |E|)$ 的线性算法，根据并行宽度 ($\omega$)、关键路径深度 ($\delta$) 和耦合密度 ($\gamma$) 自动选择：
        1.  **并行拓扑 ($\tau_P$)**：子任务独立执行，最后合并。
        2.  **顺序拓扑 ($\tau_S$)**：按依赖顺序执行，传递完整上下文。
        3.  **层级拓扑 ($\tau_H$)**：由领导智能体进行分发、监控和调解。
        4.  **混合拓扑 ($\tau_X$)**：将 DAG 分层，层内并行，层间顺序。
    *   **自适应合成协议**：使用基于嵌入相似度的一致性评分（CS）来验证输出。若一致性过低，则触发“重新路由”，增加耦合约束并重试。

### 3. 实验设计
*   **数据集/场景**：
    *   **编程**：SWE-bench Verified（500 个实例），考察多文件 Bug 修复。
    *   **推理**：GPQA Diamond（198 个实例），考察研究生水平的科学问题。
    *   **RAG**：HotpotQA（500 个实例），考察多跳问答。
*   **对比方法（Baselines）**：
    *   **Single Best**：各基准测试中表现最好的单一模型。
    *   **MoA-3L**：3 层 Mixture-of-Agents 架构。
    *   **Static-Parallel/Sequential**：固定的并行或顺序编排模式。
    *   **LLM-Blender**：基于排序的输出选择系统。
    *   **Self-MoA**：单一模型的多次采样自洽性投票（计算量对齐）。
*   **底层模型**：GPT-4o-mini, Claude 3.5 Haiku, Gemini 2.0 Flash, Llama 3.3 70B, Qwen 2.5 72B。

### 4. 资源与算力
*   **算力说明**：论文主要基于 API 调用进行实验，未提及具体的 GPU 训练时长。
*   **实验成本**：
    *   SWE-bench 实验约消耗 $1,200 API 费用。
    *   GPQA 约 $180，HotpotQA 约 $350。
*   **运行效率**：路由算法在单核 CPU 上运行时间 <50ms，相对于 LLM 推理延迟几乎可以忽略。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了三大核心领域（代码、推理、检索），总计超过 1,000 个测试实例。
*   **消融实验**：非常充分。分别验证了自适应路由、合成协议、DAG 耦合分析、失败重路由等组件的贡献。
*   **客观性**：每组实验运行 3 次并报告标准差；使用了 15% 的保留开发集进行超参数（如耦合阈值）校准，以防止测试集泄露。

### 6. 主要结论与发现
*   **性能提升**：AdaptOrch 在所有基准测试中均表现最优，比单一模型提升 12-23%，比静态编排提升 4.5-9.8%。
*   **拓扑分布**：混合拓扑 ($\tau_X$) 是最常用的（约 50%），说明现实任务通常兼具并行和顺序特征。
*   **领域差异**：编程任务更倾向于混合/并行拓扑；而 GPQA 推理任务则更依赖顺序/层级拓扑。
*   **Token 效率**：相比 MoA 等固定多层架构，AdaptOrch 通过按需分配计算资源，显著降低了 Token 消耗。

### 7. 优点
*   **理论支撑**：提出了形式化的缩放定律，为“编排优先”的观点提供了数学依据。
*   **高灵活性**：不绑定特定模型，能够随模型能力的提升而自动受益。
*   **实用性强**：路由算法极快，且自适应合成协议解决了并行执行中的冲突问题。
*   **成本感知**：相比盲目增加模型层数，该方法在准确率和成本之间取得了更好的帕累托最优。

### 8. 不足与局限
*   **分解依赖**：系统性能高度依赖于“分解智能体”的质量，若初始任务拆解错误，后续拓扑再好也难以挽回。
*   **耦合估计较粗糙**：目前对子任务间耦合度的估计采用离散值（0, 0.3, 0.7, 1.0），未来可能需要更精细的连续值估计。
*   **并发限制**：高并行宽度（$\omega$）可能导致触发 API 的频率限制（Rate Limits）。
*   **覆盖范围**：尚未在多模态任务或长文本生成任务中进行验证。

（完）
