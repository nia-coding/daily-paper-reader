---
title: "ABot-M0: VLA Foundation Model for Robotic Manipulation with Action Manifold Learning"
title_zh: ABot-M0：基于动作流形学习的机器人操控 VLA 基础模型
authors: "Yandan Yang, Shuang Zeng, Tong Lin, Xinyuan Chang, Dekang Qi, Junjin Xiao, Haoyun Liu, Ronghan Chen, Yuzhi Chen, Dongjie Huo, Feng Xiong, Xing Wei, Zhiheng Ma, Mu Xu"
date: 2026-02-11
pdf: "https://arxiv.org/pdf/2602.11236v1"
tags: ["query:sr-ai"]
score: 10.0
evidence: 用于机器人操控和具身智能体的 VLA 基础模型
tldr: ABot-M0是一个通用的机器人操纵基础模型框架，旨在解决异构数据和表示不一致的问题。它构建了包含600万条轨迹的UniACT数据集，并提出动作流形学习（AML）方法，利用DiT架构将高维动作预测转化为低维流形投影，结合双流感知机制增强空间理解，实现了跨平台、跨任务的高效泛化。
selection_source: fresh_fetch
motivation: 针对机器人领域数据碎片化、表示不一致及训练目标不匹配导致的通用具身智能体构建难题。
method: 构建大规模统一数据集UniACT，并提出基于动作流形假设的AML学习方法及集成几何先验的双流感知架构。
result: 实验证明各组件可独立运行并产生叠加效应，显著提升了动作预测的效率、稳定性和跨平台的泛化能力。
conclusion: ABot-M0通过系统化的数据治理和创新的动作学习范式，为实现“一个大脑，多种形态”的通用具身智能提供了有效路径。
---

## 摘要
在不同硬件上构建通用具身智能体仍然是机器人领域的核心挑战，通常被称为“一脑多形”范式。碎片化的数据、不一致的表示以及未对齐的训练目标阻碍了这一进程。我们提出了 ABot-M0，这是一个构建了系统化数据整理流水线的框架，同时联合优化模型架构和训练策略，实现了将异构原始数据端到端地转化为统一、高效的表示。我们从六个公开数据集中清洗、标准化并平衡样本，构建了 UniACT-dataset，这是一个包含超过 600 万条轨迹和 9500 小时数据的大规模数据集，涵盖了多种机器人形态和任务场景。统一预训练提高了跨平台和跨任务的知识迁移与泛化能力，支持通用具身智能。为了提高动作预测的效率和稳定性，我们提出了动作流形假设（Action Manifold Hypothesis）：有效的机器人动作并非存在于完整的高维空间中，而是位于受物理定律和任务约束支配的低维光滑流形上。基于此，我们引入了动作流形学习（AML），它使用 DiT 骨干网络直接预测清晰、连续的动作序列。这将学习过程从去噪转变为向可行流形的投影，从而提高了解码速度和策略稳定性。ABot-M0 通过双流机制支持模块化感知，该机制集成了 VLM 语义与几何先验，以及来自即插即用 3D 模块（如 VGGT 和 Qwen-Image-Edit）的多视图输入，在不修改骨干网络的情况下增强了空间理解能力，并缓解了标准 VLM 在 3D 推理方面的局限性。实验表明，各组件独立运行并具有累加效益。我们将发布所有代码和流水线，以供复现和未来研究。

## Abstract
Building general-purpose embodied agents across diverse hardware remains a central challenge in robotics, often framed as the ''one-brain, many-forms'' paradigm. Progress is hindered by fragmented data, inconsistent representations, and misaligned training objectives. We present ABot-M0, a framework that builds a systematic data curation pipeline while jointly optimizing model architecture and training strategies, enabling end-to-end transformation of heterogeneous raw data into unified, efficient representations. From six public datasets, we clean, standardize, and balance samples to construct UniACT-dataset, a large-scale dataset with over 6 million trajectories and 9,500 hours of data, covering diverse robot morphologies and task scenarios. Unified pre-training improves knowledge transfer and generalization across platforms and tasks, supporting general-purpose embodied intelligence. To improve action prediction efficiency and stability, we propose the Action Manifold Hypothesis: effective robot actions lie not in the full high-dimensional space but on a low-dimensional, smooth manifold governed by physical laws and task constraints. Based on this, we introduce Action Manifold Learning (AML), which uses a DiT backbone to predict clean, continuous action sequences directly. This shifts learning from denoising to projection onto feasible manifolds, improving decoding speed and policy stability. ABot-M0 supports modular perception via a dual-stream mechanism that integrates VLM semantics with geometric priors and multi-view inputs from plug-and-play 3D modules such as VGGT and Qwen-Image-Edit, enhancing spatial understanding without modifying the backbone and mitigating standard VLM limitations in 3D reasoning. Experiments show components operate independently with additive benefits. We will release all code and pipelines for reproducibility and future research.

---

## 论文详细总结（自动生成）

以下是对论文《ABot-M0: VLA Foundation Model for Robotic Manipulation with Action Manifold Learning》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心挑战**：构建通用的具身智能体（“一脑多形”范式）面临三大障碍：
    1.  **数据规模不足且碎片化**：具身数据获取成本高，且各数据集之间相互隔离。
    2.  **数据质量与表示不一致**：不同平台的动作表示、坐标系和控制频率各异，难以提取通用模式。
    3.  **训练目标不匹配**：现有的视觉语言动作（VLA）模型多基于 VLM，其编码器侧重语义识别而非 3D 结构或物理动力学。
*   **研究目标**：通过系统化的数据治理和模型架构优化，将异构原始数据转化为统一高效的表示，实现跨平台、跨任务的通用机器人操控能力。

### 2. 方法论：核心思想与关键技术
*   **UniACT 数据集构建**：
    *   整合了 OXE、AgiBot-Beta、RoboCoin 等 6 个开源数据集，构建了包含 **600 万条轨迹、9500+ 小时、20+ 种机器人形态** 的大规模数据集。
    *   **标准化处理**：统一转换为 LeRobot v2 格式；动作统一为末端执行器（EEF）的增量动作（Delta Actions）；旋转采用旋转向量（Rotation Vectors）以避免奇异性。
    *   **双臂统一策略**：采用“补零至双臂”（Pad-to-dual）策略，使单一模型能同时处理单臂和双臂任务。
*   **动作流形学习（Action Manifold Learning, AML）**：
    *   **动作流形假设**：认为有效的机器人动作并非随机分布，而是存在于受物理定律和任务约束支配的低维、光滑流形上。
    *   **核心技术**：放弃传统的噪声预测（$\epsilon$-pred）或速度预测（$v$-pred），让 DiT（Diffusion Transformer）骨干网络**直接预测清晰的动作序列（a-pred）**。这降低了学习复杂度，提高了解码速度和策略稳定性。
*   **双流感知架构**：
    *   **语义流**：利用 Qwen3-VL 提取深层视觉语义特征。
    *   **几何流（即插即用）**：引入 VGGT 提取场景结构特征，或利用 Qwen-Image-Edit 合成多视图输入，增强 VLM 欠缺的 3D 空间推理能力。

### 3. 实验设计
*   **测试基准（Benchmarks）**：
    *   **LIBERO**：评估知识迁移和长程任务能力。
    *   **LIBERO-Plus**：评估在视觉、语言、光照等扰动下的鲁棒性。
    *   **RoboCasa GR1 Tabletop**：评估复杂家居场景下的双臂协同。
    *   **RoboTwin 2.0**：评估在重度随机化场景下的泛化性。
*   **对比方法**：包括 $\pi_0$、$\pi_{0.5}$、OpenVLA、UniVLA、GR00T-N1.6、X-VLA 等当前最强的 VLA 基准模型。

### 4. 资源与算力
*   **训练细节**：论文提到预训练阶段使用了 **1024 的总 Batch Size**，训练步数为 **100K Steps**，学习率为 1e-5。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号（如 H100 或 A100）及具体数量，但从其处理的数据规模（6M 轨迹）和模型参数量（VLM 4B + DiT 0.16B）推断，需要大规模 GPU 集群支持。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个主流仿真基准上进行了详尽测试，涵盖了单臂、双臂、全身控制等多种形态。
*   **消融实验**：
    *   对比了三种数据采样策略（轨迹均匀、任务均匀、形态均匀），证明了“任务均匀采样”在跨形态泛化上的优越性。
    *   验证了 VLM 不同层特征对动作预测的影响。
    *   对比了 AML 与传统噪声预测在不同去噪步数和动作块大小（Chunk Size）下的表现。
*   **客观性**：实验设置了统一的训练环境进行对比，并引入了 LIBERO-Plus 这种专门测试鲁棒性的基准，实验设计较为客观、全面。

### 6. 主要结论与发现
*   **AML 的优越性**：直接预测动作（AML）比预测噪声更高效，尤其在处理高维动作空间和长程任务时，稳定性显著提升。
*   **数据治理至关重要**：统一的动作表示和标准化的清洗流程是模型泛化能力的基础。
*   **3D 模块的增益**：通过即插即用的 3D 模块注入几何先验，能显著提升 VLA 模型在空间敏感任务（如插入、折叠）中的表现。
*   **性能领先**：ABot-M0 在 LIBERO（98.6%）、RoboCasa（58.3%）等多个榜单上刷新了 SOTA 记录。

### 7. 优点
*   **系统性强**：不仅关注模型架构，还建立了一套完整的数据清洗、标准化和平衡流水线。
*   **理论创新**：提出的“动作流形假设”为具身智能的动作生成提供了新的视角，有效解决了高维动作空间学习难的问题。
*   **模块化设计**：感知层支持即插即用的 3D 专家模块，无需重新训练骨干网络即可增强空间能力。

### 8. 不足与局限
*   **硬件细节缺失**：未披露具体的算力消耗和训练时长，不利于其他团队评估复现成本。
*   **真实世界验证**：虽然在多个复杂的仿真环境中表现优异，但论文中缺乏大规模真实机器人部署的量化数据（尽管提到支持真实部署）。
*   **数据偏差风险**：尽管进行了数据平衡，但由于原始数据中单臂数据（如 OXE）占比极高，模型在极复杂的双臂精细操作中可能仍存在分布偏差。

（完）
