---
title: "G2CP: A Graph-Grounded Communication Protocol for Verifiable and Efficient Multi-Agent Reasoning"
title_zh: G2CP：一种用于可验证且高效多智能体推理的图基通信协议
authors: "Karim Ben Khaled, Davy Monticolo"
date: 2026-02-13
pdf: "https://arxiv.org/pdf/2602.13370v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 用于多智能体协作的图基通信协议
tldr: 针对大语言模型多智能体系统中自然语言通信导致的语义漂移、幻觉传播及效率低下问题，本文提出了一种基于图结构的通信协议 G2CP。该协议将智能体间的对话转化为对共享知识图谱的显式图操作（如遍历、更新等），从而实现可验证的推理路径。在工业知识管理场景下的实验表明，G2CP 显著降低了通信成本，提升了任务准确率，并消除了级联幻觉，为多智能体协作提供了高效且可审计的新范式。
selection_source: fresh_fetch
motivation: 旨在解决多智能体系统中使用自然语言通信所引发的语义歧义、幻觉累积及 Token 消耗过高的问题。
method: 提出一种基于图的通信协议，使智能体通过在共享知识图谱上执行结构化的图操作而非自由文本进行协作。
result: "实验显示该协议减少了 73% 的通信 Token 消耗，并将任务准确率提升了 34%，同时实现了推理过程的全程审计。"
conclusion: G2CP 成功将多智能体通信从语言驱动转向结构驱动，显著提升了复杂推理任务的效率、准确性与可验证性。
---

## 摘要
由大语言模型驱动的多智能体系统面临一个关键挑战：智能体通过自然语言进行通信，这导致了语义漂移、幻觉传播以及低效的 Token 消耗。我们提出了 G2CP（Graph-Grounded Communication Protocol，图基通信协议），这是一种结构化的智能体通信语言，其中的消息是图操作而非自由文本。智能体在共享知识图谱上交换显式的遍历指令、子图片段和更新操作，从而实现可验证的推理轨迹并消除歧义。我们在一个工业知识管理系统中验证了 G2CP，在该系统中，专门的智能体（诊断、程序、综合和摄取）协同工作以回答复杂查询。在 500 个工业场景和 21 个真实维护案例上的实验结果表明，与自由文本基线相比，G2CP 将智能体间的通信 Token 减少了 73%，任务完成准确率提高了 34%，消除了级联幻觉，并生成了完全可审计的推理链。G2CP 代表了多智能体系统中从语言通信到结构化通信的根本转变，对任何需要精确智能体协作的领域都具有重要意义。代码、数据和评估脚本已公开。

## Abstract
Multi-agent systems powered by Large Language Models face a critical challenge: agents communicate through natural language, leading to semantic drift, hallucination propagation, and inefficient token consumption. We propose G2CP (Graph-Grounded Communication Protocol), a structured agent communication language where messages are graph operations rather than free text. Agents exchange explicit traversal commands, subgraph fragments, and update operations over a shared knowledge graph, enabling verifiable reasoning traces and eliminating ambiguity. We validate G2CP within an industrial knowledge management system where specialized agents (Diagnostic, Procedural, Synthesis, and Ingestion) coordinate to answer complex queries. Experimental results on 500 industrial scenarios and 21 real-world maintenance cases show that G2CP reduces inter-agent communication tokens by 73%, improves task completion accuracy by 34% over free-text baselines, eliminates cascading hallucinations, and produces fully auditable reasoning chains. G2CP represents a fundamental shift from linguistic to structural communication in multi-agent systems, with implications for any domain requiring precise agent coordination. Code, data, and evaluation scripts are publicly available.

---

## 论文详细总结（自动生成）

以下是对论文《G2CP: A Graph-Grounded Communication Protocol for Verifiable and Efficient Multi-Agent Reasoning》的结构化深入总结：

### 1. 核心问题与研究背景
*   **核心问题**：当前基于大语言模型（LLM）的多智能体系统（MAS）默认使用**自然语言**作为通信媒介。这导致了三大关键挑战：
    *   **语义漂移（Semantic Drift）**：信息在多个智能体传递过程中意义逐渐偏离。
    *   **幻觉传播（Hallucination Propagation）**：一个智能体产生的虚假信息会误导下游所有智能体的推理。
    *   **计算浪费**：冗长的自然语言对话消耗了远超实际信息量的 Token。
*   **研究背景**：在工业维护等高精度领域，自然语言的歧义性（如“主泵”指代不明）可能导致严重的诊断错误。论文旨在将智能体通信从“语言驱动”转向“结构驱动”。

### 2. 方法论
*   **核心思想**：提出 **G2CP（图基通信协议）**。智能体不再发送自由文本，而是发送针对共享**知识图谱（KG）**的显式操作指令（如遍历、更新）。
*   **关键技术细节**：
    *   **形式化定义**：消息被定义为五元组 $\langle sender, receiver, perf, op, ctx \rangle$。
    *   **言语行为（Performatives）**：借鉴 FIPA-ACL 标准，定义了 `REQUEST`（请求执行）、`INFORM`（告知结果）、`QUERY`（查询存在性）、`UPDATE`（更新图结构）等 7 种行为。
    *   **图操作（Graph Operations）**：
        *   **遍历（TRAVERSE）**：指定起始节点、边类型过滤、跳数（Hop）和返回格式（子图、路径或叶节点）。
        *   **更新（UPDATE）**：对图进行增删改操作。
    *   **智能体架构**：系统包含四类专业智能体：诊断智能体（处理因果关系）、程序智能体（检索维护步骤）、综合智能体（分析历史模式）和摄取智能体（维护图数据）。
*   **算法流程**：
    1.  **查询解析**：Dispatcher 智能体利用 LLM 将用户自然语言查询转化为实体和意图。
    2.  **实体链接**：通过嵌入向量匹配将实体映射到图节点。
    3.  **协议转换**：根据意图动态生成 G2CP 操作指令。
    4.  **执行与反馈**：智能体在 Neo4j 图数据库上执行确定性的 Cypher 查询，并以结构化子图形式返回结果。

### 3. 实验设计
*   **数据集/场景**：
    *   **合成数据集**：构建了一个包含 1,247 个节点和 3,892 条边的“Turbomatic MKII 液压机”知识库。
    *   **真实场景**：来自液压设备制造商和汽车零部件供应商的 21 个真实工业维护案例。
*   **对比方法（Benchmarks）**：
    *   **FTMA (Free-Text Multi-Agent)**：完全使用自然语言通信的基线。
    *   **JSMA (JSON-Structured Multi-Agent)**：使用预定义 JSON 格式通信的基线。
    *   **Single-Agent**：不进行智能体分解的单体 RAG 系统。
*   **评估指标**：任务完成准确率、Token 效率、幻觉率、级联错误率、可审计性得分。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或训练时长。
*   **模型使用**：实验中提到使用了 **GPT-4** 用于查询理解，**Llama 3 70B** 用于最终响应生成。图存储使用了 **Neo4j**，消息传递使用了 **Apache Kafka**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对合成场景进行了 **500 组**复杂查询测试。
    *   针对真实工业案例进行了 **21 组**实测。
    *   进行了**消融实验**，分析了节点 ID、边约束、跳数限制等组件对性能的影响。
    *   包含**人类评估**：由专业工业维护技术人员对输出的清晰度、完整性和信任度进行打分。
*   **充分性评价**：实验设计较为充分，涵盖了从合成数据到真实场景的跨越，并提供了统计显著性分析（$p < 0.001$），对比维度全面，客观性较强。

### 6. 主要结论与发现
*   **性能提升**：G2CP 的任务准确率达到 90%，比自然语言基线（67%）提升了 **34%**。
*   **成本降低**：智能体间通信的 Token 消耗降低了 **73%**（从平均 2,847 降至 768）。
*   **消除幻觉**：幻觉率从 23% 降至 **2%**，级联错误率降至 **0%**。
*   **可审计性**：由于所有推理步骤都对应显式的图操作，系统实现了 100% 的推理链可审计性。
*   **可扩展性**：随着图规模增加，G2CP 的响应时间呈亚线性增长，表现优于自然语言方法。

### 7. 优点
*   **确定性与可验证性**：将不可控的语言生成转化为可重放、可验证的图操作，解决了 LLM 在严肃领域应用的核心痛点。
*   **高效性**：极大地压缩了智能体协作的通信开销。
*   **理论完备**：提供了关于确定性、审计性和通信复杂度的形式化证明。
*   **兼容性**：成功将经典的智能体通信协议（FIPA-ACL）与现代神经架构（LLM）相结合。

### 8. 不足与局限
*   **知识图谱依赖**：系统高度依赖高质量的结构化知识图谱。对于缺乏图谱的领域，前期构建成本较高（论文提到需 200-500 小时）。
*   **知识边界限制**：如果所需信息不在图中，G2CP 无法像自由文本智能体那样利用 LLM 的“常识”进行推理，存在灵活性损失。
*   **动态数据处理**：对于实时传感器数据等非结构化流数据的集成，目前仍需混合外部 API，协议尚未完全覆盖。

（完）
