---
title: "VeRO: An Evaluation Harness for Agents to Optimize Agents"
title_zh: VeRO：一个用于智能体优化智能体的评估框架
authors: "Varun Ursekar, Apaar Shanker, Veronica Chatrath, Yuan, Xue, Sam Denton"
date: 2026-02-25
pdf: "https://arxiv.org/pdf/2602.22480v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 代码智能体与软件工程优化的评估框架
tldr: 针对智能体优化（即通过迭代改进目标智能体）缺乏系统性评估的问题，本文推出了 VERO 框架。该框架提供了一个包含版本化快照、预算控制评估和结构化执行追踪的可重复评估环境，并配套了基准测试集。通过 VERO，研究者能够对比不同优化器配置，分析并验证哪些修改能有效提升智能体性能，为编码智能体的自我优化研究提供了重要的基础设施和实证参考。
selection_source: fresh_fetch
motivation: 传统的软件工程评估工具无法有效处理智能体中确定性代码与随机大模型补全交织的特性，导致智能体优化任务缺乏系统性的评估手段。
method: 开发了名为 VERO 的评估框架，通过版本化管理、结构化追踪和基准测试套件，为智能体优化提供可重复的实验环境。
result: 利用 VERO 进行的实证研究对比了多种优化器配置，并识别出了能够可靠提升目标智能体性能的关键修改类型。
conclusion: VERO 填补了智能体优化评估领域的空白，为提升编码智能体的自主优化能力提供了核心支撑工具。
---

## 摘要
编程智能体的一个重要新兴应用是智能体优化：通过“编辑-执行-评估”循环对目标智能体进行迭代改进。尽管这一任务具有重要意义，但学术界仍缺乏对编程智能体在该任务上表现的系统性理解。智能体优化与传统的软件工程有本质区别：目标智能体将确定性代码与随机的大语言模型（LLM）补全交织在一起，需要对中间推理过程和下游执行结果进行结构化捕获。为了应对这些挑战，我们推出了 VERO（版本控制、奖励与观测），它提供了：(1) 一个具有版本化智能体快照、预算受控评估和结构化执行追踪的可复现评估框架；(2) 一套包含目标智能体、任务及参考评估程序的基准测试集。利用 VERO，我们进行了一项实证研究，比较了不同任务下的优化器配置，并分析了哪些修改能可靠地提升目标智能体的性能。我们发布了 VERO，旨在支持将智能体优化作为编程智能体核心能力的相关研究。

## Abstract
An important emerging application of coding agents is agent optimization: the iterative improvement of a target agent through edit-execute-evaluate cycles. Despite its relevance, the community lacks a systematic understanding of coding agent performance on this task. Agent optimization differs fundamentally from conventional software engineering: the target agent interleaves deterministic code with stochastic LLM completions, requiring structured capture of both intermediate reasoning and downstream execution outcomes. To address these challenges, we introduce VERO (Versioning, Rewards, and Observations), which provides (1) a reproducible evaluation harness with versioned agent snapshots, budget-controlled evaluation, and structured execution traces, and (2) a benchmark suite of target agents and tasks with reference evaluation procedures. Using VERO, we conduct an empirical study comparing optimizer configurations across tasks and analyzing which modifications reliably improve target agent performance. We release VERO to support research on agent optimization as a core capability for coding agents.

---

## 论文详细总结（自动生成）

这是一份关于论文《VeRO: An Evaluation Harness for Agents to Optimize Agents》的结构化深入总结：

---

# 论文总结：VeRO —— 智能体优化智能体的评估框架

## 1. 核心问题与整体含义
*   **研究背景**：随着大模型智能体（LLM Agents）在生产环境的普及，优化智能体性能（改进提示词、工具逻辑、工作流）成为了一个高频且繁琐的人工任务。
*   **核心问题**：现有的编码智能体基准（如 SWE-Bench）侧重于通用软件工程，而“智能体优化”任务具有独特性——它涉及确定性代码与随机 LLM 补全的交织。目前缺乏一个系统化的框架来衡量编码智能体作为“优化器”去迭代改进另一个“目标智能体”的能力。
*   **研究动机**：通过自动化“编辑-执行-评估”循环，实现智能体的自我演进或自主优化，从而降低人工开发成本。

## 2. 方法论
论文提出了 **VeRO (Versioning, Rewards, and Observations)** 框架，其核心思想是将智能体优化形式化为一个受预算约束的黑盒优化问题。

*   **核心思想**：定义优化任务 $P$，目标是在给定预算 $B$（评估调用次数）内，找到能使目标智能体在测试集上表现最大化的代码实现 $A^*$。
*   **关键技术组件**：
    1.  **版本化（Versioning）**：利用 Git Worktree 隔离修改，自动提交（auto-commit）每次变更，确保优化轨迹可追溯、可回滚。
    2.  **预算强制执行（Budget Enforcement）**：通过 gated 接口追踪评估请求，一旦达到预设次数即停止，确保不同优化器之间的公平比较。
    3.  **结构化追踪（Structured Tracing）**：捕获目标智能体的中间推理步骤、工具调用及得分，为优化器提供多维度的反馈信号。
    4.  **标准化接口**：提供 `DatasetViewer`（数据查看）、`ExperimentRunner`（运行评估）、`GitControl`（版本管理）等工具，使优化过程标准化。
*   **算法流程**：优化器（编码智能体）观察当前代码和执行轨迹 -> 提出修改假设 -> 实施代码编辑 -> 触发自动评估 -> 根据反馈（奖励/观察）进入下一轮迭代。

## 3. 实验设计
*   **任务与数据集**：涵盖了五个维度：
    *   **GAIA**：多步推理与工具使用。
    *   **GPQA**：研究生水平的科学问答。
    *   **MATH**：数学竞赛题目。
    *   **TAU-Bench Retail**：真实世界的工具交互。
    *   **SimpleQA**：事实性短篇问答。
*   **对比方法（优化器配置）**：
    *   **VeRO Scaffold**：论文自研的最小化框架，分为 Default（全工具）、Orchestrator（偏重代理委派）、Resources-Only（仅限提示词修改）。
    *   **Claude Code**：Anthropic 发布的原生编码智能体，分为 Pure（无 VeRO 工具）和 VeRO Tools（集成 VeRO 接口）版本。
    *   **底层模型**：对比了 Claude 3.7 Sonnet、Claude 3.5 Opus 及 GPT-4.5 (文中称为 GPT-5.2-Codex)。
*   **Benchmark 设定**：以 GPT-4o-mini 驱动的初始智能体作为 Baseline，衡量优化后的性能提升（Lift）。

## 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **资源消耗**：由于该研究侧重于推理侧的智能体交互，主要的资源消耗体现在大模型 API 的调用成本上。实验涉及 105 组基准运行，每组包含多次迭代评估，具有较高的 API 成本。

## 5. 实验数量与充分性
*   **实验规模**：
    *   **基准研究**：5 个任务 × 7 种配置 × 3 次重复 = 105 组实验。
    *   **鲁棒性研究**：测试了优化后的代码在不同目标模型（Gemini, Qwen, GPT-4 等）上的迁移效果。
    *   **案例研究**：针对 Pawn（简单）和 Knight（复杂）两种智能体进行了 4 种指令模板的消融实验。
*   **充分性评价**：实验设计较为全面，不仅关注最终得分，还通过语义标签（Prompt/Tool/Workflow）分析了优化行为的分布，并探讨了指令敏感性和跨任务泛化性，实验结果具有统计意义和客观性。

## 6. 主要结论与发现
1.  **VeRO 的必要性**：相比于没有专用工具的编码智能体，集成 VeRO 框架的优化器平均性能提升了 8% 以上。
2.  **任务差异性**：工具使用类任务（GAIA, Retail）优化空间大；而纯推理任务（MATH, GPQA）极难通过代码修改获得提升。
3.  **优化行为偏好**：当前的编码智能体高度依赖“提示词修改”（占比 >50%），在深层架构和复杂工具逻辑的修改上表现乏力。
4.  **指令敏感性**：优化器的表现受指令模板影响极大。对于简单智能体，详细的“食谱”指导有效；对于复杂智能体，给予“创作自由”反而表现更好。
5.  **方差-性能权衡**：高方差的指令模板往往能带来更高的性能峰值，但稳定性较差。

## 7. 优点
*   **框架创新**：首次为“智能体优化智能体”这一前沿领域提供了标准化的基础设施。
*   **工程严谨性**：引入 Git 版本控制和严格的预算管理，解决了智能体实验中常见的不可复现和过度拟合训练集的问题。
*   **深度洞察**：通过对 Git Diff 的语义分析，揭示了当前 LLM 在自动化编程任务中的行为模式（如熵的下降）。

## 8. 不足与局限
*   **预算定义局限**：仅以评估次数为预算，未考虑 Token 消耗、API 费用或墙钟时间。
*   **缺乏人类对比**：未提供人类专家在相同约束下优化智能体的基准数据。
*   **环境依赖**：依赖外部 API（如 Wikipedia），存在网络波动或内容更新导致的评估噪声。
*   **泛化风险**：针对特定任务优化的智能体可能会在其他任务上出现性能退化（负迁移）。

---
（完）
