---
title: "SWE-Hub: A Unified Production System for Scalable, Executable Software Engineering Tasks"
title_zh: SWE-Hub：一个用于可扩展、可执行软件工程任务的统一生产系统
authors: "Yucheng Zeng, Shupeng Li, Daxiang Dong, Ruijie Xu, Zimo Chen, Liwei Zheng, Yuxuan Li, Zhe Zhou, Haotian Zhao, Lun Tian, Heng Xiao, Tianshu Zhu, Longkun Hao, Jianmin Wu"
date: 2026-02-28
pdf: "https://arxiv.org/pdf/2603.00575v1"
tags: ["query:sr-ai"]
score: 10.0
evidence: 用于可扩展软件工程智能体任务的统一生产系统
tldr: SWE-Hub 是一个统一的软件工程数据生产系统，旨在解决现有训练和评估数据在可执行性、规模化和真实性方面的不足。该系统通过 Env Agent 自动化环境构建，利用 SWE-Scale 实现大规模 Bug 修复实例合成，并借助 Bug Agent 和 SWE-Architect 生成涵盖系统级回归和仓库级构建的复杂任务。它为软件工程智能体的全生命周期开发提供了持续、可扩展且高质量的任务流。
selection_source: fresh_fetch
motivation: 现有软件工程智能体的发展受限于缺乏可执行、可扩展且真实的训练与评估数据，且现有数据难以捕捉长周期能力。
method: 提出了由 Env Agent、SWE-Scale、Bug Agent 和 SWE-Architect 组成的统一生产栈，实现从环境自动化到多样化任务生成的全流程覆盖。
result: 成功构建了一个能够持续交付跨越软件工程全生命周期、具备高保真度和长周期特性的可执行任务流水线。
conclusion: SWE-Hub 通过整合环境自动化与大规模合成技术，为开发更强大的软件工程智能体奠定了坚实的数据基础。
---

## 摘要
软件工程智能体的进展日益受到用于训练和评估的可执行、可扩展且真实的数据稀缺的限制。这种稀缺性源于现有流程中的三个根本挑战：环境脆弱且难以跨语言复现；大规模合成真实的系统级漏洞计算成本高昂；现有数据主要由短周期修复组成，未能捕捉到如架构一致性等长周期能力。我们推出了 SWE-Hub，这是一个端到端系统，通过将环境自动化、可扩展合成和多样化任务生成统一到一个连贯的生产栈中，实现了数据工厂的抽象。在其基础层，Env Agent 通过将原始代码库快照自动转换为具有标准化接口的可复现、多语言容器环境，建立了一个共享的执行基底。基于此基底，SWE-Scale 引擎解决了高吞吐量生成的需求，结合跨语言代码分析与集群规模验证，以合成海量的局部漏洞修复实例。Bug Agent 通过合成涉及跨模块依赖的系统级回归，并配以描述可观察症状而非根本原因的类用户问题报告，生成高保真修复任务。最后，SWE-Architect 通过将自然语言需求转化为代码库规模的“构建代码库”任务，将任务范围从修复扩展到创建。通过集成这些组件，SWE-Hub 建立了一个统一的生产流水线，能够持续交付贯穿整个软件工程生命周期的可执行任务。

## Abstract
Progress in software-engineering agents is increasingly constrained by the scarcity of executable, scalable, and realistic data for training and evaluation. This scarcity stems from three fundamental challenges in existing pipelines: environments are brittle and difficult to reproduce across languages; synthesizing realistic, system-level bugs at scale is computationally expensive; and existing data predominantly consists of short-horizon repairs, failing to capture long-horizon competencies like architectural consistency. We introduce \textbf{SWE-Hub}, an end-to-end system that operationalizes the data factory abstraction by unifying environment automation, scalable synthesis, and diverse task generation into a coherent production stack. At its foundation, the \textbf{Env Agent} establishes a shared execution substrate by automatically converting raw repository snapshots into reproducible, multi-language container environments with standardized interfaces. Built upon this substrate, \textbf{SWE-Scale} engine addresses the need for high-throughput generation, combining cross-language code analysis with cluster-scale validation to synthesize massive volumes of localized bug-fix instances. \textbf{Bug Agent} generates high-fidelity repair tasks by synthesizing system-level regressions involving cross-module dependencies, paired with user-like issue reports that describe observable symptoms rather than root causes. Finally, \textbf{SWE-Architect} expands the task scope from repair to creation by translating natural-language requirements into repository-scale build-a-repo tasks. By integrating these components, SWE-Hub establishes a unified production pipeline capable of continuously delivering executable tasks across the entire software engineering lifecycle.

---

## 论文详细总结（自动生成）

这是一份关于论文《SWE-Hub: A Unified Production System for Scalable, Executable Software Engineering Tasks》的结构化深度总结：

### 1. 论文的核心问题与整体含义
**研究动机与背景：**
当前软件工程（SWE）智能体的发展面临严重的数据瓶颈。现有的训练和评估数据存在三个核心缺陷：
*   **环境脆弱性：** 真实代码库环境复杂，跨语言复现困难，手动配置成本极高。
*   **合成真实性不足：** 现有的自动合成 Bug 多局限于单文件或单函数，缺乏涉及跨模块依赖的系统级故障。
*   **任务维度单一：** 大多数基准测试（如 SWE-bench）集中在短周期的 Bug 修复，缺乏对架构规划、仓库级构建等长周期能力的考察。

**整体含义：**
百度千帆团队提出了 **SWE-Hub**，这不再是一个静态的数据集，而是一个**端到端的“数据工厂”生产系统**。它通过统一的执行基底，实现了从环境自动化构建到大规模、多维度、可执行任务生成的全流程工业化生产。

---

### 2. 方法论：核心思想与关键技术
SWE-Hub 采用分层架构，核心思想是**解耦环境逻辑与任务逻辑**：

*   **环境层（Execution Substrate）：**
    *   **Env Agent & Test Agent：** 自动识别代码库的构建工具（Maven, Pytest, NPM 等），生成幂等的安装脚本，并将其固化为 Docker 镜像。
    *   **标准化接口：** 定义了“环境就绪”标准，即无论何种语言，均通过统一的 `test_entrypoint` 触发验证并输出标准 JSON 结果。
*   **生产线 A：SWE-Scale（大规模修复合成）：**
    *   利用 **Tree-sitter** 作为多语言解析基底，通过预定义的 YAML 模板支持跨语言（Python, Java, Go 等）的语法树操作。
    *   采用**程序化修改**（如算子翻转、控制流混淆）和 **LLM 辅助修改**（语义重写）生成 Bug 候选。
*   **生产线 B：Bug Agent（真实回归合成）：**
    *   模拟“涟漪效应”，在 API 边界或配置层注入细微错误，导致故障在远离根因的模块触发。
    *   **Issue Agent** 负责生成类用户报告，严格遵循“症状与根因分离”原则，防止信息泄露。
*   **生产线 C：SWE-Architect（长周期构建）：**
    *   **代码挖空（Hollowing）：** 从真实仓库中提取核心模块并清空实现，保留结构。
    *   **逆向工程文档：** 通过 DOC Agent 和 API Agent 自动生成结构化的需求文档（PRD），要求智能体从零实现复杂功能。

---

### 3. 实验设计
由于本文属于**系统架构类论文（System Paper）**，其重点在于展示生产流水线的完备性与可扩展性，而非单一模型的打分：
*   **场景覆盖：** 涵盖了从局部修复（SWE-Scale）、系统级调试（Bug Agent）到仓库级构建（SWE-Architect）的全生命周期任务。
*   **Benchmark 关联：** 系统设计参考并扩展了 SWE-bench（修复类）和 NL2Repo（构建类）的评估范式。
*   **对比维度：** 论文对比了现有合成方法（如 SWE-Smith, SWE-Gym）在环境解耦、多语言支持、任务多样性方面的差异，强调了 SWE-Hub 的统一性。

---

### 4. 资源与算力
*   **算力架构：** 论文明确提到系统构建在 **Kubernetes (K8s)** 原生集群之上，利用容器化实现任务的并行验证。
*   **具体参数：** 文中**未明确说明**具体的 GPU 型号、数量或总训练时长。这主要是因为该系统侧重于数据生成（CPU 密集型的编译与测试验证）而非模型训练。系统设计目标是吞吐量随计算资源线性扩展。

---

### 5. 实验数量与充分性
*   **实验组数：** 论文通过详细的分类法（Taxonomy）展示了其合成能力的广度，例如在表 1 中列出了 10 余种程序化修改算子。
*   **充分性：** 作为一个生产系统，论文通过展示其对多种主流语言（Python, Java, JS, C++, Go, Rust 等）的自动化适配流程，证明了系统的通用性。
*   **客观性：** 系统引入了“验证门控”机制，所有生成的任务必须通过“Pass-to-Fail”信号校验，确保了数据的客观可执行性，避免了无效数据的产生。

---

### 6. 主要结论与发现
*   **工厂化是关键：** 解决 SWE 智能体瓶颈的路径不是手动标注，而是构建能够持续转化原始代码库为可执行任务的工厂。
*   **环境解耦的价值：** 通过将环境固化为镜像并标准化接口，可以极大地降低跨语言任务生成的工程门槛。
*   **真实性源于复杂性：** 真实的 Bug 往往具有非局部性（Non-local），Bug Agent 证明了通过模拟合同违约（Contract Violation）可以生成比简单变异更高质量的调试任务。

---

### 7. 优点（亮点）
*   **高度统一：** 首次将环境自动化、大规模合成、真实性增强和长周期任务整合进一个生产栈。
*   **多语言扩展性：** 基于 Tree-sitter 和配置驱动的设计，使得支持新语言仅需编写 YAML 模板。
*   **抗泄露设计：** 在 Issue 生成中引入了严格的约束，确保智能体必须通过推理而非匹配关键词来解决问题。
*   **可执行性保障：** 强调“由构建即验证”（Executable by construction），确保每个任务都有确定的 Oracle。

---

### 8. 不足与局限
*   **验证覆盖悖论：** 系统依赖于代码库现有的测试套件。如果原始测试覆盖率低，系统就无法验证那些发生在未覆盖区域的 Bug，存在“幸存者偏差”。
*   **计算开销：** 虽然 K8s 支持扩展，但大规模运行编译和集成测试仍需消耗巨大的 CPU 和存储资源。
*   **应用限制：** 对于极度依赖外部硬件环境或复杂分布式中间件的仓库，Env Agent 的自动化构建可能仍面临挑战。

（完）
