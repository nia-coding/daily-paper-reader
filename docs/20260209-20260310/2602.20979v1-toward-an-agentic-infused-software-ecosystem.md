---
title: Toward an Agentic Infused Software Ecosystem
title_zh: 迈向智能体注入的软件生态系统
authors: Mark Marron
date: 2026-02-24
pdf: "https://arxiv.org/pdf/2602.20979v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 用于自动化开发任务的智能体注入软件生态系统
tldr: 本文探讨了如何通过构建“智能体注入式软件生态系统”（AISE）来充分发挥AI智能体在软件开发中的潜力。研究提出了支撑该生态系统的三大支柱：不断进化的AI智能体、作为协作媒介的编程语言与API，以及提供外部交互能力的运行时环境。该框架旨在通过三者的协同演进，实现人类开发者与AI智能体的高效协作，推动软件开发范式的转型。
selection_source: fresh_fetch
motivation: 现有的软件开发生态系统尚未充分适配AI智能体从简单代码补全向独立开发任务转型的需求。
method: 提出了由AI智能体、编程语言/API以及运行时环境三大支柱构成的AISE框架。
result: 明确了构建未来软件生态系统的核心要素，并强调了各支柱之间协同发展的重要性。
conclusion: 实现AISE愿景需要对软件生态进行整体性重构，以支持人类与AI智能体在未来的深度协作。
---

## 摘要
充分利用 AI 智能体在软件开发中的能力，需要对软件生态系统本身进行重新思考。为此，本文概述了“智能体注入软件生态系统”（AISE）的构建，该系统建立在三大支柱之上。第一大支柱当然是 AI 智能体本身。在过去的五年中，它们已从简单的代码补全转向复杂的独立开发任务，且这一趋势仍将持续。第二大支柱是这些智能体用于完成任务的编程语言和 API（或工具），它们日益成为人类与 AI 智能体交互与协作的通信基座。最后一个支柱是智能体运行的运行时环境和生态系统，它们为程序化智能体提供了与外部世界交互（并产生作用）的能力。为了实现 AISE 的愿景，必须以整体的方式推进这三大支柱，且至关重要的是，要以一种能够让当前的 AI 智能体、未来的 AI 智能体以及与之协作的人类开发者产生协同效应的方式进行。

## Abstract
Fully leveraging the capabilities of AI agents in software development requires a rethinking of the software ecosystem itself. To this end, this paper outlines the creation of an Agentic Infused Software Ecosystem (AISE), that rests on three pillars. The first, of course, is the AI agents themselves, which in the past 5 years have moved from simple code completion and toward sophisticated independent development tasks, a trend which will only continue. The second pillar is the programming language and APIs (or tools) that these agents use to accomplish tasks, and increasingly, serve as the communication substrate that humans and AI agents interact and collaborate through. The final pillar is the runtime environment and ecosystem that agents operate within, and which provide the capabilities that programmatic agents use to interface with (and effect actions in) the external world. To realize the vision of AISE, all three pillars must be advanced in a holistic manner, and critically, in a manner that is synergistic for AI agents as they exist today, those that will exist in the future, and for the human developers that work alongside them.

---

## 论文详细总结（自动生成）

这篇论文由 Mark Marron 撰写，提出了一种名为“智能体注入软件生态系统”（Agentic Infused Software Ecosystem, AISE）的新型架构。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的软件开发生态系统（编程语言、API、运行时）是为人类设计的，存在大量隐式行为和复杂的上下文依赖。AI 智能体在其中工作时，往往需要通过“统计学猜测”来处理不明确的信息，导致其生成的代码缺乏正确性保证，且难以管理长上下文和发现工具。
*   **研究动机**：为了充分释放 AI 智能体的潜力，不能仅改进模型本身，必须重新设计整个软件栈。作者认为需要从“人类中心”转向“人类-智能体协同中心”，通过消除偶然复杂性，建立一个显式、可机械化验证且具备韧性的生态系统。

### 2. 方法论与关键技术
论文提出了 AISE 的三大支柱，并详细介绍了相关的技术实现：

*   **Bosque 编程语言（核心基座）**：
    *   **显式意图**：消除循环（改用高阶函数）、消除可变状态和别名，使代码逻辑对 AI 显式化，提高 Token 效率。
    *   **强类型与不变性**：支持类型别名（如 `Fahrenheit`）和正则表达式约束（如 `ZipCode`），便于智能体理解数据语义。
*   **智能体扩展（Agentic Bosque）**：
    *   **`api` 与 `agent` 调用**：引入显式语法区分确定性工作流（API）和概率性智能体调用。
    *   **环境与权限控制**：在调用时显式声明所需的环境变量（如 `PAYMENT_LIMIT`）和资源权限（如特定 URI 访问权）。
    *   **空洞（Holes）与延迟开发**：使用 `?_` 占位符表示未完成的任务，支持“延迟编程”（Prorogued Programming），允许智能体或人类后续填充。
*   **SUNDEW 验证工具**：
    *   **机械化验证**：利用 Bosque 的确定性语义，将代码映射到 SMT-Lib（满足模理论），通过 Z3 等求解器实现全自动的属性检查（如检查转账金额是否超限）。
    *   **内省智能体**：允许智能体在规划阶段调用验证工具，形成“生成-验证-修正”的闭环。
*   **MINT 运行时环境**：
    *   **智能体 HATEOAS**：借鉴 REST 架构，提供 `/actions` 和 `/search` 路由，支持智能体对服务能力的渐进式发现。
    *   **沙箱与护栏**：基于 URI 和 Glob 通配符实现资源隔离，自动监控敏感数据泄露。

### 3. 实验设计
*   **场景示例**：论文主要通过具体的代码示例来展示功能，包括：
    1.  **符号函数（Sign Function）**：展示 Bosque 的简洁性和 SUNDEW 的验证速度。
    2.  **账单拆分与转账（Split Bill）**：展示智能体提取非结构化数据并调用受控 API 的完整工作流。
*   **Benchmark 提及**：文中提到了现有的基准测试如 `DafnyBench`、`AppWorld` 和 `LeetCode` 风格的问题，但本论文更多是作为系统架构的论证，而非单一算法的性能对比。
*   **对比对象**：对比了传统语言（Java/TypeScript）的复杂性，以及形式化验证语言（Lean/Dafny）在规范编写上的高难度。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号或大规模训练时长。
*   **性能数据**：提到 SUNDEW 验证一个简单的符号函数属性仅需 **7ms**。这表明该方法侧重于符号推理的效率，而非大规模神经网络的训练。

### 5. 实验数量与充分性
*   **实验规模**：本文属于“愿景与系统设计”类论文，实验主要集中在功能验证和概念证明（Proof of Concept）。
*   **充分性评价**：对于一个新生态系统的提议，论文通过代码实例和逻辑推演论证了可行性。但由于缺乏大规模真实开发场景下的量化对比（如智能体在 AISE 下的成功率提升百分比），其客观效果仍需未来更大规模的工程实践来检验。

### 6. 主要结论与发现
*   **协同演进**：软件生态系统的三大支柱（智能体、语言/API、运行时）必须协同设计，才能解决上下文管理、工具发现和安全性挑战。
*   **显式化是关键**：通过将意图、权限和不变性显式编码在语法中，可以显著降低智能体的认知负荷并提高生成代码的正确率。
*   **验证即反馈**：将形式化验证工具作为智能体的在线反馈回路，可以实现比单纯“测试驱动开发”更稳健的智能体规划。

### 7. 优点
*   **整体性视角**：不局限于模型改进，而是从底层语言到高层运行时的全栈重构。
*   **形式化根基**：利用 SMT 求解器提供数学级的正确性保证，解决了 LLM 幻觉带来的安全隐患。
*   **Token 友好**：Bosque 的设计天然节省 Token，且 BAPI 协议比 JSON 更高效，适合长上下文管理。

### 8. 不足与局限
*   **迁移成本极高**：AISE 依赖于 Bosque 这一新语言，现有的海量遗留代码（Java, Python 等）难以直接受益。
*   **生态依赖**：该愿景的实现需要开发者、工具链和云服务商共同转向这一新标准，推广难度巨大。
*   **实验覆盖有限**：目前仅展示了小型、自包含的逻辑示例，对于具有复杂副作用或大规模分布式系统的处理能力尚待验证。

（完）
