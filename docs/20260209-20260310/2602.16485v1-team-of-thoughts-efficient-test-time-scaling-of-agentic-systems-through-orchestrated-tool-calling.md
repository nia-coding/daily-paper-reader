---
title: "Team of Thoughts: Efficient Test-time Scaling of Agentic Systems through Orchestrated Tool Calling"
title_zh: 思维团队：通过编排式工具调用实现智能体系统的高效测试时扩展
authors: "Jeffrey T. H. Wong, Zixi Zhang, Junyi Liu, Yiren Zhao"
date: 2026-02-18
pdf: "https://arxiv.org/pdf/2602.16485v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 具有编排工具调用和异构智能体的多智能体系统架构
tldr: 针对现有推理系统多采用同质化模型、难以发挥异构模型互补优势的问题，本文提出Team-of-Thoughts（ToT）架构。该框架采用编排器-工具模式，引入编排器校准机制以筛选最佳协调模型，并结合智能体自我评估协议来识别其领域专长。在推理阶段，系统根据能力画像动态激活最合适的工具智能体。实验证明，ToT在数学推理和代码生成任务上表现卓越，显著提升了测试时缩放的效率。
selection_source: fresh_fetch
motivation: 现有的多智能体系统通常依赖静态且同质的模型配置，限制了利用不同模型在特定领域独特优势的能力。
method: 提出一种基于编排器-工具范式的异构智能体架构，通过编排器校准和智能体自我评估实现动态任务分配。
result: "在AIME24和LiveCodeBench基准测试中，该方法分别取得了96.67%和72.53%的准确率，大幅超越同质化角色扮演基准。"
conclusion: 通过协同编排异构智能体，Team-of-Thoughts为多智能体系统在复杂推理任务中的性能优化提供了高效的新路径。
---

## 摘要
现有的多智能体系统（MAS）通常依赖于静态、同质的模型配置，这限制了它们利用经过不同后训练（post-trained）模型的独特优势的能力。为解决这一问题，我们提出了“思维团队”（Team-of-Thoughts），这是一种新型的 MAS 架构，通过“编排器-工具”范式利用异构智能体的互补能力。我们的框架引入了两种优化性能的关键机制：(1) 一种编排器校准方案，用于识别具有卓越协调能力的模型；(2) 一种自我评估协议，工具智能体通过该协议分析自身的领域专长，以应对后训练技能的差异。在推理过程中，编排器根据这些熟练度概况动态激活最合适的工具智能体。在五个推理和代码生成基准测试上的实验表明，Team-of-Thoughts 始终表现出优越的任务性能。值得注意的是，在 AIME24 和 LiveCodeBench 上，我们的方法分别达到了 96.67% 和 72.53% 的准确率，显著优于得分分别为 80% 和 65.93% 的同质角色扮演基准。

## Abstract
Existing Multi-Agent Systems (MAS) typically rely on static, homogeneous model configurations, limiting their ability to exploit the distinct strengths of differently post-trained models. To address this, we introduce Team-of-Thoughts, a novel MAS architecture that leverages the complementary capabilities of heterogeneous agents via an orchestrator-tool paradigm. Our framework introduces two key mechanisms to optimize performance: (1) an orchestrator calibration scheme that identifies models with superior coordination capabilities, and (2) a self-assessment protocol where tool agents profile their own domain expertise to account for variations in post-training skills. During inference, the orchestrator dynamically activates the most suitable tool agents based on these proficiency profiles. Experiments on five reasoning and code generation benchmarks show that Team-of-Thoughts delivers consistently superior task performance. Notably, on AIME24 and LiveCodeBench, our approach achieves accuracies of 96.67% and 72.53%, respectively, substantially outperforming homogeneous role-play baselines, which score 80% and 65.93%.

---

## 论文详细总结（自动生成）

这篇论文由伦敦帝国理工学院和微软研究院的研究人员合作完成，提出了一种名为 **Team-of-Thoughts (ToT)** 的新型多智能体系统（MAS）架构。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的测试时扩展（Test-time Scaling, TTS）方法和多智能体系统（MAS）通常依赖于单一模型或同质模型（即同一模型的多个副本），这限制了系统利用不同模型在特定领域（如数学、代码、逻辑）独特优势的能力。
*   **研究动机**：
    *   **同质化局限**：同质智能体共享相同的参数化先验，难以提供真正的多样化视角。
    *   **效率低下**：传统的 MAS 往往需要所有智能体参与每一轮对话，导致推理成本（Token 消耗）极高且延迟大。
    *   **动态性缺失**：缺乏根据任务特性动态选择最合适模型并进行高效编排的机制。

### 2. 方法论：核心思想与关键技术
ToT 框架将异构 LLM 重新构想为可被动态调用的“工具”，通过一个编排器（Orchestrator）进行协调。
*   **核心思想**：利用异构模型（Heterogeneous Models）的互补性，通过编排器进行策略性预算分配和并行推理。
*   **关键技术细节**：
    1.  **编排器校准（Orchestration Calibration）**：通过在校准集上评估不同模型整合信息、解决冲突和协调工具的能力，选出最适合担任“领导者”的模型（实验发现最强推理模型不一定是最佳编排器）。
    2.  **智能体自我评估协议（Self-assessment Protocol）**：工具智能体在验证集上对自己的表现进行审计，识别擅长的任务类型（如算术、逻辑、特定编程语言），生成“能力画像”。
    3.  **动态工具调用与并行化**：推理时，编排器根据查询类型和智能体画像，仅激活最相关的工具智能体。这些智能体并行生成推理轨迹，最后由编排器汇总生成最终答案。
    4.  **Token 扩展策略**：通过控制激活的智能体数量 $|S|$ 和每个智能体的生成长度 $N_i$，实现比盲目增加采样次数更高效的计算资源分配。

### 3. 实验设计
*   **数据集/场景**：
    *   **数学推理**：AIME2024、AIME2025。
    *   **代码生成**：HumanEval+、MBPP+、LiveCodeBench v6（2025年最新数据）。
*   **对比基准（Benchmarks）**：
    *   **单模型基准**：Claude 4.5, GPT-5 Mini, Gemini 3 Flash, DeepSeek v3.2, Qwen3-VL, Phi-4 等。
    *   **多智能体基准**：AgentVerse（基于同质模型的角色扮演框架）。
    *   **共识基准**：Majority Voting（多数投票法）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **推理配置**：提到了推理时的 Token 限制。数学任务设置了 20,000 Token 的上下文窗口，代码任务为 4,096 Token。ToT 框架统一使用 16,384 Token 的窗口以处理工具描述和推理汇总。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 个主流基准测试，涉及 7 个不同的模型家族（闭源与开源结合）。
*   **消融实验**：
    *   对比了三种选择策略：随机分配、编排器评估、智能体自我评估。
    *   分析了激活工具智能体数量（1, 2, 4, 7个）对性能的影响。
*   **充分性评价**：实验设计较为充分，通过跨领域（数学与代码）和跨模型家族的对比，验证了异构编排的有效性。使用了最新的 LiveCodeBench 数据，增强了结果的时效性和客观性。

### 6. 主要结论与发现
*   **性能卓越**：ToT 在 AIME24 上达到 96.67% 准确率，在 LiveCodeBench 上达到 72.53%，显著超过单模型和同质 MAS。
*   **异构性至关重要**：使用具有不同后训练背景的异构模型比单纯增加同质智能体数量更能提升系统鲁棒性。
*   **编排能力差异**：实验发现 DeepSeek v3.2 在数学编排上表现最佳，而 GPT-5 Mini 在代码编排上更胜一筹。
*   **高效率**：相比 AgentVerse，ToT 在提升准确率的同时，将推理成本降低了一个数量级。

### 7. 优点与亮点
*   **架构创新**：将 MAS 转化为“编排器-工具”模式，解决了传统 MAS 冗余推理和 Token 浪费的问题。
*   **动态扩展**：引入了自我画像和动态激活机制，实现了测试时计算资源（Inference Compute）的按需分配。
*   **并行性**：原生支持工具智能体并行推理，大幅降低了多智能体协作的端到端延迟。

### 8. 不足与局限
*   **编排器依赖**：系统的上限受限于编排器的整合能力，若编排器无法正确处理工具间的冲突，可能导致性能下降。
*   **校准成本**：虽然推理高效，但在部署前需要对所有候选模型进行校准和自我评估，这增加了一次性的预处理开销。
*   **多轮对话限制**：目前主要集中在单轮编排汇总，对于需要多轮深度交互的极复杂任务，其表现尚待验证。
*   **算力细节缺失**：缺乏具体的硬件环境描述，不便于其他研究者评估实际的运行能效比。

（完）
