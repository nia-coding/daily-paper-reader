---
title: Evolutionary System Prompt Learning for Reinforcement Learning in LLMs
title_zh: 大语言模型中强化学习的演化系统提示词学习
authors: "Lunjun Zhang, Ryan Chen, Bradly C. Stadie"
date: 2026-02-16
pdf: "https://arxiv.org/pdf/2602.14697v3"
tags: ["query:sr-ai"]
score: 9.0
evidence: 通过强化学习与演化实现自改进的智能体系统
tldr: 本研究提出进化系统提示词学习（E-SPL），旨在通过同时优化模型权重和系统提示词来实现大语言模型的自主进化。E-SPL在强化学习迭代中，利用LLM自反思驱动的变异和交叉等遗传算子进化提示词，并同步进行权重更新。该方法实现了声明性知识与程序性知识的有效分工，显著提升了模型在复杂推理和智能体任务中的样本效率与泛化性能。
selection_source: fresh_fetch
motivation: 旨在解决大语言模型在自主进化过程中，如何同时优化系统提示词（声明性知识）和模型权重（程序性知识）的问题。
method: 提出E-SPL方法，在强化学习迭代中通过LLM自反思驱动的变异与交叉算子进化系统提示词，并同步更新模型权重。
result: "实验显示E-SPL在AIME到BeyondAIME的泛化任务中将成功率从38.8%提升至45.1%，显著优于单一的RL或提示词进化方法。"
conclusion: 强化学习与系统提示词进化具有深度协同效应，结合两者能显著提升模型的样本效率和泛化能力。
---

## 摘要
构建能够从经验中自主自我改进的智能体系统是人工智能的一个长期目标。如今的大语言模型（LLMs）主要通过两种机制进行自我改进：用于上下文更新的自我反思，以及用于权重更新的强化学习（RL）。在这项工作中，我们提出了演化系统提示词学习（E-SPL），这是一种联合改进模型上下文和模型权重的方法。在每个 RL 迭代中，E-SPL 并行采样多个系统提示词下的轨迹，然后联合对 LLM 权重进行 RL 更新，并对系统提示词进行演化更新。系统提示词通过变异和交叉进行演化，这是由 LLM 自我反思驱动的两个遗传算子；选择基于在 RL 迭代中更新的相对性能评分。E-SPL 促进了编码在提示词中的陈述性知识与编码在权重中的程序性知识之间的自然划分，从而提高了推理和智能体任务的性能。例如，在由易到难（AIME $\rightarrow$ BeyondAIME）的泛化设置中，E-SPL 将 RL 成功率从 38.8% 提高到 45.1%，同时也优于反思性提示词演化（40.0%）。总的来说，我们的结果表明 RL 和系统提示词演化具有深度协同作用，将两者结合可以在样本效率和泛化能力方面产生持续的收益。代码：https://github.com/LunjunZhang/E-SPL

## Abstract
Building agentic systems that can autonomously self-improve from experience is a longstanding goal of AI. Large language models (LLMs) today primarily self-improve via two mechanisms: self-reflection for context updates, and reinforcement learning (RL) for weight updates. In this work, we propose Evolutionary System Prompt Learning (E-SPL), a method for jointly improving model contexts and model weights. In each RL iteration, E-SPL samples trajectories under multiple system prompts in parallel, then jointly applies RL updates to LLM weights and evolutionary updates to system prompts. System prompts evolve via mutation and crossover, two genetic operators driven by LLM self-reflection; selection is based on relative performance ratings updated across RL iterations. E-SPL encourages a natural division between declarative knowledge encoded in prompts and procedural knowledge encoded in weights, resulting in improved performance across reasoning and agentic tasks. For instance, in an easy-to-hard (AIME $\rightarrow$ BeyondAIME) generalization setting, E-SPL improves RL success rate from 38.8% $\rightarrow$ 45.1% while also outperforming reflective prompt evolution (40.0%). Overall, our results demonstrate that RL and system prompt evolution are deeply synergistic, and combining the two yields consistent gains in sample efficiency and generalization. Code: https://github.com/LunjunZhang/E-SPL

---

## 论文详细总结（自动生成）

这篇论文由多伦多大学、西北大学和 Bridgewater AI Labs 的研究人员合作完成，提出了一种名为 **E-SPL（Evolutionary System Prompt Learning）** 的框架，旨在通过同时进化“系统提示词”和更新“模型权重”来实现大语言模型（LLM）的自主持续改进。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **核心问题**：目前 LLM 的自我改进主要分为两条路径：一是通过**自我反思**更新上下文（提示词工程），二是通过**强化学习（RL）**更新模型权重。这两者通常是孤立进行的。
*   **研究动机**：作者认为提示词（陈述性知识，如规则、策略）与权重（程序性知识，如直觉、执行力）之间存在互补性。如果能将演化算法（EA）的全局探索能力与 RL 的细粒度优化能力结合，可以产生协同效应，提升模型的泛化能力和样本效率。

### 2. 方法论：E-SPL 框架
*   **核心思想**：在同一个训练循环中，利用 RL 更新权重，同时利用 LLM 驱动的演化算子更新系统提示词池。
*   **关键技术细节**：
    *   **联合优化循环**：在每个 RL 迭代中，并行使用多个不同的系统提示词采样轨迹。
    *   **权重更新（RL）**：采用策略梯度方法（如 GRPO 或 CISPO），在多种提示词下进行训练，防止模型权重过拟合于单一提示词。
    *   **提示词演化（EA）**：
        *   **适应度评估**：直接复用 RL 产生的轨迹数据，使用 **TrueSkill 评分系统**根据相对表现对提示词进行排名，无需额外的验证集。
        *   **变异（Mutation）**：让 LLM 反思表现最好的提示词的失败案例，并以“git diff”格式提出修改建议。
        *   **交叉（Crossover）**：分析不同提示词在不同题目上的差异化优势，将多个父代提示词的有效片段重新组合。
    *   **知识分工**：提示词负责存储显性的策略和避坑指南，权重更新则专注于内化这些策略并提高执行的直觉。

### 3. 实验设计
*   **数据集/场景**：
    *   **数学推理**：AIME 2025、HMMT 2025，以及一个“由易到难”的泛化设置（从 AIME 训练，在更难的 BeyondAIME 上测试）。
    *   **智能体搜索**：在 Natural Questions 和 HotpotQA 上测试模型使用搜索引擎回答多跳问题的能力。
*   **对比方法（Benchmarks）**：
    *   **RL Only**：固定提示词，仅更新权重（GRPO, CISPO）。
    *   **Self-Reflection**：仅通过反思更新提示词，权重冻结。
    *   **Evolution Only**：仅进行提示词演化，权重冻结。
    *   **E-SPL (Evolution + RL)**：本文提出的联合优化方法。

### 4. 资源与算力
*   **模型使用**：数学任务使用 **DeepSeek v3.1 (671B MoE)**；智能体搜索任务使用 **OpenAI gpt-oss-120b**。
*   **微调技术**：采用 **LoRA (Rank 32)** 进行高效微调。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。但提到了 Batch Size 为 10，每个提示词采样 5 个轨迹等超参数。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了数学推理和工具使用两大领域，使用了多个主流基准测试。
*   **消融实验**：作者针对“交叉算子的作用”以及“演化采样策略（参考模型 vs 在线模型）”进行了专门的消融研究。
*   **充分性评价**：实验设计较为充分，通过跨模型（DeepSeek 和 GPT-OSS）和跨领域验证了方法的通用性。特别是“由易到难”的泛化实验有力地证明了该方法在处理未知难度任务时的优势。

### 6. 主要结论与发现
*   **协同效应**：E-SPL 的表现优于单纯的 RL 或单纯的提示词演化。在 AIME $\rightarrow$ BeyondAIME 任务中，成功率从 38.8% 提升至 **45.1%**。
*   **泛化能力**：通过演化出的系统提示词，模型学会了“生成式自我验证”和“特定领域的启发式策略”，这些显性规则显著降低了 RL 优化的压力。
*   **学习速度**：E-SPL 在训练初期表现出更快的收敛速度和更高的渐进性能。

### 7. 优点
*   **知识解耦**：成功实现了陈述性知识（提示词）与程序性知识（权重）的分离，使模型改进更具结构性。
*   **高效率**：演化过程直接复用 RL 采样数据，几乎没有引入额外的计算开销。
*   **可解释性**：演化出的系统提示词是自然语言，人类可阅读、可监控、可手动修正，比纯权重更新更透明。

### 8. 不足与局限
*   **启发式错误风险**：演化出的提示词有时会包含“过度泛化”或不完全准确的规则（论文中坦诚地列举了两个案例）。
*   **算力细节缺失**：缺乏具体的算力消耗报告，难以评估中小规模团队复现该实验的成本。
*   **自指性限制**：目前用于反思和修改提示词的“参考模型”是冻结的，尚未实现完全的“递归自我改进”（即模型自己改进自己的改进算法）。

（完）
