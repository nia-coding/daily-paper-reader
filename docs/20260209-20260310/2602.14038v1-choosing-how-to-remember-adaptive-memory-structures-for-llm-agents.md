---
title: "Choosing How to Remember: Adaptive Memory Structures for LLM Agents"
title_zh: 选择如何记忆：大语言模型智能体的自适应记忆结构
authors: "Mingfei Lu, Mengjia Wu, Feng Liu, Jiawei Xu, Weikai Li, Haoyang Wang, Zhengdong Hu, Ying Ding, Yizhou Sun, Jie Lu, Yi Zhang"
date: 2026-02-15
pdf: "https://arxiv.org/pdf/2602.14038v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 大模型智能体处理长程交互的自适应记忆结构
tldr: 针对LLM智能体在长程交互中内存结构单一、缺乏上下文适应性的问题，本文提出FluxMem框架。该框架通过集成多种互补的内存结构，并利用离线监督学习根据交互特征动态选择最优结构。此外，引入三层内存层次结构和基于Beta混合模型的概率门控机制进行内存融合，显著提升了智能体在复杂任务中的表现。
selection_source: fresh_fetch
motivation: 现有的LLM智能体内存系统通常采用固定结构，无法根据不同的交互模式灵活调整，导致在处理异构任务时性能受限。
method: 提出FluxMem框架，通过离线学习选择内存结构，并结合三层内存层次结构与Beta混合模型实现分布感知的内存融合。
result: "在PERSONAMEM和LoCoMo两个长程基准测试中，该方法分别实现了9.18%和6.14%的平均性能提升。"
conclusion: 自适应内存结构选择和概率内存融合机制是提升LLM智能体长程交互一致性和效率的关键。
---

## 摘要
记忆对于使基于大语言模型（LLM）的智能体在长程交互中保持连贯行为至关重要。然而，现有的智能体记忆系统存在两个关键缺陷：它们依赖于通用的记忆结构，且未将记忆结构的选择建模为上下文自适应的决策，这限制了它们处理异构交互模式的能力，并导致性能欠佳。我们提出了一个统一框架 FluxMem，旨在为 LLM 智能体实现自适应记忆组织。我们的框架为智能体配备了多种互补的记忆结构。它显式地学习根据交互层级的特征在这些结构中进行选择，并利用源自下游响应质量和记忆利用率的离线监督。为了支持稳健的长程记忆演化，我们进一步引入了三级记忆层次结构，以及一个基于贝塔混合模型（Beta Mixture Model）的概率门控，用于分布感知的记忆融合，从而取代了脆弱的相似度阈值。在两个长程基准测试 PERSONAMEM 和 LoCoMo 上的实验表明，我们的方法分别实现了 9.18% 和 6.14% 的平均提升。

## Abstract
Memory is critical for enabling large language model (LLM) based agents to maintain coherent behavior over long-horizon interactions. However, existing agent memory systems suffer from two key gaps: they rely on a one-size-fits-all memory structure and do not model memory structure selection as a context-adaptive decision, limiting their ability to handle heterogeneous interaction patterns and resulting in suboptimal performance. We propose a unified framework, FluxMem, that enables adaptive memory organization for LLM agents. Our framework equips agents with multiple complementary memory structures. It explicitly learns to select among these structures based on interaction-level features, using offline supervision derived from downstream response quality and memory utilization. To support robust long-horizon memory evolution, we further introduce a three-level memory hierarchy and a Beta Mixture Model-based probabilistic gate for distribution-aware memory fusion, replacing brittle similarity thresholds. Experiments on two long-horizon benchmarks, PERSONAMEM and LoCoMo, demonstrate that our method achieves average improvements of 9.18% and 6.14%.

---

## 论文详细总结（自动生成）

以下是对论文《Choosing How to Remember: Adaptive Memory Structures for LLM Agents》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的 LLM 智能体记忆系统通常采用“一刀切”的固定结构（如纯向量检索、纯图结构或纯线性存储），忽略了不同交互场景（如时间演进、复杂关系推理、主题切换）对记忆组织形式的差异化需求。
*   **核心挑战**：
    1.  **单一结构假设**：固定结构无法同时高效处理异构的交互模式。
    2.  **缺乏自适应选择**：记忆结构的选择未被视为一种可优化的决策过程，导致在分布偏移时表现脆弱。
*   **整体含义**：本文提出了 **FluxMem** 框架，旨在让智能体能够根据当前对话的上下文特征，动态地在多种记忆结构中进行选择和切换，从而提升长程交互的一致性和响应质量。

### 2. 方法论
FluxMem 的核心由四个关键组件构成：
*   **三层记忆层次结构**：
    *   **STIM（短期交互记忆）**：缓冲最近的 4 页对话，模拟人类工作记忆，采用 LRU 策略。
    *   **MTEM（中期情境记忆）**：存储结构化的情境单元，是 FluxMem 的核心，支持多种组织形式。
    *   **LTSM（长期语义记忆）**：存储抽象的、稳定的知识（如用户画像、事实），通过剪枝机制维持容量。
*   **多结构组织与检索**：支持 **线性（Linear）**、**图（Graph）** 和 **层级（Hierarchical）** 三种基础结构，分别对应时间顺序、实体关系和主题抽象。
*   **上下文感知结构选择器**：
    *   **特征提取**：提取交互层级的特征（如页面计数、实体密度、主题转换频率、语义复杂度等）。
    *   **分类决策**：使用轻量级 MLP 分类器预测最适合当前上下文的记忆结构。
    *   **离线监督学习**：通过下游响应质量和记忆利用率作为奖励信号，离线生成标签来训练选择器。
*   **基于 Beta 混合模型（BMM）的概率门控**：
    *   **核心思想**：取代固定的相似度阈值。
    *   **流程**：将新信息与现有记忆的匹配分数归一化，利用 EM 算法拟合一个双分量 Beta 混合模型（区分高兼容和低兼容分布），根据后验概率决定是否融合记忆，增强了对噪声的鲁棒性。

### 3. 实验设计
*   **数据集**：
    *   **LoCoMo**：超长对话基准（平均 300 轮），包含多跳、时间、开放域和单跳推理任务。
    *   **PERSONAMEM**：动态用户画像基准，测试偏好演进、事实召回和个性化推荐。
*   **对比方法（Baselines）**：包括 LangMem, Mem0, ZEP, MemoryOS, A-Mem, O-Mem, MemR3, MEMOS, HippoRAG 等 9 种主流记忆系统。
*   **评估指标**：准确率（PERSONAMEM）、F1、BLEU-1、ROUGE-L、BERTScore（LoCoMo）。

### 4. 资源与算力
*   **硬件**：使用了 **2 张 NVIDIA A100 GPU**。
*   **模型**：实验基于 **GPT-4o**（temperature=0）进行。
*   **训练细节**：结构选择器是一个极小的 MLP（输入 12 维，隐藏层 4 维），训练开销极低；主要的计算开销在于 LLM 的推理和记忆检索。

### 5. 实验数量与充分性
*   **实验规模**：在两个大规模长程基准上进行了全面测试，涵盖了 10 余种对比方法。
*   **消融实验**：分别移除了线性、图、层级结构以及 BMM 门控机制，验证了各组件的必要性。
*   **参数敏感性分析**：对 BMM 的后验阈值 $\tau_{BMM}$ 和最小保留参数 $m_{min}$ 进行了多组实验。
*   **案例研究**：通过时间依赖推理、复杂关系推理和主题演进三个具体案例展示了不同结构的实际效果。
*   **评价**：实验设计客观、公平，覆盖了长程交互的主要难点，消融实验充分证明了“自适应选择”是性能提升的核心驱动力。

### 6. 主要结论与发现
*   **性能提升**：FluxMem 在 PERSONAMEM 上平均提升 **9.18%**，在 LoCoMo 上平均提升 **6.14%**。
*   **结构互补性**：没有一种结构能胜任所有任务。线性结构对时间敏感任务至关重要，图结构擅长关系推理，层级结构则在处理主题漂移时表现更好。
*   **概率融合的优越性**：基于 BMM 的自适应门控显著优于手动调节的固定阈值，能更有效地过滤噪声并保留关键信息。

### 7. 优点
*   **创新视角**：首次将记忆结构的选择建模为一种“上下文自适应决策”，填补了现有研究的空白。
*   **生物启发**：三层记忆架构和 BMM 概率机制具有一定的认知心理学和神经科学依据。
*   **轻量化**：结构选择器和 BMM 门控的计算开销极小，易于集成到现有的智能体流水线中。

### 8. 不足与局限
*   **离线监督依赖**：结构选择器的训练依赖于高质量的离线标注数据或奖励模型，在完全未知的领域可能难以快速迁移。
*   **结构多样性限制**：目前仅支持三种基础结构，虽然能组合，但对于某些极特殊的非结构化数据可能仍有局限。
*   **推理延迟**：虽然选择器很轻量，但维护多套记忆结构（如构建图和层级树）在超大规模交互中可能会增加一定的存储和预处理延迟。

（完）
