---
title: "MagicAgent: Towards Generalized Agent Planning"
title_zh: MagicAgent：迈向通用智能体规划
authors: "Xuhui Ren, Shaokang Dong, Chen Yang, Qing Gao, Yunbin Zhao, Yongsheng Liu, Xinwei Geng, Xiang Li, Demei Yan, Yanqing Li, Chenhao Huang, Dingwei Zhu, Junjie Ye, Boxuan Yue, Yingnan Fu, Mengzhe Lv, Zezeng Feng, Boshen Zhou, Bocheng Wang, Xuanjing Huang, Yu-Gang Jiang, Tao Gui, Qi Zhang, Yunke Zhang"
date: 2026-02-22
pdf: "https://arxiv.org/pdf/2602.19000v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 通用智能体规划的基础模型
tldr: MagicAgent 是一系列旨在实现通用智能体规划的基础模型。针对高质量交互数据稀缺和异构任务冲突等挑战，研究团队开发了轻量级可扩展的合成数据框架，并采用监督微调与多目标强化学习相结合的两阶段训练范式。实验表明，MagicAgent 在多个权威规划基准测试中表现卓越，显著超越了同规模甚至部分领先的闭源模型。
selection_source: fresh_fetch
motivation: 旨在解决智能体规划中高质量数据稀缺以及异构任务训练时产生的梯度干扰和泛化难题。
method: 引入轻量级合成数据框架生成多样化轨迹，并结合监督微调与动态环境下的多目标强化学习进行两阶段训练。
result: 模型在 Worfbench、NaturalPlan 和 BFCL-v3 等多个基准测试中表现优异，性能优于大多数 100B 以下模型及部分闭源模型。
conclusion: MagicAgent 证明了通过系统化的数据合成与多目标优化，可以有效提升大语言模型在复杂场景下的通用规划能力。
---

## 摘要
大语言模型（LLMs）从被动文本处理器向自主智能体的演进，已使规划成为现代智能的核心组成部分。然而，实现通用规划仍然难以捉摸，这不仅是因为高质量交互数据的稀缺，还因为异构规划任务之间固有的冲突。这些挑战导致模型在孤立任务上表现出色，却难以泛化，而现有的多任务训练尝试则受到梯度干扰的困扰。在本文中，我们提出了 MagicAgent，这是一系列专门为通用智能体规划设计的基座模型。我们引入了一个轻量级且可扩展的合成数据框架，该框架可在多种规划任务中生成高质量轨迹，包括分层任务分解、工具增强规划、多约束调度、程序逻辑编排以及长时程工具执行。为了缓解训练冲突，我们提出了一种两阶段训练范式，包括监督微调，随后是在静态数据集和动态环境上进行的多目标强化学习。实验结果表明，MagicAgent-32B 和 MagicAgent-30B-A3B 表现卓越，在 Worfbench 上达到 75.1% 的准确率，在 NaturalPlan 上达到 55.9%，在 τ²-Bench 上达到 57.5%，在 BFCL-v3 上达到 86.9%，在 ACEBench 上达到 81.2%，并在我们的内部 MagicEval 基准测试中取得了强劲结果。这些结果大幅超越了现有的 100B 以下模型，甚至超过了领先的闭源模型。

## Abstract
The evolution of Large Language Models (LLMs) from passive text processors to autonomous agents has established planning as a core component of modern intelligence. However, achieving generalized planning remains elusive, not only by the scarcity of high-quality interaction data but also by inherent conflicts across heterogeneous planning tasks. These challenges result in models that excel at isolated tasks yet struggle to generalize, while existing multi-task training attempts suffer from gradient interference. In this paper, we present \textbf{MagicAgent}, a series of foundation models specifically designed for generalized agent planning. We introduce a lightweight and scalable synthetic data framework that generates high-quality trajectories across diverse planning tasks, including hierarchical task decomposition, tool-augmented planning, multi-constraint scheduling, procedural logic orchestration, and long-horizon tool execution. To mitigate training conflicts, we propose a two-stage training paradigm comprising supervised fine-tuning followed by multi-objective reinforcement learning over both static datasets and dynamic environments. Empirical results demonstrate that MagicAgent-32B and MagicAgent-30B-A3B deliver superior performance, achieving accuracies of $75.1\%$ on Worfbench, $55.9\%$ on NaturalPlan, $57.5\%$ on $τ^2$-Bench, $86.9\%$ on BFCL-v3, and $81.2\%$ on ACEBench, as well as strong results on our in-house MagicEval benchmarks. These results substantially outperform existing sub-100B models and even surpass leading closed-source models.

---

## 论文详细总结（自动生成）

### MagicAgent：迈向通用智能体规划——论文深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
论文的核心问题在于如何构建具有**通用规划能力**的智能体基础模型。尽管大语言模型（LLM）已展现出作为自主智能体的潜力，但实现通用规划面临两大瓶颈：
*   **高质量数据稀缺**：传统的基于人类交互或沙盒模拟的数据获取方式成本高、效率低，且难以覆盖多样化的规划场景。
*   **异构任务冲突（跷跷板效应）**：不同类型的规划任务（如工具调用与多约束调度）在联合训练时存在梯度干扰，导致模型在某一领域提升时，另一领域性能下降。

#### 2. 方法论：核心思想与关键技术
MagicAgent 提出了一个涵盖数据生成、模型架构与训练范式的统一框架：
*   **轻量级合成数据框架**：
    *   **分层任务分解**：通过构建工具依赖图（$G_{depend}$）和参数共享图（$G_{share}$），生成“原子计划”，再通过连接、掩码、转换等算子合成复杂轨迹。
    *   **工具增强规划**：采用 ReAct 范式，利用“推理代理”生成思维链，并结合“虚拟 API 模拟器”生成环境反馈。
    *   **多约束调度与逻辑编排**：将调度建模为约束满足问题（CSP），将编排建模为有向无环图（DAG）构建问题。
    *   **长时程执行**：通过任务扩散技术生成具有深层依赖关系的复杂工具链。
*   **两阶段训练范式**：
    *   **阶段 I：监督微调（SFT）**：使用 NovelSum 指标筛选高多样性样本，解决数据分布不均问题。
    *   **阶段 II：强化学习（RL）**：
        *   **离线 RL**：使用 GRPO 算法和统一的多任务可验证奖励函数（涵盖格式、步骤、工具、工作流、调度五个维度）。
        *   **在线 RL ($\chi$PO)**：提出探索-利用策略优化，通过 Token 熵正则化、思考/动作层级的熵平滑（鼓励思考多样性，确保动作果断）以及信息瓶颈（压缩冗余推理）来提升动态环境下的泛化力。
*   **负载均衡 MoE 策略**：针对 MoE 架构，引入全局批次负载均衡损失（Global-batch LBL）和路由器 z-loss，防止专家在多任务学习中出现表征坍塌或闲置。

#### 3. 实验设计
*   **数据集/场景**：
    *   **公开基准**：NaturalPlan（调度）、Worfbench（编排）、$\tau^2$-Bench（长时程工具执行）、BFCL-v3（函数调用）、ACEBench（工具使用）。
    *   **内部基准**：MagicEval-Plan 和 MagicEval-Tool（基于真实匿名用户日志）。
    *   **动态环境**：ALFWorld（具身智能交互环境）。
*   **对比方法**：
    *   **超大规模模型（>100B）**：GPT-5.2、Kimi-K2、DeepSeek-V3.1、GLM-4.7、Qwen3-Max 等。
    *   **同规模模型（<100B）**：Qwen3-32B、Llama3.3-70B、ERNIE-4.5-21B、Olmo-3.1-32B 等。

#### 4. 资源与算力
*   **训练算力**：论文明确指出使用了 **32 张 GPU**（4 台 8-GPU 机器）。
*   **推理效率测试**：使用了 **4 张 NVIDIA A800 GPU** 进行 Dense 与 MoE 模型的对比测试。
*   **训练细节**：SFT 阶段学习率为 1e-5，RL 阶段 Actor 学习率为 1e-6，使用了 bfloat16 精度。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 5 个权威公开基准和 2 个内部基准上进行了测试，涵盖了从 3B 到 235B 的数十个对比模型。
*   **消融实验**：针对 MoE 的训练策略（z-loss、不同层级的 LBL 等）做了详细的对比实验，并可视化了专家激活频率。
*   **客观性与公平性**：实验遵循了各基准的官方评估协议，并针对 RL 实验使用了多个随机种子以确保结果的稳定性。实验设计全面，从静态准确率到动态环境成功率均有覆盖。

#### 6. 主要结论与发现
*   **性能领先**：MagicAgent-32B 在多个规划基准上刷新了 100B 以下模型的记录，甚至在 Worfbench 和 ACEBench 等任务上超越了 GPT-5.2 和 DeepSeek-V3.1。
*   **MoE 优势**：MagicAgent-30B-A3B（MoE 版）在保持与 Dense 模型相当性能的同时，推理延迟降低了约 40%-55%，TTFT（首字延迟）显著下降。
*   **强化学习增益**：$\chi$PO 算法在 ALFWorld 环境中显著提升了 OOD（分布外）泛化能力，证明了平衡探索与利用的必要性。

#### 7. 优点
*   **系统化的数据工程**：不依赖昂贵的人工标注，通过图论和模拟器构建了一套可扩展的合成数据流水线。
*   **创新的 RL 机制**：$\chi$PO 算法通过对“思考”和“动作”进行差异化熵处理，解决了智能体在复杂决策中的探索难题。
*   **工程落地性强**：针对 MoE 架构的优化方案直接解决了多任务训练中的负载不均问题，具有很高的实用价值。

#### 8. 不足与局限
*   **模型规模限制**：虽然在 100B 以下表现优异，但尚未在千亿级参数规模上验证该框架的极限。
*   **环境模拟偏差**：合成数据依赖于虚拟 API 模拟器，可能与极其复杂的真实物理世界或未定义的边缘情况存在 gap。
*   **语言覆盖**：虽然在 ACEBench 中测试了中英双语，但对于更多小语种的通用规划能力尚待验证。

（完）
