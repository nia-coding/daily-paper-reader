---
title: "SWE-Adept: An LLM-Based Agentic Framework for Deep Codebase Analysis and Structured Issue Resolution"
title_zh: SWE-Adept：一种基于大语言模型（LLM）的深度代码库分析与结构化问题解决的智能体框架
authors: "Kang He, Kaushik Roy"
date: 2026-03-01
pdf: "https://arxiv.org/pdf/2603.01327v1"
tags: ["query:sr-ai"]
score: 10.0
evidence: 基于大模型的仓库级软件工程和问题解决智能体框架
tldr: 针对大语言模型在处理仓库级软件工程任务时面临的代码定位难和修改系统性不足的问题，本文提出了SWE-Adept框架。该框架采用双智能体架构，通过定位智能体进行基于依赖关系的深度优先搜索以精准锁定问题，并利用修复智能体结合Git版本控制和共享工作记忆进行结构化修复。实验表明，该方法在SWE-Bench基准测试中显著提升了问题解决率，为自动化软件工程提供了高效的解决方案。
selection_source: fresh_fetch
motivation: 大语言模型在处理复杂的仓库级软件工程任务时，难以在庞大的代码库中精准定位问题并进行系统性的代码修改。
method: 提出由定位和修复智能体组成的双智能体框架，引入智能深度优先搜索进行代码定位，并利用带检查点的共享工作记忆实现结构化修复。
result: "在SWE-Bench Lite和Pro测试集上，SWE-Adept在定位和修复准确度上均优于现有方法，端到端解决率最高提升了4.7%。"
conclusion: SWE-Adept通过深度代码库分析和结构化问题解决机制，有效提升了智能体在复杂软件工程任务中的自主处理能力。
---

## 摘要
大语言模型（LLM）在独立编程任务中表现出强大的性能。然而，它们在仓库级软件工程（SWE）方面仍面临挑战，这需要（1）通过有效的上下文管理进行深度代码库导航以实现准确定位，以及（2）采用系统化的方法进行迭代式、测试驱动的代码修改以解决问题。为了应对这些挑战，我们提出了 SWE-Adept，这是一个基于 LLM 的双智能体框架，其中定位智能体负责识别与问题相关的代码位置，而解决智能体负责实施相应的修复。在问题定位方面，我们引入了智能体导向的深度优先搜索，能够选择性地遍历代码依赖关系。这最大限度地减少了智能体上下文窗口中与问题无关的内容，并提高了定位准确性。在问题解决方面，我们采用了自适应规划和结构化问题解决方法。我们为智能体配备了用于进度跟踪和基于 Git 的版本控制的专门工具。这些工具与共享工作内存交互，该内存存储了按执行步骤索引的代码状态检查点，从而便于精确的检查点检索。这种设计实现了可靠的智能体驱动型版本控制操作，用于系统化的问题解决，包括通过分支探索替代方案以及回滚失败的编辑。在 SWE-Bench Lite 和 SWE-Bench Pro 上的实验表明，SWE-Adept 在问题定位和解决方面均始终优于以往的方法，将端到端解决率提高了多达 4.7%。

## Abstract
Large language models (LLMs) exhibit strong performance on self-contained programming tasks. However, they still struggle with repository-level software engineering (SWE), which demands (1) deep codebase navigation with effective context management for accurate localization, and (2) systematic approaches for iterative, test-driven code modification to resolve issues. To address these challenges, we propose SWE-Adept, an LLM-based two-agent framework where a localization agent identifies issue-relevant code locations and a resolution agent implements the corresponding fixes. For issue localization, we introduce agent-directed depth-first search that selectively traverses code dependencies. This minimizes issue-irrelevant content in the agent's context window and improves localization accuracy. For issue resolution, we employ adaptive planning and structured problem solving. We equip the agent with specialized tools for progress tracking and Git-based version control. These tools interface with a shared working memory that stores code-state checkpoints indexed by execution steps, facilitating precise checkpoint retrieval. This design enables reliable agent-driven version-control operations for systematic issue resolution, including branching to explore alternative solutions and reverting failed edits. Experiments on SWE-Bench Lite and SWE-Bench Pro demonstrate that SWE-Adept consistently outperforms prior approaches in both issue localization and resolution, improving the end-to-end resolve rate by up to 4.7%.

---

## 论文详细总结（自动生成）

这是一份关于论文《SWE-Adept: An LLM-Based Agentic Framework for Deep Codebase Analysis and Structured Issue Resolution》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文针对大语言模型（LLM）在处理**仓库级软件工程（SWE）**任务时的两大瓶颈：
*   **定位难：** 现代代码库规模庞大且依赖关系复杂，远超 LLM 的上下文窗口。现有方法往往因引入过多无关代码导致“上下文污染”，降低了定位准确度。
*   **修复缺乏系统性：** 现有的修复智能体通常采用“思考-编辑”的自由循环，缺乏明确的规划、进度跟踪和可靠的状态回滚机制。在长程任务中，错误的编辑往往会累积，导致智能体迷失方向。

**SWE-Adept** 旨在通过一个双智能体框架，结合深度代码导航和基于版本控制的结构化修复，实现自主且精准的软件问题解决。

### 2. 方法论：核心思想与关键技术
SWE-Adept 由两个专门的智能体组成：**定位智能体（Localization Agent）**和**修复智能体（Resolution Agent）**。

*   **代码库表示（Indexing）：**
    *   使用 `tree-sitter` 进行定义级索引（函数/类级别），构建**代码结构树（Code-Structure Tree）**。
    *   每个代码单元包含元数据及其相邻节点的引用，支持高效的依赖感知导航。
*   **定位智能体：智能体导向的深度优先搜索（DFS）：**
    *   **智能体驱动遍历：** 智能体根据问题描述选择性地沿依赖路径进行 DFS 遍历，而非盲目展开。
    *   **轻量化检索：** 搜索工具仅返回代码骨架和元数据，极大地节省了上下文空间。
    *   **两阶段过滤：** 先基于预览和启发式信息初步筛选，再加载完整代码进行精细重排序。
*   **修复智能体：结构化问题解决：**
    *   **假设驱动修复：** 智能体提出多个修复假设，并为每个假设制定动态的 To-Do 列表。
    *   **工具-内存接口（Tool-Memory Interface）：** 引入 `hypothesis_plan`（规划）和 `hypothesis_git`（版本控制）工具族。
    *   **语义检查点机制：** 每次完成 To-Do 步骤后，系统会自动在后端**共享工作记忆**中存储 Git 检查点（Hash 值）。智能体可以通过语义化的步骤名称（而非复杂的 Hash）实现分支切换、回滚失败编辑或合并最终方案。

### 3. 实验设计
*   **数据集：**
    *   **SWE-Bench Lite：** 包含 300 个真实的 GitHub 问题实例。
    *   **SWE-Bench Pro：** 难度更高，包含 150 个随机采样的实例（针对跨文件/函数修改）。
*   **对比方法：**
    *   **嵌入式检索：** CodeSage-Large, CodeRankEmbed。
    *   **智能体框架：** SWE-agent, RepoGraph, OrcaLoca, LocAgent。
*   **评估指标：** 定位准确率（Acc@3/Acc@5）和端到端解决率（Resolve Rate）。
*   **底层模型：** 使用了 GPT-5.2 和 Claude-Sonnet-4.5。

### 4. 资源与算力
*   **算力说明：** 论文未明确提及训练所需的 GPU 型号或时长，因为该框架主要基于现有的闭源 LLM API 构建。
*   **推理成本：** 论文详细记录了 API 开销。在使用 Claude-Sonnet-4.5 时，平均每个实例的成本为 **$1.76**；使用 GPT-5.2 时为 **$0.41**。
*   **索引效率：** 构建一个仓库的索引和结构树通常在 1 分钟内完成。

### 5. 实验数量与充分性
*   **实验规模：** 在两个主流基准测试上进行了完整测试（共 450 个实例），并针对不同模型（GPT 和 Claude）进行了交叉验证。
*   **消融实验：** 论文分别验证了“专门定位模块”和“专门修复模块”的贡献，并对比了“使用原始 Git 命令”与“使用封装后的工具接口”的效果。
*   **客观性：** 实验采用了标准的 SWE-Bench 评估流程，使用 Docker 容器运行测试用例，确保了结果的公平性和可重复性。

### 6. 主要结论与发现
*   **性能领先：** SWE-Adept 在所有指标上均优于现有方法。在 SWE-Bench Pro 上，端到端解决率比最强基准提升了 **4.0% - 4.7%**。
*   **定位是关键：** 函数级定位准确率提升了约 5.4%，这直接增强了后续修复的成功率。
*   **结构化修复的必要性：** 消融实验显示，直接让智能体使用原始 Git 命令在复杂任务中极易出错，而通过“工具-内存”接口进行语义化管理能显著提升长程任务的可靠性。

### 7. 优点
*   **上下文管理高效：** 通过 DFS 和两阶段过滤，在保持深度搜索的同时，有效控制了 Token 消耗。
*   **版本控制自主化：** 首次将 Git 分支和检查点机制深度集成到智能体的决策流中，使其具备了“试错”和“多路径探索”的能力。
*   **解耦设计：** 定位与修复智能体职责分离，避免了单一智能体上下文过载。

### 8. 不足与局限
*   **语言限制：** 目前主要针对 Python 代码库进行了优化和测试，扩展到其他语言（如 Java, C++）可能需要额外的解析器适配。
*   **模型依赖：** 框架高度依赖于高性能闭源模型（GPT/Claude）的指令遵循能力，在开源模型上的表现尚待验证。
*   **成本问题：** 尽管比部分图搜索方法更省 Token，但对于大规模工业级应用，频繁调用 API 的成本依然是一个考虑因素。

（完）
