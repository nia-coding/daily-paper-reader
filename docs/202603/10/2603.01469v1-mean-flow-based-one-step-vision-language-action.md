---
title: Mean-Flow based One-Step Vision-Language-Action
title_zh: 基于均值流的一步式视觉-语言-动作模型
authors: "Yang Chen, Xiaoguang Ma, Bin Zhao"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.01469v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 基于均值流的一步式视觉-语言-动作方法
tldr: 针对基于Flow-Matching的视觉-语言-动作（VLA）模型在机器人操作中因迭代采样导致的高延迟问题，本文提出了一种基于均值流（Mean-Flow）的一步生成框架。该方法通过解决动作生成中的噪声诱导问题，消除了传统流匹配的一致性约束，实现了高效的一步动作预测。实验证明，该模型在保持高性能的同时，生成速度显著优于SmolVLA和扩散策略，为实时机器人操控提供了高效的骨干架构。
selection_source: fresh_fetch
motivation: 现有的Flow-Matching VLA框架因迭代采样过程导致生成延迟较高，限制了其在实时机器人任务中的应用。
method: 提出基于均值流的一步生成方法，通过解决动作生成中的噪声诱导问题来消除传统流匹配的一致性约束。
result: 在真实机器人实验中，该方法的生成速度分别比SmolVLA和扩散策略快8.7倍和83.9倍。
conclusion: 该研究证明了均值流一步生成框架在提升VLA模型效率方面的巨大潜力，是高效机器人操作的理想选择。
---

## 摘要
基于流匹配（Flow-Matching）的视觉-语言-动作（VLA）框架的最新进展在生成高频动作块方面展现出显著优势，特别是在高灵巧性的机器人操作任务中。尽管取得了这些显著成就，但由于固有的迭代采样需求和架构限制，它们的实际应用受到生成延迟过长的制约。为了解决这一关键瓶颈，我们提出了一种基于均值流（Mean-Flow）的一步式 VLA 方法。具体而言，我们解决了动作生成过程中的噪声诱导问题，从而消除了传统流匹配方法固有的连贯性约束。这显著提高了生成效率，并实现了单步动作生成。真实世界的机器人实验表明，所提出的基于均值流的一步式 VLA 的生成速度分别是 SmolVLA 和扩散策略（Diffusion Policy）的 8.7 倍和 83.9 倍。这些结果阐明了其作为基于 VLA 的机器人操作的高效骨干网络的巨大潜力。

## Abstract
Recent advances in FlowMatching-based Vision-Language-Action (VLA) frameworks have demonstrated remarkable advantages in generating high-frequency action chunks, particularly for highly dexterous robotic manipulation tasks. Despite these notable achievements, their practical applications are constrained by prolonged generation latency, which stems from inherent iterative sampling requirements and architectural limitations. To address this critical bottleneck, we propose a Mean-Flow based One-Step VLA approach. Specifically, we resolve the noise-induced issues in the action generation process, thereby eliminating the consistency constraints inherent to conventional Flow-Matching methods. This significantly enhances generation efficiency and enables one-step action generation. Real-world robotic experiments show that the generation speed of the proposed Mean-Flow based One-Step VLA is 8.7 times and 83.9 times faster than that of SmolVLA and Diffusion Policy, respectively. These results elucidate its great potential as a high-efficiency backbone for VLA-based robotic manipulation.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Mean-Flow based One-Step VLA** 的新型视觉-语言-动作（VLA）模型框架，旨在解决机器人操作中生成式模型推理延迟过高的核心痛点。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的生成式 VLA 模型（如基于扩散模型或流匹配 FlowMatching 的模型）虽然在处理高频、复杂的机器人动作方面表现优异，但由于需要多次迭代采样（如欧拉积分），导致推理延迟较高，难以满足实时高频控制的需求。
*   **研究动机**：流匹配模型在减少推理步数（NFE）时会产生累积误差，导致动作轨迹偏离。作者希望引入“均值流”（Mean-Flow）理论，通过学习时间段内的平均向量场，实现高质量的一步（One-Step）动作生成，从而彻底打破生成效率的瓶颈。

### 2. 核心方法论
*   **核心思想**：不同于传统流匹配学习“瞬时向量场”（Instantaneous Vector Field），Mean-Flow 学习的是给定时间区间 $[r, t]$ 内的**平均去噪向量场**。这使得模型可以直接预测从噪声到数据的整体映射，而无需多步迭代。
*   **关键技术细节**：
    *   **均值流恒等式（MeanFlow Identity）**：通过数学推导，建立均值向量场 $u$ 与瞬时向量场 $v$ 之间的关系，利用雅可比向量积（JVP）来计算修正项，从而指导网络训练。
    *   **自适应损失函数（Adaptive Loss）**：为了应对 VLA 数据中的多模态分布和均值流目标的高方差，引入了带有超参数 $\gamma$ 的自适应损失函数，以抑制离群值的干扰，稳定训练过程。
    *   **混合训练目标**：在训练中引入“流比例”（Flow Ratio），让模型同时学习瞬时向量场（局部精度）和均值向量场（全局趋势），平衡了生成的稳定性和准确性。
*   **算法流程**：预训练的 VLM（SmolVLM-2）提取多模态特征，Mean-Flow 动作专家根据特征预测均值向量场，在推理时仅需一次前向传播即可生成整个动作块（Action Chunk）。

### 3. 实验设计
*   **实验场景**：使用自制的 SO-101 六自由度机械臂进行真实世界实验。
*   **任务设置**：
    1.  **抓取放置（Pick-place）**：随机位置抓取方块并放入盒中。
    2.  **堆叠（Stacking）**：将玩具堆叠在色块上（高精度要求）。
    3.  **分类（Sorting）**：长时程任务，将不同颜色方块放入对应盒子。
*   **Benchmark 与对比方法**：
    *   **SmolVLA**：基于流匹配的轻量化 VLA 基准。
    *   **Diffusion Policy**：基于扩散策略的经典基准。
*   **评估指标**：任务成功率、平均动作生成速度（推理延迟）。

### 4. 资源与算力
*   **训练算力**：使用单张 **NVIDIA RTX 4090** GPU。
*   **训练时长**：单任务训练 60,000 步，多任务混合训练 200,000 步。
*   **推理/部署环境**：笔记本电脑（Intel i5-10200H CPU + **NVIDIA GTX 1650** GPU），强调了模型在消费级硬件上的高效性。

### 5. 实验数量与充分性
*   **实验规模**：
    *   每个任务收集了 100 个演示视频，总计 300 条轨迹。
    *   参数敏感性实验：针对流比例、损失函数 $\gamma$ 值、NFE 步数、动作块大小进行了详细的消融实验。
    *   每组配置运行 10 轮，每轮 20 次尝试，确保了统计意义上的可靠性。
*   **充分性评价**：实验设计较为全面，涵盖了从超参数调优到真实任务对比的完整闭环。对比实验在相同的物理场景和硬件条件下进行，保证了公平性。

### 6. 主要结论与发现
*   **极高的推理效率**：在一步生成模式下，Mean-Flow VLA 的速度比 SmolVLA 快 **8.7 倍**，比 Diffusion Policy 快 **83.9 倍**。
*   **性能平衡**：虽然一步生成的精度在极高精度任务（如堆叠）中略低于多步流匹配，但在大多数任务中表现稳健。当 NFE 增加到 5 时，性能达到饱和，兼顾了速度与精度。
*   **关键参数**：发现流比例为 0.2、$\gamma$ 为 0.5、动作块大小为 20 是该框架下的最优配置。

### 7. 论文优点
*   **理论创新**：成功将 Mean-Flow 理论引入机器人 VLA 领域，解决了生成式模型“慢”的顽疾。
*   **无需蒸馏**：与一致性模型（Consistency Models）不同，该方法不需要复杂的预训练模型蒸馏过程，训练流程更简单。
*   **实战导向**：实验全部在真实机器人上完成，而非仅限于仿真环境，证明了算法的实用价值。
*   **低硬件门槛**：展示了在低端 GPU（GTX 1650）上实现高效控制的可能性。

### 8. 不足与局限
*   **高精度任务挑战**：在需要极高空间精度的堆叠任务中，一步生成的成功率（64%）明显低于多步流匹配（81.5%），说明一步生成在处理极细微调整时仍有提升空间。
*   **泛化性验证有限**：实验主要集中在自制机械臂和三个特定任务上，尚未在大规模跨机器人数据集（如 Open X-Embodiment）上验证其零样本泛化能力。
*   **动作块依赖**：性能受动作块大小影响较大，对于不同动态特性的任务，可能需要手动调整该超参数。

（完）
