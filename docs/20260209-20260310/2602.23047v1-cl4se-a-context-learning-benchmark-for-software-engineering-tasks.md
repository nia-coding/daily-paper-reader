---
title: "CL4SE: A Context Learning Benchmark For Software Engineering Tasks"
title_zh: CL4SE：软件工程任务的上下文学习基准测试
authors: "Haichuan Hu, Ye Shang, Guoqing Xie, Congqing He, Quanjun Zhang"
date: 2026-02-26
pdf: "https://arxiv.org/pdf/2602.23047v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 软件工程任务的上下文学习基准
tldr: "本研究针对软件工程（SE）领域大模型上下文学习缺乏系统分类和基准测试的问题，提出了CL4SE基准。该基准定义了四种SE特定上下文类型（可解释示例、项目特定、程序化决策、正负反馈），并对应四个核心任务。通过对1.3万余个样本和5种主流LLM的评估，证明了上下文学习能显著提升SE任务性能（平均24.7%），为SE上下文设计提供了标准化框架和实证指导。"
selection_source: fresh_fetch
motivation: 现有研究缺乏针对软件工程特定上下文类型的系统分类和量化评估基准，难以衡量不同上下文对SE工作流的影响。
method: 提出CL4SE基准，包含四种细粒度SE上下文分类，并构建了涵盖30多个开源项目、1.3万个样本的高质量数据集进行多维度评估。
result: "实验显示上下文学习使SE任务性能平均提升24.7%，其中程序化上下文在代码审查任务中提升高达33%，项目特定上下文显著增强了代码摘要效果。"
conclusion: CL4SE建立了首个标准化的SE上下文学习评估框架，为任务特定的上下文设计提供了可操作的实证见解并促进了该领域的复现研究。
---

## 摘要
上下文工程（Context engineering）已成为释放大语言模型（LLMs）在软件工程（SE）任务中潜力的关键范式，能够在无需模型微调的情况下在测试阶段提升性能。尽管取得了成功，但现有研究仍缺乏针对软件工程特定上下文类型的系统分类法，以及用于量化不同上下文在核心软件工程工作流中异构影响的专用基准测试。为了填补这一空白，我们提出了 CL4SE（软件工程上下文学习），这是一个综合性基准测试，包含四种面向软件工程的细粒度上下文类型分类（可解释示例、项目特定上下文、程序化决策上下文以及正负样本上下文），每种类型都映射到一个代表性任务（代码生成、代码摘要、代码审查和补丁正确性评估）。我们构建了包含来自 30 多个开源项目的 13,000 多个样本的高质量数据集，并在 9 个指标上评估了 5 种主流大语言模型。广泛的实验表明，上下文学习在所有任务中带来了平均 24.7% 的性能提升。具体而言，程序化上下文将代码审查性能提升了高达 33%（Qwen3-Max），正负混合上下文将补丁评估提升了 30%（DeepSeek-V3），项目特定上下文将代码摘要的 BLEU 指标提升了 14.78%（GPT-Oss-120B），而可解释示例将代码生成的 PASS@1 提升了 5.72%（DeepSeek-V3）。CL4SE 建立了首个软件工程上下文学习的标准评估框架，为特定任务的上下文设计提供了可操作的经验见解，并发布了大规模数据集以促进该领域的复现研究。

## Abstract
Context engineering has emerged as a pivotal paradigm for unlocking the potential of Large Language Models (LLMs) in Software Engineering (SE) tasks, enabling performance gains at test time without model fine-tuning. Despite its success, existing research lacks a systematic taxonomy of SE-specific context types and a dedicated benchmark to quantify the heterogeneous effects of different contexts across core SE workflows. To address this gap, we propose CL4SE (Context Learning for Software Engineering), a comprehensive benchmark featuring a fine-grained taxonomy of four SE-oriented context types (interpretable examples, project-specific context, procedural decision-making context, and positive & negative context), each mapped to a representative task (code generation, code summarization, code review, and patch correctness assessment). We construct high-quality datasets comprising over 13,000 samples from more than 30 open-source projects and evaluate five mainstream LLMs across nine metrics. Extensive experiments demonstrate that context learning yields an average performance improvement of 24.7% across all tasks. Specifically, procedural context boosts code review performance by up to 33% (Qwen3-Max), mixed positive-negative context improves patch assessment by 30% (DeepSeek-V3), project-specific context increases code summarization BLEU by 14.78% (GPT-Oss-120B), and interpretable examples enhance code generation PASS@1 by 5.72% (DeepSeek-V3). CL4SE establishes the first standardized evaluation framework for SE context learning, provides actionable empirical insights into task-specific context design, and releases a large-scale dataset to facilitate reproducible research in this domain.

---

## 论文详细总结（自动生成）

以下是对论文《CL4SE: A Context Learning Benchmark For Software Engineering Tasks》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：大语言模型（LLMs）在软件工程（SE）任务中表现优异，但其性能高度依赖于**上下文工程（Context Engineering）**。尽管上下文学习（ICL）被广泛应用，但目前缺乏：
    1.  针对 SE 任务特性的上下文类型系统分类。
    2.  专门的基准测试（Benchmark）来量化不同上下文对核心 SE 工作流的异构影响。
*   **核心问题**：如何系统地定义、构建并评估不同类型的上下文信息对 LLM 处理复杂软件工程任务的实际增益？

### 2. 方法论：核心思想与技术细节
论文提出了 **CL4SE** 框架，其核心思想是将 SE 任务的上下文需求进行细粒度分类，并建立任务与上下文的映射关系：
*   **四种上下文分类法（Taxonomy）**：
    1.  **项目特定上下文（Project-specific）**：利用项目内的编码规范、术语和文档风格。
    2.  **可解释示例（Interpretable examples）**：不仅提供输入输出对，还包含推理过程和解题策略。
    3.  **正负样本上下文（Positive & Negative）**：同时提供正确和错误的参考案例，建立双重参考系。
    4.  **程序化决策上下文（Procedural decision-making）**：保留多轮对话和决策演变过程，而非仅提供最终结果。
*   **任务映射**：将上述四种上下文分别应用于**代码摘要（CS）**、**代码生成（CG）**、**补丁正确性评估（PCA）**和**代码审查（CR）**。
*   **技术实现**：使用向量数据库（Chroma）和嵌入模型（Qwen3-Embedding-4B）进行上下文检索，确保检索到的示例在时间戳上早于测试样本，避免数据泄露。

### 3. 实验设计
*   **数据集规模**：构建了包含 **13,000+ 样本**的大规模数据集，涵盖 30 多个开源项目。
    *   **代码生成**：从 LeetCode 爬取并经过模型过滤和难度分级的 2,083 个问题。
    *   **代码摘要**：来自 10 个顶级 Python 项目（如 PyTorch, Transformers）的 8,225 个函数-摘要对。
    *   **代码审查**：来自 32 个 GitHub 项目的 1,916 条多轮对话 PR 数据。
    *   **补丁评估**：基于 Defects4J v2.0 的 2,274 个补丁（含正确与过拟合补丁）。
*   **对比模型**：评估了 5 种主流 LLM：Qwen3-Max、DeepSeek-V3、Qwen-Coder-Plus、GPT-Oss-120B、Claude-3.5-Haiku。
*   **评估指标**：包括 PASS@1（生成）、ROUGE/BLEU/METEOR/BERTScore（摘要）、Accuracy/Precision/Recall/F1（审查与补丁评估）。

### 4. 资源与算力
*   **硬件环境**：实验在拥有 **8 张 RTX 5880 GPU** 的集群上运行。
*   **部署方式**：
    *   **闭源模型**：通过官方 API 访问（Qwen3, DeepSeek, Claude），并发请求数设为 32。
    *   **开源模型**：GPT-Oss-120B 部署在 4 张 GPU 上，使用 bfloat16 精度进行推理。
*   **参数设置**：所有模型 Temperature 设为 0，以保证输出的确定性。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，包括 0-shot 到 5-shot 的性能演变测试、有无解释信息的消融实验、正负样本组合对比实验等。
*   **充分性与公平性**：
    *   样本量大且来源真实（GitHub 真实 PR 和项目）。
    *   涵盖了多种编程语言（Python, JS, SQL）。
    *   通过严格的时间戳过滤防止数据泄露，实验设计客观、公平，能够反映模型在真实开发场景下的适应力。

### 6. 主要结论与发现
*   **整体增益**：上下文学习使 SE 任务性能平均提升 **24.7%**。
*   **任务特异性规律**：
    *   **代码摘要（1-shot 最佳）**：项目特定上下文在 1-shot 时达到峰值，随后因冗余导致性能下降（“Less is More”）。
    *   **代码审查（5-shot 最佳）**：程序化决策上下文随 Shot 数增加呈单调上升趋势，模型需要更多对话历史来理解决策逻辑。
    *   **补丁评估**：正负样本混合（2-shot）效果优于单一类型的正样本或负样本。
    *   **代码生成**：可解释性示例对中等难度题目提升最明显，对简单题目存在“天花板效应”。
*   **模型差异**：通用模型（如 GPT-Oss-120B）在上下文学习中的弹性通常高于代码专用模型，能通过上下文显著弥补零样本能力的不足。

### 7. 优点（亮点）
*   **系统性分类**：首次提出了细粒度的 SE 特定上下文分类法，超越了简单的 Prompt Engineering。
*   **真实性高**：数据集基于真实世界的开源协作过程（如多轮 PR 讨论），而非人工构造的简单任务。
*   **实证指导**：为开发者提供了具体的策略建议（例如：代码摘要只需 1 个同项目示例，而代码审查则越多越好）。

### 8. 不足与局限
*   **低资源语言覆盖不足**：实验主要集中在 Python 等主流语言，对于 Rust、Solidity 或硬件描述语言（Verilog）等低资源语言的有效性尚未验证。
*   **动态演进挑战**：基准测试基于静态快照，无法模拟软件项目中代码规范和决策逻辑随时间演进（Context Drift）的动态过程。
*   **长尾效应**：对于极罕见的边缘案例，由于缺乏历史上下文示例，该方法的增益可能受限。

（完）
