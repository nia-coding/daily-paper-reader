---
title: "Hand2World: Autoregressive Egocentric Interaction Generation via Free-Space Hand Gestures"
title_zh: Hand2World：基于自由空间手势的自回归第一视角交互生成
authors: "Yuxi Wang, Wenqi Ouyang, Tianyi Wei, Yi Dong, Zhiqi Shen, Xingang Pan"
date: 2026-02-10
pdf: "https://arxiv.org/pdf/2602.09600v2"
tags: ["query:sr-ai"]
score: 9.0
evidence: 用于具身智能的第一人称交互世界模型
tldr: 本研究针对增强现实和具身智能需求，提出了Hand2World自回归框架，旨在从单张场景图像生成由手势驱动的第一人称交互视频。该方法通过投影3D手部网格实现遮挡无关的条件控制，并引入Plücker射线嵌入以解耦相机与手部运动，解决了动作歧义和背景漂移问题。通过蒸馏双向扩散模型为因果生成器，实现了具备几何一致性的长时程、高质量交互合成。
selection_source: fresh_fetch
motivation: 旨在解决第一人称视角下，手势交互生成中存在的动作歧义、数据分布偏移以及长视频生成的稳定性挑战。
method: 采用基于3D手部网格的遮挡无关条件控制，并结合像素级Plücker射线嵌入来显式注入相机几何信息。
result: 在多个基准测试中显著提升了视频的感知质量和3D一致性，并成功支持了任意长度的交互生成。
conclusion: Hand2World为构建低延迟、高保真且符合物理规律的第一人称交互式世界模型提供了有效的技术路径。
---

## 摘要
第一视角交互式世界模型对于增强现实和具身智能至关重要，在这些领域中，视觉生成必须以低延迟、几何一致性和长期稳定性来响应用户输入。我们研究了在自由空间手势下，从单张场景图像生成第一视角交互的任务，旨在合成高逼真度的视频，其中手部进入场景、与物体交互，并在头部运动下诱发合理的场景动态。这一设定引入了根本性的挑战，包括自由空间手势与重接触训练数据之间的分布偏移、单目视图中手部运动与相机运动之间的歧义，以及生成任意长度视频的需求。我们提出了 Hand2World，这是一个统一的自回归框架，通过基于投影 3D 手部网格的遮挡无关手部条件来解决这些挑战，使得可见性和遮挡可以从场景上下文中推断出来，而不是编码在控制信号中。为了稳定第一视角视点的变化，我们通过逐像素的 Plücker 射线嵌入注入显式的相机几何信息，从而解耦相机运动与手部运动，并防止背景漂移。我们进一步开发了一个全自动的单目标注流水线，并将双向扩散模型蒸馏为因果生成器，从而实现任意长度的合成。在三个第一视角交互基准上的实验表明，该方法在感知质量和 3D 一致性方面有显著提升，同时支持相机控制和长时程交互生成。

## Abstract
Egocentric interactive world models are essential for augmented reality and embodied AI, where visual generation must respond to user input with low latency, geometric consistency, and long-term stability. We study egocentric interaction generation from a single scene image under free-space hand gestures, aiming to synthesize photorealistic videos in which hands enter the scene, interact with objects, and induce plausible world dynamics under head motion. This setting introduces fundamental challenges, including distribution shift between free-space gestures and contact-heavy training data, ambiguity between hand motion and camera motion in monocular views, and the need for arbitrary-length video generation. We present Hand2World, a unified autoregressive framework that addresses these challenges through occlusion-invariant hand conditioning based on projected 3D hand meshes, allowing visibility and occlusion to be inferred from scene context rather than encoded in the control signal. To stabilize egocentric viewpoint changes, we inject explicit camera geometry via per-pixel Plücker-ray embeddings, disentangling camera motion from hand motion and preventing background drift. We further develop a fully automated monocular annotation pipeline and distill a bidirectional diffusion model into a causal generator, enabling arbitrary-length synthesis. Experiments on three egocentric interaction benchmarks show substantial improvements in perceptual quality and 3D consistency while supporting camera control and long-horizon interactive generation.

---

## 论文详细总结（自动生成）

以下是对论文《Hand2World: Autoregressive Egocentric Interaction Generation via Free-Space Hand Gestures》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于**第一视角（Egocentric）交互式世界模型**的构建。在增强现实（AR）和具身智能领域，系统需要根据用户的手势输入实时生成具有几何一致性和长期稳定性的视觉反馈。
*   **研究动机**：现有的单目视频生成方法在处理“自由空间手势”（即用户在空中做的无接触动作）驱动场景交互时存在三大挑战：
    1.  **分布偏移**：训练数据多为手部被物体遮挡的接触视频，而输入手势是完整的，导致模型生成错误的遮挡关系。
    2.  **运动歧义**：单目视角下难以区分手部运动和头部（相机）运动，导致背景漂移。
    3.  **长度限制**：传统扩散模型难以生成任意长度且保持连贯的交互视频。

### 2. 方法论：核心思想与关键技术
Hand2World 提出了一个统一的自回归框架，核心技术包括：
*   **遮挡无关的手部条件控制（Occlusion-Invariant Conditioning）**：
    *   放弃传统的2D掩码（Mask），改用基于 **3D MANO 手部模型**投影生成的“轮廓+线框（Silhouette-and-Wireframe）”信号。
    *   该信号编码了完整的手部几何结构，不包含可见性信息，迫使生成器根据场景上下文自行推断遮挡关系，解决了训练/推理间的分布偏移。
*   **显式相机几何注入**：
    *   引入逐像素的 **Plücker 射线嵌入（Plücker-ray embeddings）**。通过轻量级适配器将相机旋转、平移和内参转化为几何特征，并以加法形式注入视频扩散主干网络（基于 Wan2.1-1.3B），从而解耦相机运动与手部运动。
*   **自回归生成器蒸馏**：
    *   采用 **CausVid 范式**和**自强迫（Self-forcing）策略**，将双向视频扩散模型（Teacher）蒸馏为因果自回归生成器（Student）。
    *   支持 **KV 缓存（KV Cache）** 的块状推理，实现了任意长度的流式视频合成。
*   **全自动标注流水线**：利用 HaMeR 提取手部网格，利用 Depth Anything V3 估计相机轨迹，实现了从无标注单目视频中进行大规模扩展训练。

### 3. 实验设计
*   **数据集**：在三个主流的第一视角交互基准上进行评估：
    1.  **ARCTIC**：侧重于双手精细操作和物体状态变化（如开箱）。
    2.  **HOT3D**：包含大幅度的头部运动和多样的室内场景。
    3.  **HOI4D**：涵盖 800+ 物体类别和复杂的室内环境。
*   **对比方法**：包括 CosHand、InterDyn、Mask2IV 以及基础模型 Wan2.1-1.3B-Control。
*   **评估指标**：FVD（感知质量）、DINO（语义一致性）、PSNR/SSIM/LPIPS（保真度）、Flow-ERR（时间连贯性）、Depth-ERR 和 Cam-ERR（3D/相机一致性）。

### 4. 资源与算力
*   **硬件**：使用了 **8 × NVIDIA A100-80GB GPU**。
*   **训练细节**：
    *   **第一阶段**：预训练相机适配器（约 10,000 步），冻结主干网络。
    *   **第二阶段**：联合微调适配器与 LoRA 模块（秩为 256），约 100,000 步。
    *   **精度**：采用 bf16 混合精度训练。
    *   **推理速度**：在 A100 上，384p 分辨率下可达到 **8.9 FPS**。

### 5. 实验数量与充分性
论文进行了详尽的实验验证：
*   **跨数据集验证**：在三个不同特性的数据集上均取得了 SOTA 性能。
*   **消融实验**：针对相机适配器、线框信号、时间稳定化组件（插值、边缘过滤等）分别进行了消融，证明了各模块的必要性。
*   **长度消融**：测试了从 81 帧到 324 帧的生成质量变化，验证了自回归生成的稳定性。
*   **客观性**：实验对比了多种不同类型的基准方法，且使用了统一的预处理和评估标准，实验设计较为客观公平。

### 6. 主要结论与发现
*   **性能飞跃**：相比最强的基准方法，Hand2World 在 ARCTIC 数据集上将 **FVD 降低了 76%**，相机轨迹误差（Cam-ERR）降低了 42%。
*   **几何解耦**：显式的相机控制能有效消除“背景漂移”现象，使背景在头部转动时保持稳定。
*   **遮挡鲁棒性**：3D 手部网格投影显著优于 2D 掩码，能够生成正确的手-物遮挡关系，避免了幻影遮挡伪影。
*   **长时程能力**：蒸馏后的 AR 模型在处理 400 帧以上的长序列时仍能保持合理的交互逻辑。

### 7. 优点（亮点）
*   **创新的条件表示**：通过 3D 几何投影解决遮挡歧义，是一个非常优雅且有效的工程方案。
*   **显式几何建模**：引入 Plücker 射线嵌入，为视频生成模型提供了坚实的 3D 几何约束。
*   **实用性强**：支持单目输入、全自动标注和流式生成，非常适合 AR 设备部署。

### 8. 不足与局限
*   **物理约束缺失**：由于自由空间手势不具备物理反馈，用户输入的动作可能违反物理规律（如手部穿透固体物体），模型有时会产生不真实的物理交互。
*   **依赖估计精度**：系统的性能高度依赖于前端单目估计器（如 HaMeR 和 DA3）的准确性，若估计出错，生成质量会显著下降。
*   **算力需求**：尽管经过蒸馏，实时生成（>30 FPS）在移动端设备上仍面临挑战。

（完）
