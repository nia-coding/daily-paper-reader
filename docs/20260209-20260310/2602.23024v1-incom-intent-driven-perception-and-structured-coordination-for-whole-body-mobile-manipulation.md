---
title: "InCoM: Intent-Driven Perception and Structured Coordination for Whole-Body Mobile Manipulation"
title_zh: InCoM：面向全身移动操作的意图驱动感知与结构化协作
authors: "Jiahao Liu, Cui Wenbo, Haoran Li, Dongbin Zhao"
date: 2026-02-26
pdf: "https://arxiv.org/pdf/2602.23024v1"
tags: ["query:sr-ai"]
score: 10.0
evidence: 具身智能将感知与物理动作结合用于操作
tldr: 针对全身移动操作中底盘与机械臂动作耦合及动态视角下感知分配不均的挑战，本文提出InCoM框架。该框架通过推断潜在运动意图动态调整感知权重，并利用几何-语义对齐增强多模态感知。同时，设计了解耦协调流匹配解码器以优化动作生成。在ManiSkill-HAB任务中，InCoM显著提升了成功率，展现了强大的全身协调与鲁棒感知能力。
selection_source: fresh_fetch
motivation: 现有的全身移动操作方法面临底盘与手臂动作强耦合导致的优化困难，以及视角变换时感知注意力分配不合理的问题。
method: 提出意图驱动的感知重加权机制、几何-语义结构化对齐以及解耦协调流匹配动作解码器。
result: "在三个ManiSkill-HAB场景中，该方法在成功率上分别超越现有最先进方法28.2%、26.1%和23.6%。"
conclusion: InCoM通过意图驱动的感知与结构化协调，有效解决了全身移动操作中的感知分配与动作耦合难题。
---

## 摘要
全身移动操作是通用机器人智能体的基本能力，需要移动底座与机械臂的协调控制，以及在动态变化的视角下具备鲁棒的感知能力。然而，现有方法面临两个关键挑战：底座与手臂动作之间的强耦合使全身控制优化变得复杂，且在移动操作过程中，随着视角的切换，感知注意力往往分配不当。我们提出了 InCoM，这是一个面向全身移动操作的意图驱动感知与结构化协作框架。InCoM 通过推断潜在的运动意图来动态重新加权多尺度感知特征，从而实现感知注意力的阶段自适应分配。为了支持鲁棒的跨模态感知，InCoM 进一步引入了几何-语义结构化对齐机制，增强了多模态对应关系。在控制方面，我们设计了一个解耦协调流匹配动作解码器，显式建模底座与手臂的协调动作生成，缓解了由控制耦合引起的优化困难。在不获取特权感知信息的情况下，InCoM 在三个 ManiSkill-HAB 场景中的成功率分别比现有最先进方法高出 28.2%、26.1% 和 23.6%，证明了其在全身移动操作方面的强大有效性。

## Abstract
Whole-body mobile manipulation is a fundamental capability for general-purpose robotic agents, requiring both coordinated control of the mobile base and manipulator and robust perception under dynamically changing viewpoints. However, existing approaches face two key challenges: strong coupling between base and arm actions complicates whole-body control optimization, and perceptual attention is often poorly allocated as viewpoints shift during mobile manipulation. We propose InCoM, an intent-driven perception and structured coordination framework for whole-body mobile manipulation. InCoM infers latent motion intent to dynamically reweight multi-scale perceptual features, enabling stage-adaptive allocation of perceptual attention. To support robust cross-modal perception, InCoM further incorporates a geometric-semantic structured alignment mechanism that enhances multimodal correspondence. On the control side, we design a decoupled coordinated flow matching action decoder that explicitly models coordinated base-arm action generation, alleviating optimization difficulties caused by control coupling. Without access to privileged perceptual information, InCoM outperforms state-of-the-art methods on three ManiSkill-HAB scenarios by 28.2%, 26.1%, and 23.6% in success rate, demonstrating strong effectiveness for whole-body mobile manipulation.

---

## 论文详细总结（自动生成）

### 论文总结：InCoM —— 面向全身移动操作的意图驱动感知与结构化协作

#### 1. 核心问题与整体含义（研究动机和背景）
全身移动操作（Whole-body Mobile Manipulation）要求机器人同时控制移动底座和机械臂，在动态、部分可观测的环境中完成复杂任务。现有方法面临两大挑战：
*   **控制耦合难题**：底座与手臂动作之间存在强耦合，微小的底座偏差会被机械臂末端放大。现有的联合建模往往是单向依赖，难以捕捉双向互补的协作关系。
*   **动态感知注意力分配**：在移动操作中，视角随运动不断变化。导航阶段需要关注全局环境（避障），而操作阶段需要关注局部细节（抓取）。现有模型多采用固定尺度的感知编码器，导致感知注意力在任务推进过程中分配不均，降低了成功率。

#### 2. 方法论：核心思想与关键技术
InCoM 框架通过“意图驱动感知”和“结构化动作协调”来解决上述问题，包含三个核心模块：

*   **意图驱动金字塔感知模块 (IDPPM)**：
    *   **核心思想**：根据机器人当前的运动意图，动态调整多尺度特征的权重。
    *   **技术细节**：利用历史动作序列和全局视觉特征，通过 History Transformer 推断潜在意图向量，并生成多尺度权重（浅层对应局部细节，深层对应全局上下文）。
    *   **辅助监督**：引入运动学引导损失（Kinematic-guided Loss），利用底座和手臂的动作增量（L2 范数）作为目标分布，通过 KL 散度约束感知权重的学习，使模型在快速移动时关注深层特征，在精细操作时关注浅层特征。

*   **双流亲和力细化模块 (DARM)**：
    *   **核心思想**：将跨模态（3D 点云与 2D 图像）的对齐分解为几何一致性和语义对应。
    *   **技术细节**：并行构建几何亲和力分支（基于位置编码）和语义亲和力分支（基于高维特征）。利用 Sinkhorn-Knopp 算法从几何分支提取软约束，引导语义分支的注意力分布，从而在不依赖大规模预训练的情况下实现鲁棒的跨模态融合。

*   **解耦协调流匹配解码器 (DCFM)**：
    *   **核心思想**：在解耦动作空间以降低优化难度的同时，通过双向信息交换保持协作。
    *   **技术细节**：采用条件流匹配（Conditional Flow Matching）替代传统的扩散模型，构建噪声到专家动作的线性路径。底座和手臂拥有独立的解码分支，但在中间层通过交叉注意力机制交换“轨迹摘要 Token”，实现双向协调。

#### 3. 实验设计
*   **数据集/场景**：在 **ManiSkill-HAB** 仿真平台上进行了三个复杂场景的评估：
    1.  **SetTable**：涉及开关抽屉/冰箱、移动碗和苹果等多种技能。
    2.  **TidyHouse**：多任务学习，要求将 9 类物体放置到指定容器。
    3.  **PrepareGroceries**：在狭窄空间（冰箱与柜台间）进行物体转运，强调避障。
*   **Benchmark 设置**：移除了模拟器提供的“特权信息”（如物体精确位姿、抓取成功信号），仅依靠视觉和本体感受，更贴近现实。
*   **对比方法**：包括 Diffusion Policy (DP), ACT, DSPv2, WB-VIMA, AC-DiT 以及大规模预训练模型 $\pi_0$。

#### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **训练参数**：提到了 Batch Size 为 64，训练步数为 100k steps，使用 Adam 优化器，学习率为 $1 \times 10^{-4}$，并采用了余弦退火学习率调度。

#### 5. 实验数量与充分性
*   **实验规模**：在三个大场景下进行了数十组子任务实验，每项任务使用 1000 条成功轨迹进行训练。
*   **消融实验**：系统地验证了 IDPPM（及其权重机制）、DARM 和 DCFM 的贡献，证明了每个模块对最终成功率的提升均有显著作用。
*   **可视化分析**：提供了感知权重随任务阶段变化的曲线图，以及特征感受野的热力图分析，直观展示了模型如何根据意图切换关注点。
*   **客观性**：通过移除特权信息并与多种 SOTA 方法（包括预训练大模型）对比，实验设计较为严谨、公平。

#### 6. 主要结论与发现
*   InCoM 在三个场景中的平均成功率分别比次优方法高出 **28.2%、26.1% 和 23.6%**。
*   **意图驱动的感知重加权**是提升性能的关键，尤其是在需要频繁切换导航与操作模式的任务中。
*   **双向动作协调**比单向依赖（如手臂依赖底座）更能产生稳定、连续的全身运动轨迹。
*   即使不使用特权信息，通过结构化的感知与控制设计，端到端策略也能在复杂家庭环境中表现出极强的鲁棒性。

#### 7. 优点（亮点）
*   **感知灵活性**：创新性地将运动意图与多尺度特征加权结合，解决了移动操作中的视角变化难题。
*   **对齐鲁棒性**：DARM 模块通过几何引导语义对齐，有效降低了跨模态融合对大规模数据的依赖。
*   **高效解码**：DCFM 结合了流匹配的高效采样和双向交叉注意力的协作能力，生成的动作更平滑。

#### 8. 不足与局限
*   **泛化性限制**：模型主要在特定任务数据上从零训练，对于从未见过的物体类别或极端动作模式的泛化能力尚待验证。
*   **意图模型简化**：当前的意图推断主要依赖短时历史动作，对于极长程（Long-horizon）任务的复杂逻辑推理能力可能不足。
*   **环境依赖**：实验主要在仿真环境中完成，真实世界中的传感器噪声和物理交互不确定性可能对 DARM 的几何对齐产生挑战。

（完）
