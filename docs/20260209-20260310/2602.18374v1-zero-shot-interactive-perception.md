---
title: Zero-shot Interactive Perception
title_zh: 零样本交互式感知
authors: "Venkatesh Sripada, Frank Guerin, Amir Ghalamzan"
date: 2026-02-20
pdf: "https://arxiv.org/pdf/2602.18374v1"
tags: ["query:sr-ai"]
score: 10.0
evidence: 将 VLM 感知与机器人操作及记忆驱动的交互相结合。
tldr: 针对复杂场景下的遮挡和歧义问题，本文提出零样本交互式感知框架（ZS-IP）。该框架结合了多策略操作（推、抓）与记忆驱动的视觉语言模型（VLM），通过引入创新的“推线”（pushlines）视觉增强技术和记忆引导机制，使机器人能自主通过物理交互获取隐藏信息并完成语义查询任务，在保持非目标物体完整性的同时，显著提升了交互感知的成功率。
selection_source: fresh_fetch
motivation: 传统的被动感知难以处理复杂场景中的遮挡和歧义，需要机器人通过物理交互主动探索环境以获取隐藏信息。
method: 提出ZS-IP框架，利用包含“推线”增强的观测模块、记忆引导推理模块及多策略控制器，将VLM的语义推理转化为具体的物理交互动作。
result: 实验证明ZS-IP在Franka Panda机械臂上的表现优于MOKA等现有方法，特别是在处理遮挡和执行推动任务时表现出更高的成功率。
conclusion: ZS-IP通过结合VLM推理与动作特定的视觉增强，有效提升了机器人在零样本设置下解决复杂交互感知任务的能力。
---

## 摘要
交互式感知 (IP) 使机器人能够通过与物体进行物理交互并改变环境状态，从而在工作空间中提取隐藏信息并执行操作计划，这对于解决复杂、部分可观测场景中的遮挡和歧义至关重要。我们提出了零样本交互式感知 (ZS-IP)，这是一个将多策略操作（推和抓）与记忆驱动的视觉语言模型 (VLM) 相结合的新型框架，用于引导机器人交互并解决语义查询。ZS-IP 集成了三个关键组件：(1) 增强观测 (EO) 模块，利用传统关键点和我们提出的 pushlines（一种专为推动作定制的新型 2D 视觉增强技术）来增强 VLM 的视觉感知；(2) 记忆引导动作模块，通过上下文查找强化语义推理；(3) 机器人控制器，根据 VLM 输出执行推、拉或抓取。与针对取放操作优化的基于网格的增强不同，pushlines 捕捉了接触密集型动作的可操作性 (affordances)，显著提高了推操作的性能。我们在具有不同遮挡和任务复杂度的多样化场景中，使用 7 自由度 Franka Panda 机械臂对 ZS-IP 进行了评估。实验表明，ZS-IP 的表现优于被动式和基于视角的感知技术（如基于标记的视觉提示 MOKA），特别是在推任务中，同时能够保持非目标元素的完整性。

## Abstract
Interactive perception (IP) enables robots to extract hidden information in their workspace and execute manipulation plans by physically interacting with objects and altering the state of the environment -- crucial for resolving occlusions and ambiguity in complex, partially observable scenarios. We present Zero-Shot IP (ZS-IP), a novel framework that couples multi-strategy manipulation (pushing and grasping) with a memory-driven Vision Language Model (VLM) to guide robotic interactions and resolve semantic queries. ZS-IP integrates three key components: (1) an Enhanced Observation (EO) module that augments the VLM's visual perception with both conventional keypoints and our proposed pushlines -- a novel 2D visual augmentation tailored to pushing actions, (2) a memory-guided action module that reinforces semantic reasoning through context lookup, and (3) a robotic controller that executes pushing, pulling, or grasping based on VLM output. Unlike grid-based augmentations optimized for pick-and-place, pushlines capture affordances for contact-rich actions, substantially improving pushing performance. We evaluate ZS-IP on a 7-DOF Franka Panda arm across diverse scenes with varying occlusions and task complexities. Our experiments demonstrate that ZS-IP outperforms passive and viewpoint-based perception techniques such as Mark-Based Visual Prompting (MOKA), particularly in pushing tasks, while preserving the integrity of non-target elements.

---

## 论文详细总结（自动生成）

以下是对论文《Zero-shot Interactive Perception》（零样本交互式感知）的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：机器人在杂乱或部分可观测的环境中，如何通过物理交互（如推动、抓取）来消除遮挡和歧义，从而获取隐藏信息并回答复杂的语义查询（例如“橡皮擦下面是什么？”）。
*   **研究动机**：
    *   **被动感知局限性**：传统的视觉系统在物体被完全遮挡时失效。
    *   **主动感知不足**：仅改变相机视角（Active Perception）无法移动障碍物。
    *   **现有交互方法缺陷**：现有的视觉语言模型（VLM）驱动的方法多局限于简单的“取放”动作，缺乏对复杂物理交互（如推动）的精细建模，且难以处理需要时间记忆的连续任务。

### 2. 方法论
ZS-IP 框架将 VLM 的高层推理与底层的物理操作增强相结合，核心组件包括：
*   **感知分析器 (Perception Analyser)**：利用 GPT-4o 作为核心，判断当前观测是否足以回答查询。若不足，则启动交互流程。
*   **增强观测 (Enhanced Observation, EO)**：这是本文的核心创新，通过在图像上叠加视觉提示来辅助 VLM 决策：
    *   **推线 (Pushlines, $EO_P$)**：利用主成分分析 (PCA) 根据物体分割掩码生成主轴，定义推/拉的起始点（P）和终点（D）。这比传统的网格点更能捕捉物体的物理属性。
    *   **抓取关键点 ($EO_G$)**：使用最远点采样 (FPS) 在物体上生成 5 个关键点（含质心），引导抓取。
    *   **虚拟网格 ($EO_{AP}$)**：通过 ArUco 标记锚定的 2D 空间网格，用于空间定位。
*   **记忆引导动作模块**：建立一个存储历史状态 $S_t$（含图像、场景摘要、机器人位姿）的记忆库 $M_t$，防止机器人重复无效动作，并支持跨步骤的逻辑推理。
*   **多策略控制器**：支持四种交互模式：移动相机、推动、抓取（取放）、举起物体至固定相机前观察。

### 3. 实验设计
*   **硬件平台**：7 自由度 Franka Panda 机械臂，配备腕部 RGB-D 相机（RealSense）和桌面固定相机。
*   **实验场景**：使用 YCB 物体集构建了 8 个不同难度的任务（从简单的“推开物体看下方”到复杂的“保持空间关系移动物体”）。
*   **Benchmark 与对比方法**：
    *   **MOKA**：最先进的基于标记的视觉提示运动生成框架。
    *   **PIVOT**：迭代视觉提示方法。
    *   **消融实验**：对比了不同 VLM（GPT-4o, Gemini 2.0 Flash, Claude 3.5 Sonnet）以及有无记忆/上下文的效果。
*   **评估指标**：成功率 (SR)、总移动距离 (TL/TLS)、位置误差 (PE) 和 Oracle 成功率 (OSR)。

### 4. 资源与算力
*   **模型使用**：主要使用 **GPT-4o** 作为感知和决策引擎，结合 **Grounded SAM** 进行物体分割。
*   **算力说明**：文中**未明确提及**具体的 GPU 型号、数量或训练时长。由于该框架是“零样本”（Zero-shot）的，主要依赖于预训练大模型的推理能力，而非在特定数据集上进行大规模重训练。

### 5. 实验数量与充分性
*   **实验规模**：每个任务执行 10 次试验，并引入了位置和方向的随机变化。
*   **充分性评价**：
    *   **较为充分**：涵盖了从简单到极高复杂度的 8 类任务，并进行了跨模型（GPT vs Claude vs Gemini）和跨框架（vs MOKA/PIVOT）的对比。
    *   **客观性**：通过定量指标（如移动距离和误差）评估效率，而不仅仅是成功率。
    *   **局限性**：每组 10 次试验在统计学上样本量尚可，但在处理极度多变的现实环境时，可能需要更多样化的物体组合。

### 6. 主要结论与发现
*   **ZS-IP 显著优于基准**：在处理遮挡任务时，ZS-IP 的成功率远高于 MOKA，尤其是在需要“推动”动作的场景中。
*   **推线 (Pushlines) 的有效性**：相比于 MOKA 使用的离散网格点，本文提出的推线能更准确地引导机器人执行接触密集型任务，减少了场景破坏。
*   **记忆的重要性**：在多步推理任务（如 Task VII）中，记忆模块能有效避免循环动作，并帮助 VLM 理解物体的初始状态。
*   **模型差异**：GPT-4o 在空间推理和指令遵循方面表现优于其他测试模型。

### 7. 优点与亮点
*   **创新性视觉增强**：提出的“推线”概念填补了 VLM 在非抓取类操作（推动）中缺乏物理引导的空白。
*   **闭环交互**：实现了从“语义查询 -> 物理交互 -> 重新感知 -> 最终回答”的完整闭环。
*   **零样本能力**：无需针对特定物体进行微调，具有很强的通用性和开箱即用的潜力。

### 8. 不足与局限
*   **深度信息精度**：受限于低分辨率深度图，在极度拥挤的环境中精细操作仍有挑战。
*   **实时性瓶颈**：调用大型 VLM 进行多轮推理存在明显的延迟，难以满足高速实时交互需求。
*   **动作空间限制**：目前的推动仅限于 R2 空间，抓取限于 SO(2) 旋转，尚未实现全 6-DOF (SE(3)) 的复杂操作。
*   **常识推理偏差**：VLM 有时会产生幻觉（如认为任务已完成但实际失败），或在物理常识（如堆叠稳定性）上判断失误。

（完）
