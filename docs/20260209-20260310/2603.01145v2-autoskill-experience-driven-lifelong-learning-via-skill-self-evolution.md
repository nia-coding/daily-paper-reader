---
title: "AutoSkill: Experience-Driven Lifelong Learning via Skill Self-Evolution"
title_zh: AutoSkill：通过技能自我演化的经验驱动型终身学习
authors: "Yutao Yang, Junsong Li, Qianjun Pan, Bihao Zhan, Yuxuan Cai, Lin Du, Jie Zhou, Kai Chen, Qin Chen, Xin Li, Bo Zhang, Liang He"
date: 2026-03-01
pdf: "https://arxiv.org/pdf/2603.01145v2"
tags: ["query:sr-ai"]
score: 9.0
evidence: 通过技能自我演化实现 LLM 智能体的终身学习
tldr: 针对大语言模型在交互中难以积累个性化能力的局限，本文提出AutoSkill框架。该框架通过从对话轨迹中自动提取、维护和演化“技能”，将零散的交互经验转化为可重用的显式知识。AutoSkill作为模型无关的插件层，支持在不重新训练的情况下动态注入相关技能，实现了智能体的终身学习与个性化能力沉淀，为构建个人数字代理提供了可扩展路径。
selection_source: fresh_fetch
motivation: 现有的LLM智能体难以从重复的交互经验中巩固并复用用户特定的偏好和需求。
method: 提出一种经验驱动的终身学习框架，通过技能自演化机制从对话中抽象、维护并动态注入标准化技能。
result: 实现了无需重新训练即可在不同会话、用户和任务间共享与复用个性化能力。
conclusion: AutoSkill为实现具备终身学习能力的个性化智能体提供了一种实用且可扩展的技术方案。
---

## 摘要
在实际的大语言模型（LLM）应用中，用户会反复表达稳定的偏好和需求，例如减少幻觉、遵循机构写作规范或避免过于专业的措辞，然而这类交互经验很少被整合为可重用的知识。因此，LLM 智能体往往无法在不同会话之间积累个性化能力。我们提出了 AutoSkill，这是一个经验驱动的终身学习框架，使 LLM 智能体能够从对话和交互轨迹中自动推导、维护和重用技能。AutoSkill 从用户经验中抽象出技能，支持其持续的自我演化，并在不重新训练底层模型的情况下，将相关技能动态注入到未来的请求中。作为一个与模型无关的插件层，它与现有的 LLM 兼容，并引入了标准化的技能表示，以便在智能体、用户和任务之间进行共享和迁移。通过这种方式，AutoSkill 将短暂的交互经验转化为显式、可重用且可组合的能力。本文描述了 AutoSkill 的动机、架构、技能生命周期和实现，并将其与先前关于记忆、检索、个性化和智能体系统的研究进行了对比定位。AutoSkill 为实现终身个性化智能体和个人数字代理提供了一条实用且可扩展的路径。

## Abstract
In practical LLM applications, users repeatedly express stable preferences and requirements, such as reducing hallucinations, following institutional writing conventions, or avoiding overly technical wording, yet such interaction experience is seldom consolidated into reusable knowledge. Consequently, LLM agents often fail to accumulate personalized capabilities across sessions. We present AutoSkill, an experience-driven lifelong learning framework that enables LLM agents to automatically derive, maintain, and reuse skills from dialogue and interaction traces. AutoSkill abstracts skills from user experience, supports their continual self-evolution, and dynamically injects relevant skills into future requests without retraining the underlying model. Designed as a model-agnostic plugin layer, it is compatible with existing LLMs and introduces a standardized skill representation for sharing and transfer across agents, users, and tasks. In this way, AutoSkill turns ephemeral interaction experience into explicit, reusable, and composable capabilities. This paper describes the motivation, architecture, skill lifecycle, and implementation of AutoSkill, and positions it with respect to prior work on memory, retrieval, personalization, and agentic systems. AutoSkill highlights a practical and scalable path toward lifelong personalized agents and personal digital surrogates.

---

## 论文详细总结（自动生成）

以下是对论文《AutoSkill: Experience-Driven Lifelong Learning via Skill Self-Evolution》的结构化深入总结：

### 1. 核心问题与背景（研究动机）
*   **核心问题**：在实际应用中，用户常会跨会话重复表达稳定的偏好（如“不要幻觉”、“遵循特定写作风格”），但现有的 LLM 智能体通常将每次交互视为孤立事件，无法将这些经验转化为可复用的能力。
*   **研究背景**：
    *   **现有方案局限**：参数微调（Fine-tuning）成本高且难以频繁更新；传统的记忆机制（如 RAG）侧重于存储事实性文本，而非可操作的行为模式（Behavioral Knowledge）。
    *   **目标**：建立一种无需重新训练、能够自动从交互中提取、维护并演化“显式技能”的终身学习框架。

### 2. 方法论
AutoSkill 采用一种**无需训练（Training-free）**、由提示词驱动的模块化架构，核心包含两个耦合的循环：

*   **技能增强响应循环（在线过程）**：
    1.  **查询重写**：将用户输入重写为适合检索的独立查询。
    2.  **混合检索**：结合语义向量（Dense）和关键词（BM25）进行混合检索，公式为 $Rel(q_t, s) = \lambda \hat{d} + (1-\lambda) \hat{b}$。
    3.  **技能注入**：将检索到的 Top-K 技能以 Markdown 格式注入提示词，引导模型生成符合用户习惯的回复。
*   **技能自我演化循环（后台过程）**：
    1.  **技能提取**：仅基于用户侧的查询序列（避免模型自我污染），抽象出具有持久性的技能候选者。
    2.  **技能管理决策**：通过 LLM 判断新候选技能与现有技能库的关系，执行 **Add（新增）**、**Merge（合并）** 或 **Discard（丢弃）**。
    3.  **版本化合并**：若选择合并，则进行语义融合并更新版本号（如从 v0.1.0 升至 v0.1.1），实现技能的持续精炼。
*   **核心产物**：**SKILL.md**。这是一种标准化的技能表示格式，包含名称、描述、执行指令（Prompt）、触发器、标签、示例和版本信息。

### 3. 实验设计
*   **数据集**：使用 **WildChat-1M**（包含 100 万条真实用户与 ChatGPT 的交互记录）。
*   **实验场景**：从数据集中筛选出轮数超过 8 轮的对话，划分为四个子集：中文 GPT-3.5、英文 GPT-3.5、中文 GPT-4、英文 GPT-4。
*   **对比与评估**：
    *   **统计分析**：分析了提取出的 1858 个技能的类别分布（编程、写作、数据处理等）、标签频率及平台相关性。
    *   **案例研究**：展示了“顶级心理咨询师”、“专业文本重写”、“八字命理大师”等具体技能卡片，验证技能的复杂约束遵循能力。
    *   **演化验证**：通过版本号的变化（如某技能经历 34 次迭代）证明了框架的持续优化能力。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **原因分析**：由于 AutoSkill 是一个**无需训练（Training-free）**的框架，其核心开销在于推理（Inference）。系统实现采用了模块化设计，支持通过 API 调用现有 LLM（如 GPT-4）作为后端引擎，因此不涉及大规模的参数更新算力消耗。

### 5. 实验数量与充分性
*   **实验规模**：分析了超过 1.7 万场对话，最终提取并分析了 1858 个显式技能。
*   **充分性评价**：
    *   **客观性**：实验覆盖了中英双语和不同能力的模型家族（GPT-3.5/4），具有较好的泛化参考价值。
    *   **局限性**：实验侧重于**系统性描述和定性分析**（如技能分布、案例展示），缺乏与传统 RAG 或其他终身学习基准（如 StuLife）在特定任务准确率上的硬性指标（Benchmark）对比。

### 6. 主要结论与发现
*   **经验可转化性**：证明了可以将零散的交互经验转化为结构化的 `SKILL.md` 产物。
*   **技能分布规律**：高频技能集中在编程、专业写作和数据处理领域，且用户对特定社交平台（如 Twitter, Instagram）有明确的格式需求。
*   **持续演化可行性**：通过版本化合并机制，技能可以随着用户反馈的增加而变得更加精准和严谨，而非简单的重复累积。

### 7. 优点（亮点）
*   **显式化与可解释性**：技能以 Markdown 形式存储，用户和开发者可以直观地查看、编辑和修正，解决了传统记忆系统“黑盒化”的问题。
*   **插件化设计**：作为模型无关的层，可以轻松集成到现有的智能体框架或通过 OpenAI 兼容代理部署。
*   **版本控制**：引入了类似软件工程的版本管理，确保了技能演化的可追溯性。
*   **训练无关**：极大地降低了个性化智能体的部署门槛和成本。

### 8. 不足与局限
*   **依赖底层模型能力**：技能提取和管理的质量高度依赖于所使用的 LLM 的推理能力，弱模型可能产生噪声技能。
*   **缺乏定量基准对比**：论文未提供在标准 NLP 任务或智能体基准测试中的量化提升数据。
*   **潜在的检索压力**：随着技能库规模无限增长，如何保证在高并发场景下的检索效率和精度仍需进一步探讨。
*   **冲突处理**：当多个检索到的技能指令发生冲突时，框架目前的优先级处理机制尚不明确。

（完）
