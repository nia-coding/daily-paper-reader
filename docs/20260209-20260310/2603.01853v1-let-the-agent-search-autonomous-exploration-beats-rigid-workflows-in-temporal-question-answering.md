---
title: "Let the Agent Search: Autonomous Exploration Beats Rigid Workflows in Temporal Question Answering"
title_zh: 让智能体自主搜索：在时序问答中，自主探索优于僵化的工作流
authors: "Xufei Lv, Jiahui Yang, Yifu Gao, Linbo Qiao, Houde Liu"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.01853v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 智能体的自主探索与决策
tldr: 针对时序知识图谱问答（TKGQA）中多步推理和时间约束的挑战，本文提出AT2QA。不同于以往依赖固定工作流或微调的方法，AT2QA赋予大模型自主权，通过通用搜索工具与知识图谱迭代交互。在MultiTQ实验中，该方法在零样本设置下显著超越了现有SOTA，证明了自主探索在处理复杂时序查询中的优越性。
selection_source: fresh_fetch
motivation: 现有的时序问答方法通常依赖僵化的检索流程或昂贵的微调，难以灵活应对复杂的多步时序推理需求。
method: 提出AT2QA，一种无需训练的自主智能体，通过通用搜索工具与时序知识图谱进行迭代交互和动态检索。
result: "在MultiTQ数据集上达到88.7%的Hits@1，比之前最先进的方法提升了10.7%，在多目标查询上更是提升了20.1%。"
conclusion: 实验证明，赋予大模型自主决策权而非使用固定工作流，能更有效地解决复杂的时序知识图谱问答任务。
---

## 摘要
时序知识图谱问答（TKGQA）要求在时间约束下进行多跳推理。先前基于大语言模型（LLM）的方法通常依赖于僵化的、手工设计的检索工作流，或者昂贵的有监督微调。我们展示了仅通过赋予现成 LLM 自主性（即让其决定下一步操作），即使在严格的零样本（zero-shot）设置下也能获得显著提升。基于这一见解，我们提出了 AT2QA，这是一种用于时序问答的自主、免训练智能体，它通过通用搜索工具与时序知识图谱进行迭代交互，以实现动态检索。在 MultiTQ 上的实验表明了巨大的改进：AT2QA 达到了 88.7% 的 Hits@1（比之前的 SOTA 提高了 10.7%），其中包括在具有挑战性的多目标查询上实现了 20.1% 的提升，这表明智能体自主性在时序问答中可以有力地超越微调方法。代码和完整的采样轨迹集可在 https://github.com/AT2QA-Official-Code/AT2QA-Official-Code 获取。

## Abstract
Temporal Knowledge Graph Question Answering (TKGQA) demands multi-hop reasoning under temporal constraints. Prior approaches based on large language models (LLMs) typically rely on rigid, hand-crafted retrieval workflows or costly supervised fine-tuning. We show that simply granting an off-the-shelf LLM autonomy, that is, letting it decide what to do next, already yields substantial gains even in a strict zero-shot setting. Building on this insight, we propose AT2QA, an autonomous, training-free agent for temporal question answering that iteratively interacts with the temporal knowledge graph via a general search tool for dynamic retrieval. Experiments on MultiTQ demonstrate large improvements: AT2QA achieves 88.7% Hits@1 (+10.7% over prior SOTA), including a +20.1% gain on challenging multi-target queries, showing that agentic autonomy can decisively outperform fine-tuning for temporal question answering. Code and the full set of sampled trajectories are available on https://github.com/AT2QA-Official-Code/AT2QA-Official-Code

---

## 论文详细总结（自动生成）

这是一份关于论文《Let the Agent Search: Autonomous Exploration Beats Rigid Workflows in Temporal Question Answering》的深度结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：时序知识图谱问答（TKGQA）要求模型在处理事实时必须考虑时间维度（即 `<主语, 关系, 宾语, 时间戳>` 四元组）。这涉及复杂的多跳推理和多粒度的时间约束。
*   **核心问题**：现有的 TKGQA 方法存在四大局限：
    1.  **缺乏自纠错能力**：传统嵌入方法或静态 LLM 工作流一旦中间步骤出错，错误会迅速累积。
    2.  **训练成本高昂**：许多 SOTA 方法依赖于昂贵的有监督微调（SFT）或强化学习（RL）。
    3.  **解释性不足**：难以提供可验证的推理证据链。
    4.  **自主性受限**：现有的 LLM 方案通常强制模型遵循人工设计的僵化工作流，限制了模型探索和适应复杂查询的能力。
*   **研究动机**：作者发现，现代 LLM 本身已具备足够的智能来决定“下一步该做什么”。因此，与其设计复杂的工作流，不如赋予模型自主权，让其通过工具自主搜索和验证。

### 2. 方法论：AT2QA 框架
AT2QA（Autonomous and Training-free Agent）是一个无需参数更新的自主智能体框架，核心包含两个部分：

*   **工具增强的推理框架（Tool-Augmented Reasoning）**：
    *   **结构化搜索工具**：提供一个 `<search>` 工具，支持语义检索和符号过滤。它允许模型指定时间窗口（`ctime`）、实体过滤（`centity`）和关系过滤（`crel`）。
    *   **混合排序**：工具支持基于相关性的排序和基于时间的排序，帮助智能体理解事实的时序演变。
    *   **迭代推理与自纠错**：模型在“思考-行动-观察”的循环中运行。如果检索到的证据与假设冲突，模型可以自主调整搜索约束（如扩大时间范围）进行回滚和修正。
*   **免训练的经验挖掘（Training-Free Experience Mining）**：
    *   借鉴 **GRPO**（群体相对策略优化）的思想，但不更新参数。
    *   从模型自身生成的成功轨迹中，利用 LLM 评估其指令价值，筛选出高质量的“优势经验”作为 Few-shot 示例，从而稳定模型的工具调用行为。

### 3. 实验设计
*   **数据集**：使用 **MultiTQ**，这是目前最大的多粒度 TKGQA 基准测试集（基于 ICEWS05-15 构建），包含约 50 万个问答对，涵盖单跳、多跳、比较推理等多种类型。
*   **对比方法**：
    *   **传统嵌入方法**：EmbedKGQA, CronKGQA, MultiQA。
    *   **基于 LLM 的静态工作流/微调方法**：ARI, TempAgent, TimeR4, MemoTime, RTQA, PoK, 以及最新的微调模型 Temp-R1。
*   **评价指标**：Hits@1（即精确匹配率）。

### 4. 资源与算力
*   **模型底座**：主要使用 **DeepSeek-V3.2** API。
*   **算力与成本**：论文明确指出该方法是 **Training-free（免训练）** 的，因此不需要 GPU 训练算力。
*   **推理开销**：在 MultiTQ 的 56,014 个测试问题上运行，总成本约为 **150 美元**（基于官方 API 价格）。这证明了该方法在大规模应用时的低成本特性。

### 5. 实验数量与充分性
*   **实验规模**：在超过 5.6 万个测试样本上进行了评估，样本量非常充足。
*   **消融实验**：
    *   测试了不同 LLM 底座（Qwen3-Max, DeepSeek-R1, Kimi-2.5 等）的零样本表现，证明了框架的通用性。
    *   针对搜索工具的组件（时间窗口、结构化过滤、排序）进行了逐一消融，验证了各功能的必要性。
*   **轨迹分析**：通过 CDF（累积分布函数）曲线分析了“金牌事实”在推理轨迹中的位置，定量证明了模型的**自纠错**（在轨迹后期才找到关键事实）和**自验证**（找到事实后继续搜索以确保完整性）行为。
*   **客观性**：对比了从传统方法到最新微调 SOTA 的各类模型，实验设计全面且公平。

### 6. 主要结论与发现
*   **自主性胜过微调**：AT2QA 在 MultiTQ 上达到了 **88.7% 的 Hits@1**，比之前最强的微调模型 Temp-R1 提升了 **10.7%**。
*   **复杂查询表现卓越**：在最具挑战性的“多目标（Multiple targets）”查询中，AT2QA 的准确率提升了 **20.1%**，证明了自主探索在处理多跳推理时的巨大优势。
*   **潜能激发**：Pass@k 分析显示，模型在 k=10 时几乎能解决所有难题。这表明模型本身具备解决问题的能力，关键在于如何通过合适的 Prompt 和工具接口将其潜能“诱导”出来，而非重新训练。

### 7. 优点与亮点
*   **完全免训练**：无需任何梯度更新，极大地降低了技术门槛和计算成本。
*   **强大的自纠错机制**：通过迭代交互，模型能从错误的检索路径中自主返回并修正，克服了级联错误。
*   **高可解释性**：模型生成的工具调用轨迹和检索到的事实构成了完整的证据链，便于审计。
*   **即插即用**：该框架对底座模型具有较强的通用性，能够随 LLM 能力的提升而自然增强。

### 8. 不足与局限
*   **推理延迟**：由于采用多轮迭代交互（最高支持 20 轮），其响应速度（Latency）明显慢于单次检索的 RAG 或静态工作流。
*   **效率与稳定性风险**：自主性可能导致模型在面对模糊查询时进行多余的探索，甚至出现循环调用，增加了推理成本。
*   **对底座模型依赖性**：虽然框架通用，但仍需要底座模型具备较强的指令遵循和逻辑推理能力，在较弱的小模型上效果可能打折扣。

（完）
