---
title: "DreamSAC: Learning Hamiltonian World Models via Symmetry Exploration"
title_zh: DreamSAC：通过对称性探索学习哈密顿世界模型
authors: "Jinzhou Tang, Fan Feng, Minghao Fu, Wenjun Lin, Biwei Huang, Keze Wang"
date: 2026-03-08
pdf: "https://arxiv.org/pdf/2603.07545v1"
tags: ["query:sr-ai"]
score: 10.0
evidence: 用于物理属性和守恒定律的哈密顿世界模型
tldr: 针对现有世界模型在物理外推泛化能力上的不足，本文提出 DreamSAC 框架。该框架引入“对称性探索”策略，通过哈密顿量启发式好奇心引导智能体主动收集物理信息丰富的数据，并结合哈密顿世界模型与对比学习，从像素观测中提取守恒的物理状态。实验证明，DreamSAC 在 3D 物理模拟的外推任务中显著优于现有基准。
selection_source: fresh_fetch
motivation: 现有的世界模型往往学习统计相关性而非底层的物理守恒定律，导致其在面对未知的物理属性时缺乏外推泛化能力。
method: 提出一种基于哈密顿量的对称性探索策略来收集数据，并设计了一个结合对比学习目标的哈密顿世界模型，以从原始像素中识别不变的物理状态。
result: 在 3D 物理模拟任务中，DreamSAC 在需要外推泛化的场景下表现显著优于当前最先进的基准模型。
conclusion: 通过主动探索物理对称性和利用哈密顿动力学建模，可以显著提升世界模型对复杂物理规律的理解和外推能力。
---

## 摘要
学习到的世界模型在内插泛化方面表现出色，但在面对新颖物理属性的外推泛化时往往失败。这种局限性的产生是因为它们学习的是统计相关性，而非环境底层的生成规则，例如物理不变性和守恒定律。我们认为，学习这些不变性是实现鲁棒外推的关键。为此，我们首先引入了“对称性探索”（Symmetry Exploration），这是一种无监督探索策略，智能体受基于哈密顿量的内在好奇心奖励驱动，主动探测并挑战其对守恒定律的理解，从而收集具有物理信息的数据。其次，我们设计了一个基于哈密顿量的世界模型，该模型利用收集到的数据进行学习，并采用一种新颖的自监督对比目标，从原始的、依赖视角的像素观测中识别出不变的物理状态。我们的框架 DreamSAC 在这些主动策划的数据上进行训练，在需要外推能力的 3D 物理模拟任务中，显著优于现有的最先进基准模型。

## Abstract
Learned world models excel at interpolative generalization but fail at extrapolative generalization to novel physical properties. This limitation arises because they learn statistical correlations rather than the environment's underlying generative rules, such as physical invariances and conservation laws. We argue that learning these invariances is key to robust extrapolation. To achieve this, we first introduce \textbf{Symmetry Exploration}, an unsupervised exploration strategy where an agent is intrinsically motivated by a Hamiltonian-based curiosity bonus to actively probe and challenge its understanding of conservation laws, thereby collecting physically informative data. Second, we design a Hamiltonian-based world model that learns from the collected data, using a novel self-supervised contrastive objective to identify the invariant physical state from raw, view-dependent pixel observations. Our framework, \textbf{DreamSAC}, trained on this actively curated data, significantly outperforms state-of-the-art baselines in 3D physics simulations on tasks requiring extrapolation.

---

## 论文详细总结（自动生成）

### 论文总结：DreamSAC —— 通过对称性探索学习哈密顿世界模型

#### 1. 核心问题与整体含义
现有的世界模型（如 DreamerV3）在处理熟悉场景的“内插泛化”时表现优异，但在面对未知的物理属性（如改变重力、摩擦力或质量）时，其“外推泛化”能力往往会崩溃。这是因为这些模型主要学习像素层面的统计相关性，而非环境底层的生成规则（如物理守恒定律）。
**研究动机：** 论文旨在将学习目标从描述像素统计转向发现物理不变性（Invariances），通过主动探索和物理约束的建模，构建一个能够理解能量守恒和对称性的物理接地（Physics-grounded）世界模型。

#### 2. 方法论
DreamSAC 框架结合了物理先验、主动探索和自监督表示学习，核心技术包括：
*   **哈密顿世界模型 ($H_\phi$)：** 将系统动力学建模为受控哈密顿系统。状态被分解为广义坐标 ($q$) 和动量 ($p$)。使用 **Lie Transformer** 构建 $G$-不变（如 SE(3) 对称性）的哈密顿函数，确保模型在不同视角下遵循相同的物理规律。
*   **对称性探索（Symmetry Exploration）：** 提出一种基于物理的好奇心奖励 $r_{sym} \approx |H_\phi(Z_{t+1}) - H_\phi(Z_t)|$。该奖励激励智能体通过动作去“打破”当前的对称性（即做功），从而收集最能暴露模型物理理解偏差的数据。
*   **视角鲁棒性对比损失 ($L_{vr}$)：** 为了从依赖视角的像素中提取不随视角变化的物理状态，引入了自监督对比学习。通过对同一图像进行透视变换、抖动等增强，强制编码器忽略视角噪声，提取纯粹的动力学特征。
*   **双重积分策略：** 训练时使用显式欧拉积分以保证梯度稳定性；推理和想象时使用辛跳跃积分（Symplectic Leapfrog）以保证长期的能量守恒。

#### 3. 实验设计
*   **实验场景：** 涵盖了 DeepMind Control Suite (DMCS)（如 Cheetah, Hopper, Walker, Humanoid）和 GymFetch（FetchPush, FetchReach）等 3D 物理模拟环境。
*   **Benchmark 与对比方法：**
    *   **基准：** 状态领先的 DreamerV3。
    *   **对比变体：** DreamerV3+随机策略、DreamerV3+RND（随机网络蒸馏探索）、DreamSAC+随机策略。
*   **测试维度：** 
    1.  **预测精度：** 图像重建的均方误差（MSE）。
    2.  **外推泛化（OOD）：** 包括结构性 OOD（未见过的视角、物体数量、目标位置）和参数化 OOD（未见过的重力、摩擦力、质量）。
    3.  **下游任务适应：** 在预训练后进行微调的奖励表现。

#### 4. 资源与算力
*   **硬件：** 论文提到使用了 **NVIDIA A100 GPU** 进行实验。
*   **效率：** 相比 DreamerV3，DreamSAC 由于需要计算哈密顿梯度和双重积分，训练步长耗时增加了约 **35%**，显存占用增加约 **12%**。但论文强调其样本效率更高，达到相同性能所需的交互步数更少。

#### 5. 实验数量与充分性
*   **实验规模：** 论文在超过 10 个不同的任务环境中进行了验证，每个实验均采用 **5 个随机种子** 取平均值，并提供了标准差。
*   **消融实验：** 针对对比损失 ($L_{vr}$)、哈密顿结构 ($H_\phi$) 和对象中心编码器 (SAVi) 进行了专门的消融研究，证明了各组件的必要性。
*   **评价：** 实验设计涵盖了从视觉变化到物理参数变化的多个维度，对比基准选择合理，实验结果具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **外推能力显著：** DreamSAC 在 OOD 任务上的表现比 DreamerV3 高出 **22% 至 163%**。
*   **物理一致性：** 学习到的哈密顿量在无动作序列中保持恒定，证明模型确实捕捉到了能量守恒定律。
*   **快速适应：** 通过“差异化微调”策略（冻结视角鲁棒的编码器，仅微调物理参数），模型能极快地适应新的物理环境（如从正常重力切换到 1.5 倍重力）。

#### 7. 优点
*   **物理接地性强：** 不同于纯黑盒模型，它将经典的哈密顿力学融入深度强化学习，具有更好的解释性。
*   **主动探索机制：** 提出的“对称性探测”奖励解决了无监督学习中数据冗余的问题，使智能体能针对性地收集物理信息。
*   **视角无关性：** 成功解决了像素输入与物理守恒状态之间的矛盾。

#### 8. 不足与局限
*   **保守系统假设：** 目前模型假设环境是哈密顿系统（能量守恒），对于具有高耗散性（如粘性流体、软体摩擦）的环境，建模可能不够精确。
*   **数值稳定性：** 辛积分器在处理极高速度碰撞（硬接触）时可能存在数值刚性问题，需要极小的步长。
*   **计算开销：** 相比传统 RSSM 模型，推理延迟略高，可能限制其在超高频实时控制（>100Hz）中的应用。

（完）
