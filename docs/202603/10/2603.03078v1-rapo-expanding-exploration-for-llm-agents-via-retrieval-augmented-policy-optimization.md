---
title: "RAPO: Expanding Exploration for LLM Agents via Retrieval-Augmented Policy Optimization"
title_zh: RAPO：通过检索增强策略优化扩展大语言模型智能体的探索
authors: "Siwei Zhang, Yun Xiong, Xi Chen, Zi'an Jia, Renhong Huang, Jiarong Xu, Jiawei Zhang"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.03078v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 通过强化学习中的检索增强策略优化来扩展 LLM 智能体的探索
tldr: 针对大语言模型智能体在强化学习中探索受限的问题，本文提出检索增强策略优化（RAPO）框架。该框架通过引入外部检索机制，在训练中结合混合策略展开和检索感知优化，使智能体能基于细粒度的外部步骤进行更广泛的探索。实验证明，RAPO在多项任务上显著提升了性能和训练效率，为复杂推理任务提供了更强的探索能力。
selection_source: fresh_fetch
motivation: 现有智能体强化学习过度依赖同策略探索，限制了对新推理视角的发现，且缺乏对细粒度步骤级探索动态的有效利用。
method: 提出两阶段框架，通过混合策略展开让智能体学习检索到的外部步骤轨迹，并利用检索感知优化机制校准策略梯度。
result: "在14个数据集上的实验显示，RAPO实现了5.0%的平均性能增益，同时训练速度提升了1.2倍。"
conclusion: RAPO通过将检索机制与强化学习深度结合，有效扩展了智能体的探索空间，是提升LLM智能体推理能力的有效方案。
---

## 摘要
智能体强化学习 (Agentic RL) 在基于大语言模型 (LLM) 的智能体中展现出巨大潜力。这些工作使 LLM 智能体能够通过多步、集成工具的推理来处理复杂任务。然而，现有 Agentic RL 方法的一个固有局限是它们依赖纯同策略 (on-policy) 范式进行探索，这限制了探索仅限于智能体自身生成的输出，阻碍了发现新的推理视角以进一步提升性能。虽然近期的一些工作引入了辅助异策略 (off-policy) 信号来增强探索，但它们通常利用完整的异策略轨迹进行轨迹级策略估计，忽视了智能体展开过程中细粒度、步级探索动态的必要性。在本文中，我们重新审视了 Agentic RL 中的探索问题，并提出了检索增强策略优化 (RAPO)，这是一种新型强化学习框架，通过引入检索在训练期间显式扩展探索。为此，我们将 Agentic RL 训练过程分解为两个阶段：(i) 混合策略智能体展开，以及 (ii) 检索感知策略优化。具体而言，我们提出了一种混合策略智能体展开策略，允许智能体在检索到的异策略步级轨迹上持续推理。它动态扩展了智能体的推理感受野，使其能够在外部行为的引导下进行更广泛的探索。随后，我们引入了检索感知策略优化机制，通过检索奖励和重要性塑造来校准策略梯度估计，从而稳定训练并优先考虑检索启发的探索。大量实验表明，RAPO 在三个智能体推理任务的 14 个数据集上实现了 +5.0% 的平均增益，同时训练效率提升了 1.2 倍。

## Abstract
Agentic Reinforcement Learning (Agentic RL) has shown remarkable potential in large language model-based (LLM) agents. These works can empower LLM agents to tackle complex tasks via multi-step, tool-integrated reasoning. However, an inherent limitation of existing Agentic RL methods is their reliance on a pure on-policy paradigm for exploration, restricting exploration to the agent's self-generated outputs and preventing the discovery of new reasoning perspectives for further improvement. While recent efforts incorporate auxiliary off-policy signals to enhance exploration, they typically utilize full off-policy trajectories for trajectory-level policy estimation, overlooking the necessity for the fine-grained, step-level exploratory dynamics within agentic rollout. In this paper, we revisit exploration in Agentic RL and propose Retrieval-Augmented Policy Optimization (RAPO), a novel RL framework that introduces retrieval to explicitly expand exploration during training. To achieve this, we decompose the Agentic RL training process into two phases: (i) Hybrid-policy Agentic Rollout, and (ii) Retrieval-aware Policy Optimization. Specifically, we propose a Hybrid-policy Agentic Rollout strategy, which allows the agents to continuously reason over the retrieved off-policy step-level traces. It dynamically extends the reasoning receptive field of agents, enabling broader exploration conditioned on external behaviors. Subsequently, we introduce the Retrieval-aware Policy Optimization mechanism, which calibrates the policy gradient estimation with retrieval reward and importance shaping, stabilizing training and prioritizing retrieval-illuminating exploration. Extensive experiments show that RAPO achieves an +5.0% average gain on fourteen datasets across three agentic reasoning tasks, while delivering 1.2x faster training efficiency.

---

## 论文详细总结（自动生成）

以下是对论文《RAPO: Expanding Exploration for LLM Agents via Retrieval-Augmented Policy Optimization》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的智能体强化学习（Agentic RL）主要依赖**同策略（On-policy）探索**，即智能体仅从自己生成的推理路径中学习。这导致探索空间被局限在模型自身的固有偏见内，难以发现更优的推理视角。
*   **核心挑战**：虽然一些方法引入了异策略（Off-policy）信号，但通常是在轨迹级别进行静态估计，忽视了智能体在多步推理过程中细粒度的、步骤级的动态探索需求。
*   **整体含义**：本文旨在通过检索机制，在训练阶段显式地为智能体注入外部高质量的推理步骤，从而打破探索边界，提升智能体处理复杂任务的能力。

### 2. 方法论
RAPO 框架将训练分为两个核心阶段：
*   **混合策略智能体展开（Hybrid-policy Agentic Rollout）**：
    *   **步级轨迹缓存（Step-Trace Buffer）**：构建一个存储“推理历史 -> 下一步动作”键值对的数据库。
    *   **动态检索推理**：在 Rollout 过程中，智能体以一定概率（如 50%）不进行自生成，而是根据当前上下文从缓存中检索一个异策略的优质步骤。
    *   **扩展感受野**：智能体被迫在检索到的外部步骤基础上继续推理，从而接触到其原生策略无法触及的推理路径。
*   **检索感知策略优化（Retrieval-aware Policy Optimization）**：
    *   **检索奖励（Retrieval Reward）**：利用**熵（Entropy）**的变化来量化检索的贡献。如果检索步骤显著降低了模型的不确定性（熵减），则给予正向奖励；同时鼓励在模型高不确定性（高熵）状态下触发检索。
    *   **检索重要性塑造（Retrieval Importance Shaping）**：由于检索到的 Token 不参与梯度回传，会导致信号稀疏。该机制通过检索 Token 的占比来重新调整同策略 Token 的梯度权重，确保模型能有效吸收检索带来的探索增益。

### 3. 实验设计
*   **数据集与场景**：涵盖 14 个数据集，分为三大任务：
    1.  **计算推理**：GSM8K, MATH, MATH500, AIME24, AIME25。
    2.  **知识密集型推理**：HotpotQA, 2WikiMultihopQA, Musique, Bamboogle 等。
    3.  **Web 智能体推理**：GAIA, SimpleQA, WebWalkerQA, BrowseComp。
*   **Benchmark 与对比方法**：
    *   **工具集成方法**：Search-o1, Search-R1, Tool-Star。
    *   **异策略学习方法**：SFT, RolloutReplay, LUFFY。
    *   **强化学习基准**：单步 RL（GRPO, DAPO, GPPO）和多步 Agentic RL（GiGPO, Tree-GRPO, ARPO, AEPO）。
*   **主干模型**：Qwen2.5 (3B/7B/32B), Llama3 (8B)。

### 4. 资源与算力
*   **硬件配置**：实验明确提到使用了 **4 台或 8 台 NVIDIA RTX A100 (80GB) GPU**。
*   **训练效率**：论文指出 RAPO 相比 GRPO 实现了 **1.2 倍的训练加速**。这主要归功于检索步骤替代了部分生成计算，且通过检索 Token 掩码减少了梯度计算量。

### 5. 实验数量与充分性
*   **实验规模**：论文在 14 个不同难度的基准数据集上进行了测试，实验组数非常多，覆盖了从基础数学到复杂网页导航的多种场景。
*   **消融实验**：针对检索奖励、重要性塑造、检索粒度（步级 vs 轨迹级）等关键组件进行了详尽的消融分析。
*   **鲁棒性测试**：验证了在不同质量的 Buffer（由不同规模模型生成）以及存在检索噪声（随机扰动）的情况下的表现。
*   **评价**：实验设计全面且严谨，通过聚类分析可视化了 Rollout 的多样性，客观地证明了方法在扩展探索空间上的有效性。

### 6. 主要结论与发现
*   **性能提升**：RAPO 在所有基准测试中均显著优于 SOTA 方法，平均性能增益达 **+5.0%**。
*   **探索多样性**：语义空间聚类分析显示，混合策略生成的轨迹比纯同策略轨迹具有更高的多样性和覆盖面。
*   **推理效率**：受检索引导的智能体倾向于生成更短、更有效的推理路径，减少了冗余的工具调用。
*   **自我调节**：即使检索到的是随机噪声，RAPO 也能通过检索奖励机制识别并抑制负面影响，表现出极强的鲁棒性。

### 7. 优点
*   **创新性探索范式**：首次将检索（RAG）显式地集成到 RL 的训练展开阶段，而非仅仅用于推理。
*   **细粒度控制**：步级的检索比传统的轨迹级异策略学习更能捕捉复杂的动态推理逻辑。
*   **效率与效果并重**：在提升性能的同时，通过减少生成开销和优化梯度计算，实现了训练加速。

### 8. 不足与局限
*   **Buffer 依赖性**：虽然具有鲁棒性，但框架的效果仍高度依赖于初始异策略数据（Step-Trace Buffer）的质量。
*   **存储开销**：维护一个大规模的步级键值对缓存会占用额外的内存和存储资源。
*   **任务局限**：目前主要验证了文本和工具调用类任务，在需要极长程规划或视觉反馈的复杂具身智能场景下的泛化能力尚待验证。

（完）
