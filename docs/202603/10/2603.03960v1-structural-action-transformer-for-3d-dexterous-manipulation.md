---
title: Structural Action Transformer for 3D Dexterous Manipulation
title_zh: 用于三维灵巧操作的结构化动作 Transformer
authors: "Xiaohan Lei, Min Wang, Bohong Weng, Wengang Zhou, Houqiang Li"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.03960v1"
tags: ["query:sr-ai"]
score: 9.0
evidence: 机器人控制中的3D灵巧操作
tldr: 针对高自由度机器人手在跨构型技能迁移中的挑战，本文提出结构化动作 Transformer (SAT)。该方法摒弃了传统的时间中心化动作表示，将动作块重构为无序的关节轨迹序列，利用 Transformer 处理变长构型。通过引入具身关节码本编码运动学先验，并结合 3D 点云与流匹配目标，SAT 在异构数据集预训练及多任务微调中表现优异，显著提升了采样效率和跨构型迁移能力。
selection_source: fresh_fetch
motivation: 现有的 2D 观测和时间中心化动作表示难以捕捉 3D 空间关系，且无法有效处理不同机器人构型间的异构性。
method: 将动作建模为变长的关节轨迹序列，并引入具身关节码本编码功能角色，利用流匹配从 3D 点云生成动作。
result: 在多项仿真和真实世界任务中，该方法在性能和采样效率上均优于现有基准，并实现了有效的跨构型技能迁移。
conclusion: 这种以结构为中心的表示方法为实现高自由度、异构机器人的大规模策略扩展提供了新路径。
---

## 摘要
通过异构数据集的模仿学习使机器人实现人类水平的灵巧性，受到跨具身技能迁移挑战的阻碍，特别是对于高自由度（high-DoF）机械手。现有方法通常依赖于二维观测和以时间为中心的动作表示，难以捕捉三维空间关系，且无法处理具身异构性。本文提出了结构化动作 Transformer (SAT)，这是一种新的三维灵巧操作策略，通过引入以结构为中心的视角来挑战这一范式。我们将每个动作块（action chunk）重新定义为关节级轨迹的可变长度、无序序列，而非时间序列。这种结构化公式使 Transformer 能够原生处理异构具身，将关节数量视为可变的序列长度。为了编码结构先验并消除歧义，我们引入了具身关节码本（Embodied Joint Codebook），用于嵌入每个关节的功能角色和运动学特性。我们的模型通过连续时间流匹配目标，学习从三维点云生成这些轨迹。我们通过在大规模异构数据集上进行预训练，并在仿真和真实世界的灵巧操作任务上进行微调，验证了我们的方法。我们的方法始终优于所有基线，展示了卓越的样本效率和有效的跨具身技能迁移。这种以结构为中心的表示为扩展高自由度、异构操作器的策略提供了一条新路径。

## Abstract
Achieving human-level dexterity in robots via imitation learning from heterogeneous datasets is hindered by the challenge of cross-embodiment skill transfer, particularly for high-DoF robotic hands. Existing methods, often relying on 2D observations and temporal-centric action representation, struggle to capture 3D spatial relations and fail to handle embodiment heterogeneity. This paper proposes the Structural Action Transformer (SAT), a new 3D dexterous manipulation policy that challenges this paradigm by introducing a structural-centric perspective. We reframe each action chunk not as a temporal sequence, but as a variable-length, unordered sequence of joint-wise trajectories. This structural formulation allows a Transformer to natively handle heterogeneous embodiments, treating the joint count as a variable sequence length. To encode structural priors and resolve ambiguity, we introduce an Embodied Joint Codebook that embeds each joint's functional role and kinematic properties. Our model learns to generate these trajectories from 3D point clouds via a continuous-time flow matching objective. We validate our approach by pre-training on large-scale heterogeneous datasets and fine-tuning on simulation and real-world dexterous manipulation tasks. Our method consistently outperforms all baselines, demonstrating superior sample efficiency and effective cross-embodiment skill transfer. This structural-centric representation offers a new path toward scaling policies for high-DoF, heterogeneous manipulators.

---

## 论文详细总结（自动生成）

以下是对论文《Structural Action Transformer for 3D Dexterous Manipulation》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：实现人类水平的机器人灵巧操作（Dexterous Manipulation）面临两大挑战：一是**跨具身（Cross-embodiment）迁移难**，不同机器人手（如 ShadowHand, Allegro）的自由度（DoF）和运动学结构差异巨大；二是**动作表示效率低**，传统方法采用“以时间为中心”的视角（将动作视为固定维度的向量序列），难以处理变长的关节数量，且忽略了关节间的结构先验。
*   **核心目标**：提出一种能够原生处理异构机器人构型、捕捉 3D 空间关系并实现高效跨具身技能迁移的通用策略框架。

### 2. 方法论
*   **核心思想：以结构为中心的动作表示**：
    *   将动作块（Action Chunk）从传统的 $(T, D_a)$（$T$ 为时间步，$D_a$ 为动作维度）重构为 $(D_a, T)$。
    *   在这种视角下，每个 Token 不再是一个时间点的动作向量，而是**单个关节在整个预测时界内的完整轨迹**。
    *   **变长处理**：利用 Transformer 处理变长序列的特性，将关节数量 $D_a$ 视为序列长度，从而自然地兼容不同自由度的机器人。
*   **关键技术细节**：
    *   **具身关节码本（Embodied Joint Codebook）**：为消除无序关节序列的歧义，为每个关节分配一个三元组嵌入 $(e, f, r)$，分别代表具身 ID、功能类别（如指间关节、掌指关节）和旋转轴。这使得模型能识别不同机器人间具有相似功能的关节。
    *   **3D 观测标记化**：使用 FPS（最远点采样）和 PointNet 提取局部几何 Token，结合全局场景 Token 和 T5 编码的语言指令。
    *   **连续时间流匹配（Flow Matching）**：采用条件归一化流（CNF）建模动作分布，通过学习条件速度场 $\epsilon_\theta$ 将高斯噪声转换为关节轨迹序列，推理时使用 ODE 求解器（如 Euler 法）生成动作。

### 3. 实验设计
*   **数据集与场景**：
    *   **预训练**：混合了人类演示（Ego-Exo4D, HOI4D, ADT）、机器人演示（Fourier ActionNet, DexCap）和仿真数据（Adroit, DexArt, Bi-DexHands）。
    *   **仿真评测**：在 Adroit（3个任务）、DexArt（4个任务）、Bi-DexHands（4个任务）共 11 个复杂灵巧操作任务上进行微调评估。
    *   **真实世界**：在双臂 xArm + xHand 平台上执行 6 个任务（如摘笔帽、递送玩具、协作搬运等）。
*   **对比基线**：
    *   2D 策略：Diffusion Policy, HPT, UniAct。
    *   3D 策略：3D Diffusion Policy (3DDP), 3D ManiFlow。

### 4. 资源与算力
*   **算力说明**：论文提到实验得到了中国科学技术大学（USTC）信息科学技术学院 MCC 实验室 GPU 集群及超级计算中心的支持。
*   **具体细节**：文中**未明确列出**具体的 GPU 型号（如 A100/H100）数量及总训练时长。但提到了优化器参数（AdamW）、预训练 Warmup 步数（10,000 步）以及模型参数量（SAT 核心仅 19.36M，远小于对比基线）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 11 个仿真任务和 6 个真实世界任务，总计 17 个测试场景。
    *   进行了详尽的**消融实验**：包括时间维度压缩率、预训练数据组成（人类 vs 机器人 vs 仿真）、模型组件（全局/局部点云、因果掩码）、以及关节码本的组成部分。
*   **充分性评价**：实验设计非常充分且具有说服力。通过跨数据集预训练和少样本（Few-shot）微调实验，客观地证明了该方法在处理异构数据和提升采样效率方面的优势。

### 6. 主要结论与发现
*   **结构化表示的优越性**：将动作建模为关节轨迹序列比传统的时间步序列更适合高自由度机器人，能显著提升成功率。
*   **跨具身迁移可行性**：通过“功能类别”嵌入，模型能够从人类手的运动数据中学习到可迁移至机器人的灵巧操作先验。
*   **参数效率**：SAT 在参数量仅为基线模型 1/10 甚至更小的情况下，性能依然全面超越了基于 2D 图像和 3D 点云的现有最强策略。

### 7. 优点（亮点）
*   **范式创新**：打破了动作表示的固有思维，将 Transformer 的序列建模能力从“时间轴”转移到了“结构轴”。
*   **通用性强**：具身关节码本的设计为解决机器人领域的“异构性”提供了一个优雅且可扩展的方案。
*   **3D 空间感知**：直接处理原始点云，相比 2D VLA 模型，在需要精确接触和空间定位的灵巧操作任务中表现更稳健。

### 8. 不足与局限
*   **感知遮挡问题**：在真实世界双臂协作中，单视角相机容易产生严重的遮挡，导致策略失效，未来可能需要多视角融合。
*   **运动学匹配误差**：人类演示与机器人硬件之间仍存在运动学或接触几何的微小差异，可能导致动作分配错误，目前尚需通过闭环反馈或触觉补偿来进一步优化。
*   **实时性挑战**：虽然流匹配推理较快，但在极高频控制场景下，ODE 求解的计算开销仍需关注。

（完）
